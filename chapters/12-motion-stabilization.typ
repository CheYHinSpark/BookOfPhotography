#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 时间采样、运动与防抖 <chap:motion-stabilization>

快门时间不只是曝光方程中的乘数，也是一个时间积分窗口。场景、相机姿态或照明在窗口内变化时，传感器把一段轨迹压缩成一个像素值；运动的先后顺序通常丢失，只留下位置的时间占据。对匀速平移，这种积分表现为线段卷积核；对手持抖动，它是随机曲线；对滚动快门，各行还拥有不同时间原点。

本章先从时间变化的投影算子推导运动核与 sinc 型 MTF，再把主体速度、相机平移和角速度换算到像面。随后用随机轨迹方差与成功概率重新解释“安全快门”和防抖档数，并说明 OIS、IBIS 与电子防抖各自降低什么。最后在 @chap:sensor-physics 的行读出模型上加入运动与周期照明，得到滚动快门剪切和频闪条纹。运动估计和去模糊可参见 @szeliski2022 与 @fergus2006；滚动快门几何可参见 @meingast2005。

前两章讨论的是给定瞬间下的光学点扩散，本章加入曝光窗口中的时间变化，使“快门”从光量乘数变成成像算子。得到的运动核与滚动快门模型随后会和镜头 MTF、像素孔径一起进入采样章，因此静态锐度与动态成功率不会再被混为一谈。

#key-box[
  *两个时间量不可混淆*　单行曝光时间 $T$ 决定该行内部的运动积分长度；整幅扫描时间 $T_("read")$ 决定顶部与底部的采样时刻差。缩短 $T$ 可以减轻行内模糊，却不必减轻滚动快门倾斜。
]

== 曝光期间的运动积分

=== 时间变化的成像算子

令无运动时的连续像为 $f(bold(x))$，时刻 $t$ 的几何变换为 $cal(W)_t$。忽略曝光期间的照明变化，归一化观测为

$
  g(bold(x))
  =1/T integral_0^T
  f(cal(W)_t^(-1)(bold(x))) dif t.
$ <eq:time-varying-image-integral>

若所有位置只发生同一平移 $bold(r)(t)$，则

$
  g(bold(x))
  =1/T integral_0^T f(bold(x)-bold(r)(t)) dif t
  =(f ast h_m)(bold(x)),
$ <eq:motion-convolution>

其中运动核为

$
  h_m(bold(x))
  =1/T integral_0^T
  delta(bold(x)-bold(r)(t)) dif t.
$ <eq:motion-path-kernel>

核记录轨迹在各位置停留的时间比例。相机先向左再向右，与先向右再向左可能产生相同 $h_m$；单张模糊照片通常不能恢复时间顺序。

#figure(
  image("../svg/motion_path_kernel.svg", width: 98%),
  caption: [曝光轨迹与运动核。匀速平移给出线段核；加速或抖动产生非均匀曲线核；运动随位置改变时，不能用一个全局卷积描述整幅图。],
) <fig:motion-path-kernel>

若旋转、透视或主体深度使位移场 $bold(r)(bold(x),t)$ 随位置变化，@eq:motion-convolution 的空间不变卷积模型失效。此时应使用空间变化 PSF、分块近似或完整的时间变化重投影。

=== 匀速线段核

令像面速度为常向量 $bold(v)$，并把曝光区间中心设为零，则 $bold(r)(t)=bold(v)t$、$t in [-T/2,T/2]$。运动方向上的线段长度为

$ L=norm(bold(v)) T. $ <eq:motion-blur-length>

沿运动方向坐标 $s$ 的一维归一化核为

$
  h_m(s)
  =1/L "rect"(s/L),
$ <eq:uniform-line-kernel>

其中 $"rect"(u)=1$ 当 $abs(u)<=1/2$，其他位置为零。其光学传递函数为

$
  H_m(bold(nu))
  ="sinc"(bold(nu) dot bold(v) T),
$ <eq:motion-otf>

这里 $"sinc"(x)=sin(pi x)/(pi x)$。沿运动方向的第一个零点位于 $nu=1/L$；垂直方向在这一理想模型下不受影响。

#figure(
  image("../svg/motion_mtf.svg", width: 97%),
  caption: [匀速线段模糊的频率响应。模糊长度增加会把 sinc 零点推向低频；零点附近的信息不能靠普通锐化稳定恢复。],
) <fig:motion-mtf>

若光学 PSF、像素孔径和运动核近似线性空间不变，总 MTF 可写成各自 OTF 的乘积。运动核的零点会使反卷积成为病态问题：在零点处除法发散，附近频率也会强烈放大噪声。所谓“后期去模糊”必须依赖正则化、非负性、图像先验或多帧信息，而不是无损逆运算。

=== 曝光门函数与闪光冻结

若快门门函数或照明随时间变化，@eq:motion-path-kernel 应改为加权核

$
  h_m(bold(x))
  =1/Z integral
  w(t) delta(bold(x)-bold(r)(t)) dif t,
  quad Z=integral w(t) dif t.
$ <eq:weighted-motion-kernel>

$w(t)$ 可以是机械快门透过、闪光脉冲与环境光之和。很短的闪光脉冲会把权重集中到轨迹的一小段，因而冻结受闪光主导的主体；但环境光项仍在整个快门时间内积分，可能形成清晰闪光像与拖影的叠加。

== 从世界速度到像面速度

=== 主体横向运动

针孔模型下，世界点的像面坐标为 $u=f X/Z$。求导得

$
  dot(u)
  =f (dot(X) Z-X dot(Z))/Z^2.
$ <eq:image-velocity-depth>

若主体只以横向速度 $V_x=dot(X)$ 运动且 $dot(Z)=0$，则

$ dot(u)=f V_x/Z=f omega_x, $ <eq:transverse-image-velocity>

其中 $omega_x=V_x/Z$ 是主体相对于相机的角速度。像元间距为 $p$ 时，曝光内像素模糊长度为

$ ell_("px")=(f abs(V_x) T)/(Z p). $ <eq:subject-blur-pixels>

同一线速度的远处主体角速度较小；同一角速度在长焦或更小像元间距下跨越更多像素。但最终可见模糊还取决于输出缩放与允许弥散尺度，不能只按一比一像素查看。

#figure(
  image("../svg/image_velocity_geometry.svg", width: 97%),
  caption: [主体与相机运动到像面速度的映射。主体横向速度按 $f/Z$ 放大；相机小角旋转近似产生全局位移，平移分量则随深度 $1/Z$ 改变。],
) <fig:image-velocity-geometry>

=== 相机平移与旋转的光流

令归一化像坐标为 $x=X/Z,y=Y/Z$，相机平移速度为 $bold(T)=(T_x,T_y,T_z)$、角速度为 $bold(Omega)=(Omega_x,Omega_y,Omega_z)$。在一种常见符号约定下，静态场景的像流为

$
  dot(x)
  =(-T_x+x T_z)/Z
  +x y Omega_x-(1+x^2)Omega_y+y Omega_z,
$ <eq:optical-flow-x>

$
  dot(y)
  =(-T_y+y T_z)/Z
  +(1+y^2)Omega_x-x y Omega_y-x Omega_z.
$ <eq:optical-flow-y>

平移项含 $1/Z$，因此产生视差与空间变化模糊；旋转项不显含深度，在小视场内常近似为全局运动。符号会随“相机相对世界”或“世界相对相机”的约定改变，但深度依赖结构不变。

#paragraph-heading[主体运动与相机运动的可分性]

单张图像只测得相对像流。一个主体向右运动，与相机向左转动可能在该主体区域产生相似轨迹；背景、深度、陀螺仪或多帧连续性才能帮助分离。防抖系统主要估计和补偿相机姿态，不知道画面中哪个人正在跑动。

摇摄主动让相机角速度接近主体角速度，使主体的相对 $bold(v)$ 变小，同时背景获得方向性模糊。它不是“防抖失败”，而是把清晰度资源分配给主体的控制策略。

== 相机抖动的随机模型

=== 角轨迹与像面轨迹

对小角度相机转动 $bold(theta)(t)$，像面位移近似

$ bold(r)(t) approx f bold(theta)(t). $ <eq:angle-to-image-shift>

令曝光内平均角位置为

$
  overline(bold(theta))
  =1/T integral_0^T bold(theta)(t) dif t,
$ <eq:mean-exposure-angle>

用轨迹围绕均值的二阶矩定义角模糊尺度

$
  sigma_theta^2(T)
  =1/T integral_0^T
  norm(bold(theta)(t)-overline(bold(theta)))^2 dif t.
$ <eq:angular-path-variance>

对应像面 RMS 尺度约为 $sigma_x=f sigma_theta$，以像素表示为 $sigma_("px")=f sigma_theta/p$。端点位移相同的两条轨迹可能有不同 @eq:angular-path-variance，因此只比较曝光开始和结束姿态并不足以预测模糊核。

=== “安全快门”是概率陈述

假设某个快门时间下，残余像面位移两轴独立、均服从方差 $sigma_x^2(T)$ 的高斯分布，则径向位移 $R$ 近似为 Rayleigh 分布。允许模糊半径为 $c$ 时，成功概率为

$
  P(R<=c)
  =1-exp[-c^2/(2sigma_x^2(T))].
$ <eq:sharpness-success-probability>

$c$ 取决于像素间距、输出放大、观看距离、主体细节与主观阈值；$sigma_x(T)$ 取决于姿势、呼吸、按快门动作、支撑、焦距和防抖状态。因此不存在跨人、跨输出和跨概率阈值的唯一“安全快门”。

#figure(
  image("../svg/shake_probability.svg", width: 98%),
  caption: [安全快门的概率解释。给定容许半径，成功率随抖动方差连续变化；防抖把成功率曲线推向更长时间，而不是建立绝对分界。],
) <fig:shake-probability>

若局部经验上 $sigma_theta(T)=C T^alpha$，固定成功概率近似要求

$ T prop f^(-1/alpha). $ <eq:safe-shutter-scaling>

传统 $T prop 1/f$ 规则隐含 $alpha approx 1$ 与固定允许像面尺寸；若抖动更接近扩散过程 $alpha approx 1/2$，标度会更接近 $1/f^2$。真实手持运动是多频随机过程，经验规则只能当作某一成功率、某一输出条件下的先验。

=== 防抖档数

设无防抖快门 $T_0$ 与开启防抖后的 $T_1$ 达到相同成功率和清晰阈值，则防抖收益定义为

$ s_("IS")=log_2(T_1/T_0) $ <eq:stabilization-stops>

档。这是统计等效时间倍率，不表示每张照片都能延长 $2^s$。评测必须报告焦距、距离、方向、拍摄姿势、样本数、清晰阈值与目标成功率；只挑最清晰的一张会严重高估系统能力。

=== 防抖只能补偿一部分运动

防抖系统用陀螺仪或图像估计相机运动，再移动镜组、传感器或数字取景区域进行补偿。它只能在有限速度、频率和行程范围内工作；高频振动、低频漂移、陀螺偏置、时延和执行器饱和都会留下残余。防抖镜组本身还可能改变边缘像质或瞬态构图，所以镜头测试必须固定防抖状态。

#struct-box(title: [可以继续深入])[控制理论用开环传递函数、灵敏度函数和运动功率谱计算闭环后的残余方差。本书后文只需要结论：防抖在有限频带内降低相机运动，不能冻结主体，也不能保证每一张都成功。]

#figure(
  image("../svg/stabilization_loop.svg", width: 98%),
  caption: [防抖闭环与有限带宽。OIS 移动镜组，IBIS 移动传感器，电子防抖在采样后估计并重映射；三者都受带宽、行程、延迟与估计误差限制。],
) <fig:stabilization-loop>

#paragraph-heading[OIS、IBIS 与电子防抖]

- *光学防抖（OIS）*移动镜组改变光线偏折，执行模型与具体镜头焦距和位置相关；
- *机身防抖（IBIS）*移动或旋转传感器补偿像面轨迹，超广角边缘和近距离平移会暴露单一刚体补偿的不足；
- *电子防抖*从图像或陀螺仪估计运动后裁切、重采样或多帧融合，不能恢复已在单帧曝光内积分掉的高频；
- *三脚架与支撑*改变输入运动谱，有时需要关闭会自行搜寻运动的控制模式，具体取决于系统设计。

无论哪一种防抖，都主要补偿相机运动。跑动人物、风吹树叶或星体视运动仍由相对角速度决定；冻结主体必须缩短有效积分、使用短闪光或进行可信的运动补偿重建。

== 滚动快门的时空采样

=== 行时间与几何剪切

沿用 @eq:rolling-shutter-start-time，行 $y$ 的曝光中心时刻为

$
  t_c(y)
  =t_0+(y/H)T_("read")+T/2.
$ <eq:rolling-row-center-time>

若竖直物体只沿水平方向匀速运动 $x(t)=x_0+v_x t$，该行观测位置为

$
  x_("obs")(y)
  =x_0+v_x[t_0+(y/H)T_("read")+T/2].
$ <eq:rolling-shear-position>

因此剪切斜率和顶部到底部的总错位分别为

$
  (dif x_("obs"))/(dif y)
  =v_x T_("read")/H,
  quad Delta x=v_x T_("read").
$ <eq:rolling-shear-slope>

单行模糊长度仍是 $abs(v_x)T$。这再次说明 $T$ 控制行内模糊，$T_("read")$ 控制整幅几何倾斜。竖直运动会与扫描方向耦合产生伸缩，旋转叶片则因角速度随位置变化形成弯曲或分段形状。

#figure(
  image("../svg/rolling_shutter_flicker.svg", width: 98%),
  caption: [滚动快门与时间变化的空间编码。左侧匀速运动产生剪切；右侧周期照明在各行不同相位被积分，形成水平明暗条纹。],
) <fig:rolling-shutter-flicker>

=== 周期照明与条纹

令照明辐照度为

$
  E(t)
  =E_0[1+m cos(2pi f_L t+phi)],
$ <eq:periodic-light-source>

其中 $f_L$ 是调制频率。行 $y$ 在区间 $[t_s(y),t_s(y)+T]$ 上积分，得到

$
  H(y)
  =E_0 T {1
  +m "sinc"(f_L T)
  cos[2pi f_L(t_s(y)+T/2)+phi]}.
$ <eq:rolling-flicker-bands>

相邻行的相位差约为 $2pi f_L T_("read")/H$。曝光时间很短时 $"sinc"(f_L T) approx 1$，条纹对比接近照明调制度；若 $T$ 恰好覆盖整数个周期，sinc 因子为零，理想模型下条纹被平均。实际 PWM 波形、多个频率与非矩形门函数会使结果更复杂。

“防频闪快门”会估计灯光周期并选择快门相位或时间，使积分更一致；它无法保证混合光源、显示屏扫描和相机行读出的所有频率同时兼容。

=== 机械与电子快门的约束比较

#figure(
  table(
    columns: (1.05fr, 1.2fr, 1.35fr, 1.45fr),
    align: (left, left, left, left),
    [*方式*], [*时间结构*], [*主要优势*], [*主要限制*],
    [机械焦平面快门], [两帘扫描；高速时为狭缝], [传感器无需逐行控制曝光终止], [磨损、声响、闪光同步上限；高速并非全局],
    [电子前帘], [电子开始、机械结束], [较低震动与延迟], [极高速大光圈可能受两种帘位置差影响],
    [电子滚动快门], [逐行开始与结束], [静音、无帘震、可高速连拍], [几何倾斜、频闪条纹、扫描时间依模式],
    [电子全局快门], [全像素同时积分], [消除行间时间偏移], [存储节点、满阱、噪声、功耗或成本权衡],
  ),
  caption: [常见快门方式的时间结构。具体相机可能组合多种机制，判断时应查看每行实际积分区间。],
) <tab:shutter-timing-comparison>

机械焦平面快门在高于同步速度时同样以狭缝扫过画面，不能简单称为“全局曝光”。不过两帘的扫描时间、方向与电子滚动读出不同；闪光脉冲、LED 调制和主体速度与它们相互作用的方式也不同。

== 常见误解

#paragraph-heading[“运动模糊只是少一点锐度，锐化即可补回”]

线段核的 OTF 有真实零点，某些方向频率被完全压到零；锐化无法从单张观测唯一恢复这些分量，只会放大附近噪声。盲去模糊还要同时估计未知轨迹，问题更不适定。

#paragraph-heading[“安全快门是一条确定边界”]

手持清晰率是随机分布，且依赖焦距、像素、输出、姿势和阈值。$1/f$ 只是某种运动标度和成功率下的经验先验，不是物理定律。

#paragraph-heading[“防抖可以冻结移动主体”]

防抖估计相机运动并移动镜组、传感器或画面坐标。主体相对背景的运动不在同一个刚体模型中，除非系统识别并专门跟踪它。

#paragraph-heading[“缩短快门就会消除滚动快门倾斜”]

缩短单行曝光 $T$ 会减小行内模糊，但几何剪切由 $T_("read")$ 决定。只有缩短总扫描时间、改变读出方向或使用全局曝光，才直接减少行间时间差。

#paragraph-heading[“机械快门一定同时曝光整幅画面”]

低速档可能存在全开阶段，高速档通常由移动狭缝定义曝光。若主体在狭缝扫过期间运动，机械快门也会产生时间几何效应。

#pagebreak(weak: true)

#struct-box(title: [本章小结])[
  - 运动核是曝光轨迹的时间占据测度；只有空间一致平移才能用单一卷积表示。
  - 匀速线段核长度 $L=norm(bold(v))T$，其 OTF 为 sinc，并在运动方向产生不可逆零点。
  - 主体横向像速约为 $f V/Z$；相机旋转近似与深度无关，平移光流含 $1/Z$。
  - 手持模糊是随机事件；安全快门应报告允许弥散、成功率与输出条件。
  - 防抖在有限频带内降低相机姿态方差，受延迟、行程与估计误差限制，不能冻结主体。
  - 滚动快门的单行时间 $T$ 控制行内模糊，总扫描时间 $T_("read")$ 控制行间剪切。
  - 周期照明条纹由行起始相位与 $"sinc"(f_L T)$ 积分因子共同决定。
  - 机械、电子前帘、电子滚动和全局快门具有不同时间结构，不应只按“有无机械声”分类。
]

== 思考与计算

#block[
  1. 一点像以 $120 "px/s"$ 匀速运动，曝光分别为 1/30、1/125 和 1/500 s。计算模糊长度与线段核第一个频率零点；说明何时像素采样会先成为限制。
  2. 由 @eq:motion-path-kernel 证明核积分为 1。若物体在前半段静止、后半段以速度 $v$ 匀速运动，画出核形状并给出静止点的冲激权重。
  3. 焦距 200 mm、像元间距 $4 "µm"$，主体距相机 50 m，以 $8 "m/s"$ 横向运动。求 1/1000 s 内的像素模糊长度；若输出缩小 4 倍，如何重新定义允许阈值？
  4. 对 @eq:optical-flow-x 和 @eq:optical-flow-y，分别令平移与旋转为零。说明为什么纯旋转可用全局单应近似，而平移在非平面场景中产生深度相关视差。
  5. 假设 $sigma_theta(T)=C T^alpha$。从 @eq:sharpness-success-probability 推导固定成功率下 $T$ 与焦距 $f$ 的标度；比较 $alpha=1$ 与 $alpha=1/2$。
  6. 某焦距下无防抖 1/60 s 的清晰成功率为 80%，开启防抖后 1/8 s 达到同一成功率。计算等效档数；说明为什么不能据此保证 1/8 s 的每张照片都清晰。
  7. 滚动快门总扫描时间 $T_("read")=24 "ms"$，单行曝光 $T=2 "ms"$，主体像速 $v_x=1500 "px/s"$。计算整幅剪切与行内模糊；把单行曝光缩至 $0.25 "ms"$ 后哪些量改变？
  8. LED 调制频率为 100 Hz，滚动快门单行曝光分别为 1、5、10、20 ms。计算 $abs("sinc"(f_L T))$，判断理想正弦照明的条纹对比在哪些时间附近最小。
  9. 设计一个实验测量手持清晰成功率曲线。要求至少包含 5 个快门时间、30 次重复、两种焦距、开启/关闭防抖，并明确清晰阈值、随机化顺序与置信区间。
  10. 比较机械高速狭缝、电子滚动与电子全局快门拍摄旋转风扇和 PWM 灯的结果。分别预测叶片几何、亮度条纹、闪光同步与噪声/满阱方面的差异。
]
