#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 多帧成像与计算摄影 <chap:multiframe-computational>

单张照片只能使用一次曝光、一个采样相位和一个时间区间。多帧成像把这些限制改写为联合估计：重复观测可以平均独立噪声，不同曝光可以覆盖更宽亮度范围，亚像素位移可以补充采样相位，不同焦点可以覆盖更深空间。代价是场景必须在某种意义上保持一致，而相机要知道哪些变化来自手抖、主体运动、视差、焦点或照明。

本章不把计算摄影写成手机功能清单，而用一个统一模型解释配准、平均、HDR、超分辨率、焦点堆栈和夜景融合。图像配准与多视几何可参见 @szeliski2022，HDR 辐射图可参见 @debevec1997，多帧超分辨率可参见 @farsiu2004，移动设备上的多帧弱光成像可参见 @hasinoff2016。

@chap:denoising-ai 主要在固定观测上改变估计方法，本章则用更多时间、曝光或采样相位增加证据。多帧并不会自动获胜：它把单帧噪声问题换成配准、运动和时间一致性问题。完成这最后一组成像手段后，下一章将把全书的局部规律转成系统选型约束。

#key-box[
  *多帧的本质交换*　它用时间、存储和计算换取更多观测；只有当这些观测能被映射到同一个潜在场景时，信息才会相加。无法解释的帧间变化不会凭空消失，而会成为重影、软化、拼接缝或先验驱动的删改。
]

== 配准与多帧平均

=== 一个统一的多帧前向模型

记潜在场景图像为 $bold(x)$，第 $r$ 帧写成

$
  bold(y)_r
  =D_r H_r W_r bold(x)+bold(n)_r.
$ <eq:multiframe-forward-model>

$W_r$ 把共同场景变换到该帧坐标，包含相机运动、主体运动和视差；$H_r$ 表示光学、离焦和曝光期间运动模糊；$D_r$ 表示像素积分、CFA 与离散采样；$bold(n)_r$ 是随机噪声和未建模误差。若曝光不同，还应显式加入曝光尺度与响应曲线。

这个式子不是要求所有软件真的构造巨型矩阵，而是提供一张检查表：

- 各帧是否观察同一时刻意义下的场景？
- 几何差异能否由一个全局变换描述，还是需要深度与局部运动？
- 帧内模糊是否已经不同？
- 采样相位、曝光和噪声方差是否相同？
- 哪些像素在某些帧中被遮挡、饱和或根本不在视野内？

#figure(
  image("../svg/multiframe_forward_model.svg", width: 98%),
  caption: [多帧成像的统一前向模型。每帧从同一潜在场景出发，却具有不同几何、模糊、采样和噪声；配准与曝光归一化后才能按任务融合。],
) <fig:multiframe-forward-model>

=== 配准究竟在估计什么

选择参考帧后，配准估计一个变换 $T_r$，使重采样后的帧

$
  tilde(y)_r(bold(p))
  =y_r(T_r(bold(p)))
$ <eq:registered-frame-resampling>

与参考坐标中的结构尽量一致。变换复杂度必须与场景匹配：

- 远景和小幅手抖常可由二维平移或旋转近似；
- 相机绕投影中心旋转，或场景近似为平面时，可用单应变换；
- 相机发生平移且场景有深度差时，不同深度产生不同视差；
- 人物、树叶、水面和烟雾需要局部运动场，甚至没有唯一像素对应；
- 滚动快门使变换随扫描行与时间改变。

@chap:projective-geometry 已经说明单应模型的条件，@chap:motion-stabilization 讨论运动与滚动快门。本章只强调：使用更复杂的变换可以降低某些残差，也会增加错误自由度。把水波强行扭到参考帧，数值上可能“对齐”，语义上却不再是同一表面点。

常见配准证据有两类。特征法先找角点或纹理描述，再用鲁棒估计排除错误匹配；直接法调整变换，使亮度或梯度差最小。前者依赖可重复结构，后者依赖曝光与外观近似一致。实际系统常把陀螺仪、镜头模型、图像金字塔和局部匹配组合使用，但这些实现细节不改变模型边界。

=== 亚像素配准误差会变成额外 PSF

设真实几何已大致校正，但第 $r$ 帧仍有残余平移 $bold(e)_r$。若直接平均，等效配准核为

$
  h_("reg")(bold(u))
  =1/N sum_(r=1)^N delta(bold(u)-bold(e)_r).
$ <eq:registration-error-kernel>

最终图像被这个核额外卷积。残差均值不为零会整体偏移，残差分布较宽会软化边缘；偶发大错位则形成重影。增加帧数只会更准确地平均这个错误分布，并不会让错误自己抵消。

亚像素变换还必须重采样。过于锐利的插值会振铃，过于平滑的插值会损失高频，多次来回变换会重复低通。较稳妥的流程是组合几何变换后只重采样一次，并让最终输出网格与目标分辨率一致。

=== 独立噪声平均的平方根规律

若配准后的帧为

$
  tilde(y)_r=x+n_r,
  quad "E"[n_r]=0,
  quad "Var"[n_r]=sigma^2,
$

且各帧噪声独立，则等权平均

$
  hat(x)=1/N sum_(r=1)^N tilde(y)_r
$ <eq:multiframe-simple-average>

满足

$
  "Var"[hat(x)]=sigma^2/N,
  quad "SNR"_N=sqrt(N) "SNR"_1.
$ <eq:multiframe-average-snr>

标准差减半需要 4 帧，减为四分之一需要 16 帧。换算到散粒受限曝光，4 帧平均近似等于同一总积分时间的一次长曝光，但前者允许在帧间估计运动、排除异常或避免单帧饱和；它也要多次支付读出噪声。

若各帧方差不同，独立高斯近似下的逆方差加权为

$
  hat(x)
  =frac(sum_r tilde(y)_r/sigma_r^2,
         sum_r 1/sigma_r^2),
  quad
  "Var"[hat(x)]
  =1/(sum_r 1/sigma_r^2).
$ <eq:inverse-variance-multiframe-fusion>

长曝光阴影通常权重更高，高光一旦接近饱和则应迅速降权。权重若依赖图像内容，融合会变成非线性估计，边缘与运动区域必须另行处理。

=== 为什么固定图样与相关噪声不按同一规律下降

若每帧噪声分成稳定项 $c$ 和独立项 $epsilon_r$：

$
  n_r=c+epsilon_r,
  quad "Var"[c]=sigma_c^2,
  quad "Var"[epsilon_r]=sigma_epsilon^2,
$

则平均后的方差为

$
  "Var"[hat(x)-x]
  =sigma_c^2+sigma_epsilon^2/N.
$ <eq:multiframe-correlated-noise-floor>

帧数增加后，独立部分继续下降，固定图样、条带、热像素和模型偏差形成平台。相机的微小位移有时能让场景相对固定传感器缺陷移动，配准到场景坐标后再平均会削弱这些缺陷；若相机完全不动，它们会与场景一起稳定留下。

#figure(
  image("../svg/averaging_limits.svg", width: 98%),
  caption: [多帧平均的收益与边界。独立噪声按 $1/sqrt(N)$ 下降，相关或固定项形成噪声底；主体运动和视差若未正确解释，会转化为重影或双边缘。],
) <fig:averaging-limits>

#paragraph-heading[鲁棒融合与运动区域]

逐像素中位数能排除少数瞬时异常，截尾平均和稳健权重则在平均效率与异常排除间折中。这适合去除偶尔经过的行人、飞虫或传感器瞬态错误，却隐含“多数帧代表背景”的假设。若主体在大部分帧中移动，把它当异常删除并不比保留重影更接近唯一真相。

实际算法常把画面分成静态区与运动区：静态区充分合并，运动区降低帧数、选择参考帧或使用局部运动补偿。这会产生新的接缝风险——同一照片内不同区域拥有不同有效曝光时间、噪声纹理和运动时刻。

== HDR 合成：不同曝光覆盖不同区间

=== 先回到共同辐射尺度

设场景某点的线性辐射量为 $L$，第 $r$ 帧曝光尺度为 $a_r$。理想线性 RAW 在未饱和区近似

$
  y_r-b_r=a_r L+n_r,
$ <eq:hdr-linear-exposure-model>

其中 $b_r$ 是黑电平。归一化估计为 $(y_r-b_r)/a_r$。若输入是带未知曲线的 JPEG，则观测更接近

$
  z_r=g(a_r L)+n_r,
$ <eq:hdr-response-curve-model>

必须估计或知道响应函数 $g$，否则不能把不同曝光码值当作同一线性量直接平均 @debevec1997。白平衡、局部色调和机内锐化若随帧改变，还会破坏单一响应曲线假设。

综合多帧的辐射估计可写成

$
  hat(L)
  =frac(
    sum_r w_r(y_r)(y_r-b_r)/a_r,
    sum_r w_r(y_r)
  ).
$ <eq:hdr-weighted-radiance-estimate>

$w_r$ 在读出噪声主导的深阴影应较小，在接近通道饱和处也应趋近零。中间区可按方差、距离饱和的余量和配准可信度共同加权。不同颜色通道有不同饱和点，权重不能只看合成亮度。

#figure(
  image("../svg/hdr_exposure_coverage.svg", width: 98%),
  caption: [包围曝光的互补测量区间。长曝光提高阴影 SNR 却较早饱和，短曝光保住高光却让阴影靠近噪声底；融合使用各帧可靠区，而不是简单取平均。],
) <fig:hdr-exposure-coverage>

#paragraph-heading[包围档数不等于可用动态范围直接相加]

三张相差两档的照片并不自动增加六档可用范围。相邻曝光必须有足够重叠区校准响应与颜色；最短曝光仍有饱和上限，最长曝光仍有噪声下限；镜头耀斑、暗电流和量化也不会因包围而消失。

合理包围应从场景与任务反推：最短帧让重要高光各通道保留余量，最长帧让重要阴影达到目标 SNR，中间帧提供重叠和运动保险。机械地固定“±2 EV 三张”只是方便，不是普遍最优设计。

#paragraph-heading[去鬼影是在多个时刻之间作选择]

若树叶、云、水面、人物或灯光在曝光间变化，$L$ 不再是同一个未知量。算法可以：

- 选一张参考帧保留运动时刻，只从其他帧借用静态区域；
- 估计局部运动后对齐，但遮挡与非刚体变形仍没有一一对应；
- 对变化区域使用单帧色调映射，以噪声换取无鬼影；
- 对周期运动或水流保留时间平均，把变化当作表达而非错误。

不存在同时保留每一帧所有瞬间的单一静态照片。去鬼影不是单纯清除伪影，而是在“哪个时刻”和“哪些区域属于同一场景”之间作选择。

#paragraph-heading[HDR 采集、色调映射与 HDR 显示]

HDR 采集扩大可估计的场景辐射范围；色调映射把该范围压缩或重分配到输出；HDR 显示则用更高峰值、更低黑位或不同传递函数呈现结果。@chap:tone-encoding 已经讨论后两者。

一组包围曝光可以输出普通 SDR，也可以生成 HDR 文件；一张单曝光 RAW 也可通过色调映射产生“局部 HDR”外观。不要用处理风格倒推采集动态范围，更不要把局部对比增强当成恢复了已经裁切的高光。

== 堆栈、超分辨率与时间统计

#figure(
  image("../svg/multiframe_tasks.svg", width: 98%),
  caption: [多帧任务由融合算子区分。平均降低独立噪声，HDR 选择不同曝光的可靠区，超分辨率联合互补采样相位，焦点和时间堆栈则按空间或时间规则选择。],
) <fig:multiframe-tasks>

=== 多帧超分辨率：补充采样，不补光学零点

@chap:sampling-resolution 已经给出亚像素位移采样模型。多帧超分辨率先估计每帧的位移、模糊和采样差异，再寻找一个能同时解释各帧观测的高分辨率图像。不同帧的子像素相位提供单帧晶格没有的约束，因此可恢复超过单帧奈奎斯特、但仍由光学系统传递的频率 @farsiu2004。若镜头 OTF 已为零、各帧完全同相位或配准误差大于要恢复的尺度，提高输出像素数只会得到插值或先验纹理。

#struct-box(title: [可以继续深入])[多帧超分辨率可写成联合数据一致性与正则项的优化问题，并按各帧噪声协方差加权。本书后文只需要它的成立条件：存在互补采样相位、配准足够准确，而且目标频率仍通过光学系统。]

像素位移模式还可让同一场景位置在不同帧落到不同 CFA 颜色上，减少去马赛克插值并改善色彩采样。它要求场景、光照和焦点高度稳定；树叶、水面和移动人物会在高分辨率合成中产生彩边或网格状伪影。

#paragraph-heading[景深合成：每个位置选择不同焦点]

焦点堆栈拍摄一组不同对焦距离的图像，估计各帧局部清晰度，再生成选择图或加权图。理想情况下，输出每个位置来自最清晰的一帧；实际困难包括：

- 镜头对焦时视角和放大率变化，即“呼吸效应”；
- 前景虚化会遮住背景，后景清晰帧中被遮挡区域可能从未完整出现；
- 清晰度测度容易把噪声、高反差光晕当成细节；
- 选择图边界不平滑会形成接缝、双边缘和局部光晕；
- 主体在堆栈期间移动，会让不同区域来自不一致时刻。

因此景深合成不是把景深公式作废，而是用多次不同焦平面观测重组一张图。对微距和静物，稳定支撑、固定光线、足够焦点重叠和较小步进比后期“自动补洞”更可靠。

#paragraph-heading[时间堆栈不是只有平均]

对同一像素的时间序列 $y_1,...,y_N$，不同统计量表达不同问题：

- 求和或按曝光时间平均近似更长积分，可形成车轨、水流平滑和低噪声静态场景；
- 中位数保留多数时刻的值，可移除短暂行人，却会删除只出现少数帧的真实主体；
- 最大值合成保留任一帧最亮值，适合视觉化星轨或光绘，但不等价于物理长曝光的能量积分；
- 最小值、分位数和时间方差可用于特殊分析，但不应混称为“自然曝光”。

操作名称相似，物理意义却不同。判断输出能否用于测量或纪实，必须记录采用的是积分、平均、选择还是非线性统计。

#paragraph-heading[全景拼接只保留必要边界]

全景也是多帧计算，但其核心几何已在 @chap:projective-geometry 说明：相机绕投影中心旋转且场景较远时，单应变换可很好拼接；相机平移和近景视差会产生接缝。多尺度融合可隐藏亮度差与低频接缝 @burt1983，却不能让被遮挡结构同时在两种视点下成立。

本书不再展开柱面、球面投影和接缝优化的工程细节。摄影上最重要的是在拍摄时固定曝光、白平衡与焦点，保持足够重叠，并尽量绕入瞳附近旋转以减少近景视差。

== 手机计算摄影

=== 快门按钮对应一段时间窗口

手机通常持续缓存预览帧。按下快门时，系统可能从按键前后的一组图像中选择参考帧，再依据清晰度、表情、手抖和曝光合并其他帧。最终照片的“时刻”因此可能是一个参考瞬间加上一段时间内的统计信息，而不是单个机械快门事件。

小传感器和小物理孔径限制单帧光子数，多帧正好用时间补偿：静态区可充分平均，运动区退回少量帧；短帧保高光，长帧提阴影；自然手抖提供亚像素采样相位。移动设备的优势不在于取消物理限制，而在于自动调度这些交换 @hasinoff2016。

#figure(
  image("../svg/mobile_computational_pipeline.svg", width: 98%),
  caption: [手机多帧处理链。连续缓存提供候选观测，系统选择参考帧、局部配准并按运动置信度融合，再完成颜色与色调；帧内模糊、错配、多摄视差和深度先验分别产生不同伪影。],
) <fig:mobile-computational-pipeline>

#paragraph-heading[夜景模式为何既更干净又可能更“塑料”]

弱光下，系统会在总拍摄时间、单帧快门、ISO、帧数和运动风险间分配预算。手持时采用较短帧并对齐，放在稳定支撑上则可使用更长帧；检测到人物或宠物运动时，运动区域可能使用更短有效积分。

若配准成功，阴影噪声和色斑显著下降；若细纹理在帧间不稳定，鲁棒融合会把它当异常，产生水彩或塑料感。树叶、水面、烟雾和远处密集灯光是常见压力区域。结果越干净，不代表所有区域都实际用了相同帧数和曝光时间。

#paragraph-heading[多摄像头融合不是无缝光学变焦]

不同摄像头具有不同位置、焦距、孔径、传感器、颜色响应和对焦距离。远景可较好对齐，近景会因基线产生视差和遮挡；一个镜头看见的边缘可能被另一个镜头遮住。切换或融合还要统一白平衡、噪声纹理、锐度和透视。

所谓连续变焦常在光学切换、数字裁切和跨摄融合之间过渡。中间倍率可能主要依赖裁切与超分辨率；弱光时长焦小孔径相机信噪比不足，系统还可能回退到主摄裁切。判断画质应看具体倍率、照度、主体距离和输出尺寸，而不是只看摄像头数量。

#paragraph-heading[数字虚化依赖深度与遮挡模型]

数字虚化先估计每个像素的深度或前后层级，再按模拟孔径生成空间变化模糊。深度可来自双摄视差、双像素、飞行时间传感器、运动视差或单目学习先验。

困难集中在深度不连续处：头发、透明玻璃、网纱、反射、高光和细枝同时包含前景与背景贡献，单一深度标签并不完整。真实光学虚化还受入瞳形状、像差、遮挡和离焦方向影响；简单高斯模糊容易在轮廓处漏出背景、切断发丝或产生纸片感。

数字虚化可以是有效的表达工具，但不应把深度图边缘的算法错误归因于镜头焦外。关闭效果、比较不同距离和检查发丝与透明物，是最快的诊断方法。

== 拍摄与融合的最小决策流程

#paragraph-heading[先选“需要合并的量”]

- 要降低噪声：尽量固定曝光、焦点和构图，保证静态区域能配准；
- 要扩展动态范围：让最短帧保住高光、最长帧满足阴影 SNR，并留重叠区；
- 要提高采样：需要受控亚像素位移、稳定场景和足够好的镜头传递；
- 要扩展景深：按景深重叠安排焦点，不改变光线和主体；
- 要表达时间：先决定使用积分、平均、中位数还是最大值，而不是事后随意叠加。

#paragraph-heading[拍摄阶段优先减少不可建模变化]

稳定支撑能减少全局运动，却不冻结主体；更短单帧能减少帧内模糊，却要用更多帧补足光子；固定白平衡和手动曝光可减少帧间外观变化，但 HDR 本身需要受控曝光差。对焦呼吸、光圈变化、频闪和自动镜头切换也应关闭或记录。

多帧成功往往来自“让变化只有一种”：做 HDR 时只改变曝光，做焦点堆栈时只改变焦点，做像素位移时只改变已知采样相位。变量同时漂移，后期就很难判断该校正哪一个。

#paragraph-heading[输出检查要寻找多帧特有伪影]

除普通噪声、锐化和颜色问题外，还应检查：

- 人物和树叶边缘是否有半透明重影；
- 前后景交界是否出现双边缘或拉伸纹理；
- 静态区和运动区的噪声颗粒是否突然变化；
- 高光附近是否有融合接缝、通道换色或局部光晕；
- 重复纹理、文字和星点是否因错配被复制或删除；
- 不同镜头过渡区的透视、色彩和清晰度是否跳变。

保留单帧、融合前对齐结果和运动掩模，能帮助判断问题来自拍摄、配准还是融合。只保存最终 JPEG，会把这些证据压扁成一个无法拆解的结果。

== 常见误解

#paragraph-heading[“平均 N 张，画质就一定提高 $sqrt(N)$ 倍”]

平方根规律要求同一信号、独立零均值噪声和正确配准。固定图样、主体运动、错配与模型偏差不会按该规律下降；强相关帧的有效样本数远小于文件数。

#paragraph-heading[“HDR 就是把三张曝光拉到一起”]

不同曝光必须先回到共同辐射尺度，并根据噪声、饱和和运动加权。直接平均非线性 JPEG 会改变亮度和颜色，强行对齐运动区域则产生鬼影。

#paragraph-heading[“多帧超分辨率突破了镜头极限”]

它能补充单帧采样相位，恢复镜头仍传递但晶格未充分采样的频率；光学 OTF 零点、严重运动模糊和场景外内容仍不能由位移帧唯一恢复。

#paragraph-heading[“焦点堆栈等于所有地方都像同一张照片那么自然”]

不同位置来自不同焦点和可能不同时间。遮挡、焦点呼吸、虚化前景与选择图接缝会改变空间关系，结果是计算合成而非一次统一光学成像。

#paragraph-heading[“手机夜景只是自动长曝光”]

它通常是多帧、运动感知、区域自适应的联合处理。静态和运动区域可能采用不同有效曝光与处理强度，输出时间也不一定等于某一单帧。

#struct-box(title: [本章小结])[
  - 多帧可统一写成 $bold(y)_r=D_r H_r W_r bold(x)+bold(n)_r$；融合前必须解释几何、模糊、采样、曝光和噪声差异。
  - 配准模型应匹配场景：平移、单应、局部运动和深度模型各有条件；残余错位会成为额外 PSF，而不是随帧数消失。
  - 独立等方差噪声平均后方差为 $sigma^2/N$、SNR 提升 $sqrt(N)$；相关项与固定图样形成无法靠继续平均突破的平台。
  - HDR 融合先把不同曝光归一到共同辐射尺度，再使用未饱和且高 SNR 的区间；去鬼影实质上还要选择时间与区域。
  - 多帧超分辨率补充采样相位，不突破光学零点；焦点堆栈与时间堆栈分别使用空间选择和时间统计，物理意义不同。
  - 手机计算摄影用缓存、传感器和多摄像头自动调度时间—画质交换，但帧内模糊、视差、错配和深度边界仍留下可诊断伪影。
  - 多帧拍摄最好一次只控制一种变化，并保留单帧与中间结果；文件数不是有效观测数，最终外观也不自动证明各区域来自同一时刻。
]

#pagebreak(weak: true)

== 思考与计算

#block[
  1. 单帧静态区域信号为 400 电子、独立读出噪声为 5 电子。分别计算 1、4、16 帧数字求和与平均的信号、方差和 SNR；与一次收集四倍、十六倍光子的长曝光比较读出噪声代价。
  2. 噪声由每帧共享的标准差 3 电子固定项和标准差 8 电子独立项组成。用 @eq:multiframe-correlated-noise-floor 计算 1、4、16、64 帧平均后的标准差，并求帧数趋于无穷的极限。
  3. 两帧配准残差分别为 $-0.4$ 与 $+0.4$ 像素。写出 @eq:registration-error-kernel，说明它对阶跃边缘产生怎样的双边结构；若再增加许多具有同样两点分布的帧，模糊会否消失？
  4. 三张线性 RAW 曝光尺度为 $1,4,16$，某场景点的未裁切码值分别为 $120,470,1850$，黑电平为 20。分别估计归一化辐射量；若最长曝光白电平为 1600，应如何重新加权？
  5. 为室内窗景设计包围曝光。要求窗外云层不裁切，室内暗角达到 SNR=10，并给相邻曝光至少一档高质量重叠。列出需要先测量的量，而不是直接套用固定档数。
  6. 拍摄有风树叶的手持 HDR。分别设计“保留参考时刻”“表现运动平均”和“最大化阴影 SNR”三种融合目标，说明它们为何不可能给出同一运动区域结果。
  7. 四帧采样相位分别为 $(0,0),(0.5,0),(0,0.5),(0.5,0.5)$ 像素。解释它们怎样补充二维采样；若每帧配准误差标准差为 0.4 像素，预期高分辨率收益为何会下降？
  8. 为一件带前后交叠细枝的微距静物设计焦点堆栈。记录焦点步进、光圈、呼吸校正、遮挡边界和主体稳定性，并预测最可能出现接缝的位置。
  9. 同一组夜景帧分别做平均、中位数和最大值合成。对静态建筑、偶尔经过的行人、车灯和随机噪声，逐项预测三种结果；哪一种最接近物理长曝光？
  10. 选择一张手机夜景或数字虚化照片，检查静态区与运动区噪声、头发与透明物边界、重复灯光和镜头过渡。根据伪影推断处理链最可能在哪一步失效。
]
