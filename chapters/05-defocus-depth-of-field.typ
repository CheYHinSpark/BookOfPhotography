#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 对焦、离焦与景深 <chap:defocus-depth-of-field>

理想薄透镜只把一个物平面精确成像到一个像平面。传感器一旦固定，严格合焦的轴向位置在一阶模型中没有厚度；所谓景深，是把有限离焦斑判定为“仍可接受”后得到的容差区间。因此，景深不是镜头在空间中创造的一堵清晰墙，而是光学模糊、传感器采样、输出放大和观看条件共同定义的工程量。

本章从光束锥的相似三角形出发，推导离焦斑直径、近景界、远景界与超焦距；再讨论弥散圆阈值的感知来源、画幅等效、近摄极限和焦外点扩散函数。最后把自动对焦写成估计与反馈控制问题。摄影光学中的景深和焦外分析可参见 @ray2002 与 @kingslake1992。

@chap:gaussian-optics 给出了严格合焦的物像共轭，本章把“是否合焦”改写成“误差是否仍可接受”。这个变化把镜头几何第一次连接到输出尺寸和观看条件；随后讨论光量、衍射和采样时，我们都将继续使用这种“物理分布加任务阈值”的判断方式。

#key-box[
  *基本事实*　合焦是物平面与传感器平面满足共轭关系；景深则是允许共轭误差后得到的集合。改变输出尺寸或观看距离，即使相机与镜头完全不动，景深判定也会改变。
]

== 从共轭误差到离焦斑

=== 对焦距离与传感器位置

以下先采用空气中的理想薄透镜，并用正距离大小表示实物和实像。镜头焦距为 $f$，对焦物距为 $s_0>f$，传感器被放在对应像距

$ v_0 = f s_0/(s_0-f). $ <eq:focused-sensor-position>

另一个物点位于距离 $s>f$，其光束本应会聚在

$ v(s)=f s/(s-f). $ <eq:object-conjugate-position>

若 $v(s) != v_0$，传感器截取的不是光束锥尖端，而是一个有限截面。轴向共轭误差为

$ Delta v = v(s)-v_0. $ <eq:axial-defocus>

=== 离焦圆直径

设入瞳直径为 $D=f/N$。在忽略像差、衍射和瞳放大率的薄透镜模型中，光束从镜头处直径 $D$ 线性收缩，在 $z=v(s)$ 处变为零。由相似三角形，传感器平面上的离焦圆直径为

$ c(s;s_0)=D abs(1-v_0/v(s)). $ <eq:defocus-cone-basic>

#figure(
  image("../svg/defocus_cone.svg", width: 95%),
  caption: [离焦斑来自传感器平面与真实共轭像平面不重合。光束锥在传感器处的截面直径即几何离焦圆 $c$。],
) <fig:defocus-cone>

把 @eq:focused-sensor-position 和 @eq:object-conjugate-position 代入 @eq:defocus-cone-basic，可化为

$
  c(s;s_0)
  = f^2/N dot abs(s-s_0)/(s(s_0-f)).
$ <eq:defocus-circle-object-space>

这个公式直接给出四个趋势：

- 在 $s=s_0$ 处几何离焦为零；
- f 数 $N$ 增大，即光圈缩小时，离焦圆按 $1/N$ 缩小；
- 焦距增加会显著增大离焦敏感性，但同时改变视角和工作距离；
- 物距接近焦距时，$s_0-f$ 不能被近似为 $s_0$，普通远摄景深公式开始失效。

#paragraph-heading[几何圆不是完整点扩散函数]

@eq:defocus-circle-object-space 把孔径看成均匀圆盘，并把几何光线截面直接当作点扩散函数。真实离焦像还叠加：

- 有限孔径衍射；
- 球差、像散、场曲和色差；
- 光圈叶片形状与机械遮挡；
- 传感器微透镜、像素面积积分和去马赛克；
- 后期锐化、降噪和缩放。

因此 $c=0$ 只表示几何共轭位置正确，不表示点像具有零尺寸。实际最佳焦点也可能因像差在不同空间频率下略有不同。

== 弥散圆是输出容差

=== 从观看角分辨率反推传感器阈值

设输出图像宽度为 $W_o$，其对应的传感器或裁切区域宽度为 $W_s$，线性放大率为

$ M_o=W_o/W_s. $ <eq:output-magnification>

观察距离为 $L$，观察者在给定对比度下可容许的模糊角为 $epsilon$ 弧度，则输出面允许模糊直径约为

$ b_o approx L epsilon. $ <eq:output-blur-tolerance>

换算回传感器，允许弥散圆为

$ c_0 = b_o/M_o approx L epsilon W_s/W_o. $ <eq:sensor-coc-from-viewing>

@eq:sensor-coc-from-viewing 说明，弥散圆不是由画幅对角线神秘决定的常数。它隐含了输出尺寸、裁切、观看距离、视觉对比敏感度和“可接受”标准。传统的对角线除以某个经验数，只是把一组典型观看条件预先折叠成表格。

#paragraph-heading[像素与弥散圆]

若像素间距为 $p$，选择 $c_0=p$ 意味着要求几何离焦约不超过一个像素，但这并不是普适清晰标准：

- Bayer 传感器的颜色与亮度采样结构不同；
- 光学点扩散函数在合焦时已经覆盖若干像素；
- 最终图像可能缩小，多个传感器像素合成一个输出像素；
- 高反差边缘对模糊更敏感，低反差纹理更不敏感；
- 锐化可以改变边缘观感，却不能恢复被离焦消除的高频信息。

像素级审查适合技术测试，不等同于正常观看条件下的景深。

=== 衍射给出的另一尺度

圆孔衍射艾里斑第一暗环直径约为

$ d_("Airy")=2.44 lambda N. $ <eq:airy-diameter-preview>

当 $d_("Airy")$ 与 $c_0$ 同数量级时，继续缩小光圈虽然减小几何离焦，却同时扩大衍射斑。景深增加与焦内微细节下降并不矛盾，因为两者使用的模糊来源不同。

#warn-box[
  *景深表必须带条件*　若不说明弥散圆、画幅裁切和输出观看条件，“某焦距在 f/8 的景深是多少”没有唯一答案。
]

== 近景界、远景界与超焦距

=== 超焦距的定义

令无穷远物点在传感器上的离焦圆恰等于阈值 $c_0$。在 @eq:defocus-circle-object-space 中取 $s arrow.r infinity$，有

$ c_infinity = f^2/(N(s_0-f)). $ <eq:infinity-defocus>

令 $c_infinity=c_0$，得到超焦距

$ H=f^2/(N c_0)+f. $ <eq:hyperfocal-distance>

这里 $H$ 是对焦距离，而不是从前焦点量起的距离。远摄条件下 $f$ 相对 $H$ 很小，常近似写成 $H approx f^2/(N c_0)$。

=== 精确近景界与远景界

为简化式子，定义

$ H_0=H-f=f^2/(N c_0). $ <eq:reduced-hyperfocal>

在薄透镜模型中令 $c(s;s_0)=c_0$，可得近景界

$ s_n = s_0 H_0/(H_0+s_0-f), $ <eq:dof-near-exact>

以及当 $s_0<H$ 时的远景界

$ s_f = s_0 H_0/(H_0-s_0+f). $ <eq:dof-far-exact>

若 $s_0>=H$，无穷远的离焦圆已经不超过 $c_0$，远景界记为无穷远。把 $H_0=H-f$ 代入，可看到 @eq:dof-far-exact 的分母在 $s_0=H$ 时为零，正好对应超焦距定义。

#figure(
  image("../svg/dof_boundaries.svg", width: 97%),
  caption: [景深是离焦函数落在阈值 $c_0$ 以下的物距集合。对焦到超焦距 $H$ 时，远景界延伸到无穷远，近景界精确为 $H/2$。],
) <fig:dof-boundaries>

当 $f$ 相对所有物距可忽略时，常用近似为

$ s_n approx H s_0/(H+s_0), $ <eq:dof-near-approx>

$ s_f approx H s_0/(H-s_0). $ <eq:dof-far-approx>

近摄或大放大率下不应使用这组远摄近似。

=== 景深并不前后对称

令

$ A=(s_0-f)/H_0. $ <eq:dof-asymmetry-parameter>

则 $s_n=s_0/(1+A)$、$s_f=s_0/(1-A)$。焦点到远界与近界的距离分别为

$ Delta_s^("near")=s_0 A/(1+A), $ <eq:near-depth-extent>

$ Delta_s^("far")=s_0 A/(1-A). $ <eq:far-depth-extent>

只要远界有限，远侧范围总大于近侧，二者比值为 $(1+A)/(1-A)$，随对焦距离接近超焦距而迅速增大。“景深约三分之一在前、三分之二在后”只在某些 $A$ 值附近成立，不是普遍定律。

=== 对焦到超焦距

令 $s_0=H$，由 @eq:dof-near-exact 得

$ s_n=H/2, quad s_f=infinity. $ <eq:hyperfocal-dof>

这意味着从 $H/2$ 到无穷远的几何离焦不超过 $c_0$，并不意味着该区间处处同样锐利。最佳几何焦点仍在 $H$；无穷远和 $H/2$ 只是在阈值边界上。若输出放大或使用更严格的 $c_0$，超焦距会向远处移动。

== 景深的变量依赖

#paragraph-heading[光圈、焦距与工作距离不能孤立比较]

@eq:defocus-circle-object-space 看似表明景深随 $f^2$ 变化，但摄影比较经常同时固定主体构图。若焦距增加并后退，使主体放大率保持不变，物距也随之增加，焦距的部分影响被抵消。景深结论必须明确固定：

- 机位还是主体像高；
- 物方视角还是焦距；
- f 数还是入瞳直径；
- 传感器弥散圆还是最终输出阈值；
- 对焦距离还是放大率。

不声明约束条件，“长焦景深更浅”只能算不完整的经验句。

=== 画幅等效

设大画幅的线性尺寸是小画幅的 $k$ 倍。若机位、视角和输出尺寸相同，则焦距与传感器弥散圆阈值都按 $k$ 缩放：

$ f_L=k f_S, quad c_L=k c_S. $ <eq:format-scaling>

在物距和超焦距远大于焦距、可忽略 @eq:hyperfocal-distance 中加性 $f$ 项时，要使景深近似相同，需要

$ N_L=k N_S. $ <eq:dof-equivalent-fnumber>

于是两套系统的入瞳直径相同：

$ D_L=f_L/N_L=f_S/N_S=D_S. $ <eq:equivalent-entrance-pupil>

#figure(
  image("../svg/format_dof_equivalence.svg", width: 96%),
  caption: [相同机位、视角和输出下的景深等效。大画幅使用 $k$ 倍焦距与 $k$ 倍 f 数，最终得到相同入瞳直径。],
) <fig:format-dof-equivalence>

因此，在固定视角和构图时，景深更直接地与入瞳直径相关。大画幅若使用相同 f 数，入瞳更大，景深更浅；若把 f 数乘以画幅比例，则景深和相对输出衍射尺度都可近似匹配。噪声、像素数和镜头像差是否等效，需要另行分析。

=== 近摄与放大率

当放大率 $m$ 不可忽略时，镜头的有效 f 数约为 $N_("eff")=N(1+m)$，这里假设瞳放大率为 1。小范围内的物方总景深近似为

$ "DOF"_("macro") approx 2 N c_0 (1+m)/m^2. $ <eq:macro-dof>

例如 $m=1$、$N=8$、$c_0=0.03 "mm"$ 时，总景深约为 $0.96 "mm"$。微距中移动几毫米就可能完全离焦，且缩小光圈还会因有效 f 数增加而更早遇到衍射。

一般镜头应把 $1+m$ 替换为含瞳放大率的因子；内对焦微距镜头的标称焦距也可能随距离变化。因此 @eq:macro-dof 是数量级模型，不是所有镜头的标定公式。

== 焦外成像不是一个数字

=== 几何离焦点扩散函数

在理想、圆形、均匀照明的出瞳下，几何离焦点像近似为半径 $a=c/2$ 的均匀圆盘：

$
  h_("def")(r)
  = cases(
    1/(pi a^2) & r<=a,
    0 & r>a,
  ).
$ <eq:ideal-defocus-psf>

它的积分为 1，表示能量只被重新分布。局部近似线性且空间不变时，离焦图像是清晰图像与 $h_("def")$ 的卷积；但真实镜头的点扩散函数会随像高、波长、对焦方向和光圈改变。

#paragraph-heading[离焦量与焦外质量]

离焦圆直径 $c$ 衡量模糊范围；焦外质量则取决于圆盘内部的能量分布。相同 $c$ 下：

- 中心亮、边缘柔和的光斑通常产生平滑过渡；
- 边缘亮的光斑会把线条变成双边轮廓，出现“二线性”；
- 多边形光圈会把高光变成相应多边形；
- 非球面模压纹理可能形成同心“洋葱圈”；
- 轴向色差会让前后景焦外边缘带不同颜色。

所以“大光圈”和“奶油虚化”不是同义词。前者主要增大离焦尺度，后者还要求点扩散函数具有合适的内部结构。

=== 口径蚀与猫眼光斑

轴上点看到完整出瞳，焦外光斑可近似圆形；离轴点的光束可能被镜框或其他元件部分截断，出瞳投影变成椭圆或猫眼形。越靠画面边缘、光圈越大，机械截断通常越明显。

#figure(
  image("../svg/bokeh_pupil_shapes.svg", width: 95%),
  caption: [焦外点像近似携带出瞳的形状与照度分布。光圈叶片、离轴遮挡和球差都会改变光斑，而不仅是改变直径。],
) <fig:bokeh-pupil-shapes>

机械口径蚀还会降低边缘像面照度，并使焦外高光方向指向或背离画面中心。它与数字后期添加的均匀高斯模糊具有不同空间结构。

#paragraph-heading[前景与背景焦外可以不同]

理想对称薄透镜中，大小相同的正负离焦只改变光束锥截取位置；含球差的真实镜头在焦前与焦后可能呈现相反的亮边结构。为某一侧优化的柔和焦外，另一侧未必同样平滑。

这也是只看一张背景虚化样片不足以评价镜头焦外的原因。至少应比较不同像高、不同光圈、不同离焦量以及焦前/焦后两侧。

== 自动对焦作为估计问题

=== 反差检测

设镜头驱动位置为 $a$，相机从图像区域计算高频或梯度评价函数，例如

$ J(a)=sum_(bold(x) in Omega) norm(nabla I_a(bold(x)))^2. $ <eq:contrast-af-metric>

理想情况下，合焦位置附近高频能量最大，自动对焦寻找

$ hat(a)=arg max_a J(a). $ <eq:contrast-af-optimization>

单次反差值通常不告诉系统焦点在传感器前还是后，因此需要试探移动或利用历史样本。噪声、重复纹理、主体运动和局部高反差背景都可能制造错误极值。

=== 相位检测

相位检测把入瞳分成两个子孔径，分别形成一幅子像。设两个子孔径中心间基线为 $B$，物点的真实共轭像距为 $v$，传感器位于 $v_0$，两子像在传感器上的相对位移近似为

$ delta = B(1-v_0/v). $ <eq:phase-af-disparity>

它与 @eq:defocus-cone-basic 具有相同几何因子，但保留符号：$delta$ 的方向告诉系统应向前还是向后移动，对应幅度则估计需要移动多少。片上相位像素和双像素对焦都在不同形式下利用这种子孔径视差。

#figure(
  image("../svg/autofocus_models.svg", width: 97%),
  caption: [左：反差对焦搜索清晰度评价函数的极大值。右：相位对焦用两个子孔径图像的相对位移估计离焦符号与幅度。],
) <fig:autofocus-models>

=== 连续对焦与反馈延迟

运动主体的对焦不是静态求根。系统要从相位差或图像测量中估计主体距离与速度，预测曝光时刻的位置，再驱动有惯性和回差的镜组。测量噪声、主体突然变向和驱动延迟都会造成预测误差；高速连拍还要在曝光、读出、计算与镜头驱动之间分配时间。

#struct-box(title: [可以继续深入])[连续自动对焦可写成带过程噪声与测量噪声的状态空间模型，并使用 Kalman 滤波、数据关联和反馈控制预测主体运动。本书后文只使用结论：对焦点必须预测到曝光时刻，延迟与错误主体选择会造成独立于运动模糊的失焦。]

眼部识别和主体识别解决“对哪里对焦”的语义选择，相位或反差检测解决“怎样到达该焦点”的物理估计。两层算法都正确，最终照片仍可能因主体在曝光期间运动而模糊；对焦误差与运动模糊必须分开诊断。

#paragraph-heading[峰值对焦与放大观察]

峰值对焦把局部高频区域用颜色标出，本质上是实时边缘指标。纹理丰富区域即使稍离焦也可能被标记，低反差但真正合焦的表面可能没有提示。放大观察更接近直接检查采样图像，但仍受预览锐化、分辨率和显示缩放影响。

== 常见误解

#paragraph-heading[“景深范围内都同样清晰”]

离焦圆从合焦平面的最小值连续增大。近景界和远景界只是 $c=c_0$ 的阈值位置，区间内部清晰度仍有梯度。

#paragraph-heading[“超焦距让一半距离到无穷远全部合焦”]

只有对焦平面严格满足几何共轭。$H/2$ 与无穷远都处于允许弥散圆边界；提高输出要求后，它们可能不再可接受。

#paragraph-heading[“画幅本身直接决定景深”]

画幅通过所需焦距、弥散圆换算和构图约束间接参与。若匹配视角、机位、输出和入瞳直径，不同画幅可得到近似相同景深。

#paragraph-heading[“防抖或高速快门可以修复对焦错误”]

防抖降低相机姿态变化，高速快门减少运动积分；二者都不能把传感器移到正确共轭平面。反之，精确对焦也不能冻结移动主体。

#struct-box(title: [本章小结])[
  - 对焦距离决定传感器共轭位置；其他物距在传感器上形成有限离焦截面。
  - 理想薄透镜离焦圆由 @eq:defocus-circle-object-space 给出，并随物距连续变化。
  - 弥散圆阈值来自输出放大、观看距离与视觉容差，不是画幅的固有常数。
  - 超焦距由 @eq:hyperfocal-distance 给出；对焦到 $H$ 时景深近界为 $H/2$、远界为无穷远。
  - 景深前后分配不固定，“前一后三”或“前三后七”都只能是特定条件下的近似。
  - 相同视角、机位与输出下，匹配入瞳直径可近似匹配不同画幅的景深。
  - 焦外范围由离焦圆大小描述，焦外质量还取决于出瞳形状、像差、遮挡和光谱。
  - 反差对焦寻找清晰度极值，相位对焦利用子孔径视差估计离焦方向与幅度。
]

== 思考与计算

#block[
1. 从光束锥相似三角形推导 @eq:defocus-cone-basic，再代入薄透镜方程得到 @eq:defocus-circle-object-space。
2. 一支 $50 "mm"$ 镜头以 f/4 对焦在 $2 "m"$，物体实际位于 $1.8 "m"$。在理想薄透镜模型下计算传感器离焦圆直径。
3. 输出宽度 $40 "cm"$，观看距离 $60 "cm"$，对应传感器裁切宽度 $36 "mm"$。若允许角模糊为 $1$ 角分，估计传感器弥散圆 $c_0$。
4. 用 $f=35 "mm"$、$N=8$、$c_0=0.025 "mm"$ 计算超焦距。分别对焦在 $H/2,H,2H$ 时求近远景界，并解释远界公式何时失去有限正根。
5. 对 @eq:dof-asymmetry-parameter 取 $A=0.1,0.3,0.7$，计算远侧与近侧景深范围之比。经验性的“三分之一/三分之二”对应哪个 $A$？
6. 小画幅系统使用 $25 "mm"$、f/2，大画幅线性尺寸为其 2 倍。为匹配视角和景深，大画幅应使用什么焦距与 f 数？两者入瞳直径是否相同？
7. 对 $m=1$ 的微距摄影，比较标称 f/4、f/8、f/16 的有效 f 数、近似总景深和 $550 "nm"$ 波长下的艾里斑直径。
8. 写程序绘制 @eq:defocus-circle-object-space 随 $s$ 的曲线，并标出 $s_n,s_0,s_f$。改变 $c_0$、$N$ 与 $s_0$，观察景深区间如何变化。
9. 设相位对焦子孔径基线 $B=D/2$。把 @eq:phase-af-disparity 与离焦圆直径比较，说明为何更大的有效基线能提高离焦测量灵敏度，却可能降低可用光量或增加遮挡敏感性。
]
