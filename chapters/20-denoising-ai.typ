#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 降噪、细节与 AI 恢复 <chap:denoising-ai>

@chap:noise-dynamic-range 已经解释噪声从哪里来，@chap:raw-pipeline 说明噪声如何穿过 RAW 管线，@chap:convolution-restoration 则表明锐化和复原为什么会同时放大噪声。本章不再重做这些推导，而转向后期最实际的问题：面对一张已经拍下的照片，怎样减少令人分心的随机起伏，同时不把毛发、织物、星点和皮肤质感一并擦掉？

降噪不是把一层独立的“噪声贴纸”揭掉。观测只给出信号与随机误差的混合，而细小纹理和噪声都可能表现为像素间快速变化。任何降噪器都必须借助额外假设：局部应当平滑、边缘两侧不应混合、相似纹理会在别处重复，或者自然图像更可能具有某些统计结构。双边滤波可参见 @tomasi1998，非局部方法可参见 @buades2005 与 @dabov2007，学习型恢复的训练与感知权衡可参见 @lehtinen2018、@batson2019 和 @blau2018。

@chap:convolution-restoration 已经说明单帧逆问题为什么需要正则化，本章把“先验怎样进入结果”具体化为传统降噪与学习型恢复。若单张图像的证据仍然不足，除了加强先验还有另一条路：增加新的观测；这正是下一章多帧成像与本章 AI 恢复之间最重要的分界。

#key-box[
  *本章的核心边界*　降噪输出是对潜在图像的估计。数据足够时，估计主要受观测约束；数据不足时，结果越来越由先验决定。看起来更干净、更锐利，不等于原始观测包含了更多证据。
]

== 从噪声来源到处理对象

=== 一个模型不能跨所有数据域直接使用

在某个线性通道内，可把观测写成

$
  y_i=x_i+n_i,
  quad "E"[n_i|x_i]=0,
  quad "Var"[n_i|x_i]=a x_i+b.
$ <eq:heteroscedastic-noise-model>

$x_i$ 是期望信号，$a x_i$ 近似描述散粒项，$b$ 汇总与信号近似无关的读出和量化项。这个泊松—高斯近似承接 @chap:noise-dynamic-range；实际参数随通道、ISO、转换增益、温度和相机校准而变。

若之后施加非线性曲线 $z=T(y)$，小噪声近似下

$
  "Var"[z|x]
  approx [T'(x)]^2(a x+b).
$ <eq:tone-curve-noise-variance>

阴影提亮使 $T'(x)$ 较大，噪声也被放大；高光压缩则可能降低数值方差，却不增加高光中已经丢失的信息。因此同一张 RAW 在线性域、工作 RGB 和显示编码图像中具有不同的噪声分布。一个按“全图固定强度”设计的滤波器，常会在阴影不足、在中间调过强。

方差稳定化试图把信号相关噪声变成近似恒定方差，再使用统一阈值。现代 RAW 降噪也可直接输入每像素噪声图。两者的共同思想是：先告诉算法各处观测有多可靠，而不是假定所有像素都具有同样噪声。

=== 亮度噪声、色彩噪声与相关性

“亮度噪声”和“色彩噪声”不是传感器里天然分开的两种粒子，而是颜色坐标变换后的分量。若从相机通道向量 $bold(y)$ 变换到另一坐标 $bold(z)=M bold(y)$，协方差满足

$
  Sigma_z=M Sigma_y M^T.
$ <eq:color-noise-covariance-transform>

白平衡会放大较弱通道，颜色矩阵还会混合通道，所以阴影里常出现明显彩色斑块。去马赛克让相邻输出像素共享原始样本，缩放和锐化继续改变空间相关性；此时噪声不再是逐像素独立的“彩点”。

人眼通常对低频色斑比对细小亮度颗粒更敏感，因此软件往往对色度使用更强平滑。但若把颜色边界、细小彩色纹理或星点误当色噪，就会产生渗色和灰化。正确目标不是把色度通道抹平，而是根据输出任务控制可见误差。

=== 降噪是带损失函数的估计

记降噪器为 $hat(bold(x))=f(bold(y);lambda)$，$lambda$ 控制强度。若用均方误差衡量一个像素，可分解为

$
  "E"[(hat(x)-x)^2]
  =("E"[hat(x)]-x)^2+"Var"[hat(x)].
$ <eq:denoising-bias-variance>

第一项是结构被改变产生的偏差平方，第二项是估计在不同噪声实现间的波动。加大平滑通常降低方差，却增加边缘、纹理和小目标的偏差。哪一项更重要取决于任务：证件文字不能被改形，皮肤可以容忍部分随机颗粒，星点测量则宁可保留噪声也不能凭空制造天体。

#figure(
  image("../svg/signal_noise_overlap.svg", width: 98%),
  caption: [平坦区域、边缘、纹理和低信噪比结构在单帧中的可辨识性不同。噪声与细节共享局部高频，算法必须依赖额外先验才能分配权重。],
) <fig:signal-noise-overlap>

== 传统降噪的三种代表性思想

=== 局部平滑：用空间样本换方差

最简单的线性估计是邻域加权平均

$
  hat(x)_i=sum_(j in cal(N)_i) w_(i j)y_j,
  quad sum_j w_(i j)=1.
$ <eq:local-weighted-denoising>

若邻域内真实值相同、噪声独立且方差为 $sigma^2$，输出噪声方差为

$
  "Var"[hat(x)_i]
  =sigma^2 sum_j w_(i j)^2.
$ <eq:weighted-average-noise-variance>

对 $m$ 个等权样本，标准差降为 $sigma/sqrt(m)$。这正是平均能降噪的来源，也是代价的来源：只有在这些样本确实共享同一真实值时，估计才无偏。跨过边缘取平均会产生与邻域大小有关的模糊。

- *均值滤波*对邻域等权，方差下降容易计算，边缘损失也最直接；
- *高斯滤波*让近像素权重更大，减少硬窗口边界，但仍会跨结构混合；
- *中值滤波*输出排序后的中间值，对孤立脉冲和坏点更稳健，却不是泊松—高斯噪声下普遍最优的估计，也会改变细线和角点。

局部滤波的价值不只在实际使用。它清楚揭示了降噪的基本交换：用空间分辨率与结构偏差，换取更低的随机方差。

=== 边缘保持：让权重依赖像素差

双边滤波同时依据空间距离和数值相似度给权重：

$
  w_(i j)
  prop
  exp(-norm(bold(p)_i-bold(p)_j)^2/(2 sigma_s^2))
  exp(-(y_i-y_j)^2/(2 sigma_r^2)).
$ <eq:bilateral-filter-weights>

$sigma_s$ 控制空间范围，$sigma_r$ 控制多大的亮度差仍被视为同一区域。强边缘两侧的第二项很小，因而不容易互相平均；这比只看位置更符合图像结构。

但“像素值相近”不等于“来自同一表面”。低对比纹理可能被当作噪声，噪声也可能偶然把同一区域的像素推得很远。参数过小时残余噪声形成斑块，过大时方法退化为普通平滑。边缘保持不是免费午餐，而是把平滑先验换成了分段平滑先验。

=== 非局部相似性：重复结构提供更多样本

非局部均值比较像素周围的小块 $P_i,P_j$，在更大搜索区内寻找相似结构：

$
  w_(i j)
  prop exp(-norm(P_i-P_j)^2/h^2).
$ <eq:nonlocal-means-weights>

砖墙、织物、窗格和规律纹理会在图像中重复；即使位置很远，相似块也可共同估计同一结构。BM3D 进一步把相似块分组，在变换域压缩共同结构、抑制不一致噪声，再把结果聚合回图像 @dabov2007。本章只需抓住它的核心：重复性把单个像素的邻域扩展成了更多近似样本。

非局部方法也有明确边界。独一无二的小结构找不到可靠同伴；强噪声会破坏匹配；相似但并不相同的纹理可能被错误复制。搜索范围和变换细节属于实现问题，摄影判断更应关注“图中是否真的存在足够重复信息”。

#figure(
  image("../svg/denoiser_weight_maps.svg", width: 98%),
  caption: [三类方法都在加权平均，差别是权重依据。局部方法看距离，双边方法再看数值差，非局部方法比较小块；内容自适应越强，错误选择的影响也越需要检查。],
) <fig:denoiser-weight-maps>

== 降噪与细节的冲突

=== 偏差—方差不是抽象统计术语

在平坦区域，增加降噪强度主要降低随机方差；在毛发、草叶、织物、文字和星点上，同一强度会增加结构偏差。全图只有一个 $lambda$ 时，不同区域的最佳点通常不同。

#figure(
  image("../svg/bias_variance_denoising.svg", width: 98%),
  caption: [降噪强度的示意偏差—方差曲线。弱处理保留随机起伏，强处理损失结构；总误差的权重和最低点随题材、输出尺寸和观看任务变化。],
) <fig:bias-variance-denoising>

这解释了常见的“涂抹感”和“蜡像感”：算法把真实的细微空间变化向局部平均或训练集中更常见的皮肤结构收缩。相反，强度过低时，残余噪声经过后续锐化会变成砂砾状纹理。合理结果往往不是数学意义上最平滑，而是让剩余噪声细、均匀、不过度争夺注意力。

局部掩模可以让天空、阴影和焦外区使用更强降噪，让眼睛、毛发、文字和星点使用更弱降噪。掩模本身若过硬，会在边界产生“干净区—颗粒区”断层；所以空间变化强度也需要平滑过渡。

#paragraph-heading[百分之百查看不是唯一尺度]

若最终把 $m$ 个近似独立像素平均成一个输出像素，随机标准差可近似下降 $1/sqrt(m)$；打印网点、显示像素和人眼空间整合还会继续改变可见噪声。百分之百查看适合找坏点、去马赛克伪影和局部算法错误，却不等于最终观看条件。

评价降噪至少应固定：

- 最终像素尺寸或打印尺寸；
- 典型观看距离和显示亮度；
- 是否还会缩放、锐化、压缩；
- 观察的是细节真实性、整体观感还是测量可靠性。

同一参数在大幅打印上可能噪声过多，在手机屏幕上却已经过度平滑。所谓“最佳降噪值”不是原文件永久属性，而是处理版本与输出任务的配对。

=== 残差能揭示算法删掉了什么

令输入为 $bold(y)$、输出为 $hat(bold(x))$，简单残差为

$
  bold(r)=bold(y)-hat(bold(x)).
$ <eq:denoising-residual>

若降噪主要删除零均值随机噪声，残差应接近所假定的噪声形态；若残差里清楚出现睫毛、砖缝、文字或星点，说明这些结构也被当成噪声。残差不是绝对证明，因为非线性算法会改变均值和相关性，但它比只盯着“干净成片”更容易暴露过度处理。

对可重复拍摄的静态场景，多张独立曝光提供更强检验：在各帧中位置稳定的结构更可能来自场景，逐帧变化的部分更可能是随机噪声。固定图样、热像素和条带会跨帧稳定，因此仍需暗场、平场或像素校准区分。

=== 处理顺序改变噪声模型

原则上，强非线性色调、局部对比和锐化之前降噪，更容易使用接近物理来源的噪声模型；RAW 域还保留了饱和点、通道和曝光尺度信息。但 RAW 域是 CFA 马赛克，颜色尚未完整，某些纹理与去马赛克错误要在 RGB 域才显现。

所以不存在一句适合所有软件的固定顺序。更稳妥的分工是：

1. 在早期线性域处理坏点、明显色斑和信号相关噪声；
2. 在去马赛克后检查颜色边缘、细纹理和空间相关伪影；
3. 在强烈提亮或局部对比之后，必要时做温和的局部修正；
4. 最终缩放后按输出尺度决定锐化和剩余颗粒感。

#warn-box[
  *避免把算法重复叠加*　相机 JPEG、RAW 软件、AI 插件和导出平台都可能各做一次降噪与锐化。多次轻处理未必比一次可控处理温和，因为每一步都会把上一阶段的伪影当成新的图像结构。
]

== 学习型降噪与 AI 恢复

=== 训练目标决定“正确答案”的统计含义

给定低质量观测 $bold(y)$ 和干净目标 $bold(x)$，监督学习常最小化

$
  min_theta "E"[norm(f_theta(bold(y))-bold(x))^2].
$ <eq:supervised-denoising-loss>

当模型容量和数据足够时，平方误差的最优输出是条件均值

$
  f^*(bold(y))="E"[bold(x)|bold(y)].
$ <eq:conditional-mean-denoiser>

如果同一模糊噪声观测对应许多可能的细纹理，条件均值会把这些候选平均，结果稳定但可能偏软。绝对误差更接近逐分量条件中位数；感知损失和生成式目标则会偏向自然图像统计或高概率纹理。它们不是在同一标尺上“越来越先进”，而是在回答不同的损失函数。

训练数据还决定先验。以常见手机照片训练的模型，可能把胶片颗粒、天文弱信号、显微结构或罕见织物当成异常；以人像训练的模型可能更积极地平滑皮肤。模型名称不能替代对训练分布与目标任务的判断。

#paragraph-heading[没有干净配对也能学习，但需要额外条件]

若同一潜在图像有两次独立、零均值噪声观测

$
  bold(y)_1=bold(x)+bold(n)_1,
  quad bold(y)_2=bold(x)+bold(n)_2,
$

则用 $bold(y)_1$ 预测 $bold(y)_2$ 的期望平方损失，与预测干净 $bold(x)$ 的损失只差一个与模型无关的噪声常数；这使 Noise2Noise 式训练可不使用逐像素干净目标 @lehtinen2018。盲点自监督则遮住待预测像素，利用邻域预测它，要求该像素噪声在给定邻域后近似独立 @batson2019。

这些方法没有“从无到有学出真值”。它们把干净配对要求换成了独立噪声、零均值、重复观测或空间可预测性等条件。固定图样噪声、压缩块、去马赛克相关性和移动主体会破坏这些条件。

=== 感知质量与失真不能同时无限优化

恢复结果可有两个不同目标：一是与某个已知参考在像素上接近，二是落在“看起来像自然图像”的分布中。当观测高度含糊时，更强的生成先验能产生更锐利、更自然的结果，却可能偏离真实参考；更保守的均值估计像素误差较小，却容易显得平滑。这类感知—失真权衡不是某个软件的缺陷，而是歧义观测下的基本选择 @blau2018。

#figure(
  image("../svg/ai_posterior_ambiguity.svg", width: 98%),
  caption: [同一观测可能支持多种潜在图像。平方误差倾向条件均值，生成式方法可选择外观自然的候选；后者增加感知锐度时，也可能加入观测不能唯一支持的具体纹理。],
) <fig:ai-posterior-ambiguity>

“AI 恢复”因此应至少区分三种输出：

- *数据约束的估计*：主要结构在输入中有稳定响应，模型减少随机误差；
- *先验增强的估计*：输入给出轮廓或类别，具体纹理由训练分布补足；
- *生成式编辑*：输出可以合理、美观，却不再声称还原现场的具体细节。

三者都可能有创作价值，差别在于证据解释。纪实、司法、科研和文物记录应保留原始文件、处理参数和未生成版本；艺术创作可以更自由，但最好不把生成内容表述为传感器实际记录。

#paragraph-heading[怎样判断 AI 是否在“编细节”]

单张结果通常不能给出逐像素真实性证明，但可以做压力测试：

- 轻微改变降噪强度、裁切边界或输入缩放，观察细纹理是否大幅换形；
- 比较同一场景的连拍帧，检查新增结构是否跨帧稳定；
- 把输出重新降采样到原始可观测尺度，看主要结论是否仍成立；
- 检查文字、睫毛、首饰、星点、重复窗格等高风险区域；
- 保存关闭生成增强的保守版本，用它区分观感提升与证据增加。

不稳定不必然证明某个像素是假的，稳定也不保证真实；这些检查的作用是暴露输出对先验和实现细节的敏感度。

== 面向摄影质量的降噪流程

#figure(
  image("../svg/denoising_pipeline_diagnostics.svg", width: 98%),
  caption: [实用降噪流程。从输入域、噪声来源和输出条件出发，选择方法后同时检查残差与最终输出；若发现结构被删除或伪影增加，应回退方法、强度或处理域。],
) <fig:denoising-pipeline-diagnostics>

#paragraph-heading[第一步：判断瓶颈能否在后期解决]

若问题来自轻中度随机噪声，降噪通常能改善观感；若来自严重失焦、长距离运动拖影、三通道饱和或极低光子数，降噪只能平滑症状。拍摄阶段增加实际曝光、稳定相机、改善照明或使用多帧，往往比事后更强模型保留更多真实信息。

先看 RAW 而不是只看已锐化 JPEG，记录曝光、ISO、提亮量和处理历史。条带、热像素、压缩块和去马赛克假色需要不同校正，不能都交给“降噪强度”一个滑块。

#paragraph-heading[第二步：先控制色斑，再决定纹理取舍]

大面积色斑会强烈吸引注意，通常可在较低空间分辨率上抑制；亮度颗粒则更容易与真实纹理重叠。可先在合适颜色坐标中温和处理色彩噪声，再以主体区域决定亮度降噪强度。人脸、文字、树叶和星空应分别检查，不能只看天空或焦外背景。

#paragraph-heading[第三步：用最终输出做闭环]

将图像缩放到交付尺寸，加入计划中的输出锐化和压缩，再比较开关降噪的版本。若差异只在百分之百查看可见，却让正常观看的纹理变软，应减弱处理；若正常观看仍有大块色斑或条带，则需要回到更早数据域或使用局部修正。

一个可复现的处理记录至少包括：软件或模型版本、输入数据域、亮度和色彩强度、局部掩模、缩放尺寸、输出锐化以及是否启用生成增强。这样未来更换算法时可以从原始观测重做，而不是把当前 AI 输出当作唯一母版。

== 常见误解

#paragraph-heading[“把噪声完全去掉，画质一定更好”]

完全平滑的区域可能同时失去毛孔、纤维、微弱星点和自然颗粒。画质目标是让噪声不妨碍主体与输出，而不是让每个平坦区在放大后成为无变化色块。

#paragraph-heading[“彩噪可猛降，反正颜色没有细节”]

颜色边界、细小饰品、霓虹文字和星点都有真实色彩结构。色度通常可更强平滑，是感知权重选择，不是色彩通道没有空间信息。

#paragraph-heading[“AI 模型比传统滤波高级，所以总能保留更多真实细节”]

学习模型能利用更丰富先验，在常见题材上常有更好观感；遇到训练分布外结构时，它也可能更自信地删错或补错。算法复杂度不等于证据强度。

#paragraph-heading[“残差看起来像噪声，就证明没有删细节”]

真实细纹理本来就可能像随机过程，非线性算法还会改变残差统计。残差是重要诊断，不是单独的真实性证书；多帧一致性、参数稳定性和任务知识同样必要。

#paragraph-heading[“拍摄时可以少曝光，反正后期 AI 能救”]

减少实际光子会降低观测 SNR。AI 可以用先验生成更合理外观，却不能让某个已经被噪声淹没的现场细节重新成为可测证据。拍摄阶段的光子、对焦和运动控制仍是质量上限。

#pagebreak(weak: true)

#struct-box(title: [本章小结])[
  - 线性 RAW 中常用 $"Var"[n|x]=a x+b$ 描述信号相关噪声；色调曲线、颜色矩阵、去马赛克与缩放会改变方差和相关性。
  - 局部平均用空间分辨率换方差；只有邻域真实值近似相同时，$m$ 个等权样本才把标准差降为 $sigma/sqrt(m)$ 而不引入明显偏差。
  - 双边滤波利用空间与数值相似度保护强边缘；非局部方法利用重复小块扩大有效样本，两者都可能因错误选择损伤纹理。
  - 加强降噪通常降低随机方差、增加结构偏差；强度必须按区域、题材和最终输出尺寸选择。
  - 残差、重复帧、参数扰动和最终尺寸检查能暴露过度处理，但单一诊断不能证明逐像素真实性。
  - 早期线性域更接近物理噪声模型，RGB 域更容易看见颜色与去马赛克问题；处理顺序应按数据域与伪影来源决定。
  - 平方误差训练倾向输出条件均值；生成式目标可改善感知锐度，也更可能选择观测不能唯一支持的纹理。
  - AI 降噪是估计而非重新测量。应区分数据约束结构、先验补全结构和生成式编辑，并保留原始文件与处理记录。
]

== 思考与计算

#block[
  1. 某线性 RAW 区域噪声满足 $"Var"[n|x]=0.8x+16$。分别在 $x=25,400,2500$ 时计算标准差与 SNR；解释为何同一固定降噪阈值不能适合全部亮度。
  2. 一条局部曲线在某阴影值处导数为 4。用 @eq:tone-curve-noise-variance 计算该处噪声标准差被放大的倍数；为什么这不等于阴影新增了真实细节？
  3. 四个独立同方差像素做等权平均。推导输出方差；若其中两个像素位于亮度相差 $Delta$ 的另一侧边缘，计算估计均值相对本侧真实值的偏差。
  4. 对 @eq:bilateral-filter-weights，设两个像素空间距离相同，亮度差分别为 $0$、$sigma_r$ 和 $2sigma_r$。计算其辐射权重比，并说明 $sigma_r$ 过小、过大时的视觉结果。
  5. 选择一幅包含天空、头发、织物和文字的照片，制作三档降噪版本。分别检查成片、残差与最终缩小图，记录各区域最先出现的涂抹或残余噪声。
  6. 证明平方损失的条件最优估计是 @eq:conditional-mean-denoiser。构造一个潜在像素以相同概率取 0 与 1、观测无法区分两者的例子；条件均值 0.5 是否是任何一次真实状态？
  7. 在 Noise2Noise 条件下展开 $"E"[norm(f(bold(y)_1)-bold(y)_2)^2]$，说明独立零均值噪声为何只增加常数项。若两次观测含相同固定图样，结论在哪里失效？
  8. 对同一张高 ISO 照片分别先锐化后降噪、先降噪后锐化。保持最终尺寸一致，比较噪声功率、边缘光晕与残差结构，并解释差异来自哪一步改变了输入统计。
  9. 设计一个 AI 恢复压力测试：至少改变输入缩放、裁切边界、模型强度和随机种子，标记文字、睫毛、重复纹理与星点是否稳定。哪些结果只能说明不稳定，不能直接证明虚假？
  10. 为纪实照片、商业人像和纯艺术图像分别写一份处理边界。说明哪些降噪与生成增强可以接受，哪些版本必须保留，以及交付时应如何标注。
]
