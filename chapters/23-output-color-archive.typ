#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 色彩管理、显示、打印与归档 <chap:output-color-archive>

一张照片在 RAW 软件里调得正确，不代表它在浏览器、手机、客户显示器和纸张上仍会保持同一外观。文件中的 RGB 只是带有颜色解释的坐标；输出设备有自己的原色、白点、黑位和非线性，观看环境又会改变视觉适应。交付之后，平台还可能缩放、去除元数据和再次压缩。若原始文件、处理参数或颜色配置丢失，未来甚至无法重建今天看到的版本。

色度学、亮度编码与 RAW 管线三章已经讲过色彩空间、编码、格式与处理状态。本章不重复推导，而把它们组织成一个可验证输出流程：先让源数据拥有明确颜色含义，再把它转换到目标设备；分别验收屏幕与打印；按接口控制压缩；最后保存足以校验、迁移和重建的归档集合。ICC 配置体系可参见 @icc2022，JPEG 编码背景可参见 @wallace1991，开放归档的分层思想可参见 @iso14721。

@chap:task-based-system-selection 从目标输出反推拍摄系统，本章把那个目标落实为真实设备、平台和长期文件。只有经过实际观看与交付，前面讨论的色彩、锐度和动态范围才得到最终验收；最后一章将从这些结果反向追踪异常首次出现的位置，闭合全书的方法链。

#key-box[
  *输出的核心事实*　颜色管理追求的是规定条件下的可重复转换，不是让所有设备物理相同。显示器发光、纸张反光、平台处理未知，因此“完全一样”通常不是可实现目标；可实现的是明确参照、控制差异并保留验证证据。
]

== 色彩管理

=== RGB 数字必须和颜色解释绑定

同一三元组 $(200,80,40)$ 在 sRGB、Display P3、Adobe RGB 或某显示器原生空间中代表不同颜色。源配置文件描述数值怎样映到规定的颜色连接空间（PCS），目标配置文件再把 PCS 映到设备控制值。概念上可写成

$
  bold(d)
  =f_("dst")^(-1)
  (A f_("src")(bold(s))),
$ <eq:icc-source-destination-transform>

其中 $bold(s)$ 是源数值，$f_("src")$ 包含源曲线、矩阵或查找表，$A$ 表示必要的白点与参照转换，$f_("dst")^(-1)$ 产生目标设备值。真实 ICC 管线还包含渲染意图、黑点处理和设备状态；公式只展示“先解释、再转换”的顺序。

#figure(
  image("../svg/color_management_chain.svg", width: 98%),
  caption: [ICC 颜色管理链。源配置把源数值解释到 PCS，目标配置再映射到规定状态下的显示或打印设备；校准建立可重复状态，表征测量该状态并生成配置文件。],
) <fig:color-management-chain>

若文件缺少配置，软件只能猜测；很多应用默认按 sRGB 解释。把实际 Adobe RGB 数值误当 sRGB 会改变彩度与颜色，反之亦然。“分配配置文件”改变数值的含义而不改数值，“转换到配置文件”则改变数值以尽量保持颜色；两种操作不能混用。

#paragraph-heading[校准与表征不是同一件事]

*校准*把设备调到目标状态，例如显示器白点、白场亮度、黑位与灰阶响应；*表征*测量该状态下输入数值与输出颜色的关系，并生成配置文件。配置文件不能修复不稳定背光、严重不均匀或超出目标能力的设备。

一次合理显示校准应记录：设备模式、白点目标、白场亮度、黑位、传递曲线、环境光和测量仪器。若第二天把显示器亮度滑块改动很大，原配置已不再准确；操作系统仍显示“配置文件已启用”并不证明设备状态未变。

#paragraph-heading[工作空间、设备空间和连接空间分工不同]

工作 RGB 用于编辑与保存中间值，应具有足够色域、位深与稳定定义；设备空间描述具体显示器或打印组合；PCS 提供设备间连接参照。把显示器原生配置当作编辑工作空间，会让文件依赖一台设备，也容易在其他软件中被误解释。

宽工作空间保留更多超出当前设备的颜色，却要求更高精度与正确管理。若最后目标只是普通网页，仍应在交付前转换到兼容空间并检查色域映射；直接删除配置文件不是“变成 sRGB”。

#paragraph-heading[色域映射与渲染意图]

当源颜色超出目标色域，不可能同时保持所有绝对色差。常见策略包括：

- *相对色度*：目标白与源白对齐，色域内颜色尽量保持，超色域压到边界；
- *感知式*：联动压缩一片颜色，牺牲绝对数值以保留排序和过渡；
- *绝对色度*：保留源白相对 PCS 的关系，常用于模拟纸张白；
- *饱和度优先*：偏向保持鲜艳，通常不以摄影色准为首要目标。

实际效果取决于配置文件是否包含对应表和软件实现。渲染意图名称不是保证；关键照片应比较映射前后、开启色域警告并做目标设备实样。

== 输出到屏幕

=== 显示器状态与环境必须一起固定

白场过亮会诱使编辑者把照片调暗，黑位被环境反射抬高会隐藏暗部差异，彩色墙面和灯光会改变视觉白。校准目标应服务观看环境，而不是盲目追随一个通用亮度数字。

#figure(
  image("../svg/display_environment_calibration.svg", width: 98%),
  caption: [显示器校准与观看环境。设备的白点、亮度、黑位、灰阶和原色必须稳定，环境光与界面周边又会改变有效黑位和视觉适应；两者都要记录并定期验证。],
) <fig:display-environment-calibration>

暗室印前工作常使用较低白场亮度，普通办公室和移动观看需要更高亮度；HDR 显示还会随窗口面积、持续时间和面板热限制改变峰值。不要用显示器亮度滑块逐张“修正”照片，应先固定设备与环境，再编辑图像。

#paragraph-heading[SDR 与 HDR 不只是亮度范围不同]

SDR 常使用相对显示参照与 sRGB/Rec.709 类传递，HDR 可使用 PQ 的绝对亮度参照或 HLG 的系统相对参照。文件还要携带原色、白点、传递函数和母版元数据，显示器再按自身峰值与环境做映射。

把 HDR 码值当普通 sRGB 会严重错误；把 SDR 简单拉亮也不会生成真实 HDR 高光层次。交付前应在目标系统检查：应用是否支持 HDR、操作系统是否正确合成 SDR/HDR、截图和网页是否保留信令，以及不支持设备怎样回退。

=== 像素密度要转换成观看角度

若屏幕宽度为 $W$ 英寸、横向像素为 $N$，像素密度

$
  "PPI"=N/W.
$ <eq:screen-ppi>

但 PPI 仍不决定可见细节。观看距离 $d$ 决定像素和纹理的角尺寸；@chap:perception-composition-style 已经把输出周期换算到 cycles/degree。高 PPI 手机近看与低 PPI 大屏远看可能有相似角采样。

输出尺寸应按版面与观看距离选择，不必为所有平台上传原始全像素。过大文件会被未知算法重缩放，既浪费带宽又可能产生二次锐化；过小文件则在高密度屏幕或放大查看时暴露插值。

#paragraph-heading[交付空间选择兼容性，而非理论最大色域]

sRGB 仍是未知网页与应用环境中较稳妥的最低共同接口，前提是正确嵌入配置并实际测试。Display P3 等宽色域适合受控生态和高彩度内容，但未管理应用可能误显；HDR 还需要完整信令与回退版本。

一个可靠交付包可同时包含：明确标记的标准 SDR/sRGB 版本、目标平台需要的宽色域或 HDR 版本，以及一张参考缩略图或说明。不要用同一扩展名假设接收方会自动理解全部语义。

== 输出到打印

=== 屏幕发光，纸张调制环境光

显示器通过 RGB 原色加色发光；打印通过油墨吸收部分入射光，观察到的是纸张、墨层、表面与环境照明共同反射的光谱。纸张白通常比屏幕白暗，最深墨黑也会反射光，因此可用对比范围较窄。

#figure(
  image("../svg/screen_print_mapping.svg", width: 98%),
  caption: [屏幕、打印与软打样。屏幕自发光，打印依赖纸白、墨水和观察光；软打样用打印配置和渲染意图在已校准屏幕上模拟部分差异，最终仍需纸张实样。],
) <fig:screen-print-mapping>

高光屏、哑光纸和棉质艺术纸的表面散射不同，会改变黑位、局部对比和清晰感。打印不是把屏幕像素“喷到纸上”，而是把期望颜色映射到一个不同物理系统。

#paragraph-heading[打印机、墨水、纸张和状态共同定义配置]

打印配置必须对应具体打印机、墨水、纸张、质量模式和线性化状态。换纸、换墨、清洁喷头或驱动模式变化，都可能让旧配置失效。网上下载的同型号配置只能描述其规定组合与样机，不保证你的设备状态一致。

打印前应做喷嘴检查、使用正确介质设置并关闭重复颜色管理。如果应用和驱动同时转换颜色，可能发生“双重管理”；如果两边都关闭，数值又可能被当作未定义设备值。

#paragraph-heading[软打样预览不可打印的差异]

软打样把图像经打印配置与渲染意图转换，再在显示器上模拟纸白、墨黑与色域压缩。可重点检查：

- 暗部是否因墨黑抬高而合并；
- 高彩度颜色是否压缩、换色或失去层次；
- 纸白偏暖是否改变整体适应；
- 黑点补偿是否让阴影层级更自然；
- 相对色度与感知式映射哪个更符合题材。

屏幕无法完全模拟纸张的表面纹理、方向反射和真实观察光，尤其不能把发光高光变成同样物理强度。因此软打样是预测工具，不是免打样证明。

#paragraph-heading[PPI、打印机 DPI 和观看距离不能混用]

图像 PPI 表示每英寸放置多少图像像素；打印机 DPI 常表示喷墨点或成像点密度，一个图像像素通常由多个墨点和筛网结构表达。把 2400 DPI 当作需要 2400 PPI 图像，会夸大文件需求。

若打印宽度为 $W_p$ 英寸、横向像素为 $N$，放置分辨率为 $N/W_p$ PPI。所需值应结合打印机驱动、纸张 MTF 与观看距离测试；远观海报可低于近看小幅精细打印。

#paragraph-heading[输出锐化必须在最终尺寸之后判断]

打印会因墨扩散、纸张散射和筛网降低边缘对比，通常需要比屏幕版更强的输出锐化。锐化半径以最终像素尺度定义；在缩放前套固定参数，实际打印角尺度会改变。

制作小块实样时应包含皮肤、细线、纹理、暗部和高反差边缘，分别比较三档锐化。只看显示器预览无法可靠预测纸张上光晕与细节合并。

== 压缩与交付

=== JPEG 用频率量化换带宽

经典 JPEG 通常先把 RGB 变到亮度—色度坐标，可对色度降低空间采样，再把图像分成 $8 times 8$ 块并转换到频率系数。量化会更粗略地保存视觉上不敏感的高频和色度信息，使许多系数变为零，最后再用无损熵编码缩小文件。主要不可逆性来自色度子采样与量化，不来自最后的熵编码。

#struct-box(title: [可以继续深入])[JPEG 编码器使用二维离散余弦变换、量化表、游程编码和熵编码；实现编解码器时还需处理电平偏移、系数扫描与色度采样位置。本书后文只需要知道损失发生在哪一步，以及它会产生块效应、振铃和彩色边缘软化。]

#figure(
  image("../svg/jpeg_delivery_pipeline.svg", width: 98%),
  caption: [JPEG 与平台交付链。颜色变换和色度子采样减少色彩空间带宽，DCT 把块变到频率域，量化丢弃精度；平台还可能重缩放、去配置并再次编码。],
) <fig:jpeg-delivery-pipeline>

#paragraph-heading[色度子采样利用感知差异，也会伤害彩色边缘]

4:4:4 保留每个位置的色度采样，4:2:2 水平方向减半，4:2:0 通常在水平和垂直都降低色度分辨率。自然照片常能承受一定子采样，彩色文字、界面截图、霓虹边缘和细小红蓝纹理则容易渗色。

子采样不是“颜色位深降低”，而是颜色差通道空间采样变稀。已经发生的色度混叠与边缘软化，不能通过后续转为 4:4:4 找回。

#paragraph-heading[质量滑块不是跨软件统一物理量]

不同编码器的“质量 80”可使用不同量化表、子采样和优化策略，文件大小也受图像内容影响。平滑天空容易压缩却暴露渐变断层，树叶纹理文件大且容易出现蚊噪。

交付测试应固定目标尺寸，用若干代表性图片比较可见伪影与字节数，而不是迷信一个质量数值。第一次高质量 JPEG 编码通常损失有限，反复打开—编辑—重存会让块与振铃累积。

#paragraph-heading[平台二次处理必须实测]

社交平台、聊天软件和网站可能：限制长边、重新采样、剥离配置文件、改色度子采样、重新量化、生成多档缩略图或把 HDR 映成 SDR。规则还可能随客户端与时间改变。

稳妥流程是上传一组带细节、渐变、彩色边缘和嵌入配置的测试图，再下载实际展示版本检查尺寸、配置和伪影。平台帮助文档可作起点，最终交付文件才是证据。

#paragraph-heading[母版、工作版本和交付版本职责不同]

- *原始观测*保留最大重解释空间；
- *工作文件*保存高位深、图层或参数，服务继续编辑；
- *固定母版*冻结一个高质量可独立观看的渲染；
- *交付版本*按具体平台、尺寸、色彩空间和压缩优化。

不要从社交平台 JPEG 反向更新母版，也不要把巨大宽色域工作文件直接交给未知接口。版本越靠近交付，兼容性越重要；越靠近母版，可重建性与余量越重要。

== 归档与可重建性

=== 归档至少保存四类内容

@chap:raw-pipeline 已经提出原始测量、决策记录与固定渲染三层。本章把交付版本单独列出，以便未来知道哪份文件服务哪个接口：

1. 原始 RAW、原视频、音频及原始元数据；
2. 目录、评分、编辑参数、局部蒙版、颜色配置和软件版本；
3. 高质量固定母版及必要的说明文档；
4. 重要交付版本、打印配置与输出尺寸记录。

#figure(
  image("../svg/archive_layers_integrity.svg", width: 98%),
  caption: [影像归档的内容与保障。原始观测、决策记录、固定母版和交付版本解决不同重建需求；校验、独立副本、可解释元数据和定期迁移共同保证长期可用。],
) <fig:archive-layers-integrity>

只保存 RAW 可能保留测量，却丢失作者当时的选择；只保存最终 JPEG 保留外观，却失去重新处理与高质量输出余量。合理归档不是在二者之间选一个。

#paragraph-heading[校验值回答“文件是否还是原来的字节”]

密码学散列函数把文件映到固定长度校验值 $h=H(F)$。复制或定期巡检时重新计算并比较，可以发现传输错误、介质静默损坏和意外改写。它不能修复文件，也不能证明颜色配置正确；它只验证字节完整性。

校验清单应与文件分开保存并纳入备份，记录算法与生成时间。仅看文件能否打开不足以发现局部位翻转，缩略图正常也不证明 RAW 主数据完整。

#paragraph-heading[多副本要跨独立故障域]

若每份副本独立丢失概率为 $p$，$k$ 份同时丢失的理想概率为 $p^k$。现实故障常相关：同一电源浪涌、勒索软件、误删除、火灾和账户封禁可同时影响多份，所以“两个硬盘放同一桌”远不如数字看起来安全。

冗余应跨介质、地点与权限边界，并至少有一份不持续可写或具版本历史。所谓 3-2-1 可作记忆起点，真正目标是让主要故障模式彼此独立。

#paragraph-heading[同步不是备份，目录也不是校验]

同步会快速传播删除和损坏；RAID 提高在线可用性，却不提供历史版本和异地保护；整齐目录方便查找，不检测位损坏。它们都可成为系统组件，但不能替代备份。

备份必须能恢复。定期抽样还原文件、打开母版、验证配置和重算校验，才能证明策略有效；从未演练的备份只是一项希望。

#paragraph-heading[格式与软件会老化，需要迁移计划]

介质寿命、接口、私有 RAW 解码器、插件、字体、配置文件和编辑数据库都可能失效。迁移不是等文件打不开再转格式，而是定期检查支持状态，把内容复制到新介质并验证字节与语义。

开放或广泛支持格式降低风险，却不保证永久。转换 RAW 到 DNG 或 TIFF 时应保留原始文件，记录转换工具与验证结果，并检查黑白电平、裁切、元数据和预览是否保持预期含义。

== 输出与归档的最小工作流

#paragraph-heading[编辑前固定参照]

稳定显示器状态和环境，确认文件源配置与工作空间，保留原始文件只读副本。若目标包含打印，尽早取得实际纸张配置；若包含 HDR 或宽色域，先验证目标应用链是否支持。

#paragraph-heading[为每个目标生成独立交付版本]

从高质量母版出发，先缩放到目标尺寸，再做输出锐化、色彩转换和最终压缩。记录长边、色彩空间、位深、传递函数、质量设置与用途。不要用一个“万能 JPEG”承担网页、打印与长期母版三种任务。

#paragraph-heading[交付后验证实际结果]

屏幕交付要在目标平台下载回看，打印要在目标纸张和照明下看实样。发现差异时先判断来自颜色解释、色域映射、亮度环境、缩放还是压缩，不要立即在原图上全局加亮或加饱和。

#paragraph-heading[项目结束时建立归档清单]

生成校验值，确认原始、参数、配置、母版与交付版本齐全；复制到独立位置；记录恢复说明和下一次巡检日期。归档不是拍摄结束后的杂务，而是保证作品未来仍可解释的一段图像管线。

== 常见误解

#paragraph-heading[“显示器校准后，别人看到的一定一样”]

校准只控制你的设备状态。对方设备、应用、环境和视觉适应仍不同；颜色管理能减少可控误差，不能控制未知终端。

#paragraph-heading[“嵌入 sRGB 会把所有颜色限制得很差”]

交付空间是接口选择。sRGB 的色域有限，却有较高兼容性；宽色域可保留更多颜色，但要求接收链正确管理。应从目标设备而非身份标签选择。

#paragraph-heading[“300 DPI 是所有打印的最佳分辨率”]

图像 PPI、打印机 DPI、纸张 MTF 和观看距离不同。300 PPI 是常见工作值，不是跨尺寸、介质与设备的物理定律。

#paragraph-heading[“最高 JPEG 质量等于无损”]

经典 JPEG 在高质量设置下仍可能有色度子采样与系数量化。肉眼难见不等于数学无损，反复重存还会累积误差。

#paragraph-heading[“云同步后就不用备份”]

同步会传播删除、账户错误和勒索加密。真正备份需要独立副本、历史版本、完整性校验和恢复演练。

#struct-box(title: [本章小结])[
  - 颜色管理以源配置解释数值，经 PCS 与目标配置生成设备值；分配配置与转换配置是不同操作。
  - 校准使设备状态可重复，表征测量该状态并生成配置；显示器、环境光和界面周边必须共同固定与验证。
  - 屏幕自发光，打印依赖纸白、墨水和观察光；PPI、输出锐化与软打样都要结合介质、尺寸和观看距离。
  - JPEG 用色度子采样与 DCT 量化换取带宽；平台二次缩放和重编码还会继续改变尺寸、颜色与伪影。
  - 原始观测、决策记录、固定母版和交付版本职责不同；长期归档还需要校验、独立副本、可解释元数据与迁移演练。
]

== 思考与计算

#block[
  1. 解释把 Adobe RGB 文件“分配为 sRGB”和“转换到 sRGB”的区别，并预测同一 RGB 数值的颜色与数值怎样变化。
  2. 设计显示器校准记录：白点、亮度、黑位、传递曲线、设备模式、环境光、仪器和复测阈值；哪些变化会让旧配置失效？
  3. 6000 像素宽图像分别输出为 20、40、100 cm。计算 PPI，并结合观看距离讨论三者是否需要相同输出锐化。
  4. 比较相对色度、感知式与绝对色度意图在高彩度花朵、低彩度人像和模拟暖色纸张中的取舍。
  5. 为暗调照片制作屏幕版与哑光纸版；记录黑点、纸白模拟、阴影层级和三档锐化，指出软打样无法完全预测的差异。
  6. 对 $8 times 8$ 渐变块求 DCT，改变高频量化步长并逆变换；比较块效应、振铃、均方误差与主观可见性。
  7. 制作相同质量的 4:4:4 与 4:2:0 JPEG，比较红色细字、人像和树叶的色边与文件大小。
  8. 上传带 sRGB 配置、细渐变和已知尺寸的测试图，再下载检查尺寸、配置、量化表与伪影，记录平台处理链。
  9. 为项目列出原始观测、决策记录、母版和交付版本，建立校验与独立副本；若单份年丢失概率为 0.02，计算 1—3 份同时丢失的理想概率，再用相关故障修正布局。
]
