#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 衍射与有限孔径 <chap:diffraction-aperture>

几何光学把光线看作无限细，并允许理想合焦点具有零面积；波动光学则指出，任何有限孔径都会把点物体成像为有限衍射图样。缩小光圈能抑制几何像差和离焦，却扩大衍射斑。摄影中的“最佳光圈”正是多个模糊机制、传感器采样和输出任务之间的折中，而不是镜头铭牌上隐藏的固定数字。

本章用波的叠加说明有限孔径为何产生衍射，并直接使用圆孔艾里图样、Rayleigh 判据和衍射极限 MTF 的摄影结论。随后把光学截止频率与像素 Nyquist 频率放在同一坐标系中，并讨论光圈形状、星芒、像差—衍射权衡以及计算恢复的边界。傅里叶光学的系统论述可参见 @goodman2017 与 @bornwolf1999。

到 @chap:noise-dynamic-range 为止，我们已经知道光怎样被计量、探测并附带随机误差，但几何光学仍允许理想点像无限小。本章补上第一种不可避免的空间限制，并引入 PSF、OTF 与 MTF；这些语言会把下一章的真实镜头像差、再下一章的运动模糊和后面的像素采样接到同一条清晰度链上。

#key-box[
  *衍射不是开关*　有限孔径从任何 f 数开始都存在衍射。缩小光圈使空间频率对比度连续下降；是否“看得出来”取决于波长、传感器采样、输出放大、场景频谱和观察者阈值。
]

== 从波前到衍射图样

=== 复振幅

对近单色标量场，可把某一平面上的波写成复振幅

$ U(bold(r))=A(bold(r)) exp(i phi(bold(r))). $ <eq:complex-field>

实际探测器在远长于光学周期的时间上积分，记录的强度与模平方成正比：

$ I(bold(r)) prop abs(U(bold(r)))^2. $ <eq:intensity-modulus>

相位 $phi$ 不被普通像素直接记录，却决定不同孔径位置贡献在像面上相长还是相消。几何光学丢弃相位，因此无法从自身推出衍射环和星芒。

=== 从孔径到焦平面

波前上的各位置都会向像面贡献复振幅；这些贡献先相加，再由传感器记录模平方。近场图样会随传播距离明显改变，远场角分布则与孔径函数的傅里叶频谱对应。摄影镜头可在焦平面上形成这种角谱，因此不需要把传感器放到物理上极远的位置。

#figure(
  image("../svg/diffraction_regimes.svg", width: 96%),
  caption: [有限孔径后的传播区域。近场图样仍随距离显著变化；远场角分布等价于瞳函数的傅里叶频谱。透镜可在焦平面上形成这一角谱。],
) <fig:diffraction-regimes>

#struct-box(title: [可以继续深入])[惠更斯—菲涅耳积分、菲涅耳数和夫琅禾费近似可以严格描述波从孔径传播到观察面的过程。本书后文只使用摄影焦平面上的结果：瞳函数经傅里叶变换决定点扩散函数。]

=== 瞳函数的傅里叶变换

令瞳面坐标为 $bold(rho)$，瞳函数为

$ P(bold(rho))=A(bold(rho)) exp(i phi(bold(rho))). $ <eq:complex-pupil>

$A$ 包含孔径形状、渐晕和振幅透过率，$phi$ 包含离焦与像差。夫琅禾费区域或理想透镜焦平面的振幅满足

$ U(bold(q)) prop cal(F)[P](bold(q)), $ <eq:pupil-fourier-transform>

于是点扩散函数为

$ "PSF"(bold(q)) prop abs(cal(F)[P](bold(q)))^2. $ <eq:psf-from-pupil>

孔径越窄，其傅里叶频谱越宽；孔径边缘越尖锐，频谱中的旁瓣与方向结构越明显。这是“小孔径使主斑变宽，同时产生清晰衍射纹”的数学原因。

== 圆孔与艾里图样

=== 圆孔的结果

理想圆孔的傅里叶变换具有旋转对称性，因此点光源形成中央亮斑和逐渐衰减的同心环，这就是艾里图样。完整曲线可由贝塞尔函数写出，但摄影判断通常只需要第一暗环尺度和整条 MTF 的下降趋势。

#figure(
  image("../svg/airy_pattern.svg", width: 96%),
  caption: [圆孔艾里图样及其径向强度。第一暗环位于 $u=3.8317$，中央主瓣之外仍有逐渐衰减的能量环。],
) <fig:airy-pattern>

=== 第一暗环

$J_1$ 的第一个正零点约为 $3.8317$，所以

$ sin theta_1=1.22 lambda/D. $ <eq:airy-angular-radius>

近轴条件下，像面第一暗环半径为

$ r_1=1.22 lambda f/D=1.22 lambda N, $ <eq:airy-radius>

第一暗环直径为

$ d_("Airy")=2.44 lambda N. $ <eq:airy-diameter>

中央主瓣约包含总能量的 $83.8%$；剩余能量分布在外侧亮环。艾里“圆盘”并不是一个边缘突然截断的均匀圆，单用第一暗环直径不能完整描述其成像效果。

#paragraph-heading[波长与多色光]

@eq:airy-diameter 表明红光衍射斑比蓝光大。白光点扩散函数是光源光谱、场景反射率、镜头透过率和传感器光谱响应共同加权的多波长结果。若使用 $lambda=550 "nm"$ 作为代表波长，只是在可见光中部做数量级估算。

同一镜头的红、绿、蓝通道还可能叠加色差，因此实测通道 MTF 不等于把单一波长衍射公式机械换色。

== 分辨率判据不是自然边界

=== Rayleigh 判据

两个等亮不相干点源的艾里图样相加。Rayleigh 约定把一个主峰放在另一个的第一暗环处，角间隔为

$ theta_R approx 1.22 lambda/D. $ <eq:rayleigh-criterion>

这是一个历史上方便的可分辨性准则，不是信息突然消失的物理界线。更高信噪比、已知点扩散函数和模型拟合可以在更小间隔上估计两个源；低对比度、噪声或显示限制也可能使大于 Rayleigh 间隔的结构难以看清。

=== 线对与空间频率

摄影测试更常使用周期条纹。若一黑一白构成一个周期，空间频率单位为线对每毫米（$"lp/mm"$）。系统输出调制度定义为

$ M=(I_("max")-I_("min"))/(I_("max")+I_("min")). $ <eq:modulation-definition>

输入正弦条纹调制度为 $M_("in")$，输出为 $M_("out")$，二者之比随频率变化。分辨率因此不应只报告“还能看见多少线”，还应报告该频率处保留多少对比度。

=== 摄影中的衍射截止频率

自然场景摄影通常近似为空间不相干成像。圆孔、无像差、近轴条件下，不相干强度成像的截止频率为

$ nu_c=2 "NA"/lambda approx 1/(lambda N). $ <eq:incoherent-cutoff>

普通自然场景摄影使用上式即可。激光、显微照明等相干系统具有不同传递函数，不能把其公式直接套到普通摄影。

== PSF、OTF 与 MTF

=== 线性空间不变近似

在较小视场内，若系统对不同位置使用相同点扩散函数 $h$，不相干成像可写成卷积

$ g(bold(x))=integral h(bold(x)-bold(xi)) f(bold(xi)) dif bold(xi)=(h ast f)(bold(x)). $ <eq:incoherent-convolution>

其中 $f$ 是理想像强度，$g$ 是模糊像。真实镜头在大视场、不同焦距或不同物距下通常是空间变 PSF，@eq:incoherent-convolution 只是局部模型。

=== 光学传递函数

点扩散函数的归一化傅里叶变换称为光学传递函数：

$ "OTF"(bold(nu))=cal(F)[h](bold(nu))/cal(F)[h](bold(0)). $ <eq:otf-definition>

其模为调制传递函数

$ "MTF"(bold(nu))=abs("OTF"(bold(nu))), $ <eq:mtf-definition>

其相位为相位传递函数。MTF 描述正弦对比度保留，不能独自描述边缘位置偏移、非对称拖尾或相位反转；完整 OTF 比 MTF 信息更多。

=== 衍射极限圆孔 MTF

圆形无像差瞳的 MTF 从零频处的 1 平滑下降，在截止频率处到达 0，没有“衍射开始”的突变点。缩小光圈会把整条曲线向低频压缩，而不是只让某个固定尺寸以下的细节突然消失。

#figure(
  image("../svg/mtf_sampling.svg", width: 97%),
  caption: [波长 $550 "nm"$ 时不同 f 数的理想衍射 MTF。竖线给出 $6 "um"$ 与 $4 "um"$ 像素的 Nyquist 频率。],
) <fig:mtf-sampling>

=== 系统 MTF

若多个环节都可近似为线性空间不变滤波，系统 OTF 是各环节 OTF 的乘积：

$ "OTF"_("sys")="OTF"_("lens") "OTF"_("pixel") "OTF"_("motion") dots. $ <eq:system-otf-product>

矩形像素宽度为 $p_a$ 时，单轴像素孔径 MTF 近似为

$ "MTF"_("pixel")(nu)=abs("sinc"(p_a nu)), $ <eq:pixel-aperture-mtf>

这里 $"sinc"(x)=sin(pi x)/(pi x)$。光学低通滤镜、微透镜、运动模糊和重采样都可继续乘入；锐化则可能在某些频率提高数值响应，但同时放大噪声并引入非线性或过冲。

== 衍射与像素采样

=== Nyquist 频率

像素间距为 $p$ 的规则采样，其单轴 Nyquist 频率为

$ nu_N=1/(2p). $ <eq:pixel-nyquist>

对于 $p=6 "um"=0.006 "mm"$，$nu_N=83.3 "lp/mm"$。以 $lambda=0.55 "um"$ 计算，不同 f 数的衍射量如下。

#figure(
  table(
    columns: (0.65fr, 1.15fr, 1.25fr, 1.25fr),
    align: (center, center, center, center),
    [*f 数*], [*艾里直径*], [*截止频率*], [*Nyquist 处 MTF*],
    [f/4], [$5.37 "um"$], [$454.5 "lp/mm"$], [$0.77$],
    [f/8], [$10.74 "um"$], [$227.3 "lp/mm"$], [$0.54$],
    [f/11], [$14.76 "um"$], [$165.3 "lp/mm"$], [$0.39$],
    [f/16], [$21.47 "um"$], [$113.6 "lp/mm"$], [$0.16$],
    [f/22], [$29.52 "um"$], [$82.6 "lp/mm"$], [$0$],
  ),
  caption: [$6 "um"$ 像素、$550 "nm"$ 单色光下的理想衍射数量级。],
) <tab:diffraction-sampling-example>

艾里直径在 f/8 已大于一个像素，并不意味着图像只能剩下“一个艾里斑一个像素”的分辨率。该系统在 Nyquist 频率仍保留约 $54%$ 的理想光学调制度。到 f/22，光学截止略低于采样 Nyquist，高频才在进入采样前被理想圆孔完全切除。

#paragraph-heading[欠采样、匹配与过采样]

- 若光学系统在 Nyquist 以上仍有显著能量，传感器会把这些频率混叠到低频，出现摩尔纹和假细节；
- 若光学截止接近 Nyquist，采样资源与光学带宽大致匹配；
- 若光学截止远低于 Nyquist，传感器过采样模糊图像，但仍可改善定位、降噪、去马赛克和缩放稳定性。

“像素小于艾里斑就没有意义”过于粗糙。PSF 被多个像素采样，反而有利于估计其中心和形状；收益会递减，却不会在某一尺寸突然归零。

#paragraph-heading[Bayer 阵列的额外复杂性]

彩色滤波阵列让各颜色通道的空间采样率低于总像素格点，而且不同波长的衍射截止不同。去马赛克把光学频谱、颜色相关性和插值先验混合起来。因此，单一“传感器 Nyquist”只能描述裸格点上限，不能完整预测彩色细节。

== 光圈形状与星芒

=== 边缘的傅里叶方向

瞳函数中的直边在其法向方向产生延伸频谱。多边形光圈的每条边都会贡献一条垂直于边的衍射亮线。由于相反方向的亮线重合：

- 偶数片光圈叶片通常产生同数目的主星芒；
- 奇数片叶片通常产生两倍数目的主星芒。

#figure(
  image("../svg/aperture_starbursts.svg", width: 96%),
  caption: [瞳形状与远场结构。圆孔产生旋转对称艾里环；六边形产生 6 条主星芒，七边形通常产生 14 条主星芒。],
) <fig:aperture-starbursts>

真实光圈叶片有曲率、厚度和制造误差，镜头内反射与传感器微结构也可能叠加额外光芒。太阳星芒、夜景路灯星芒和强反光点的形态不一定只由叶片数决定。

#paragraph-heading[为什么缩小光圈星芒更明显]

大光圈时，入瞳常接近圆形，直边相对主孔径较短；缩小光圈后，多边形边界更明确，主瓣变宽且衍射能量在特定方向集中，星芒更容易在高动态范围场景中被看见。星芒可见度还取决于光源亮度、曝光、传感器饱和和后期对比度。

== 像差与衍射的权衡

#paragraph-heading[收缩光圈为何可能提高画质]

球差、彗差和像散通常由远离光轴的瞳区贡献更多。缩小光圈截去这些边缘光线，使几何点扩散函数变窄、角落渐晕和场外像差减弱。与此同时，衍射主瓣按 $N$ 线性增大。

因此，系统 MTF 随光圈可能先升后降：大光圈由像差主导，中等光圈接近像差-衍射平衡，小光圈由衍射主导。中心和边缘、不同空间频率、不同波长达到峰值的 f 数并不相同。

=== 一个简化的模糊预算

对固定离焦量，几何离焦直径可写成 $c_("def")=A/N$；衍射尺度写成 $c_("diff")=B N$，其中圆孔第一暗环直径取 $B=2.44 lambda$。若暂用方差式合成近似

$ c_("tot")^2 approx (A/N)^2+(B N)^2, $ <eq:blur-budget>

最小值出现在

$ N_("opt") approx sqrt(A/B). $ <eq:blur-optimum>

#figure(
  image("../svg/blur_tradeoff.svg", width: 96%),
  caption: [简化模糊预算中的光圈折中。几何离焦随 $1/N$ 减小，衍射随 $N$ 增大，合成尺度形成任务相关的最小值。],
) <fig:blur-tradeoff>

@eq:blur-budget 不是严格 PSF 定律：艾里图样和几何圆盘都非高斯，像差还可能非对称。更严谨的方法是在频域相乘 OTF，或直接卷积各 PSF。这个模型的价值是展示“最佳光圈”为什么依赖离焦量 $A$、波长 $lambda$ 和评价指标。

#paragraph-heading[最佳光圈依赖任务]

- 若目标是画面中心某一中频的最大对比度，最佳值可能较大；
- 若要求角落、色差和景深共同可接受，可能需要进一步收缩；
- 若最终只做小尺寸输出，衍射损失可能被下采样隐藏；
- 若需要高倍率裁切，Nyquist 附近 MTF 更重要；
- 若场景有明显深度，稍小光圈带来的离焦改善可能超过合焦平面的衍射损失。

镜头评测所说的“甜点光圈”必须连同焦距、对焦距离、像高、波长和空间频率解释。

== 计算恢复的边界

#paragraph-heading[被衰减与被切除不同]

只要 OTF 在某频率非零，理论上可以用反卷积提高该频率响应；但除法同时放大噪声和模型误差。接近截止频率时 OTF 很小，恢复问题病态；超过理想截止频率后 OTF 为零，单幅线性观测没有该频率信息可供恢复。

锐化可以让边缘看起来更清楚，却不等于提高光学截止频率。生成式算法还可能依据训练先验合成合理纹理，这些纹理不是由原始光子观测唯一支持的细节。

#paragraph-heading[多帧为何可能更多]

相机或主体的亚像素位移可以提供新的采样相位，多帧超分辨率因而可能恢复单帧采样未充分记录、但仍通过光学 OTF 的频率。它不能绕过孔径的物理带宽；若频率已被光学系统完全切除，多帧只能依靠额外先验猜测。

== 常见误解

#paragraph-heading[“f/11 才开始衍射”]

衍射在任何有限孔径都存在。f/11 只可能是某个画幅、像素、波长和观察条件下，衍射下降变得可见的经验位置。

#paragraph-heading[“艾里斑比像素大就失去分辨率”]

点扩散函数本来就应由多个样本描述。应比较系统 MTF、采样频率和输出任务，而不是把两个直径作二元判断。

#paragraph-heading[“最高像素机身必须一直开最大光圈”]

大光圈可能有更强像差、场曲和景深不足。高像素传感器只是把这些差异采得更充分，并不会把较小光圈突然变得不可用。

#paragraph-heading[“后期锐化可以完全抵消衍射”]

锐化能补偿部分非零频率衰减，也会放大噪声与振铃；对 OTF 零点和截止外信息无能为力。

#struct-box(title: [本章小结])[
  - 远场衍射振幅是复瞳函数的傅里叶变换，点扩散函数是其模平方。
  - 圆孔第一暗环半径为 $1.22 lambda N$，直径为 $2.44 lambda N$。
  - Rayleigh 判据是可分辨性的工程约定，不是信息的硬边界。
  - 不相干圆孔截止频率近似为 $1/(lambda N)$，其 MTF 平滑下降而非突然归零。
  - 系统 OTF 在近似线性空间不变时可写成镜头、像素、运动等环节 OTF 的乘积。
  - Nyquist 频率由像素间距决定；艾里斑跨越多个像素不等于采样无意义。
  - 多边形孔径的边缘方向决定星芒；偶数叶片常给同数星芒，奇数叶片常给双倍星芒。
  - 收缩光圈抑制像差和离焦，却增加衍射；最佳光圈依赖任务、像高、频率和输出。
]

== 思考与计算

#block[
1. 分别计算 $lambda=450,550,650 "nm"$ 时 f/4、f/8、f/16 的艾里第一暗环直径。多色 PSF 能否用三个直径的算术平均表示？
2. 一支口径直径 $D=50 "mm"$ 的镜头在 $550 "nm"$ 下，Rayleigh 角分辨率是多少弧秒？距离 $1 "km"$ 处对应多大的横向间隔？
3. 像素间距为 $4 "um"$ 的传感器，分别在 f/4、f/8、f/16 时比较 Nyquist 频率与 $550 "nm"$ 的衍射截止频率。哪些条件下更可能发生光学欠采样？
4. 推导矩形像素孔径的 sinc MTF。若有效感光宽度只有像素间距的 $80%$，填充因子如何改变 Nyquist 处的像素孔径响应？
5. 分别为 5、6、7、8 片直边光圈画出远场主星芒方向。说明为什么奇数与偶数叶片的可见主线数量规律不同。
6. 在 @eq:blur-budget 中取 $lambda=550 "nm"$、$A=80 "um"$，计算 $N_("opt")$。改变离焦量使 $A$ 减半，最佳 f 数如何变化？
7. 写程序绘制不同 f 数的衍射 MTF，并叠加传感器 Nyquist 线与像素孔径 MTF。比较只看艾里直径和查看完整频率响应得到的判断差异。
]
