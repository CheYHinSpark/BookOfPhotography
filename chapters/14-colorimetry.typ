#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 色度学 <chap:colorimetry>

彩色照片中的三个数并不是物体“真正颜色”的直接读数。物体反射率、光源谱功率分布、镜头透过率、传感器光谱响应、白平衡、颜色矩阵和输出原色依次改变这些数；观察者的锥体响应、适应状态和周围环境又改变最终知觉。色度学的任务不是给每个光谱贴一个绝对颜色标签，而是在规定观察者与观看条件后，建立可重复的配色等价关系。

本章首先把光谱到三刺激值写成从函数空间到三维向量空间的线性算子，并由其零空间解释同色异谱。随后从原色色度和白点推导 RGB 到 XYZ 的矩阵，区分三维色彩空间、非负设备色域与非线性编码。第三部分把光源和反射率的不可分离性作为白平衡的出发点，说明相关色温、对角增益与 von Kries 型色适应各自能表达什么。最后推导 CIELAB 与色差，强调“近似感知均匀”不等于存在跨场景通用的可见阈值。系统论述可参见 @wyszecki2000、@wandell1995 与 @cie2018。

@chap:sampling-resolution 留下的是若干相机光谱通道的离散观测，它们并不自动等于标准 RGB，更不等于物体的唯一光谱。本章建立从光谱、相机响应到标准观察者和颜色坐标的桥梁；得到线性颜色关系之后，下一章才讨论怎样用有限码值高效表示亮度与颜色。

#key-box[
  *三种对象不可混淆*　光谱 $s(lambda)$ 是无限维物理函数；XYZ 或 LMS 是规定观察者下的三个线性响应；RGB、Lab 等是为设备、编辑或度量选择的坐标。坐标可以变，物理光谱和观察者条件不会因此自动改变。
]

== 从光谱到三刺激值

=== 入眼光谱与锥体响应

令到达眼睛的谱辐亮度为 $L_lambda(lambda)$。在低到中等亮度、中央视觉且忽略空间和时间适应差异的近似下，三类锥体的响应可写成

$
  l=k integral L_lambda(lambda) overline(l)(lambda) dif lambda,
  quad
  m=k integral L_lambda(lambda) overline(m)(lambda) dif lambda,
  quad
  s=k integral L_lambda(lambda) overline(s)(lambda) dif lambda,
$ <eq:lms-cone-integrals>

其中 $overline(l),overline(m),overline(s)$ 是长、中、短波锥体的有效光谱灵敏度，$k$ 取决于单位和归一化。三个响应都是光谱与权函数的内积，因此满足叠加与齐次性。两束光相加，其锥体响应也相加；整体光谱乘以常数，三响应同时乘以该常数。

真实视觉还包含杆体、锥体非线性、适应、视场位置、年龄和个体差异。标准色度学先抽取一个线性配色层，使“两个刺激能否在规定条件下配成同色”成为可测问题，而不是声称三条曲线已穷尽颜色知觉。

#figure(
  image("../svg/spectral_to_tristimulus.svg", width: 98%),
  caption: [光谱到三刺激值的线性投影。LMS、XYZ 与线性 RGB 可以是同一三维响应空间的不同基；色度坐标再把整体尺度消去。],
) <fig:spectral-to-tristimulus>

=== 配色函数与负配色量

配色实验让观察者用三种规定原色匹配一个测试刺激。若某测试光无法由原色的非负组合匹配，就把一部分原色加到测试光一侧；在线性方程中，这等价于某个配色量取负值。负值不是“负光”，而是把光加在等式另一边的记账方式。

设三种原色的谱分布为 $p_1,p_2,p_3$，测试光为 $s$。配色关系写成

$
  s tilde c_1 p_1+c_2 p_2+c_3 p_3,
$ <eq:color-matching-equivalence>

其中 $tilde$ 表示规定观察条件下同色，而非光谱函数相等。若改变原色，只要新旧原色张成同一个三维配色空间，坐标向量之间便由可逆 $3 times 3$ 矩阵转换。

CIE RGB 配色函数包含负区间。CIE XYZ 通过线性变换选择了三个非物理的数学原色，使可见光谱对应的 $X,Y,Z$ 在标准条件下一般非负，并使 $Y$ 配色函数与明视觉光谱光视效率函数一致。XYZ 的三个轴不是三种真实灯光，也不分别等于三类锥体响应。

=== CIE XYZ 的积分定义

对规定的标准观察者，XYZ 三刺激值定义为

$
  X=k integral L_lambda(lambda) overline(x)(lambda) dif lambda,
  quad
  Y=k integral L_lambda(lambda) overline(y)(lambda) dif lambda,
  quad
  Z=k integral L_lambda(lambda) overline(z)(lambda) dif lambda.
$ <eq:cie-xyz-integrals>

若 $L_lambda$ 使用绝对辐射单位并取相应光度常数，$Y$ 可对应亮度；反射样品计算中常选 $k$ 使参考白的 $Y=100$。所以 XYZ 既可以是绝对量，也可以是相对量，必须同时给出归一化。

CIE 1931 2° 标准观察者用于较小中央视场，CIE 1964 10° 标准观察者用于较大视场。两组配色函数并不相同；精密颜色测量必须报告观察者、光源、几何和视场，而不能只写一个 XYZ 三元组。

=== 同色异谱与零空间

把可见波段离散为 $N$ 个波长样本，光谱写成 $bold(s) in RR^N$，三条配色函数组成矩阵 $A in RR^(3 times N)$。三刺激值为

$ bold(t)=A bold(s). $ <eq:discrete-tristimulus-map>

通常 $N$ 远大于 3。若 $"rank"(A)=3$，秩-零度定理给出

$ "dim ker"(A)=N-3. $ <eq:tristimulus-nullity>

因此存在大量非零差光谱 $bold(d)$ 满足 $A bold(d)=0$。只要选择足够小的 $alpha$ 使 $bold(s)+alpha bold(d)$ 仍非负，便有

$ A bold(s)=A[bold(s)+alpha bold(d)]. $ <eq:metamer-nullspace>

两条光谱在标准观察者下同色，却可能被光谱仪、另一位观察者或另一组相机滤色片区分，这就是同色异谱。三刺激值能够预测配色，不足以唯一反演光谱。

#figure(
  image("../svg/metamer_nullspace.svg", width: 98%),
  caption: [同色异谱的线性代数结构。不同非负光谱之差可以落在三刺激算子的零空间内，因此给出同一 XYZ；光谱仪仍能区分二者。],
) <fig:metamer-nullspace>

=== 色度坐标与亮度分离

定义总刺激量 $S_T=X+Y+Z$。当 $S_T>0$ 时，CIE 1931 色度坐标为

$
  x=X/S_T,
  quad y=Y/S_T,
  quad z=Z/S_T=1-x-y.
$ <eq:cie-xy-chromaticity>

同一射线上的 $(X,Y,Z)$ 与 $k(X,Y,Z)$ 具有相同 $(x,y)$，因此 xy 图消除了整体尺度。若同时知道 $Y$ 且 $y>0$，可恢复

$
  X=x/y Y,
  quad Z=(1-x-y)/y Y.
$ <eq:xyy-to-xyz>

单色光的色度形成马蹄形光谱轨迹，两端由紫线闭合。两种光以非负功率相加时，混合色度位于两端点连线上；多种光的混合落在其色度凸包内。xy 图的欧氏距离并不近似统一的知觉色差，图上面积也不能直接解释为“可见颜色数量”。

#figure(
  image("../svg/chromaticity_gamuts.svg", width: 98%),
  caption: [CIE xy 色度图与三个示例 RGB 色域。三角形只描述非负线性 RGB 的色度范围，不包含亮度、位深、噪声或色差均匀性；ProPhoto RGB 的部分三角形位于可实现光谱轨迹之外。],
) <fig:chromaticity-gamuts>

== RGB 色彩空间

=== 原色、白点与线性矩阵

一个线性 RGB 空间至少要规定三原色色度和白点。设原色 $i in {r,g,b}$ 的色度为 $(x_i,y_i)$，先令每个原色 $Y_i=1$，得到未定标 XYZ 列向量

$
  bold(p)_i
  =mat(
    x_i/y_i;
    1;
    (1-x_i-y_i)/y_i
  ).
$ <eq:primary-unit-luminance-vector>

令 $P=[bold(p)_r bold(p)_g bold(p)_b]$。把白点色度 $(x_w,y_w)$ 归一到 $Y_w=1$，得到

$
  bold(w)
  =mat(x_w/y_w;1;(1-x_w-y_w)/y_w).
$ <eq:normalized-white-xyz>

三个原色所需的尺度为

$ bold(s)=P^(-1) bold(w). $ <eq:primary-white-scales>

于是线性 RGB 到 XYZ 的矩阵为

$
  M_("RGB" arrow.r "XYZ")
  =P "diag"(bold(s)),
  quad bold(t)=M bold(r).
$ <eq:rgb-to-xyz-matrix>

只要原色不共线，$P$ 可逆。不同 RGB 空间之间的变换必须在线性光域完成；若两个空间白点不同，还应在中间加入规定的色适应变换。把 gamma 编码值直接乘颜色矩阵会破坏线性叠加。

#figure(
  image("../svg/rgb_matrix_gamut_mapping.svg", width: 98%),
  caption: [由原色色度和白点构造 RGB 到 XYZ 矩阵。非负 RGB 立方体只覆盖颜色空间的一部分；超色域点可以保留为负中间值、裁切或压缩，三种操作含义不同。],
) <fig:rgb-matrix-gamut-mapping>

=== 常见工作与输出空间

#figure(
  table(
    columns: (1.05fr, 1.1fr, 1.1fr, 1.1fr, 1.05fr),
    align: (left, left, left, left, left),
    [*空间*], [*$R(x,y)$*], [*$G(x,y)$*], [*$B(x,y)$*], [*白点/编码*],
    [sRGB], [$(0.640,0.330)$], [$(0.300,0.600)$], [$(0.150,0.060)$], [D65 / 分段约 2.4],
    [Display P3], [$(0.680,0.320)$], [$(0.265,0.690)$], [$(0.150,0.060)$], [D65 / sRGB 型],
    [Adobe RGB (1998)], [$(0.640,0.330)$], [$(0.210,0.710)$], [$(0.150,0.060)$], [D65 / 约 2.2],
    [ProPhoto RGB], [$(0.7347,0.2653)$], [$(0.1596,0.8404)$], [$(0.0366,0.0001)$], [D50 / 分段 1.8],
  ),
  caption: [若干 RGB 空间的原色色度与典型编码。表中“约 2.4”等只是曲线概括；实际文件解释应以完整色彩配置文件和标准分段函数为准。],
) <tab:rgb-space-primaries>

sRGB 与 Display P3 共用蓝原色且白点相同，P3 的红、绿原色扩展了部分高彩度区域。Adobe RGB 主要向更饱和的绿色方向扩展。ProPhoto RGB 使用 D50，并包含不能由真实非负单色光直接实现的数学原色；这让它适合作为宽裕的工作容器，也意味着大量坐标不能由显示器直接发出。

“更宽色域”只说明集合包含关系或某些方向上的覆盖更大，不自动意味着颜色更准确。准确性还依赖输入表征、矩阵或查找表、白点、量化精度、显示校准和色域映射。若有限位深在更宽范围上均匀分码，局部码值间距反而可能变大；@chap:tone-encoding 将讨论非线性编码与量化。

=== 色域是三维集合

RGB 立方体 $[0,1]^3$ 经 @eq:rgb-to-xyz-matrix 变成 XYZ 中的平行六面体。xy 图中的三角形只是它沿亮度射线归一化后的投影。显示器在不同亮度下可达到的最大彩度可能不同，黑位、峰值亮度和通道串扰又改变三维颜色体积。因此用 xy 三角形面积给显示器作单一排名会丢掉亮度维度。

对一个目标 XYZ，线性 RGB 为

$ bold(r)=M^(-1) bold(t). $ <eq:xyz-to-rgb-coordinates>

若某分量小于 0 或大于 1，目标在该设备或编码范围之外。负值是坐标事实，不是计算错误；场景参考和中间处理应尽量保留它，直到确定输出设备。逐通道裁到 $[0,1]$ 会把不同点投到立方体表面，常改变色相与局部关系。感知式色域映射则把一片颜色整体压缩，牺牲绝对数值以保存排序和过渡。

=== 相机颜色矩阵为什么不是万能转换

相机的三个光谱响应与标准观察者的三条配色函数并不完全相同。因此，相机到 XYZ 的固定矩阵通常只是在指定光源和一组训练色样上的误差折衷。更换窄带 LED、荧光材料或混合光后，残差会改变；两个光谱可能对人眼同色却对相机不同，也可能反过来。

#struct-box(title: [可以继续深入])[颜色科学用 Luther–Ives 条件判断相机三通道是否能由一个固定矩阵精确变换为标准观察者响应，并用最小二乘、光谱统计或查找表拟合实际设备。本书后文只需要结论：颜色矩阵必须附带光源、训练样本和误差条件。]

若源与目标 RGB 空间白点相同，理想线性变换为

$
  bold(r)_2
  =M_2^(-1) M_1 bold(r)_1.
$ <eq:linear-rgb-space-transform>

白点不同则应写成 $M_2^(-1) A_("adapt") M_1$。颜色配置文件还可使用三维查找表处理非线性、通道交互和设备状态；“相机只需一个矩阵”只是局部线性近似。

== 白平衡与色适应

=== 光源与反射率不可唯一分离

对近似朗伯表面，忽略几何尺度后，到达相机的光谱可写成

$ s(lambda)=E(lambda) rho(lambda), $ <eq:illuminant-reflectance-product>

其中 $E$ 是照明谱，$rho$ 是反射率。相机通道为

$
  c_i
  =integral E(lambda) rho(lambda) q_i(lambda) dif lambda.
$ <eq:camera-color-formation>

只观测乘积无法唯一分解两个因子：对任意保持物理可行的正函数 $a(lambda)$，

$
  E_2=E_1 a,
  quad rho_2=rho_1/a
$ <eq:illuminant-reflectance-ambiguity>

给出相同 $s$。空间统计、已知灰卡、多个表面、照明先验或多光谱测量可以缩小解集，却不能让单个未知像素自动暴露“真实反射率”。白平衡因此是一种基于中性假设和观看意图的颜色解释。

#figure(
  image("../svg/illuminant_reflectance_wb.svg", width: 98%),
  caption: [照明-反射率歧义与对角白平衡。相机只积分两者乘积；中性参考可估计通道增益，但一个全局增益矩阵不能恢复未知光谱或同时校正多种光源。],
) <fig:illuminant-reflectance-wb>

=== 灰卡白平衡的数学含义

若参考面在可见波段近似中性，$rho_n(lambda)=rho_0$，其通道响应为

$ w_i=rho_0 integral E(lambda)q_i(lambda)dif lambda. $ <eq:neutral-reference-response>

对角白平衡选择

$
  D_("WB")="diag"(g_R,g_G,g_B),
  quad g_i=alpha/w_i,
$ <eq:diagonal-white-balance>

使 $D_("WB") bold(w)=alpha bold(1)$。$alpha$ 决定整体曝光或保留高光的归一化。增益小于或大于 1 都只是相对通道标定；实际 RAW 管线常固定绿色附近，再提升较弱通道。

该模型假设照明变化主要表现为三个响应通道的独立尺度变化。宽谱日光与黑体光附近常可近似，窄带光、强荧光、传感器通道高度重叠或反射谱尖锐时误差更大。白平衡还会把较弱通道的读出噪声和量化误差一起放大，并可能使某通道更早达到输出裁切。

=== 相关色温不是完整光源描述

温度为 $T$ 的理想黑体谱辐亮度由普朗克定律给出

$
  B_lambda(lambda,T)
  =(2h c^2)/(lambda^5)
  1/(exp[h c/(lambda k_B T)]-1).
$ <eq:planck-spectral-radiance>

不同 $T$ 的黑体色度形成普朗克轨迹。实际光源若位于轨迹附近，可在近似均匀色度图中寻找最近轨迹点，其温度称为相关色温 CCT；垂直于轨迹的带符号距离常用 $D_("uv")$ 描述绿-品红偏离。

同一 CCT 的两个光源可以具有不同 $D_("uv")$，即使 CCT 与 $D_("uv")$ 都相近，也可能具有完全不同的窄带谱并对有色物体产生不同显色。日光的谱随大气变化，也不等于同温度黑体。相机菜单中的“5600 K”只指定了一条近似白平衡轴，不能完整编码光源。

#figure(
  image("../svg/cct_chromatic_adaptation.svg", width: 98%),
  caption: [左：CCT 沿普朗克轨迹变化，$D_("uv")$ 描述近似法向偏离。右：von Kries 型色适应先进入锥体样响应空间，按源白与目标白逐通道缩放，再返回 XYZ。],
) <fig:cct-chromatic-adaptation>

=== von Kries 型色适应

人类视觉会部分适应环境白点。von Kries 型模型先把 XYZ 转到近似锥体响应空间，再按源白与目标白的响应比例缩放三个通道，最后转换回 XYZ。Bradford 等方法选择不同经验变换，以改善常见表面色的预测；它们仍是观察者适应的工程近似，不会把源光谱和反射率真正分离。完整色貌还受环境亮度、背景、视场和适应程度影响。

#struct-box(title: [可以继续深入])[色彩管理使用 Bradford 等色适应矩阵在不同参考白之间转换，色貌模型还会显式加入环境亮度、背景和适应程度。需要实现配置文件或严格软打样时可继续学习矩阵推导；本书后文只使用“白点转换是观看适应模型，不是恢复光源光谱”这一结论。]

#paragraph-heading[混合光下没有唯一全局白平衡]

若画面不同位置分别由 $E_1(lambda)$ 与 $E_2(lambda)$ 主导，一个全局 $D_("WB")$ 不能让两处中性物同时成为同一目标白，除非两光源在相机响应中恰好只差整体尺度。局部白平衡可以估计空间变化照明场 $D(x,y)$，但物体本色、阴影、镜面反射与光源边界仍会混淆。

摄影处理必须选择意图：完全中和室内暖光可能消除观者对环境的预期；保留暖色又会让灰卡不再数值中性。色度学能计算变换及残差，不能替创作者决定哪一种适应状态是“真实”。

== 感知均匀空间与色差

=== 从 XYZ 到 CIELAB

CIELAB 以采用白点 $(X_n,Y_n,Z_n)$ 归一化 XYZ。令 $delta=6/29$，定义

$
  f(t)=cases(
    t^(1/3) & t>delta^3,
    t/(3delta^2)+4/29 & t<=delta^3.
  )
$ <eq:cielab-nonlinearity>

暗部的线性段避免立方根在零附近具有无穷斜率，并使函数及一阶导数连续。三个坐标为

$
  L^*=116f(Y/Y_n)-16,
$ <eq:cielab-lightness>

$
  a^*=500[f(X/X_n)-f(Y/Y_n)],
  quad
  b^*=200[f(Y/Y_n)-f(Z/Z_n)].
$ <eq:cielab-opponent-coordinates>

对参考白有 $(L^*,a^*,b^*)=(100,0,0)$；理想黑的 $L^*=0$。$a^*$ 近似表示红-绿对手轴，$b^*$ 近似表示黄-蓝对手轴。Lab 必须连同采用白点解释；同一 XYZ 相对于 D50 与 D65 会得到不同 Lab。

#figure(
  image("../svg/lab_deltae.svg", width: 98%),
  caption: [CIELAB 的分段非线性、对手色几何和局部色差。Lab 试图把可辨差异拉得更均匀，但不同位置的等可见差仍近似为方向和大小变化的椭球。],
) <fig:lab-deltae>

=== 明度、彩度与色相

CIELAB 圆柱量定义为

$
  C_("ab")^*=sqrt((a^*)^2+(b^*)^2),
  quad
  h_("ab")="atan2"(b^*,a^*).
$ <eq:cielab-chroma-hue>

$C_("ab")^*$ 是相对于采用白点的彩度坐标，不等于物理光谱纯度，也不等同于随亮度归一的饱和度。接近中性轴时 $C^*$ 很小，色相角会对微小噪声极其敏感；给灰色像素报告精确色相没有稳定意义。

$L^*$ 是由相对 $Y/Y_n$ 经非线性压缩得到的明度尺度，不是场景曝光值、显示器绝对亮度或 RAW 线性亮度。把 Lab 的 $L^*$ 直接当作物理光子量进行加减会破坏辐射度关系。

=== Delta E 与局部度量

最简单的 CIE 1976 色差为

$
  Delta E_("ab")^*
  =sqrt((Delta L^*)^2+(Delta a^*)^2+(Delta b^*)^2).
$ <eq:delta-e-76>

它把 Lab 当欧氏空间。但实测等可辨差在不同明度、彩度和色相区域仍有方向与尺度变化，可用局部二次型概念表示

$
  Delta E^2
  approx Delta bold(x)^T G(bold(x)) Delta bold(x),
$ <eq:local-color-difference-metric>

其中 $G(bold(x))$ 是位置相关正定度量。CIE94 与 CIEDE2000 通过明度、彩度、色相权重及蓝区旋转项进一步拟合数据。CIEDE2000 的实现细节和测试样例可参见 @sharma2005。

“$Delta E<1$ 人眼不可见”不是普遍定律。阈值随观察者、显示亮度、色块面积、纹理、边缘、适应、比较时间和色差方向变化；工业容差还取决于错误成本。色差公式应与指定任务和观看条件一起使用。

#paragraph-heading[颜色相同不等于色貌相同]

标准 XYZ 相同保证规定配色条件下的同色，不保证不同背景、尺寸和亮度下具有同一外观。明度对比、色适应、同时色对比和 Hunt 效应等会让同一三刺激值呈现不同明暗或彩度。CIELAB 包含白点归一，却不是完整 HDR 色貌模型。

颜色管理通常先追求设备间色度匹配，再由观看条件模型或人工调色处理外观意图。摄影中常见的“通透”“厚重”“胶片感”不能由一个 Delta E 数字概括，因为它们还涉及色调曲线、局部对比、记忆色和语境。

=== 肤色不是固定 RGB 区域

皮肤的漫反射受黑色素、血红蛋白、组织散射和厚度影响；表面油脂产生接近光源色的镜面分量。简化的双色反射模型可写成

$
  bold(c)(x)
  =m_d(x) bold(c)_d
  +m_s(x) bold(c)_s,
$ <eq:dichromatic-skin-model>

其中漫反射颜色 $bold(c)_d$ 与生理和照明有关，镜面颜色 $bold(c)_s$ 接近光源响应。相机光谱、白平衡、颜色矩阵和色调曲线随后继续改变 RGB。

在固定设备、光源与处理管线中，肤色常在某些色度平面形成统计带状区域，可用于检测或校正；把该经验区域当作跨人群、跨光源的物理常数会产生系统偏差。肤色评价更适合报告参考白、曝光、色度、明度与不确定度，而不是寻找一个“正确 RGB”。

== 常见误解

#paragraph-heading[“颜色是物体自身的一组三个数”]

物体提供反射谱，入眼光谱还取决于照明；三个数来自规定观察者或设备对光谱的积分。改变光源、观察者或相机，坐标会改变。

#paragraph-heading[“XYZ 就是三类锥体响应”]

XYZ 与 LMS 都是三维线性响应空间的坐标，但 XYZ 的数学原色和配色函数按非负性及光度性质构造，并不逐轴对应锥体细胞。

#paragraph-heading[“同一色温的灯光颜色一定相同”]

CCT 只给出普朗克轨迹附近的一维坐标。$D_("uv")$、窄带谱结构和显色差异均未由单个 Kelvin 数描述。

#paragraph-heading[“白平衡可以恢复物体真实颜色”]

单张 RGB 只观测光源与反射率的少量积分，分解不唯一。灰卡能约束中性点，不能恢复每个物体的完整反射谱。

#paragraph-heading[“色域越宽，颜色越准确”]

色域是可表示集合；准确性是给定目标与坐标解释下的误差。更宽容器若缺少正确配置文件、位深或映射，反而更容易被误解释或出现量化问题。

#paragraph-heading[“负 RGB 没有物理意义，所以应立即归零”]

负分量表示目标位于所选原色凸锥之外，是合法中间坐标。只有输出到不能产生负光的设备时，才必须选择裁切或色域映射。

#paragraph-heading[“Delta E 小于 1 就绝对不可见”]

Delta E 是经验近似度量，可见阈值依赖颜色位置、观察条件与任务。固定阈值只能作为明确条件下的工程约定。

#struct-box(title: [本章小结])[
  - 三刺激值是光谱与三条配色函数的线性内积；标准色度学描述配色等价，不唯一描述光谱。
  - 光谱到三维响应的算子具有巨大零空间，因此不同非负光谱可以成为同色异谱。
  - xy 色度消除整体尺度；光谱轨迹、混合凸包与 RGB 三角形都不包含亮度和位深信息。
  - RGB 空间由原色、白点和编码共同定义；颜色矩阵必须作用于线性光数值。
  - 相机到 XYZ 的固定矩阵通常只在指定光源和训练色样上近似成立，不能脱离条件宣称普适。
  - 白平衡以中性参考约束通道比例，不能唯一分离照明与反射率；混合光下通常没有完美全局解。
  - CCT 只描述普朗克轨迹附近的一维位置，$D_("uv")$ 和完整谱形仍会显著影响颜色与显色。
  - CIELAB 通过白点归一和分段立方根近似均匀化色差；Delta E 阈值仍依赖颜色位置与观看任务。
]

#pagebreak(weak: true)

== 思考与计算

#block[
  1. 把可见波段离散成 31 个样本，配色矩阵 $A$ 的秩为 3。计算零空间维数；说明为什么加入光谱非负约束后同色异谱仍通常存在。
  2. 给定 $(X,Y,Z)=(19.01,20.00,21.78)$，计算 $(x,y)$；再由该 $(x,y)$ 和 $Y=50$ 恢复 XYZ。整体乘常数后哪些量不变？
  3. 证明两种非负光的混合色度位于两个端点色度的线段上。注意权重与各光的 $X+Y+Z$ 有关，而不只是功率混合系数。
  4. 用 sRGB 三原色色度和 D65 白点代入 @eq:primary-unit-luminance-vector 至 @eq:rgb-to-xyz-matrix，数值求出线性 sRGB 到 XYZ 的矩阵，并验证 $(1,1,1)^T$ 映到 D65 白点。
  5. 某 XYZ 通过一个 RGB 逆矩阵得到 $(1.12,0.42,-0.08)$。分别讨论保留负值、逐通道裁切和感知压缩的结果；为什么不能把裁切称为纯坐标变换？
  6. 中性灰卡在钨丝灯下得到线性 RAW $(0.42,0.75,0.18)$。若令绿色增益为 1，求使三个通道相等的红、蓝增益；再分析增益对各通道噪声和高光余量的影响。
  7. 由 @eq:illuminant-reflectance-ambiguity 构造两组不同 $E,rho$，使乘积相同且 $0<=rho<=1$。这说明仅凭单像素 RGB 为什么无法判断偏黄来自光源还是物体？
  8. 用 @eq:planck-spectral-radiance 比较 3000 K 与 6500 K 黑体在 450、550、650 nm 的相对谱辐亮度。归一化方式如何影响数值，而不影响色度？
  9. 取 D50 白点 $(X_n,Y_n,Z_n)=(96.422,100,82.521)$，计算两个给定 XYZ 样品的 Lab 与 $Delta E_("ab")^*$。再说明为何该数值不能自动替代印刷验收条件。
  10. 设计一个实验比较日光、黑体灯和窄带 LED 下的相机颜色误差。要求包含光谱测量、标准色卡、灰卡白平衡、固定颜色矩阵与重新拟合矩阵，并区分白点误差、同色异谱误差和曝光误差。
]
