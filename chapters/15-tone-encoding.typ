#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 亮度编码、位深与动态范围 <chap:tone-encoding>

传感器产生的线性 RAW 值、图像文件中的非线性码值和显示器发出的光不是同一种量。线性值适合表示能量叠加与曝光比例，却会把大量均匀码位分配给视觉不敏感的高亮区；非线性编码更接近人眼对相对对比的敏感性，却使直接平均、卷积和透明混合失去物理线性。色调映射又在编码之外主动改变场景与显示之间的亮度关系，把相机捕获的宽范围压入有限峰值和黑位。

@chap:noise-dynamic-range 的动态范围描述传感器在饱和与噪声阈值之间能测量多少。本章转向表示问题：OETF 如何把线性光编码为码值，EOTF 如何把码值变回显示亮度，有限位深怎样产生量化误差，抖动为何能把条带变成噪声，以及全局和局部色调映射如何重新分配对比。视频与数字成像的传递函数可参见 @poynton2012，人眼对比敏感性的系统讨论可参见 @barten1999。

@chap:colorimetry 规定了数值属于哪个颜色坐标，本章进一步规定这些数值怎样分配码位并映向显示亮度。颜色坐标、传递函数和位深三者合在一起才构成可解释的数据状态；下一章会把此前分开讨论的采样、颜色、曲线与锐化组织成一条实际 RAW 管线。

#key-box[
  *每个数值都要附带“在哪个域”*　同一个 0.5 可以表示一半场景曝光、一半编码码值或一半显示驱动；三者通常对应不同物理亮度。只有知道传递函数和参考白/峰值，数值才有完整含义。
]

== 线性光与非线性编码

=== 什么叫线性光

若图像通道 $L$ 与到达传感器或理想显示原色的辐射量成比例，并满足

$
  L(alpha s_1+beta s_2)
  =alpha L(s_1)+beta L(s_2),
$ <eq:linear-light-superposition>

则称该数值处于线性光域。扣除黑电平并校正响应后，未饱和 RAW 在局部近似线性；线性 RGB 也是相对于其原色光功率的线性坐标。以下操作通常要求线性光输入：

- 不同曝光归一化与 HDR 融合；
- 颜色矩阵和白平衡增益；
- 卷积、缩放预滤波与像素平均；
- 透明度合成和运动模糊积分；
- 物理光照、反射与加法混色。

线性不表示“看起来均匀”。把 $L$ 从 0.01 增到 0.02 是一档，0.50 增到 0.51 却只有很小的相对变化；若以等步长存储，暗部可见差异更容易挤在少数码位内。

=== OETF、EOTF 与 OOTF

令场景线性光为 $L_s$，编码信号为 $V$，显示亮度为 $L_d$。三个常用概念是：

- *光电转换函数 OETF*：$V=cal(E)(L_s)$，把场景或相机线性信号变为非线性电信号；
- *电光转换函数 EOTF*：$L_d=cal(D)(V)$，描述显示器如何把信号变为光；
- *光光转换函数 OOTF*：$L_d=cal(O)(L_s)$，描述场景到显示的整体关系。

若只为高效编码，理想编解码可满足 $cal(D)=cal(E)^(-1)$，使 $cal(D)(cal(E)(L))=L$。摄影系统通常还加入曝光、对比、观看环境补偿和创作曲线，所以整体 OOTF 不必恒等。OETF/EOTF 是接口定义，OOTF 则包含外观意图。

#figure(
  image("../svg/encoding_pipeline.svg", width: 98%),
  caption: [从场景线性光到显示光的域转换。创作映射、OETF、数字码值和 EOTF 具有不同职责；在执行能量线性运算前必须回到线性域。],
) <fig:encoding-pipeline>

=== “Gamma”不是一个函数的唯一名称

理想幂律编码可写成

$
  V=L^(1/gamma),
  quad L=V^gamma,
$ <eq:pure-power-transfer>

其中编码指数小于 1，显示端指数大于 1。历史 CRT 的电压到亮度近似幂律，后来“gamma”又被用于文件编码曲线、显示 EOTF、系统对比和软件滑杆，语义逐渐混杂。严谨表述应说明：方向是编码还是解码，输入输出是否归一化，是否有线性 toe，以及指数对应哪一段。

sRGB 的标准编码不是纯 $gamma=2.2$ 幂律。对归一化线性光 $L in [0,1]$，其编码函数为 @iec61966srgb

$
  E_("sRGB")(L)
  =cases(
    12.92 L & quad 0 <= L <= 0.0031308,
    1.055 L^(1/2.4) - 0.055 & quad 0.0031308 < L <= 1.
  )
$ <eq:srgb-oetf>

逆函数为

$
  D_("sRGB")(V)
  =cases(
    V / 12.92 & quad 0 <= V <= 0.04045,
    ((V + 0.055) / 1.055)^2.4 & quad 0.04045 < V <= 1.
  )
$ <eq:srgb-eotf>

线性段避免零附近无穷斜率，也让低码值行为更稳定。实际显示器可能不精确遵循该曲线，颜色管理会通过校准曲线和配置文件补偿。

#figure(
  image("../svg/transfer_curves.svg", width: 98%),
  caption: [线性、sRGB 与纯幂律的编码/解码曲线。OETF 把暗部拉开，EOTF 将码值弯回线性显示光；两侧曲线方向不可混称为一个 gamma。],
) <fig:transfer-curves>

=== 为什么非线性编码更省码位

在许多但并非全部视觉条件下，恰可辨亮度差近似满足 Weber 关系

$ Delta L/L approx k_W, $ <eq:weber-fraction>

即视觉更接近响应相对变化而非绝对变化。若 $u=log L$，则等 $Delta u$ 对应等比例亮度变化。幂律或对数型编码把更多码位分给低亮度，使量化步长更接近感知阈值。

人眼并不是固定 gamma 传感器。$k_W$ 随亮度、空间频率、适应、视场和背景变化；接近视觉阈值时 De Vries-Rose 区、暗视觉与眩光又有不同规律。传递曲线是工程分配，不是完整视觉模型。

=== 在线性域平均与在码域平均

对黑 $L_0=0$ 与白 $L_1=1$，物理平均为 $L_m=0.5$。经 @eq:srgb-oetf 编码，

$ E_("sRGB")(0.5) approx 0.735. $ <eq:linear-half-srgb-code>

若错误地直接平均 sRGB 码值，得到 $V_m=0.5$，解码后却是

$ D_("sRGB")(0.5) approx 0.214. $ <eq:encoded-half-linear-light>

因此编码码域中 50% 灰只有白色约 21.4% 的线性亮度，而一半线性亮度应存为约 73.5% 码值。

#figure(
  image("../svg/linear_encoded_mixing.svg", width: 98%),
  caption: [黑白平均的域依赖。线性平均得到 50% 光并编码为约 0.735；直接平均 sRGB 码值得到 0.5，显示时只有约 0.214 的线性光。],
) <fig:linear-encoded-mixing>

对颜色矩阵、卷积和重采样也有同样问题。若 $E$ 为非线性函数，一般有

$ E(alpha x+beta y) != alpha E(x)+beta E(y). $ <eq:nonlinear-map-no-superposition>

只有在局部范围足够小、曲线近似仿射时，码域线性操作才可能近似无害。

=== HDR 传递函数的两类参照

HDR 视频常见两条思路 @iturbt2100：

- *PQ* 把码值映到绝对显示亮度，目标范围最高定义至 10000 $"cd/m"^2$；它是显示参照的感知量化 EOTF；
- *HLG* 用分段平方根/对数 OETF 表示相对场景信号，显示端结合峰值与观看环境形成系统 OOTF。

PQ 的标准标尺最高定义至 $10000 "cd/m"^2$，不表示每台 HDR 显示器都能达到该亮度。内容母版、元数据和显示器色调映射仍决定超出设备能力的部分怎样压缩。

#struct-box(title: [可以继续深入])[ITU-R BT.2100 给出了 PQ 与 HLG 的完整分段函数、常数和系统观看条件。需要制作 HDR 视频、校准监看或实现编解码器时应直接查标准；理解摄影工作流只需先区分 PQ 的绝对显示参照与 HLG 的相对系统参照。]

== 量化、位深与抖动

=== 均匀标量量化

设归一化值 $x in [0,1]$ 用 $B$ 位无符号码表示，共有 $N=2^B$ 个码位。若包含 0 和 1 两端，步长为

$ Delta=1/(2^B-1). $ <eq:normalized-quantization-step>

舍入量化器可写为

$
  Q_B (x)
  =Delta "round"(x/Delta),
$ <eq:uniform-rounding-quantizer>

量化误差 $e_q=Q_B (x)-x$ 在不溢出时满足

$ -Delta/2<=e_q<=Delta/2. $ <eq:quantization-error-bound>

在输入跨越许多码位、误差相位近似均匀且与信号不相关的高分辨率模型中，

$
  "E"[e_q] approx 0,
  quad "Var"[e_q] approx Delta^2/12.
$ <eq:uniform-quantization-variance>

这不是所有图像的定律。缓慢渐变只跨少数码位时，误差会与信号锁定成锯齿，产生条带；固定常量输入的误差也不是随机变量。

=== 位数不等于有效精度

$B$ 位只说明量化器最多提供 $2^B$ 个码位，不保证这些码位都能分辨真实信号。图像传感器的输入还含散粒与读出噪声；若模拟噪声标准差已经跨越多个最低码位，增加文件位深会更细致地记录噪声，却不等比例增加场景可辨信息。反过来，只要量化步长明显小于输入噪声，量化通常不再是主要误差源。

#struct-box(title: [可以继续深入])[ADC 工程会用理想量化信噪比、SINAD 和有效位数 ENOB 区分标称位数与实际精度，并测量微分非线性、积分非线性和时钟抖动。本书后文只需要“位数、动态范围和有效信息量不是同一个量”。]

=== 线性编码中每档有多少码

线性归一值的最亮一档 $[1/2,1]$ 占全部码位约一半；下一档 $[1/4,1/2]$ 占四分之一。第 $k$ 个低于满幅的曝光档约有

$ N_k approx (2^B-1)/2^k $ <eq:linear-codes-per-stop>

个码差。随着 $k$ 增大，暗部每档的线性码数指数下降。14 位 RAW 的最亮一档约跨 8192 个码，而第 12 档只有约 4 个码；实际黑电平、增益和噪声还会改变有效数量。

若先作 $V=L^(1/gamma)$ 编码，第 $k$ 档所占码数近似

$
  N_(k,gamma)
  approx(2^B-1)[
    2^(-(k-1)/gamma)-2^(-k/gamma)
  ].
$ <eq:gamma-codes-per-stop>

暗部因此获得更多码位，但这些码位表示的是非线性亮度区间，不是更多光电子。

#figure(
  image("../svg/bitdepth_allocation.svg", width: 98%),
  caption: [同为 8 位，线性与幂律编码在各曝光档分配的码数不同。位深只决定总码数，传递函数决定码位密度。柱上数字为教学近似。],
) <fig:bitdepth-allocation>

=== 非线性编码后的线性光步长

若解码函数为 $L=D(V)$，码域步长为 $Delta V$，局部线性光步长近似

$ Delta L approx D'(V) Delta V. $ <eq:decoded-local-step>

对 $D(V)=V^gamma$，

$
  Delta L
  approx gamma V^(gamma-1) Delta V
  =gamma L^(1-1/gamma) Delta V.
$ <eq:power-code-linear-step>

同一码差在不同亮度对应不同物理差。评价 8 位输出是否足够，不能只比较 $1/255$，还要把码值经 EOTF 换到显示亮度并与该亮度、面积和空间频率下的视觉阈值比较。

=== 抖动为何能减少条带

在量化前加入随机抖动 $d$：

$ y=Q_B (x+d). $ <eq:nonsubtractive-dither>

适量抖动会增加细小随机起伏，却能打散与信号锁定的规则量化误差，使缓慢渐变不再形成明显条带。空间误差扩散还会把部分低频误差推向较高空间频率，借助观看距离和输出缩放获得更平滑的平均。

#struct-box(title: [可以继续深入])[量化理论会区分减法抖动、非减法抖动、TPDF 分布和噪声整形，并分析误差是否与输入独立。本书后文只使用结论：合适随机化可以用细噪声交换结构化条带，但不会恢复裁切或增加单像素信息。]

#figure(
  image("../svg/quantization_dither.svg", width: 98%),
  caption: [均匀量化与抖动。无抖动的渐变产生与信号相关的周期误差；抖动让单像素更随机，却可使空间或时间平均接近原连续值。],
) <fig:quantization-dither>

抖动不能恢复被裁切的高光，也不能创造单像素额外精度；它改变的是误差的统计和频谱形态。适量细噪声反而可能让低位深渐变更平滑，因此“看见噪声少”与“量化更准确”不是同一判断。

=== 从 14 位 RAW 到 8 位输出发生了什么

把简化处理链写成

$
  bold(z)
  =cal(Q)_8 (
    cal(E)(
      cal(T)(
        cal(C)(
          cal(W)(bold(r)_("RAW"))
        )
      )
    )
  ),
$ <eq:raw-to-eight-bit-chain>

其中 $cal(W)$ 表示白平衡，$cal(C)$ 表示颜色变换，$cal(T)$ 表示色调/外观映射，$cal(E)$ 表示输出编码，$cal(Q)_8$ 表示 8 位量化。14 位输入不是把最低 6 位简单删除：宽场景范围先被曝光、曲线和颜色变换重新分配，再量化到输出。

8 位文件可以平滑地显示来自 14 位 RAW 的最终结果，因为非线性编码和抖动提高了视觉效率；但它通常不能保留 RAW 的高光余量、阴影线性精度、白平衡可调性和重新选择色调映射的自由度。多个不同 RAW 值经曲线、裁切和量化后可能落到同一码值，逆过程不唯一。

== 曲线与色调映射

=== 单调映射与局部对比

全局色调曲线是标量函数

$ y=T(x). $ <eq:global-tone-curve>

若 $T$ 单调不减，输入亮度排序不会反转；若严格递增且无限精度、无裁切，则数学上仍可逆。实际处理中的平台、端点裁切和有限码值会让多个输入合并。

对邻近输入 $x$ 与 $x+Delta x$，输出差近似

$ Delta y approx T'(x)Delta x. $ <eq:tone-local-slope>

$T'(x)>1$ 提高该区间的线性对比，$0<T'(x)<1$ 压缩对比。若用曝光对数坐标衡量相对对比，且 $x,T(x)>0$，增益为

$
  (dif log T)/(dif log x)
  =x T'(x)/T(x).
$ <eq:log-domain-contrast-gain>

端点固定为 $T(0)=0,T(1)=1$ 时，一段斜率增大必须由其他区间斜率减小补偿。S 曲线常在中间调提高斜率，在阴影 toe 与高光 shoulder 中压缩；它不是凭空增加全范围信息，而是重新分配可见对比。

#figure(
  image("../svg/tone_curve_geometry.svg", width: 98%),
  caption: [S 型色调曲线和局部斜率。toe 与 shoulder 压缩端部，中间调提高局部对比；对数域增益还取决于 $T(x)$ 本身。],
) <fig:tone-curve-geometry>

=== 黑点、白点与裁切

给定输入黑点 $b$ 和白点 $w>b$，最简单归一化是

$
  x_n
  ="clip"((x-b)/(w-b),0,1).
$ <eq:black-white-normalization>

$x<=b$ 全部映为 0，$x>=w$ 全部映为 1，因此两端不可逆。传感器黑电平是电子读出偏置，输出黑点则是创作/显示选择；把二者混为一谈会错误删除阴影信号。

软 shoulder 用渐进减小斜率的曲线保留高光排序，硬裁切则让所有越界值相等。软 roll-off 仍会压缩色差与局部对比；如果某通道已在 RAW 中饱和，曲线只能平滑边缘，不能恢复原通道比例。

=== 两个全局色调映射例子

对归一化正亮度 $L>=0$，Reinhard 型映射

$ T_R(L)=L/(1+L) $ <eq:reinhard-simple-tonemap>

把无界输入压到 $[0,1)$，导数为 $1/(1+L)^2$，高光逐渐压缩。若希望指定 $L_w$ 映到白，可用

$
  T_(R,w)(L)
  =(L(1+L/L_w^2))/(1+L).
$ <eq:reinhard-white-point>

另一类对数映射为

$
  T_("log")(L)
  =log(1+alpha L)/log(1+alpha L_("max")),
$ <eq:logarithmic-tonemap>

其中 $alpha$ 控制暗部提升与高光压缩。所有全局算子都让相同输入 $L$ 得到相同输出，无法同时照顾同一亮度在阴影内部和明亮天空中的不同局部语境。

直方图均衡使用累计分布函数

$ T_("HE")(x)=F_X(x) $ <eq:histogram-equalization-map>

使理想连续输出近似均匀分布。它依赖整幅图的统计，可能把占面积很大的背景分配过多码位、放大噪声，并使相邻照片映射不一致；“直方图更平”不是自然外观的充分条件。

=== 保持色度还是逐通道映射

若线性 RGB 的亮度为 $Y=bold(a)^T bold(c)$，可用共同尺度

$
  bold(c)_("out")
  =T(Y)/Y bold(c)_("in"),
  quad Y>0,
$ <eq:luminance-ratio-tonemap>

保持通道比例和色度。高光压缩后结果仍可能超出目标 RGB 色域；真实相机还常加入随亮度去饱和，使强高光趋近光源白。若分别对 R、G、B 使用同一非线性曲线，通道比例通常改变，色相和彩度也会移动。

亮度映射与色域映射因此不能完全独立：提高峰值亮度可能扩大某些可显示彩度，压缩高光又会改变 Lab/XYZ 关系。稳定管线需明确在哪个颜色空间、哪种白点和哪一参照状态下应用曲线。

=== 局部色调映射

局部算子写成

$ y(x,y)=T(L(x,y);cal(N)_(x,y)), $ <eq:local-tone-operator>

其中 $cal(N)_(x,y)$ 表示邻域。一个常见框架在对数亮度中分解基底与细节：

$
  ell=log L,
  quad b=G_sigma ast ell,
  quad d=ell-b,
$ <eq:base-detail-decomposition>

再压缩低频基底并控制细节：

$
  ell'=alpha b+beta d,
  quad L'=exp(ell').
$ <eq:base-detail-recomposition>

$0<alpha<1$ 压缩大尺度动态范围，$beta$ 控制局部纹理。若 $G_sigma$ 在强边缘两侧混合，基底会跨越物体边界，重组后产生亮边或暗边 halo；边缘保持滤波可减轻但不消除尺度选择问题。$beta>1$ 还会放大皮肤纹理、噪声和压缩伪影。

#figure(
  image("../svg/global_local_tonemap.svg", width: 98%),
  caption: [全局与局部色调映射。局部方法通过基底-细节分解压缩大尺度范围并保留纹理，但可能产生 halo、噪声放大和跨图像不一致。],
) <fig:global-local-tonemap>

=== HDR 场景到有限显示器

设场景有效亮度范围为 $[L_(s,min),L_(s,max)]$，显示器在观看环境中的有效黑位与峰值为 $L_(d,min),L_(d,max)$。若

$
  L_(s,max)/L_(s,min)
  >L_(d,max)/L_(d,min),
$ <eq:scene-display-range-mismatch>

则不可能同时保持所有绝对亮度比；必须裁切、压缩或改变观看适应。环境反射亮度 $L_a$ 会同时抬高显示黑位和白位，有效对比近似

$
  C_("eff")
  =(L_("peak")+L_a)/(L_("black")+L_a).
$ <eq:ambient-effective-contrast>

在明亮环境中，即使面板本征黑位很低，$L_a$ 也会迅速吞噬阴影差异。HDR 不只是更亮高光：它还要求稳定黑位、足够局部对比、合适 EOTF、色彩体积和观看环境。

#figure(
  image("../svg/hdr_display_mapping.svg", width: 98%),
  caption: [场景曝光范围到 SDR/HDR 显示的示意映射。更高峰值与更低黑位减少压缩需求，但任何有限设备仍需根据内容和环境决定阴影、高光与中间调的分配。],
) <fig:hdr-display-mapping>

色调映射改变的是已测量信号的表达。它不能增加传感器满阱、降低拍摄时读出噪声或恢复裁切通道；局部对比增强让暗部“看起来有细节”，不等于拍摄动态范围变大。

== 常见误解

#paragraph-heading[“Gamma 就是亮度滑块”]

Gamma 可能指编码幂律、显示 EOTF 或系统关系。亮度滑块还可能改变曝光、偏置或白点；不说明输入输出域就没有唯一数学含义。

#paragraph-heading[“图像看起来正常，就可以直接在当前码值上平均”]

非线性码值为视觉分配而设计，不满足能量叠加。平均、卷积、缩放和透明合成通常应先逆 OETF，在线性光域计算，再重新编码。

#paragraph-heading[“多一位就多一档动态范围”]

位深增加总码数，动态范围还取决于编码曲线、噪声、黑位和饱和。线性理想量化下每位约改善 6 dB 量化 SNR，不等于真实相机必然多一档可用阴影。

#paragraph-heading[“8 位必然出现色阶断裂”]

是否可见取决于传递曲线、显示亮度、渐变跨度、噪声、抖动和观看条件。合适非线性编码与抖动可使 8 位最终输出平滑，但不保留高位深编辑余量。

#paragraph-heading[“抖动只是破坏画质的噪声”]

抖动增加单样本随机性，却能消除与信号相关的量化失真，让空间或时间平均表达亚码级变化。过强抖动会可见，零抖动也可能产生更难看的规则条带。

#paragraph-heading[“色调映射能恢复过曝和死黑”]

曲线只能重排已有数值。硬裁切后的高光只有下界，未被噪声分辨的阴影也没有可靠结构；平滑 roll-off 或局部增强不是信息恢复。

#paragraph-heading[“局部 HDR 效果等于更大传感器动态范围”]

局部算子改变邻域对比和适应线索，可能让有限范围看起来更丰富；传感器动态范围仍由拍摄时饱和与噪声决定。

#struct-box(title: [本章小结])[
  - 线性光值满足能量叠加；非线性编码值用于高效分码，两者上的平均、卷积和矩阵运算通常不等价。
  - OETF 把场景/线性光变为信号，EOTF 把信号变为显示光，OOTF 描述场景到显示的整体外观关系。
  - sRGB 是带线性 toe 的分段函数，不是纯 $gamma=2.2$；PQ 是绝对显示参照，HLG 更偏相对场景/系统参照。
  - $B$ 位提供 $2^B$ 个码位；量化方差 $Delta^2/12$ 只在高分辨率、误差去相关近似下成立。
  - 传递曲线决定码位在亮度区间的分配；高位深输入经色调映射后可以平滑输出为 8 位，却不再保留 RAW 调整自由度。
  - 抖动以随机误差替代结构化条带，使平均可表达亚码级变化；它不恢复裁切或增加单像素位深。
  - 单调色调曲线保持排序，局部斜率重新分配对比；toe、shoulder 和中间调增益之间存在资源权衡。
  - 局部色调映射依赖邻域，可压缩大范围并增强纹理，也可能制造 halo、噪声和不一致。
  - HDR 输出受峰值、黑位、环境反射、EOTF 和色彩体积共同限制；色调映射不等于传感器动态范围扩展。
]

#pagebreak(weak: true)

== 思考与计算

#block[
  1. 用 @eq:srgb-oetf 与 @eq:srgb-eotf 计算线性光 $L=0.001,0.01,0.18,0.5$ 的 sRGB 码值，并验证往返误差。哪些值落在线性 toe 内？
  2. 三个线性像素值为 0.1、0.3、0.9。比较“先平均后 sRGB 编码”和“先编码后平均再解码”；解释差异为何来自 Jensen 不等式。
  3. 用 @eq:linear-codes-per-stop 计算 14 位线性 RAW 从满幅以下第 1 至第 14 档各约有多少码。为何最后几个结果不能直接代表真实阴影质量？
  4. 对 $gamma=2.2$ 与 8 位编码，用 @eq:gamma-codes-per-stop 计算前 8 个曝光档的码数，并与线性编码比较。再用 @eq:decoded-local-step 分析中灰附近的线性光步长。
  5. 对 $T(x)=x/(1+x)$ 求一阶、二阶导数和对数域对比增益。在哪些亮度区间压缩最强？若先乘曝光 $a$，$T(a x)$ 与 $a T(x)$ 是否相同？
  6. 构造一条满足 $T(0)=0,T(1)=1$ 的分段单调曲线，使中间调斜率为 1.5。由积分约束说明 toe 或 shoulder 至少有一段斜率必须小于 1。
  7. 对 @eq:luminance-ratio-tonemap，证明共同正尺度保持线性 RGB 通道比例。给出一个缩放后仍超出目标色域的例子，并比较逐通道裁切后的色相变化。
  8. 实现 @eq:base-detail-decomposition。比较高斯、双边和导向滤波作为 $G_sigma$ 时的边缘 halo、纹理保持和噪声放大；分析 $alpha,beta,sigma$ 的作用。
  9. 一台显示器峰值 1000 $"cd/m"^2$、本征黑位 0.005 $"cd/m"^2$。分别在环境反射亮度 0、0.1、1、5 $"cd/m"^2$ 下用 @eq:ambient-effective-contrast 计算有效对比，并讨论为何 HDR 母版在明亮房间中需要不同映射。
]
