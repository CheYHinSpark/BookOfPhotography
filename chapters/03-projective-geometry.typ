#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading
#import "../diagrams/projective_geometry.typ": pinhole-projection

#pagebreak(weak: true)

= 中心投影与摄影透视 <chap:projective-geometry>

现在开始介绍与摄影相关的几何光学。

本章把相机近似为中心投影装置。我们先用相似三角形建立三维点到二维像点的关系，再推导视角和画幅；随后证明固定机位时焦距只产生整体缩放，而固定主体大小时更换焦距必然伴随机位变化。最后讨论消失点、平面单应变换、非直线投影和镜头畸变。相关射影几何与标定方法可参见 @hartley2004 与 @szeliski2022。

@chap:light-models 说明应按问题选择光的模型。这里先采用最简单的射线与针孔近似，只回答“空间点落在画面哪里”，暂不追问镜头怎样会聚、是否合焦以及光量多少。下一章再用真实透镜补上这些机制，因此投影与透镜不会被混成同一个问题。

#key-box[
  *本章的核心区分*　机位决定从哪个投影中心观察三维世界；焦距和画幅决定从该投影中截取多大的角域；镜头畸变描述真实像点偏离选定理想投影的位置。
]

== 针孔相机模型

#paragraph-heading[相机坐标系与虚像平面]

令相机投影中心为 $C$，相机坐标系的 $Z$ 轴沿视线方向向前，$X,Y$ 分别为横向和纵向。空间点在相机坐标系中写成

$ bold(X)_c = (X,Y,Z)^T, quad Z>0. $ <eq:camera-coordinate-point>

为了避免传感器上的倒像符号，射影几何通常把一个虚拟像平面放在投影中心前方 $Z=f$ 处。连接 $C$ 与空间点的直线和该平面相交于 $(x,y,f)$。由相似三角形，

$ x=f X/Z, quad y=f Y/Z. $ <eq:pinhole-projection>

#figure(
  pinhole-projection(width: 94%),
  placement: auto,
  caption: [针孔相机的截面模型。虚像平面位于投影中心前方，便于写出正号投影；真实传感器位于后方并形成倒像。],
) <fig:pinhole-projection>

@eq:pinhole-projection 只包含比值 $X/Z$ 和 $Y/Z$。因此，把空间点的三个坐标同时乘以任意正常数，像点不变。这是单幅图像缺少绝对尺度的直接来源。

#paragraph-heading[齐次坐标与相机标定]

射影几何常用齐次向量 $(u,v,w)^T$ 表示平面点 $(u/w,v/w)$，任意非零比例倍数都表示同一个点。这种写法能把投影、单应变换和消失点统一为矩阵关系。相机标定则把参数分成两类：内参描述焦距、主点和像素坐标，外参描述相机相对世界的旋转与平移。商品镜头标注的毫米焦距是物理长度，而标定中的焦距常以像素为单位，两者不能直接比较。

#struct-box(title: [可以继续深入])[计算机视觉把齐次三维点经 $3 times 4$ 相机矩阵投到齐次像点，并把矩阵分解为内参与外参。三维重建、摄影测量和相机标定需要完整矩阵推导；本书后文只使用中心投影、消失点和单应变换的摄影结论。]

#paragraph-heading[投影保留与丢失什么]

理想中心投影具有以下性质：

- 三维直线在不穿过投影中心时映为二维直线；
- 同一三维平面内的共线关系与交比保持；
- 平行线一般不再平行，而交于消失点；
- 长度、角度、面积和平行性一般不保持；
- 沿同一视线的所有三维点映到同一像点。

最后一条意味着深度被压缩到射线参数中。单张照片不能在没有额外约束时给出每个像素的唯一距离。

== 焦距、画幅与视角

#paragraph-heading[矩形传感器的视角]

记传感器某一方向的有效尺寸为 $d$，焦距为 $f$，则对应视角为
// 这怎么推出来的？

$ alpha(d,f)=2 arctan(d/(2f)). $ <eq:angle-of-view>

分别代入传感器宽、高和对角线，可得水平、垂直和对角视角。由上式可见视角不是焦距单独的属性，同一镜头安装在不同尺寸传感器上，截取的是同一投影中的不同角域。

#figure(
  table(
    columns: (0.8fr, 1fr, 1fr, 1fr),
    align: (center, center, center, center),
    [*焦距*], [*水平视角*], [*垂直视角*], [*对角视角*],
    [$24 "mm"$], [$73.74 degree$], [$53.13 degree$], [$84.06 degree$],
    [$35 "mm"$], [$54.43 degree$], [$37.85 degree$], [$63.44 degree$],
    [$50 "mm"$], [$39.60 degree$], [$26.99 degree$], [$46.79 degree$],
    [$85 "mm"$], [$23.91 degree$], [$16.07 degree$], [$28.56 degree$],
  ),
  caption: [尺寸为 $36 times 24 "mm"$ 的画幅在常见焦距下的几何视角。],
) <tab:full-frame-aov>

视角随焦距不是线性变化。长焦区中 $d/(2f)$ 较小，$alpha approx d/f$，焦距加倍近似使视角减半；超广角区必须保留反正切，简单比例会明显失准。

#paragraph-heading[裁切系数]

若参考画幅对角线为 $d_("ref")$，实际画幅对角线为 $d_s$，裁切系数定义为

$ c=d_("ref")/d_s. $ <eq:crop-factor>

实际焦距 $f$ 在小画幅上的视角，近似等于参考画幅上焦距 $c f$ 的视角，因此称 $c f$ 为等效焦距。这个“等效”只是视角换算，保证在相同长宽比下视角接近，不是把镜头的物理焦距改变了。
// 并不自动保证：

// - 入瞳直径相同；
// - 景深相同；
// - 单位像面积或整幅图像的信噪比相同；
// - 像素数、衍射采样或镜头像质相同。

#paragraph-heading[数字裁切]

固定镜头与机位，把图像中央裁成线性尺寸的 $1/c$，所得视角等同于把焦距乘以 $c$ 后重新取景。理想连续图像中，两者的投影几何完全相同。但是在实际数字图像中，裁切会减少像素数，并可能放大噪声、衍射和镜头像差。

== 透视由机位决定

=== 固定机位时焦距只缩放像坐标

对同一相机位置和朝向，空间点的 $X/Z,Y/Z$ 不变。焦距从 $f$ 改为 $k f$ 时，

$ x'=k x, quad y'=k y. $ <eq:focal-scaling>

所有像点相对主点作同一比例缩放，因此任意两个同方向坐标的比值保持：

$ x'_1/x'_2=x_1/x_2. $ <eq:perspective-ratio-invariant>

这意味着固定机位更换焦距不会改变物体之间的透视比例。长焦照片若与广角照片在同一投影中心拍摄，并把广角中央裁到相同视角，两者的几何关系一致。

#figure(
  image("../svg/perspective_focal_distance.svg", width: 100%),
  caption: [焦距与机位的不同作用。],
) <fig:perspective-focal-distance>

=== 保持主体大小时必须改变距离

设主体真实高度为 $H_s$，距投影中心为 $Z_s$，像高为

$ h_s=f H_s/Z_s. $ <eq:subject-image-height>

若更换焦距后仍保持 $h_s$ 不变，则 $Z_s$ 必须与 $f$ 成比例。所谓“用长焦拍同样大小的人像”，通常隐含了摄影者后退，因此透视变化来自新的 $Z_s$，而不是来自焦距本身。

设背景物体高度为 $H_b$，位于主体后方距离 $Delta$，即 $Z_b=Z_s+Delta$。背景与主体的像高之比为

$
  h_b/h_s
  = (H_b/H_s) Z_s/(Z_s+Delta).
$ <eq:background-subject-ratio>

固定主体构图而增加焦距时，$Z_s$ 增大，因子 $Z_s/(Z_s+Delta)$ 趋近 1，背景相对主体变大，看起来被“拉近”。这就是长焦压缩感的定量来源。

#paragraph-heading[近大远小与面部比例]

一个三维物体内部也有深度。设鼻尖和耳部到相机距离分别为 $Z_n$ 与 $Z_e$，两处相同尺度细节的投影比为 $Z_e/Z_n$。相机很近时，二者距离差相对于平均距离不可忽略，鼻部被相对放大；相机后退后，该比值趋近 1，面部显得更平。焦距只是为了在不同距离上恢复所需取景。

因此，“35 mm 会把鼻子拍大、85 mm 更自然”不是焦距的直接生理效应。若两支镜头位于同一机位并裁成相同构图，脸部比例相同，真实拍摄中差异主要来自工作距离。

#paragraph-heading[希区柯克变焦]

// 在一些电影里……

若相机沿光轴移动，同时连续改变焦距，使主体像高 $h_s$ 保持不变，主体轮廓近似固定，而 @eq:background-subject-ratio 持续变化。背景会膨胀或收缩，这种“推拉变焦”是区分焦距缩放和机位透视的直接实验。

== 直线投影与超广角边缘

=== 矩形投影

理想针孔或校正良好的普通镜头采用矩形投影（rectilinear projection）。一条与光轴夹角为 $theta$ 的方向映到半径

$ r=f tan theta. $ <eq:rectilinear-projection>

它把三维直线保持为图像直线，但当 $theta arrow.r 90 degree$ 时 $r$ 发散。超广角画面边缘必须用越来越大的像面距离表示有限角度增量，因此圆形物体和人脸会被强烈拉伸。这可以在完全零镜头畸变的系统中出现。

=== 其他中心方向映射

鱼眼镜头牺牲直线保持性，以有限像圆容纳更大角域。常见轴对称投影写成：

#figure(
  table(
    columns: (1.2fr, 1.25fr, 1.75fr),
    align: (left, center, left),
    [*投影*], [*径向关系*], [*主要性质*],
    [矩形], [$r=f tan theta$], [直线保持，$90 degree$ 处发散],
    [等距], [$r=f theta$], [径向距离与入射角成正比],
    [等立体角], [$r=2f sin(theta/2)$], [相等立体角映为相等面积],
    [立体投影], [$r=2f tan(theta/2)$], [局部保角],
    [正交], [$r=f sin theta$], [边缘强压缩，可覆盖半球],
  ),
  caption: [常见轴对称投影模型。不存在同时保持所有长度、角度、面积和直线的平面投影。],
) <tab:wide-angle-projections>

把鱼眼图像“校正”为矩形投影，并不是消除一个小误差，而是在投影模型之间重采样。接近 $90 degree$ 的方向会被无限拉伸，必须裁掉或容忍极低的边缘采样密度。

=== 正确观看距离

一张中心投影照片只有在适当观看位置才严格复现原来的视角几何。若传感器宽度为 $d$，照片显示宽度为 $W$，拍摄焦距为 $f$，观察者眼睛到照片的理想距离为

$ D_v = f W/d. $ <eq:correct-viewing-distance>

这使观看时照片边缘张角与拍摄时一致。广角照片常被从过远处观看，边缘拉伸因而显得夸张；长焦照片若观看过近，则会强化“压扁”的感受。透视既存在于图像几何中，也与观看几何共同作用。

== 消失点与地平线

=== 平行线为何相交

空间直线可写为

$ bold(X)(tau)=bold(X)_0+tau bold(d), $ <eq:spatial-line>

其中 $bold(d)$ 是方向。令 $tau arrow.r infinity$，平移项相对消失，直线方向的消失点满足

$ lambda bold(v)_d=K R bold(d). $ <eq:vanishing-point>

消失点只依赖相机内参、旋转和三维方向，不依赖相机平移。同一方向的一族平行线共享消失点；与像平面平行的方向，其消失点位于射影平面的无穷远处，图像中仍保持平行。

#figure(
  image("../svg/vanishing_homography.svg", width: 100%),
  caption: [左：地面平行线汇聚到地平线上的消失点。右：平面单应变换可以把一个透视四边形校正为矩形，但需要重新采样。],
  placement: auto,
) <fig:vanishing-homography>

#paragraph-heading[一点、两点与三点透视]

- 相机正对一个盒状场景时，只有深度方向的平行线汇聚，常称一点透视；
- 相机绕竖直轴旋转后，两个水平主方向分别汇聚，形成两点透视；
- 相机再向上或向下俯仰，竖直方向也产生有限消失点，形成三点透视。

这些名称描述的是场景主方向相对于像平面的关系，不是镜头焦段类别。广角只是包含更大角域，使消失点和汇聚线更容易同时进入画面。

#paragraph-heading[地平线与相机姿态]

一个三维平面上的所有方向，其消失点组成该平面的消失线。水平地面的消失线就是地平线；它的位置由相机旋转决定，与相机离地高度无关。相机升高会改变遮挡和地面可见比例，却不会在保持姿态不变时把数学地平线移出同一像素行。

若希望建筑竖线保持平行，应让传感器平面与建筑竖直方向平行，即避免相机俯仰。移轴镜头的平移功能可以在不倾斜传感器的情况下改变取景；后期透视校正则通过重采样模拟部分结果。

== 平面单应变换

=== 从三维平面到二维平面

若场景点都位于某个平面上，可选平面坐标 $(U,V)$。其像点满足

$ lambda tilde(bold(p))=H (U,V,1)^T, $ <eq:plane-homography>

其中 $H$ 是可逆 $3 times 3$ 矩阵。矩阵整体比例无意义，因此有 8 个自由度。一般位置下，四对点对应提供 8 个独立标量约束，可确定 $H$。

同一平面从两台相机观察时，也存在

$ lambda tilde(bold(p))'=H_(21) tilde(bold(p)). $ <eq:interimage-homography>

全景拼接在相机仅绕投影中心旋转时，可以对整幅远景近似使用单应变换；若相机发生平移并且场景有显著深度差，不同深度具有不同视差，单个 $H$ 无法同时对齐。

#paragraph-heading[透视校正的代价]

对建筑立面、画作或文档，可估计 $H^(-1)$ 把透视四边形校正为矩形。几何关系被恢复，并不意味着信息被恢复：

- 被压缩区域需要放大，噪声和模糊同时被放大；
- 重采样会引入插值与相关性；
- 输出矩形通常需要裁切或填充空白；
- 被遮挡或未进入视场的内容仍不存在；
- 非平面物体不能由同一单应变换正确校正。

校正后的局部分辨率可由变换雅可比矩阵分析。若某处面积放大因子很大，该区域的每个输出像素只来自更少的原始采样，视觉清晰度不会因输出像素增多而自动提高。

== 镜头畸变

=== 畸变是对理想投影的偏离

理想中心投影把直线映成直线。真实镜头若让像点相对理想位置发生系统性径向或切向偏移，就称为几何畸变。畸变主要改变像点位置，不必然使一个局部点像变模糊；因此它与球差、彗差和离焦属于不同误差类型。

令理想归一化像点为 $(x,y)$，$r^2=x^2+y^2$。常用 Brown-Conrady 模型 @brown1966 的径向因子为

$ F(r)=1+k_1 r^2+k_2 r^4+k_3 r^6. $ <eq:radial-distortion-factor>

加入切向畸变参数 $p_1,p_2$ 后，

$
  x_d=x F(r)+2p_1 x y+p_2(r^2+2x^2),
$ <eq:brown-x>

$
  y_d=y F(r)+p_1(r^2+2y^2)+2p_2 x y.
$ <eq:brown-y>

$k_i$ 描述轴对称径向偏移，$p_i$ 描述透镜元件偏心或倾斜导致的切向分量。模型只是近似展开；极端广角、波浪形畸变或数字镜头设计可能需要更高阶或分段模型。

#figure(
  image("../svg/distortion_grids.svg", width: 100%),
  caption: [同一理想方格经过负径向系数、零系数和正径向系数后的示意。桶形与枕形的符号取决于采用正向还是逆向映射。],
) <fig:distortion-grids>

在 @eq:radial-distortion-factor 的正向模型中，$k_1<0$ 通常把边缘点拉向中心，形成桶形畸变；$k_1>0$ 把边缘推向外侧，形成枕形畸变。某些软件定义的是从畸变坐标到理想坐标的逆模型，符号会反转，因此参数必须连同模型方向解释。

#paragraph-heading[标定与校正]

几何标定通过已知直线、棋盘格角点或多视图约束，同时估计内参和畸变参数。校正时通常采用逆向映射：对每个输出理想像素，计算它在原畸变图像中的连续坐标，再插值取样。这样可避免正向映射留下孔洞。

校正的代价包括：

- 桶形校正后四角常出现空白，需要裁切并损失视角；
- 边缘区域被放大，局部噪声和模糊更显著；
- 多次重复校正会积累插值损失；
- 视频防抖、呼吸补偿和畸变校正叠加时，有效裁切随时间变化。

保留 RAW 并不意味着畸变尚未处理。某些相机系统把镜头校正作为默认图像管线的一部分，RAW 元数据甚至可能要求解码器应用特定映射。

== 四种“变形”必须区分

#figure(
  table(
    columns: (1.15fr, 1.55fr, 1.5fr, 1.35fr),
    align: (left, left, left, left),
    [*现象*], [*主要原因*], [*改变焦距但固定机位*], [*典型处理*],
    [近大远小], [投影中心与物体深度], [只作整体缩放，不改变], [改变机位],
    [超广角边缘拉伸], [矩形投影 $r=f tan theta$], [裁到相同视角后相同], [改变投影或构图],
    [桶形/枕形], [真实镜头偏离理想投影], [可能随焦距和对焦变化], [标定与重采样],
    [斜拍建筑汇聚], [传感器平面相对建筑倾斜], [汇聚点不因焦距消失], [调整姿态、移轴或单应校正],
  ),
  caption: [摄影中常被统称为“畸变”的四类几何现象。],
) <tab:deformation-distinctions>

#warn-box[
  *判断顺序*　先问机位是否改变，再问采用哪种投影模型，最后才检查镜头是否偏离该模型。否则很容易把透视、边缘拉伸和光学畸变归咎于同一个“广角缺陷”。
]

#struct-box(title: [本章小结])[
  - 针孔模型满足 $x=f X/Z,y=f Y/Z$；深度只以比值形式进入图像，因此单幅投影缺少绝对尺度。
  - 相机标定把焦距、主点等内部参数与相机位置、朝向等外部参数分开描述。
  - 视角由焦距与画幅共同决定；等效焦距只等效视角。
  - 固定机位改变焦距只对像坐标作统一缩放；保持主体构图时，焦距变化迫使机位变化，从而改变透视。
  - 长焦“压缩”来自 $Z_s/(Z_s+Delta)$ 随拍摄距离增加而趋近 1。
  - 矩形投影保持直线，却在接近 $90 degree$ 时产生强边缘拉伸；鱼眼投影选择了不同的不变量。
  - 平行线方向决定消失点，平面点集可由 $3 times 3$ 单应矩阵变换。
  - 镜头畸变是对选定理想投影的系统偏离，与机位透视和矩形投影边缘拉伸不同。
]

== 思考与计算

#block[
1. 相机坐标中有三点 $(1,1,5)$、$(2,1,10)$、$(-1,2,4)$，焦距为 $50 "mm"$。求虚像平面坐标，并指出哪些三维尺度变化不会改变结果。
2. 推导 @eq:angle-of-view。计算宽度为 $23.5 "mm"$ 的传感器配 $35 "mm"$ 镜头的水平视角，并求在 $36 "mm"$ 宽参考画幅上的等效焦距。
3. 证明固定机位时，两物体像高之比与焦距无关。这个结论在加入不同程度的径向畸变后是否仍严格成立？
4. 身高 $1.75 "m"$ 的主体后方 $5 "m"$ 处有一扇同高的门。分别在主体距离 $1.5 "m"$ 与 $6 "m"$ 时保持主体像高不变，计算门与主体的像高比，并解释视觉差异。
5. 对矩形投影计算 $dif r/dif theta=f sec^2 theta$。比较 $theta=0 degree,60 degree,80 degree$ 时单位角度对应的径向伸长，说明超广角边缘采样为何迅速变稀。
6. 一张由 $36 "mm"$ 宽传感器和 $24 "mm"$ 镜头拍摄的照片，被显示为 $60 "cm"$ 宽。用 @eq:correct-viewing-distance 求理想观看距离。
7. 给定地面上的两组平行线，在图像中估计两个消失点。说明如何由它们得到地平线，以及为何相机平移不会改变该线而相机旋转会改变。
8. 写程序生成规则网格，并用 @eq:radial-distortion-factor 分别取 $k_1=-0.2,0,0.2$ 进行变换。比较正向绘制与逆向重采样的孔洞、裁切和插值问题。
]
