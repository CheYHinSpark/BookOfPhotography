#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 采样、像素与分辨率 <chap:sampling-resolution>

镜头在像面上形成的仍是连续辐照度场，数字照片却是一张有限大小的数值数组。两者之间并不是简单的“把图像切成小方格”：每个像素先在有限面积、有限波段和有限时间内积分，随后才在传感器晶格上留下一个标量；彩色滤波阵列又使不同位置测量不同光谱响应。连续信息一旦在采样时混叠，后期即使把数组放大到任意尺寸，也不能仅凭单张图像确定原来的高频属于哪个频谱副本。

本章先建立像素孔径与二维晶格采样的算子模型，再由频域中的周期复制推导奈奎斯特区域、混叠和摩尔纹。随后把光学 MTF、像素 MTF、采样密度与输出尺度接成一条分辨率链，并说明“像素数”为什么不是独立细节数。最后把 Bayer 等彩色滤波阵列写成周期欠采样算子，将去马赛克表述为依赖先验的欠定逆问题。采样理论可参见 @shannon1949 与 @szeliski2022，数字相机采样和颜色成像的联系可参见 @wandell1995。

@chap:diffraction-aperture、@chap:aberrations-mtf 与 @chap:motion-stabilization 已经说明连续像在采样前怎样失去对比，本章继续追踪它如何变成离散数组。空间采样的终点是 CFA 通道值，而不是完整颜色；下一章因此从“像素测到哪一个通道”转向“这些通道怎样对应人的配色经验与设备颜色”。

#key-box[
  *先滤波，后采样*　像素孔径、镜头、运动和光学低通滤镜在采样前改变连续频谱；晶格采样则把频谱周期复制。前者可以降低进入混叠区的能量，后者不是又一条可相乘的 MTF 曲线。
]

== 从连续像到离散数组

=== 光电子数是四重积分

令传感器表面的谱辐照度为 $E_lambda(x,y,t)$，颜色滤镜与探测器联合量子效率为 $eta_c(lambda)$，第 $(m,n)$ 个像素的空间收集函数为 $a_(m n)(x,y)$。单个光子能量为 $h c/lambda$；忽略饱和与串扰时，期望光电子数可写成

$
  mu_(m n,c)
  =integral_0^T integral eta_c(lambda) lambda/(h c)
  integral integral E_lambda(x,y,t)
  a_(m n)(x,y)
  dif x dif y dif lambda dif t.
$ <eq:pixel-spatiotemporal-spectral-integral>

@chap:exposure-metering 突出时间与辐射度，@chap:sensor-physics 突出光谱响应和电子转换；本章只固定时间与颜色通道，研究空间积分和离散化。若像素阵列均匀，中心间距为 $p_x,p_y$，空间响应只是同一孔径函数的平移，便有

$ a_(m n)(x,y)=a(x-m p_x,y-n p_y). $ <eq:shifted-pixel-aperture>

把曝光和光谱积分后的连续像记为 $e_c(x,y)$。为只讨论空间传递，以下把孔径归一化为 $integral integral a(x,y)dif x dif y=1$；绝对电子数中的有效面积仍保留在整体信号尺度内。像素的无噪声输出是

$
  z_c[m,n]
  =integral integral e_c(x,y)
  a(x-m p_x,y-n p_y) dif x dif y.
$ <eq:pixel-aperture-sample>

若 $a$ 关于原点对称，定义孔径平均后的连续场 $overline(e)_c=e_c ast a$，则 $z_c[m,n]=overline(e)_c(m p_x,n p_y)$。因此“像素值位于像素中心”只是为数组赋予坐标的约定；物理信号来自像素周围的一片面积。

#figure(
  image("../svg/pixel_aperture_sampling.svg", width: 98%),
  caption: [连续像先经像素孔径面积积分，再在中心间距为 $p$ 的晶格上形成数组。孔径宽度 $a$ 与像素间距 $p$ 是两个不同参数；点采样只是孔径趋于冲激的理想极限。],
) <fig:pixel-aperture-sampling>

=== 像素间距、孔径与填充因子

对宽度为 $a_x,a_y$ 的理想矩形孔径，可用归一化函数

$
  a(x,y)
  =1/(a_x a_y)
  "rect"(x/a_x) "rect"(y/a_y)
$ <eq:rectangular-pixel-aperture>

描述空间平均，其中 $"rect"(u)=1$ 当 $abs(u)<=1/2$，否则为零。几何填充因子为

$ eta_("fill")=(a_x a_y)/(p_x p_y). $ <eq:geometric-fill-factor>

真实像素的微透镜会把名义像素面积上的光重定向到光敏区，深沟槽隔离、金属布线、斜入射和电荷扩散又会改变有效 $a(x,y)$。所以数据表中的几何开口率不能完整决定空间响应；有效孔径甚至可能带有邻像素串扰的尾部。

像素间距 $p$ 决定采样晶格密度，孔径宽度 $a$ 决定采样前平均强度。缩小 $a/p$ 会保留更多高频，却通常减少直接收集面积并增加混叠风险；增大有效孔径会提高光子收集和抗混叠能力，却使高频对比更早下降。这是物理取舍，不是“越接近点采样越精确”的单向排序。

=== 像素孔径的传递函数

@eq:rectangular-pixel-aperture 的二维傅里叶变换为

$
  H_("pixel")(nu_x,nu_y)
  ="sinc"(a_x nu_x) "sinc"(a_y nu_y),
$ <eq:pixel-aperture-otf>

其中 $"sinc"(u)=sin(pi u)/(pi u)$。它在两个坐标方向可分，但不是旋转对称函数。若 $a_x=a_y=p$，轴向奈奎斯特频率 $nu_x=1/(2p),nu_y=0$ 处有

$ abs(H_("pixel"))="sinc"(1/2)=2/pi approx 0.637. $ <eq:pixel-mtf-at-nyquist>

若两个频率分量同时等于 $1/(2p)$，对角点的响应为 $(2/pi)^2 approx 0.405$。这两个位置的径向频率不同，不能把方形像素的 MTF 当作只依赖 $sqrt(nu_x^2+nu_y^2)$ 的曲线。

#figure(
  image("../svg/pixel_aperture_mtf.svg", width: 98%),
  caption: [矩形像素孔径的 sinc 响应。图中横坐标按每个坐标分量的 $nu p$ 表示；满孔径像素在轴向奈奎斯特点为 $2/pi$，在两个分量都达到奈奎斯特的对角点为其平方。],
) <fig:pixel-aperture-mtf>

== 二维采样定理与混叠

=== 晶格与梳状函数

定义二维采样梳状函数

$
  "III"_(p_x,p_y)(x,y)
  =sum_(m,n in ZZ)
  delta(x-m p_x) delta(y-n p_y).
$ <eq:two-dimensional-comb>

理想采样场等于孔径滤波后的连续像与梳状函数相乘：

$ s_c(x,y)=overline(e)_c(x,y) "III"_(p_x,p_y)(x,y). $ <eq:lattice-sampling-product>

空间域相乘对应频域卷积。若 $hat(overline(e))_c(nu_x,nu_y)$ 是采样前频谱，则

$
  hat(s)_c(nu_x,nu_y)
  =1/(p_x p_y)
  sum_(k,l in ZZ)
  hat(overline(e))_c(
    nu_x-k/p_x,
    nu_y-l/p_y
  ).
$ <eq:sampling-spectrum-replicas>

连续频谱因此以倒晶格间距 $1/p_x,1/p_y$ 周期复制。采样不是“截掉奈奎斯特以上频率”；它把这些频率折回基本频率胞元，并与原有低频相加。

#figure(
  image("../svg/sampling_spectrum_aliasing.svg", width: 98%),
  caption: [采样的频域解释。一维图展示频谱副本未重叠与重叠；二维方形晶格的基本奈奎斯特区域是矩形。相差整数倍采样频率的正弦在样本位置不可区分。],
) <fig:sampling-spectrum-aliasing>

=== 奈奎斯特区域不是圆

矩形晶格的一个基本频率胞元为

$
  cal(N)
  ={(nu_x,nu_y):
  abs(nu_x)<1/(2p_x),
  abs(nu_y)<1/(2p_y)}.
$ <eq:rectangular-nyquist-region>

若采样前频谱严格限制在 $cal(N)$ 内，各副本不重叠，连续场可由二维 sinc 插值精确重建：

$
  overline(e)(x,y)
  =sum_(m,n in ZZ) z[m,n]
  "sinc"(x/p_x-m)
  "sinc"(y/p_y-n).
$ <eq:ideal-sinc-reconstruction>

这一定理要求无限范围样本、无噪声、精确晶格和严格带限。自然图像的边缘并不带限，镜头与像素 MTF 也通常只是渐近降低而非在某频率后严格为零；实际相机只能让混叠足够小，不能满足理想前提的每一项。

方形奈奎斯特区域在轴向边界为 $1/(2p)$，角点的径向距离为 $sqrt(2)/(2p)$。因此一句“奈奎斯特频率是多少”必须同时给出方向。六角晶格可在某些近似各向同性带限问题中更高效地铺排频谱，但传感器读出、存储与图像处理通常围绕矩形阵列设计。

=== 为什么混叠不可由单帧唯一撤销

一维复正弦 $exp(i 2pi nu x)$ 在 $x=m p$ 处的样本为

$ exp(i 2pi nu m p). $ <eq:sampled-complex-sinusoid>

把频率改为 $nu+k/p$，其中 $k in ZZ$，样本变为

$
  exp[i 2 pi (nu+k/p)m p]
  =exp(i 2 pi nu m p) exp(i 2 pi k m)
  =exp(i 2 pi nu m p).
$ <eq:alias-equivalence>

所以一组离散样本同时与无穷多个连续频率相容。相机或算法可以依据镜头带宽、场景统计和邻域结构猜测最可能的原频率，却不能从同一组无噪声样本中逻辑排除所有别名。

对实正弦，折叠后的基带频率可写成

$
  nu_("alias")
  =min_(k in ZZ) abs(nu-k f_s),
  quad f_s=1/p,
$ <eq:folded-alias-frequency>

并伴随可能的相位和方向反转。二维纹理则按倒晶格向量折叠，原本细密的斜纹可能变成另一方向的粗纹。

=== 两点一周期仍可能看不见

取正弦 $f(x)=sin(pi x/p+phi)$，它恰在奈奎斯特频率。样本为

$ f(m p)=(-1)^m sin phi. $ <eq:nyquist-phase-samples>

当 $phi=0$ 时所有样本均为零；当 $phi=pi/2$ 时样本在 $+1$ 与 $-1$ 之间交替。所谓“每周期两个像素足够”只是在严格带限且一般相位下的唯一重建边界，不保证任意相位都有稳定对比，也不保证噪声存在时可可靠检测。

#figure(
  image("../svg/sampling_phase_moire.svg", width: 98%),
  caption: [左：奈奎斯特正弦的样本对比完全依赖相位。右：相近空间频率或角度的两组栅格产生低频拍频，形成摩尔纹；微小位移会显著改变其相位。],
) <fig:sampling-phase-moire>

斜边法之所以能得到亚像素采样的边缘扩展函数，正是利用许多像素行拥有不同采样相位；它不是让单个像素突破物理带宽。周期测试图若恰与像素网格锁相，测得对比可能随平移出现剧烈振荡，因此分辨率测试需要倾斜、相位平均或多次位移。

=== 摩尔纹、锯齿与假细节

两条相近频率 $nu_1,nu_2$ 的余弦相乘时有

$
  cos(2pi nu_1 x) cos(2pi nu_2 x)
  =1/2 cos[2pi(nu_1-nu_2)x]
  +1/2 cos[2pi(nu_1+nu_2)x].
$ <eq:moire-beat-identity>

低频差项 $abs(nu_1-nu_2)$ 是拍频的数学原型。织物、屏幕像素、建筑网格与采样晶格的高频相互作用，会产生原场景中不存在的大尺度波纹。斜线的阶梯边缘是方向量化的表现；CFA 各通道采样晶格不同，还会把亮度高频错误分配到色度，形成假色。

混叠不是普通随机噪声：它由真实高频确定地产生，因而常具有规则结构并可伪装成可信细节。缩小图像能让某些摩尔纹不再显眼，却不能证明折叠前的频率已经恢复。

=== 光学低通滤镜的取舍

采样前总传递函数可近似写为

$
  H_("pre")
  =H_("opt")
  H_("motion")
  H_("pixel")
  H_("OLPF").
$ <eq:presampling-transfer-product>

理想抗混叠滤镜应让 $cal(N)$ 外的频谱为零，同时完整通过内部频率；有限空间支撑、光谱依赖与二维方向要求使这种砖墙滤镜无法物理实现。相机的光学低通滤镜常用双折射层把点像分裂到相邻位置，以牺牲部分奈奎斯特附近对比来降低摩尔纹和假色。

没有独立 OLPF 也不表示完全没有预滤波：衍射、像差、失焦、运动、电荷扩散和像素孔径都在衰减高频。反之，高质量镜头、较大光圈、静止主体和细密周期纹理恰会增加可到达采样边界的能量。移除 OLPF 的收益与风险因此依赖镜头、光圈、像素间距、题材和后续算法，不能只用“更锐”概括。

== 像素数与可分辨细节

=== 三个空间尺度

若传感器宽度为 $W$、横向像素数为 $N_x$，像素间距近似为 $p_x=W/N_x$，轴向采样奈奎斯特频率为

$
  nu_(N,x)=1/(2p_x)=N_x/(2W)
$ <eq:sensor-nyquist-frequency>

，单位可为 cycle/mm。若以画面高度 $H$ 归一化，某空间频率 $nu$ 对应的线宽数为

$ "LW/PH"=2 nu H. $ <eq:line-widths-per-picture-height>

一个周期含一明一暗两条线，因此 line pair 与 line width 相差因子 2。厂商、测试软件和论文有时使用 LP/mm、cycles/pixel、LW/PH 或 L/PH；缺少单位定义的“分辨率 3000 线”没有可比性。

输出尺度又引入缩放因子。传感器上的 1 cycle/mm 经放大打印后对应多少 cycle/mm，取决于传感器到输出的线性倍率；屏幕一比一查看则把一个传感器像素映为一个或多个显示像素。评价最终可见细节时，必须给出输出尺寸、观看距离和重采样方法。

=== 光学带宽与采样带宽的匹配

圆孔衍射极限的非相干截止频率为

$ nu_c=1/(lambda N), $ <eq:incoherent-diffraction-cutoff>

其中 $N$ 为像方有效 f 数。在只比较轴向截止位置的粗略匹配中，令 $nu_c=nu_N$ 得

$ p approx lambda N/2. $ <eq:diffraction-sampling-match>

这不是“最佳像素尺寸”公式。真实光谱含多种 $lambda$，像差会改变 MTF 形状，像素孔径在奈奎斯特前已衰减对比，CFA 又使颜色通道拥有不同采样结构。即使 $nu_c$ 高于 $nu_N$，新增光学带宽也可能主要表现为混叠；即使 $nu_c$ 低于 $nu_N$，过采样仍可改善边缘定位、重采样和抗混叠余量。

连续、近似空间不变的各模糊环节可以在频域相乘，但晶格采样与去马赛克必须另行处理。因此可把链条写成

$
  e
  arrow.r
  H_("pre") e
  arrow.r
  cal(S)_("lattice")
  arrow.r
  cal(R)_("digital")
  arrow.r
  y_("out"),
$ <eq:resolution-operator-chain>

而不能简单写成“镜头 MTF × 传感器像素数”。

#figure(
  image("../svg/resolution_chain.svg", width: 98%),
  caption: [有效分辨率链及高像素的边际收益。连续预滤波、离散采样、CFA 重建和输出缩放回答不同问题；提高采样密度的收益取决于采样前是否仍有带宽和信噪比。],
) <fig:resolution-chain>

=== 分辨率、锐度与可检测性

“分辨率”至少有三种常见定义：

- *截止式定义*：某频率后理想系统传递为零，例如衍射截止频率；
- *阈值式定义*：MTF 降到 50%、10% 或其他约定值时的频率；
- *任务式定义*：在给定噪声和误报率下，观察者或算法能区分、定位或识别的最高频率。

MTF50 常与主观清晰感相关，却不是信息的绝对边界；锐化可以提高中频 MTF 或产生过冲，使 MTF50 上升，却没有自动增加无噪声观测中的独立自由度。另一方面，低对比细节即使处在奈奎斯特以内，也可能因散粒噪声、读出噪声或量化而不可检测。严格比较应同时报告频率响应和噪声，而不是只报告一条“解析力”。

在频率 $bold(nu)$ 处，可用任务相关的频域信噪比作概念表达：

$
  "SNR"_("out")(bold(nu))
  =abs(H_("pre")(bold(nu)))
  abs(F_("scene")(bold(nu)))
  /sqrt(P_n(bold(nu))),
$ <eq:frequency-dependent-snr>

其中 $P_n$ 是输出噪声功率谱。该式还未包括混叠项；若高频折回，分母之外还会出现结构化干扰。所谓“有效分辨率”因此依赖场景谱、噪声、混叠和判定任务，不是相机的单个永恒常数。

#paragraph-heading[过采样与欠采样]

若光学像的主要带宽远低于 $nu_N$，系统处于过采样状态。更多像素不会等比例增加新细节，但仍可能带来：更小的采样相位误差、更精细的几何定位、更温和的数字抗混叠滤波以及更灵活的裁切。代价是数据量、读出带宽、功耗和处理负担。

若光学带宽显著超过 $nu_N$，系统欠采样。单像素边缘看起来可能很“硬”，但周期纹理容易混叠；换一小段拍摄距离、焦点或相机位置，细节会跳变。欠采样系统的视觉锐利不等于忠实记录。

像素总数从 $N$ 增加到 $4N$，若画幅比例不变，线性采样密度只增加 2 倍，奈奎斯特频率也只增加 2 倍。若镜头、运动或景深已经把这段新增频带压得很低，独立细节增长会远小于 4 倍。

=== 裁切、缩放与像素合并

裁切只保留较小视场，并不改变原数组的像素间距或原始奈奎斯特频率；若把裁切区域放到同样输出尺寸，它放大了已有采样和噪声。高像素传感器允许在同一输出像素数下裁得更深，前提是光学像在新增采样频带中确有可用信号。

把图像每 $d times d$ 个样本平均后再抽取，可写成

$
  y[k,l]
  =1/d^2 sum_(i=0)^(d-1) sum_(j=0)^(d-1)
  x[d k+i,d l+j].
$ <eq:block-average-decimation>

这相当于方盒低通后按 $d$ 倍降采样，但方盒滤波的 sinc 旁瓣并不是理想抗混叠响应。高质量缩小通常使用更合适的低通核再抽取。若直接每隔 $d$ 个像素取一个值而不预滤波，原本处于新奈奎斯特之外的频率会再次混叠。

传感器内模拟或电荷域合并、数字 RAW 求和、去马赛克后平均并不等价：它们发生在读出噪声、CFA 与非线性处理链的不同位置。@chap:noise-dynamic-range 已讨论它们的噪声差异；从采样角度看，任何降采样都必须先限制到新的频率胞元。

=== 多帧亚像素位移能否突破单帧晶格

若多帧之间存在已知的亚像素位移，且场景、曝光和光学 PSF 稳定，不同帧可提供互补采样相位。设第 $r$ 帧位移为 $bold(delta)_r$，则

$
  z_r[m,n]
  =overline(e)(m p_x+delta_(r,x),n p_y+delta_(r,y))
  +epsilon_r[m,n].
$ <eq:shifted-multiframe-samples>

联合求解可恢复超过单帧奈奎斯特、但仍被光学系统传递且在噪声中可辨的频率。这是多帧超分辨的基础。它不能恢复 $H_("pre")$ 的严格零点，也会被配准误差、主体运动、滚动快门和曝光变化破坏。所谓“像素位移高分辨率”增加的是测量集合，不是把一张既有照片凭空放大。

== 彩色滤波阵列与去马赛克

=== 单个 RAW 像素通常不是 RGB 三元组

设希望表示的三个相机通道场为 $R[m,n],G[m,n],B[m,n]$，CFA 掩模 $M_R,M_G,M_B$ 在每个位置只有一个为 1。理想马赛克观测为

$
  y[m,n]
  =M_R[m,n]R[m,n]
  +M_G[m,n]G[m,n]
  +M_B[m,n]B[m,n]
  +epsilon[m,n].
$ <eq:cfa-mosaic-observation>

在 $M N$ 个位置上，观测只有 $M N$ 个标量，而待估计的三通道数组有 $3M N$ 个标量。即使忽略噪声，逐像素问题也欠定；“缺失颜色”不是藏在 RAW 中尚未读取，而是必须利用邻域、通道相关或学习先验估计。

实际 CFA 的红、绿、蓝滤镜光谱宽且重叠，$R,G,B$ 只是三个相机光谱响应，并非人眼锥体响应或标准显示原色。后面的色度学、亮度编码与 RAW 管线三章将再讨论颜色匹配、白平衡和相机颜色矩阵；本章只研究空间排列。

#figure(
  image("../svg/cfa_demosaic_model.svg", width: 98%),
  caption: [CFA 观测与去马赛克。每个位置测得一个光谱加权标量，算法用空间平滑、边缘方向、通道相关或数据先验估计其余通道，因此输出颜色是重建结果。],
) <fig:cfa-demosaic-model>

=== Bayer 掩模的频率结构

以一个 $2 times 2$ 周期中偶—偶位置为 R、奇—奇位置为 B、其余为 G。令 $s_m=(-1)^m,s_n=(-1)^n$，三个掩模可写成

$
  M_R=1/4(1+s_m)(1+s_n),
$ <eq:bayer-red-mask>

$
  M_B=1/4(1-s_m)(1-s_n),
  quad
  M_G=1/2(1-s_m s_n).
$ <eq:bayer-blue-green-masks>

$M_R+M_G+M_B=1$。掩模中的常数项和交替符号项对应频域中位于直流、水平奈奎斯特、竖直奈奎斯特与对角奈奎斯特位置的频谱搬移。马赛克化不是简单地“绿色分辨率为红色两倍”；它把亮度与色差信息的若干线性组合搬到不同频带，能否分离取决于这些频带是否重叠。

常用的概念变换是

$
  Y=1/4(R+2G+B),
  quad C_R=R-G,
  quad C_B=B-G.
$ <eq:luminance-chrominance-decomposition>

自然场景中亮度结构往往比色度结构具有更高带宽，去马赛克算法据此假设 $C_R,C_B$ 在局部更平滑，并用绿色或亮度边缘引导其他通道插值。这是统计规律，不是物理定理：红蓝细条、彩色 LED 阵列、织物和显示屏正是容易违反它的题材。

=== 从双线性插值到边缘保持

最简单的双线性去马赛克对同色邻点加权平均。它在线性缓变区域有效，却会跨越边缘混合两侧颜色，产生模糊和拉链状伪影。更高质量的线性方法可联合使用颜色差与局部梯度，例如 @malvar2004；方向自适应方法沿较平滑方向插值；频域方法利用 Bayer 掩模的谱搬移结构；现代学习方法则从训练数据估计图像先验。综述可参见 @gunturk2005。

所有算法都在数据一致性与先验之间取舍。过强平滑会抹去真实彩色细节，过强锐化或纹理先验会制造迷宫纹和假色。算法在常见自然图像上效果很好，不等于它对任意数学测试纹理都能唯一恢复。

一个基本的数据一致性要求是：重建后的通道在原采样位置重新通过 CFA 时，应尽量回到原观测。除此之外，算法还会偏好局部平滑、边缘连续、颜色差缓变或训练数据中常见的结构。过少约束会让噪声和混叠进入结果，过强约束则会把算法偏好强加给真实场景。

#struct-box(title: [可以继续深入])[去马赛克可写成带数据一致性项和图像先验的正则化逆问题，并用总变差、稀疏表示或神经网络求解。本书后文只需要结论：未采样的颜色必须估计，算法选择会改变细节与假色。]

=== 阵列排列没有无条件赢家

原始 Bayer 概念由 @bayer1976 提出。更大周期、较不规则的排列会把重复频谱能量分布到不同倒晶格位置，可能降低某些规则纹理形成强烈彩色摩尔纹的概率；代价是同色邻点距离、局部方向对称性、插值核、RAW 软件支持和标定复杂度发生变化。X-Trans 可视为这类更大周期排列的一个实例，但不能脱离像素光谱、光学预滤波与具体去马赛克算法作品牌式优劣判断。

#figure(
  table(
    columns: (1.1fr, 1.15fr, 1.35fr, 1.55fr),
    align: (left, left, left, left),
    [*传感方式*], [*每位置观测*], [*采样结构*], [*主要取舍*],
    [单色传感器], [一个宽光谱值], [全位置同一晶格], [无单帧颜色；同像素数下没有 CFA 空间欠采样],
    [$2 times 2$ Bayer], [一个 CFA 通道], [小周期、规则倒晶格], [算法成熟；高频亮度与色度可能结构化重叠],
    [更大周期 CFA], [一个 CFA 通道], [频谱搬移位置更分散], [可能分散规则伪色；邻域和重建更复杂],
    [多层/分光测量], [同位置多个响应], [通道空间共址或多传感器], [减少空间插值；光谱串扰、光损失、配准或成本增加],
  ),
  caption: [颜色采样结构的模型比较。评价必须同时给出光谱响应、光子效率、空间排列、光学预滤波和重建算法。],
) <tab:color-sampling-structures>

#paragraph-heading[假色为何在高频边缘附近出现]

若黑白细纹的亮度频率超出某些 CFA 子晶格的可分离带宽，各颜色位置会采到不同相位。去马赛克看到的是“不同颜色像素数值不同”，无法确定差异来自真实色度，还是同一亮度纹理的相位错位，于是可能生成彩边。轻微失焦、缩小光圈、OLPF 或多帧位移会改变高频能量和相位，假色可能突然消失或换色，这正是混叠而非固定物体颜色的证据。

去马赛克之后再做降噪与锐化会改变伪影形态：色度降噪可压低彩色摩尔纹，却不能恢复原亮度频率；锐化可能把残余拉链纹放大。比较相机“解析力”时必须说明处理管线，否则测到的是镜头、CFA、去马赛克、降噪与锐化的联合结果。

== 常见误解

#paragraph-heading[“奈奎斯特频率以内都能完整解析”]

采样定理要求采样前严格带限，并只保证无噪声条件下的唯一重建。像素孔径和镜头已降低边界附近 MTF，奈奎斯特正弦还可能因相位而得到零对比；“在边界以内”不等于“可靠可见”。

#paragraph-heading[“两像素一周期就是两条线”]

一个周期包含一明一暗两条线，两个像素一周期是理论最低采样密度，不保证每条线都被一个像素稳定代表。line pair、line width 与 pixel 的单位必须先统一。

#paragraph-heading[“像素数增加多少，细节就增加多少”]

像素总数是二维面积计数，线性奈奎斯特频率只按其平方根增长；新增频带中还必须有镜头传递、稳定曝光和足够 SNR。四倍像素绝不自动等于四倍可分辨线频率。

#paragraph-heading[“没有 OLPF 就一定更高分辨率”]

移除 OLPF 可提高采样边界附近对比，也增加摩尔纹与假色。若镜头或运动已经低通，差别可能很小；若场景高频强，所谓更锐可能包含折叠出的假细节。

#paragraph-heading[“一个 RAW 像素包含完整 RGB”]

常见 CFA 像素只测得一个宽光谱通道。其余两通道是由邻域和先验估计的；RAW 保留了接近传感器观测的数据，不等于每个位置都完成了颜色测量。

#paragraph-heading[“先混叠，再缩小就能消除混叠”]

缩小前低通只能抑制当前数组中的高频。已经折到低频的摩尔纹与假色处在通带内，无法仅凭降采样区分它们与真实低频结构。

#struct-box(title: [本章小结])[
  - 像素先对连续辐照度作空间、时间与光谱积分，再在晶格中心生成一个标量；像素不是几何点。
  - 像素间距 $p$ 决定频谱副本间隔，孔径宽度 $a$ 决定采样前 sinc 低通，两者不可混淆。
  - 矩形晶格的奈奎斯特区域是二维矩形；采样把频谱周期复制，副本重叠后产生不可唯一撤销的混叠。
  - 两点一周期只是理想带限重建边界，奈奎斯特处的实际样本对比仍依赖相位。
  - 摩尔纹是高频纹理与采样晶格的拍频；假色常来自 CFA 通道在不同位置采到不同亮度相位。
  - 系统分辨率由光学、运动、像素孔径、采样、CFA 重建、噪声和输出共同决定，像素总数只描述数组大小。
  - 裁切不改变原始采样间距；降采样必须先低通；多帧亚像素位移只有在光学仍传递相应频率时才能增加采样信息。
  - CFA 去马赛克从 $M N$ 个标量估计 $3M N$ 个通道值，是依赖图像先验的欠定逆问题。
]

#pagebreak(weak: true)

== 思考与计算

#block[
  1. 一块宽 36 mm、横向 6000 像素的传感器，其像素间距和轴向奈奎斯特频率各是多少？若画面高度为 24 mm，把该频率换算为 LW/PH。
  2. 满孔径方形像素满足 $a=p=4 "µm"$。用 @eq:pixel-aperture-otf 计算 $(nu_x,nu_y)=(60,0),(100,0),(60,60) "cycle/mm"$ 处的像素 MTF，并比较相同径向频率下的方向差异。
  3. 证明 @eq:sampling-spectrum-replicas。提示：先求二维梳状函数的傅里叶变换，再使用空间域乘法—频域卷积关系。
  4. 采样频率为 120 cycle/mm。分别求连续正弦 50、70、130、190 cycle/mm 折回基本频带后的频率；指出仅凭样本为何不能区分 50 与 70 cycle/mm 的某些实正弦。
  5. 对 @eq:nyquist-phase-samples 取 $phi=0,pi/6,pi/2$，列出 $m=0,1,2,3$ 的样本。若加入标准差 0.1 的高斯噪声，哪个相位最难检测该正弦？
  6. 绿光波长取 550 nm。用 @eq:incoherent-diffraction-cutoff 计算 f/4、f/8、f/16 的衍射截止频率，并与像素间距 3.8 µm 的奈奎斯特频率比较。解释为何截止频率相等不等于系统 MTF 最佳。
  7. 一幅 8000×6000 图像缩小到 4000×3000。比较直接隔点抽取、$2 times 2$ 方盒平均和理想低通后抽取的频率响应；哪一种最易留下接近新奈奎斯特的混叠？
  8. 写出 @eq:bayer-red-mask 与 @eq:bayer-blue-green-masks 在四种奇偶位置的值，验证三个掩模互斥且和为 1。再说明这些 $(-1)^m,(-1)^n$ 项在频域对应哪些谱搬移。
  9. 构造一个 $4 times 4$ 黑白条纹，使 Bayer 马赛克中 R、G、B 位置因采样相位而得到不同均值。用双线性插值推演可能出现的假色，并说明轻微平移为何会改变颜色。
  10. 四帧图像的理想位移依次为 $(0,0),(p/2,0),(0,p/2),(p/2,p/2)$。忽略噪声时写出联合采样晶格，并判断线性采样密度可提高多少；再列出三个使该结论失效的物理因素。
  11. 为两台像素数不同的相机设计分辨率比较。要求控制视角、光圈、快门、对焦和处理管线，同时报告斜边 MTF、周期纹理混叠、RAW 噪声和统一输出尺寸；说明为何单张测试卡中心截图不足以下结论。
]
