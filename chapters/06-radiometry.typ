#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 辐射度学与光度学 <chap:radiometry>

摄影语言中的“光有多少”至少可能指六件不同的事：光源总共发出多少能量，某一方向上发出多少，某个表面接收多少，单位面积向外发出多少，某束光在传播中有多密集，或者人眼觉得它有多亮。它们的单位、守恒关系和随距离变化的规律并不相同。若把这些量都叫作“亮度”，平方反比律、镜头通光能力和曝光便会互相矛盾。

辐射度学（radiometry）以能量为基础描述电磁辐射，不预设观察者；光度学（photometry）则用标准人眼的光谱敏感度对辐射量加权。相机首先是辐射探测器，因此本章先建立辐射度量，再讨论它们怎样被转换成人眼相关的光度量。系统性的辐射量定义可参见 @palmer2009 与 @boyd1983。

全章沿一条计算链展开：先用投影面积和立体角建立光传输的几何坐标，再区分六个辐射量；随后以辐亮度为起点，推导表面接收、距离衰减和镜头像面照度；最后才加入人眼权重，并讨论灰卡、测光表与实际标定。这样，每个新量都由“对哪个变量取积分或密度”自然产生。

前面的投影、透镜与景深主要回答光线落在哪里、形成多大的斑；现在转向同一束光携带多少能量。这里建立的单位和积分关系会在下一章变成曝光方程，并在后面的噪声、布光和颜色章节反复充当共同尺度。

== 光传输的几何测度

辐亮度要同时描述光“经过哪里”和“朝哪个方向传播”。面积给出位置范围，立体角给出方向范围。由于表面对光线可能倾斜，还必须先把实际面积投影到垂直于传播方向的平面上。这三个几何因素构成后续所有辐射量之间的共同底座。

#paragraph-heading[投影面积]

设一个面积元为 $dif A$，其单位法向量为 $bold(n)$，光的传播方向与法向量夹角为 $theta$。则垂直于光线方向看到的投影面积为

$ dif A_(perp) = cos theta dif A. $ <eq:projected-area>

余弦因子来自面积在垂直传播方向平面上的正交投影。同样大小的表面越倾斜，对光束“张开”的有效截面越小。当 $theta=90 degree$ 时，理想无厚度面元的投影面积为零。

#paragraph-heading[立体角]

平面角用圆弧长度除以半径定义，立体角则用球面面积除以半径平方定义。若从点 $O$ 观察一块球面区域，其面积为 $A_s$、球半径为 $r$，则立体角为

$ Omega = A_s / r^2. $ <eq:solid-angle-definition>

立体角在量纲上是无量纲量，专用单位为球面度（steradian，$"sr"$）。整个球面的立体角是 $4 pi "sr"$，半球是 $2 pi "sr"$。

对距离 $r$ 处任意取向的微小面元 $dif A$，从观察点看见的微分立体角为

$ dif Omega = (cos theta dif A) / r^2. $ <eq:differential-solid-angle>

这里的 $theta$ 是面元法线与观察方向之间的夹角。@fig:radiance-geometry 把投影面积、立体角和两个面元之间的传输几何放在同一图中。

#import "../diagrams/radiance_diagrams.typ": radiance-geometry, radiometric-quantities

#figure(
  radiance-geometry(),
  caption: [两个微小面元之间的几何关系。每个面元对另一面元张开的立体角同时受到距离平方和投影余弦控制。],
  placement: auto,
) <fig:radiance-geometry>

#paragraph-heading[有向半球上的积分]

从一个表面点向外传播的方向构成半球。若以极角 $theta$ 和方位角 $phi$ 参数化单位球面，则

$ dif Omega = sin theta dif theta dif phi. $ <eq:solid-angle-element>

因此

$
  integral_(Omega^+) cos theta dif Omega
  = integral_0^(2 pi) integral_0^(pi/2)
  cos theta sin theta dif theta dif phi
  = pi.
$ <eq:cosine-hemisphere-integral>

这个看似简单的积分后面会反复出现。朗伯表面的辐射出射度等于 $pi$ 倍辐亮度，而均匀半球辐亮度产生的辐照度也等于 $pi$ 倍辐亮度。因子 $pi$ 来自方向空间的积分，而不是某种材料常数。

== 六个基本辐射量

辐射量常用下标 $e$ 表示“能量意义上的量”，以区别后文下标 $v$ 的光度量。若再加 $lambda$，表示对波长的谱密度。例如 $L_(e,lambda)$ 是谱辐亮度，而不是把总辐亮度简单除以一个波段宽度。

#figure(
  radiometric-quantities(width: 98%),
  caption: [基本辐射量的定义关系。箭头表示对时间、面积或立体角取密度；这些操作一般会丢失原变量，不能逆向恢复。],
  placement: auto,
) <fig:radiometric-quantities>

#paragraph-heading[辐射能与辐射通量]

辐射能 $Q_e$ 的单位是焦耳。单位时间内传输的辐射能称为辐射通量（radiant flux）或辐射功率：

$ Phi_e = (dif Q_e) / (dif t), quad [Phi_e] = "W". $ <eq:radiant-flux>

一个灯具“光功率为 $10 "W"$”只说明所有方向和波长上的总辐射功率，不能说明被摄体实际接收到多少，也不能说明其光谱是否落在传感器敏感波段。

#paragraph-heading[辐照度与辐射出射度]

入射到表面单位面积上的通量密度称为辐照度（irradiance）：

$ E_e = (dif Phi_e^("in")) / (dif A), quad [E_e] = "W" dot "m"^(-2). $ <eq:irradiance>

从表面单位面积向外发出的通量密度称为辐射出射度（radiant exitance）：

$ M_e = (dif Phi_e^("out")) / (dif A), quad [M_e] = "W" dot "m"^(-2). $ <eq:radiant-exitance>

两者单位相同，但方向语义不同。照明讨论中，主体表面接收的是 $E_e$。光源表面或反射表面向外离开的总量用 $M_e$ 描述。

#paragraph-heading[辐射强度]

点源或足够远的小光源在某一方向上的单位立体角通量称为辐射强度（radiant intensity）：

$ I_e (bold(omega)) = (dif Phi_e) / (dif Omega), quad [I_e] = "W" dot "sr"^(-1). $ <eq:radiant-intensity>

辐射强度保留方向分布，却不再保留光源表面内部的位置。它适合描述远场小光源，不适合直接描述一面贴近主体的大型柔光布。

#paragraph-heading[辐亮度]

单位投影面积、单位立体角内传输的通量密度称为辐亮度（radiance）：

$
  L_e (bold(x), bold(omega))
  = (dif^2 Phi_e) / (cos theta dif A dif Omega),
  quad
  [L_e] = "W" dot "m"^(-2) dot "sr"^(-1).
$ <eq:radiance-definition>

辐亮度既保留空间位置，又保留传播方向。若再保留波长和时间，就回到 @chap:light-models 引入的 $L_(e,lambda)(bold(x),bold(omega),lambda,t)$。摄影镜头从场景各点接收的，正是有限位置、方向、波长和时间范围内的辐亮度。

#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1fr),
    align: (left, center, left, left),
    [*辐射量*], [*符号*], [*SI 单位*], [*保留的信息*],
    [辐射能], [$Q_e$], [$"J"$], [总能量],
    [辐射通量], [$Phi_e$], [$"W"$], [单位时间总量],
    [辐照度], [$E_e$], [$"W" dot "m"^(-2)$], [入射位置],
    [辐射出射度], [$M_e$], [$"W" dot "m"^(-2)$], [出射位置],
    [辐射强度], [$I_e$], [$"W" dot "sr"^(-1)$], [传播方向],
    [辐亮度], [$L_e$], [$"W" dot "m"^(-2) dot "sr"^(-1)$], [位置与方向],
  ),
  caption: [六个基本辐射量。单位相同不代表物理含义相同。],
) <tab:radiometric-quantities>

#struct-box(title: [量纲自检])[
  若一个公式声称“表面接收的辐照度”等于“光源辐射通量除以距离平方”，右侧单位是 $"W" dot "m"^(-2)$，量纲虽然正确，却仍可能缺少方向分布、投影余弦和遮挡。量纲一致是必要条件，不是充分条件。
]

== 从辐亮度到摄影曝光

六个基本量并非六套彼此独立的术语。辐亮度保留的位置与方向信息最多，对方向或面积作积分，便会依次得到辐照度、出射度和通量。下面沿这条积分关系，追踪场景辐亮度怎样经过空间传播和镜头孔径，最终形成像面照度。

=== 辐亮度如何生成通量与辐照度

由 @eq:radiance-definition 直接得到，从面积元 $dif A$ 沿立体角元 $dif Omega$ 传出的微小通量为

$ dif^2 Phi_e = L_e cos theta dif A dif Omega. $ <eq:radiance-flux-element>

若两个微小面元 $dif A_s$ 与 $dif A_r$ 相距 $r$，连接方向分别与两者法线成角 $theta_s$、$theta_r$，忽略吸收、遮挡和多次反射，则从源面元传到接收面元的通量为

$
  dif^2 Phi_(s arrow.r r)
  = L_s
  (cos theta_s cos theta_r) / r^2
  dif A_s dif A_r.
$ <eq:two-surface-transfer>

两个余弦分别来自源面元的投影面积与接收面元张开的立体角。这个公式是渲染方程几何项的局部原型，以后讨论照明和材料时，只需再加入可见性、介质透过率和反射分布。

一个点从所有入射方向接收的辐照度是辐亮度的余弦加权积分：

$ E_e (bold(x)) = integral_(Omega^+) L_i (bold(x),bold(omega)) cos theta dif Omega. $ <eq:irradiance-from-radiance>

这个式子说明，辐照度是一个已经对方向积分的量。两个照明环境可以具有相同的 $E_e$，却有完全不同的方向分布。一束窄角硬光与包围主体的均匀天光可能给出相同总辐照度，但阴影和高光结构不会相同。

若一个表面向整个出射半球具有与方向无关的辐亮度 $L_e$，称为理想朗伯出射。由 @eq:cosine-hemisphere-integral，

$ M_e = integral_(Omega^+) L_e cos theta dif Omega = pi L_e. $ <eq:lambertian-exitance>

所以 $M_e$ 与 $L_e$ 的数值并不相等，即使表面“各方向看起来一样亮”。前者把整个半球的离开通量都加在一起，后者仍以单位立体角计量。

=== 平方反比律的适用边界

@eq:two-surface-transfer 中的 $1\/r^2$ 来自接收面元所张立体角，而不是一条可脱离光源几何单独使用的“变暗定律”。是否得到熟悉的平方反比关系，取决于光源能否近似成点以及比较的究竟是辐照度、总通量还是辐亮度。

#paragraph-heading[点源与远场小光源]

设一个点源沿接收方向的辐射强度为 $I_e$。面积元 $dif A$ 在距离 $r$ 处张开的立体角为 $cos theta dif A/r^2$，因此

$ E_e = (I_e cos theta) / r^2. $ <eq:inverse-square-irradiance>

这就是带有入射投影余弦的平方反比律。它要求光源相对距离足够小，以至于能用一个位置和方向强度描述，传播介质还必须近似无吸收、无散射。若接收面正对光源，$theta=0$，才得到熟悉的 $E_e=I_e\/r^2$。

#paragraph-heading[扩展圆盘光源的精确例子]

考虑半径为 $R$、均匀辐亮度为 $L_e$ 的朗伯圆盘。接收点位于圆盘轴线上，接收面与圆盘平行，距离为 $z$。圆盘在接收点张成半角

$ alpha = arctan(R/z). $ <eq:disk-half-angle>

由 @eq:irradiance-from-radiance 对圆锥方向积分，得到

$
  E_e
  = 2 pi L_e integral_0^alpha cos theta sin theta dif theta
  = pi L_e sin^2 alpha
  = pi L_e R^2 / (R^2 + z^2).
$ <eq:disk-source-irradiance>

这个结果清楚展示了两种极限：

- 当 $z/R arrow.r infinity$ 时，$E_e approx pi L_e R^2/z^2$，圆盘表现得像远场点源，平方反比近似成立；
- 当 $z/R arrow.r 0$ 时，$E_e approx pi L_e$，接收点几乎被半球光源覆盖，再靠近也不会按 $1\/z^2$ 发散。

因此，大型近距离柔光源不能简单套用点源平方反比律。实际布光中所谓“衰减很快”，还常指主体不同位置到光源的相对距离差变大，而不是定律本身突然改变。

#paragraph-heading[物体到相机的距离]

一个无雾环境中的扩展朗伯表面远离相机时，它在像面上覆盖的面积缩小，但其辐亮度不因距离单独按平方下降。更远的面元对镜头张开的立体角变小，进入镜头的总通量下降，与此同时，它的像也按相应比例缩小。总通量与像面积共同变化，使单位像面积上的理想照度保持由 f 数决定。

#warn-box[
  *不要跨量使用平方反比律*　“光源照到主体”常讨论辐照度，点源近似下按 $1/r^2$ 变化。“主体表面向镜头的亮度”更自然地用辐亮度描述，在无损传播中不按物距作同样衰减。
]

=== 辐亮度守恒与通光量

平方反比律说明固定接收面积能取得多少通量，辐亮度守恒讨论的则是同一束光沿传播路径的密度怎样变化。下面把二者写进面积与方向共同构成的几何关系。

对折射率为 $n$ 的介质，微分通光量可定义为

$ dif G = n^2 cos theta dif A dif Omega. $ <eq:etendue>

它同时计量光束在位置空间占据的面积和在方向空间占据的立体角。理想无损光学系统不能任意压缩通光量，这是一种光学相空间体积守恒。若试图把一个大面积、宽角度光束压到更小面积，就必须增加其角度范围，或损失一部分光。

#paragraph-heading[辐亮度定理]

在几何光学、无吸收、无散射的条件下，沿一条光线保持的是

$ L_e / n^2 = "constant". $ <eq:radiance-invariant>

在同一折射率介质中，这简化为辐亮度沿光线不变。实际镜头有反射、吸收、渐晕和光阑遮挡，只能降低有效辐亮度，不能用被动元件把一个扩展光源变得比源本身具有更高辐亮度。

由 @eq:etendue 可把传输通量写成

$ dif^2 Phi_e = (L_e/n^2) dif G. $ <eq:flux-etendue>

它把辐亮度定理与通光量守恒联系起来：被动光学可以在面积和角度之间交换，但不能同时压缩二者并保持全部通量。

#paragraph-heading[摄影中的含义]

- 更大的镜头孔径让像点接收更宽的方向锥体，而不是凭空提高场景辐亮度；
- 增距镜扩大像的同时减小单位像面积上的照度，损失可由有效 f 数体现；
- 聚光镜能把扩展光源重新分配到较小区域，但照明角度、均匀性或通量必然付出代价；
- “无损增亮镜片”若声称在不改变视角、孔径、曝光时间和噪声的情况下提高扩展场景亮度，会与通光量约束冲突。

=== 镜头由 f 数控制像面照度

现在把前面的积分关系应用到相机。场景辐亮度沿无损传播不因物距自行变暗，但镜头只接受出瞳所覆盖的一部分方向，f 数正是控制这段方向锥的主要几何参数。

#paragraph-heading[出瞳张开的方向锥体]

考虑轴上、无穷远、均匀辐亮度为 $L_o$ 的扩展场景。镜头透过率记为 $tau$，从像面一点看，出瞳张成半角 $u'$。忽略像差和渐晕，像面辐照度为

$
  E_("img")
  = tau integral_(Omega_p) L_o cos theta dif Omega
  = pi tau L_o sin^2 u'.
$ <eq:image-irradiance-na>

在空气中、近轴且物体位于远处时，$sin u' approx D/(2f)=1/(2N)$，其中 $D$ 是入瞳直径，$f$ 是焦距，$N=f/D$ 是 f 数。于是

$ E_("img") approx (pi tau L_o) / (4 N^2). $ <eq:image-irradiance-fnumber>

像面照度因此近似与 f 数平方成反比。把 f 数乘以 $sqrt(2)$，照度减半，即减少一档。这也是标准光圈序列含有 $1, sqrt(2), 2, 2 sqrt(2), 4, dots$ 的原因。

#paragraph-heading[T 数、近摄与离轴衰减]

@eq:image-irradiance-fnumber 使用几何 f 数并把透过损失单独放进 $tau$。电影镜头常用 T 数把透过率吸收进标称值，使像面照度更接近 $1\/T^2$ 标度。两支同为 f/2 的镜头可能因镜片数量、镀膜和机械遮挡不同而有不同 T 数。

近距离成像时，像距增加而出瞳相对像面的角尺寸减小。对瞳放大率接近 1 的近似对称镜头，有效 f 数约为

$ N_("eff") approx N(1+m), $ <eq:effective-fnumber>

其中 $m$ 是像方放大率。一般镜头还需考虑入瞳与出瞳直径之比，@eq:effective-fnumber 不是所有镜头的精确公式。

理想薄透镜的离轴像面照度还可能出现近似 $cos^4 beta$ 衰减，其中 $beta$ 是像方视场角。真实镜头的瞳孔畸变、机械渐晕和数字校正会改变这一规律。因此，“暗角”既可能来自几何投影，也可能来自镜筒遮挡或后期处理，不能只用一个余弦四次公式解释。

#paragraph-heading[总光量、单位面积与单像素]

同一场景辐亮度和 f 数下，不同画幅的单位像面积照度近似相同。较大传感器之所以能收集更多总光，是因为它在相同曝光时间内具有更大面积。单个像素收集多少，还取决于像素面积、填充因子、量子效率和光谱响应。把“画幅进光量”“镜头进光量”和“像素进光量”混为一句话，会遗漏比较中究竟固定了视角、景深、输出尺寸还是像素数。

== 从辐射度学到光度学

到这里为止，所有量都只按辐射能定义，足以描述传感器接收的物理输入。照明工程和手持测光表却常使用流明、勒克斯与坎德拉，因为它们还要近似人眼对不同波长的敏感程度。光度学不是另一套传播定律，而是在对应辐射量的光谱积分中加入标准视觉权重。

#paragraph-heading[人眼加权]

辐射度学对每焦耳能量一视同仁，但明视觉人眼对不同波长的敏感度不同。令 $V(lambda)$ 为 CIE 明视觉光谱光视效率函数，谱辐射通量为 $Phi_(e,lambda)(lambda)$，则光通量定义为

$
  Phi_v
  = K_m integral_(Lambda) V(lambda) Phi_(e,lambda)(lambda) dif lambda,
  quad K_m = 683 "lm/W".
$ <eq:luminous-flux>

$V(lambda)$ 在约 $555 "nm"$ 处归一化为 1。下标 $v$ 表示“视觉加权”。同样的加权可施加于辐射强度、辐照度和辐亮度，分别得到发光强度、照度和亮度。

#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1fr),
    align: (left, center, left, left),
    [*辐射量*], [*光度量*], [*光度单位*], [*常用语境*],
    [辐射通量 $Phi_e$], [光通量 $Phi_v$], [流明 $"lm"$], [光源总可见输出],
    [辐射强度 $I_e$], [发光强度 $I_v$], [坎德拉 $"cd"$], [方向性光源],
    [辐照度 $E_e$], [照度 $E_v$], [勒克斯 $"lx"$], [表面接收的可见光],
    [辐亮度 $L_e$], [亮度 $L_v$], [$"cd" dot "m"^(-2)$], [显示器或表面方向亮度],
  ),
  caption: [辐射量与对应光度量。光度量是按标准观察者加权后的物理量。],
) <tab:radiometry-photometry>

#paragraph-heading[勒克斯不是相机曝光量]

一勒克斯等于每平方米一流明，它按 $V(lambda)$ 加权。相机的红、绿、蓝通道响应 $q_c (lambda)$ 与 $V(lambda)$ 不同，因此两个照度计读数相同的光源，可能给相机产生不同电子数和不同通道比例。窄带 LED 尤其容易暴露这种差异。

相机曝光若要从物理上预测，应使用光源光谱、场景反射率、镜头光谱透过率和传感器量子效率的乘积积分。勒克斯只能在光谱分布相近或经过经验标定时充当代理量。

#paragraph-heading[亮度与“看起来多亮”]

光度学中的亮度 $L_v$ 是按标准观察者定义的可测物理量，知觉中的明亮感（brightness）还受视觉适应、局部对比、视野大小和观看时间影响。同一块显示区域保持相同 $"cd" dot "m"^(-2)$，放在黑暗房间与日光环境中会显得不同。光度量已经包含人眼平均光谱权重，却仍不等于完整视觉体验。

暗视觉主要由杆状细胞参与，其光谱权重与明视觉不同。介于两者之间的中间视觉还会随适应水平改变。夜景中“眼睛很亮、相机某通道却很弱”并不矛盾，因为两套探测系统及其后续处理不同。

== 反射表面、灰卡与测光歧义

无论使用辐射量还是光度量，相机从主体方向测到的首先都是离开表面的光。要由它反推“照明有多强”，还必须知道表面如何反射。这一步引入了本书反复出现的照明与材料歧义。

#paragraph-heading[朗伯反射近似]

若表面以反射率 $rho$ 理想漫反射，并受到总辐照度 $E_e$，则能量守恒给出反射出射度

$ M_e = rho E_e. $ <eq:lambertian-reflected-exitance>

再由 $M_e=pi L_e$，得到

$ L_e = (rho E_e) / pi. $ <eq:lambertian-reflected-radiance>

这正是导论中“照明与反射率乘积不可分”的具体形式。相机从单个均匀区域主要测得 $rho E_e$ 的组合，不能仅凭像素值判断表面本来多白或照明本来多强。

#paragraph-heading[真实材料不是朗伯体]

真实表面通常同时具有漫反射、镜面反射、次表面散射和方向相关结构。更一般的关系用双向反射分布函数（BRDF）表示：

$
  L_r (bold(omega)_o)
  = integral_(Omega^+) f_r (bold(omega)_i, bold(omega)_o)
  L_i (bold(omega)_i) cos theta_i dif Omega_i.
$ <eq:brdf-preview>

同一块丝绸、皮肤或油漆从不同方向观察会有不同辐亮度，原因不是曝光参数改变，而是 $f_r$ 依赖入射与出射方向。后续材料与照明章节将讨论该模型的互易性、能量守恒和微表面解释。

#paragraph-heading[灰卡做了什么]

灰卡提供一个已知、近似中性的反射目标，使测光和白平衡有共同参照。它不能让相机自动知道场景中所有材料的反射率，也不能消除方向性高光。若灰卡倾角改变，入射辐照度中的余弦因子和反射方向分布也可能改变。使用时必须明确它是在估计入射照明、建立曝光基准，还是校准颜色通道。

反射式测光表从相机位置观察场景辐亮度，再依据标定常数假设一个代表性反射水平。入射式测光表则在主体位置用半球罩对入射方向加权。二者回答的问题不同，读数不一致并不必然意味着某一只表失准。

== 测量、标定与不确定度

前面的公式描述理想量之间的关系，实际仪器给出的则是经过有限波段、有限视场和标定规则得到的估计值。把理论量落实成可重复测量时，几何、光谱响应与不确定度必须和数值一起报告。

#paragraph-heading[测量必须说明带宽与几何]

“光源输出为 $1000 "lm"$”需要说明光谱加权和总方向积分；“主体照度为 $500 "lx"$”需要说明测量位置、探头朝向和光谱失配；“显示器亮度为 $120 "cd" dot "m"^(-2)$”还需要说明视场、白场面积和测量仪器响应。没有几何与光谱条件的数值，只是带单位的片段。

#paragraph-heading[相机也可以成为辐射计]

若掌握镜头透过率、渐晕、传感器线性响应、黑电平、增益和光谱量子效率，相机可以被标定为二维辐射测量设备。但普通照片的处理管线包含白平衡、颜色矩阵和非线性色调映射，JPEG 码值通常不再与入射辐照量成正比。定量测量应尽可能从未饱和的线性 RAW 数据开始。

#paragraph-heading[不确定度来源]

- 光源随时间和温度变化；
- 探测器存在暗电流、读出噪声和非均匀性；
- 镜头透过率随波长、焦距、光圈和像高变化；
- 面元位置与角度误差通过余弦和距离平方传播；
- 光度计与相机之间存在光谱失配；
- 场景中的间接反射使“单一光源贡献”难以隔离。

报告一个辐射量时，至少应给出测量几何、波段、时间平均方式和主要不确定度。更多小数位并不会自动带来更多物理信息。

== 常见概念错误

遇到含混的“光更多”或“画面更亮”，可以依次追问：谈的是辐射量还是人眼加权后的光度量？是对面积或方向积分后的总量，还是仍保留这些变量的密度？描述的是光源发出、表面接收，还是镜头与传感器取得？下面几种错误都来自跳过了其中至少一个问题。

#paragraph-heading[把瓦特当成主体照明强弱]

瓦特描述辐射功率或电功率，不能单独决定主体辐照度。光源的电光效率、光谱、方向分布、距离、遮光附件和表面投影都参与结果。

#paragraph-heading[把流明当成相机有效光子数]

流明按标准人眼加权，相机按自身量子效率响应。对宽谱相近光源，流明可作经验比较。对窄带或混合光谱，它可能严重误导相机曝光判断。

#paragraph-heading[认为远处物体必然按平方变暗]

点源产生的辐照度与扩展表面的辐亮度不是同一量。远处物体在像面上变小，但理想表面亮度并不因物距单独按 $1\/r^2$ 下降。

#paragraph-heading[认为大光圈提高了场景亮度]

大光圈增加像面一点所接收的方向范围，从而提高像面辐照度。它不改变场景本身的辐亮度。关闭快门之后，场景当然不会因相机光圈改变而变暗。

#struct-box(title: [本章小结])[
  - 面积投影给出 $cos theta$，微小面元张开的立体角给出 $1\/r^2$，二者共同构成表面间传输的几何项。
  - 辐照度、辐射出射度、辐射强度和辐亮度分别保留不同变量，不能只凭单位或口语名称互换。
  - 辐亮度同时保留位置与方向；无损传播中 $L_e/n^2$ 沿光线守恒。
  - 点源辐照度服从平方反比律；大型近距离扩展源通常不满足简单的 $1\/r^2$ 关系。
  - 轴上远景的像面照度近似为 $pi tau L_o/(4N^2)$，说明 f 数而非焦距或孔径直径单独控制单位像面积照度。
  - 光度量由标准人眼光谱权重定义，勒克斯和流明不是相机传感器的直接曝光单位。
  - 反射式测光面对照明与反射率不可分问题；灰卡通过增加已知参照而不是通过“告诉相机真相”来减少歧义。
]

== 思考与计算

1. 证明整个球面的立体角为 $4 pi "sr"$，并计算顶角为 $60 degree$ 的圆锥所张立体角。
2. 一个各向同性点源总辐射功率为 $Phi_e$。求其辐射强度，并求距离 $r$ 处正对点源表面的辐照度。
3. 两个面积均为 $1 "cm"^2$ 的微小平行面元相距 $1 "m"$，连线与法线重合。若源面元辐亮度为 $10 "W" dot "m"^(-2) dot "sr"^(-1)$，忽略遮挡时接收通量是多少？
4. 对 @eq:disk-source-irradiance，求局部对数斜率 $dif ln E_e / dif ln z$。它如何从近场的 0 过渡到远场的 $-2$？
5. 某镜头透过率不变，光圈从 f/2 改为 f/2.8。用 @eq:image-irradiance-fnumber 估计像面照度比。f/2.8 为何只是 $2 sqrt(2)$ 的近似十进制写法？
6. 一个 $100 "lx"$ 的单色 $555 "nm"$ 光源和一个同为 $100 "lx"$ 的单色 $650 "nm"$ 光源，辐射功率密度是否相同？相机红、绿通道的电子数能否仅由勒克斯推出？
7. 设三个传感器通道响应为 $q_R,q_G,q_B$。写出从光源光谱和反射率到 RAW 三通道期望值的积分，并指出测量灰卡后仍不能唯一恢复哪些量。
8. 设计一个只需相机、均匀墙面和可移动灯具的实验，用数据判断某一距离范围内灯具能否近似为点源。说明应固定哪些变量，以及如何在线性 RAW 中估计相对辐照度。
