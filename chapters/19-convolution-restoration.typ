#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 卷积、傅里叶分析与图像复原 <chap:convolution-restoration>

摄影系统把场景变成图像时，衍射、像差、离焦、运动、像素孔径和后续重采样都会重新分配空间结构。在线性近似下，这些作用可由一个算子 $H$ 描述；若系统在一小块视场内还近似移不变，$H$ 就简化为与点扩散函数的卷积。卷积告诉我们一个点怎样扩散到邻域，傅里叶变换则把同一过程改写为每个空间频率的复数增益。

本章先从一般积分算子退到线性移不变模型，区分 PSF、OTF、MTF 与离散 DFT 的边界约定；随后把反遮罩、梯度和拉普拉斯锐化写成频率响应，说明它们为何同时增强边缘与噪声。最后讨论逆滤波为何不稳定、正则化为何不可避免，以及 PSF 失配怎样制造伪影。傅里叶成像可参见 @goodman2017，离散图像处理可参见 @gonzalez2018。

前面已经知道模糊从光学、运动和采样何处产生，也由 @chap:perception-composition-style 明确了最终观看尺度。本章现在反向追问：如果前向模型已知，能否从观测估计更接近潜在图像的结果？答案将自然引出噪声放大和先验问题，下一章便专门处理这种“减少随机起伏却保护细节”的冲突。

#key-box[
  *锐化与复原不是同义词*　锐化选择性增加某些频率或边缘附近的对比，使图像看起来更清楚；复原则从明确的前向模型和噪声模型出发估计潜在图像。前者可以没有真实 PSF，后者必须面对不可辨识性与不确定度。
]

== 成像作为线性系统

=== 从一般积分核到卷积

设潜在连续图像为 $f(bold(x))$，观测为 $g(bold(x))$。最一般的线性空间算子可写成

$
  g(bold(x))
  = integral h(bold(x),bold(xi)) f(bold(xi)) dif bold(xi)
  + n(bold(x)).
$ <eq:space-variant-imaging>

$h(bold(x),bold(xi))$ 表示位于 $bold(xi)$ 的单位点源在像面位置 $bold(x)$ 的响应。若系统对位置平移近似不变，核只依赖差值：

$
  h(bold(x),bold(xi))=h(bold(x)-bold(xi)).
$ <eq:shift-invariant-kernel>

于是 @eq:space-variant-imaging 变为卷积

$
  g=f*h+n,
  quad
  (f*h)(bold(x))
  =integral f(bold(xi))h(bold(x)-bold(xi))dif bold(xi).
$ <eq:continuous-convolution-imaging>

对非相干强度成像，$h$ 是强度 PSF，通常满足 $h>=0$ 且无吸收归一化时 $integral h=1$。若模型处理相干复振幅，卷积核则是振幅脉冲响应，可含相位与负实部；两种模型不能混用。

#figure(
  image("../svg/convolution_psf.svg", width: 98%),
  caption: [移不变卷积的几何解释。每个加权物点生成同一 PSF 的平移副本，所有副本在线性强度域相加；位置相关 PSF 则需要更一般的核。],
) <fig:convolution-psf>

=== 离散二维卷积

采样后，有限离散核 $h_(p,q)$ 对图像 $f_(m,n)$ 的线性卷积为

$
  g_(m,n)
  =sum_p sum_q h_(p,q) f_(m-p,n-q)+eta_(m,n).
$ <eq:discrete-image-convolution>

若核和图像的物理像素间距不同，公式必须先统一尺度。核的索引原点还决定相位：把对称核中心错移一个像素，MTF 幅度可能完全不变，图像却整体平移。只报告核大小而不报告中心、归一化和边界条件，模型仍不完整。

连续光学模糊、像素面积积分、运动积分与数字滤波若都可近似为线性移不变系统，总 PSF 是逐级卷积：

$
  h_("sys")
  =h_("opt")*h_("motion")*h_("pixel")*h_("digital").
$ <eq:psf-cascade-convolution>

这个分解便于性能预算，但实际各项可能空间变化，去马赛克和锐化也可能非线性。总 PSF 能描述最终点响应，不保证能从一次测量唯一分离各子系统。

=== 空域与频域是同一算子的两种表示

采用傅里叶变换

$
  hat(f)(bold(nu))
  =integral f(bold(x))
  exp(-i 2 pi bold(nu) dot bold(x))dif bold(x),
$ <eq:spatial-fourier-transform>

卷积定理给出

$
  hat(g)(bold(nu))
  =H(bold(nu))hat(f)(bold(nu))+hat(n)(bold(nu)),
  quad H=cal(F){h}.
$ <eq:frequency-domain-forward-model>

$H$ 是光学传递函数 OTF，通常为复数；$|H|$ 是 MTF，相位 $arg H$ 是相位传递。MTF 只描述正弦对比幅度，不能判断结构是否移位、边缘是否产生不对称拖尾，也不能单独完成复原。

#figure(
  image("../svg/spatial_frequency_duality.svg", width: 98%),
  caption: [空域卷积与频域乘法的对偶。OTF 的幅度和相位共同决定输出；离散 DFT 还隐含周期边界。],
) <fig:spatial-frequency-duality>

=== DFT 为什么默认循环卷积

长度为 $N$ 的离散傅里叶变换把有限数组解释为周期序列，因此频域相乘后逆变换得到

$
  g_m
  =sum_(k=0)^(N-1) h_k f_((m-k) "mod" N),
$ <eq:dft-circular-convolution>

即尾部会绕回开头。要用 FFT 计算长度分别为 $M,L$ 的一维线性卷积，至少应填充到 $M+L-1$；二维同理。填充只避免循环重叠，不会自动提供图像外真实内容。零填充、镜像、复制和周期延拓对应不同先验，都会影响边界附近复原。

=== 卷积模型何时失效

#figure(
  image("../svg/lsi_failure_modes.svg", width: 98%),
  caption: [全局线性移不变模型的主要失效方式。实际处理中可使用空间变化核、分块近似或一般线性算子，但不能把模型误差塞进“随机噪声”而不说明。],
) <fig:lsi-failure-modes>

摄影里常见的偏离包括：

- 镜头像差和离焦使 PSF 随视场与深度变化；
- 主体各部分速度不同，不能共享单一运动核；
- 滚动快门使不同扫描行对应不同时间积分；
- 饱和、局部色调映射、边缘自适应降噪不满足线性叠加；
- 采样混叠把不同连续频率折叠到同一离散频率；
- 边界外场景未知，有限图像并不是孤立的卷积系统。

在小视场、单一深度和不过曝的区域内，卷积仍可能是非常有效的局部模型。严谨态度不是因模型不完美而放弃它，而是明确其近似尺度，并把残差分成随机噪声与结构化模型误差。

== 锐化作为频率重分配

=== 梯度、拉普拉斯与高频

离散一维前向差分

$
  (D f)_m=f_(m+1)-f_m
$ <eq:forward-difference>

对频率为 $nu$ 的离散复指数具有响应

$
  D(nu)=exp(i 2 pi nu)-1,
  quad |D(nu)|=2|sin(pi nu)|.
$ <eq:difference-frequency-response>

响应在直流处为零，随频率上升。二维拉普拉斯的五点模板

$
  Delta_d f_(m,n)
  =f_(m+1,n)+f_(m-1,n)+f_(m,n+1)+f_(m,n-1)-4 f_(m,n)
$ <eq:discrete-laplacian>

同样抑制低频并强调快速变化。锐化可写成 $f_("sharp")=f-alpha Delta_d f$ 或相反符号，取决于拉普拉斯定义。符号约定不清会直接把锐化变成额外模糊。

#figure(
  image("../svg/derivative_noise_amplification.svg", width: 98%),
  caption: [差分算子的幅度随频率增加，白噪声输出功率按算子幅度平方加权。边缘和噪声共享高频，因此“增强细节但不增强噪声”必须依赖额外统计先验。],
) <fig:derivative-noise-amplification>

若输入噪声功率谱密度为 $S_n(bold(nu))$，经过线性锐化核 $K$ 后

$
  S_(n,"out")(bold(nu))
  =|K(bold(nu))|^2 S_n(bold(nu)).
$ <eq:filtered-noise-psd>

所以锐化量不能脱离 ISO、降噪、缩放比例与输出介质讨论。对已经相关的去马赛克噪声，只用单一标准差也不足以预测输出纹理。

=== 反遮罩锐化

令 $G_sigma$ 为归一化低通核，反遮罩高频掩模为

$
  m=f-G_sigma*f.
$ <eq:unsharp-mask-residual>

加回增益 $alpha>=0$ 后

$
  f_("usm")
  =f+alpha m
  =(1+alpha)f-alpha G_sigma*f.
$ <eq:unsharp-mask-output>

频率响应为

$
  K_("usm")(bold(nu))
  =1+alpha[1-G_sigma(bold(nu))].
$ <eq:unsharp-frequency-response>

因 $G_sigma(0)=1$，直流增益为 1；高于低通截止的频率增益趋近 $1+alpha$。半径或 $sigma$ 决定增强从哪个尺度开始，$alpha$ 决定平台增益。阈值机制则试图不增强幅度很小的差分，但会把算子变成非线性。

#figure(
  image("../svg/unsharp_frequency_response.svg", width: 98%),
  caption: [反遮罩的处理图与理想化频率响应。它保持平均亮度，却把中高频逐渐抬高；更大的增益同时提高边缘、纹理、噪声与插值伪影。],
) <fig:unsharp-frequency-response>

=== halo 从哪里来

理想阶跃边缘含有广泛频率。若锐化核包含负旁瓣，卷积会在暗侧产生下冲、亮侧产生过冲。反遮罩可写成冲激核

$
  k_("usm")=(1+alpha) delta-alpha G_sigma,
$ <eq:unsharp-impulse-kernel>

其中心为正、周围为负，所以边缘附近自然形成明暗轮廓。小半径 halo 可能主要表现为清晰感，大半径则成为可见的局部对比光晕。它不是软件偶然“画坏了边缘”，而是所选核的阶跃响应。

#paragraph-heading[三类锐化的目标尺度]

- *输入锐化*尝试补偿已知光学、像素孔径和去马赛克衰减，工作尺度接近原始采样；
- *创意锐化*选择性增强主体、纹理或局部结构，本质上是表达性对比变换；
- *输出锐化*针对最终像素尺寸、打印网点、观看距离和显示 MTF 补偿。

若先在全分辨率上固定半径锐化，再大幅缩小，核的输出角尺度会改变；若缩放算法本身产生振铃，后续锐化还会再次增强。正确参数不是文件固有属性，而依赖最终采样和观看任务。

#paragraph-heading[锐化不等于信息恢复]

只要 $K(bold(nu))$ 在某频带大于 1，视觉对比就可增加，即使该频带中的真实信号已经很弱。锐化可以让模糊边缘斜率更陡，却不证明恢复了原场景的细小结构。对 OTF 零点、采样后混叠和裁切区域，线性锐化只会重排现有数值。

这并不否定锐化价值。输出任务只要求产生合适外观时，增加感知清晰度就是合理目标；问题在于不要把外观改善误报为测量证据增加。

== 反卷积与不适定逆问题

=== 朴素逆滤波为什么爆炸

忽略噪声时，由 @eq:frequency-domain-forward-model 可写

$
  hat(f)_("inv")(bold(nu))
  =frac(hat(g)(bold(nu)), H(bold(nu))).
$ <eq:naive-inverse-filter>

但实际 $hat(g)=H hat(f)+hat(n)$，所以

$
  hat(g)/H
  =hat(f)+hat(n)/H.
$ <eq:inverse-filter-noise-term>

当 $|H|$ 很小时，哪怕 $hat(n)$ 很小，$hat(n)/H$ 也会主导；若 $H=0$，对应频率模态完全不在观测中，除法没有定义。

#figure(
  image("../svg/inverse_filter_instability.svg", width: 98%),
  caption: [低通模糊的传递函数在高频趋小，朴素逆增益则趋大。弱响应频带中的噪声和模型误差会被远大于真实信号的倍数放大。],
) <fig:inverse-filter-instability>

若只在集合 $cal(O)_epsilon={bold(nu):|H(bold(nu))|>=epsilon}$ 上求逆，并在其余频率置零，得到截断逆。阈值 $epsilon$ 越小，名义分辨率越高，噪声方差也越大；这已经是最简单的正则化偏差—方差权衡。

=== 正则化：限制证据不足处的逆增益

实际复原不会在所有频率上机械地除以 $H$。在传递较强、信号明显高于噪声的频带，可以较积极地补偿；在传递很弱或噪声占主导的频带，则必须限制增益、平滑结果或直接放弃恢复。这个有意引入偏差以换取稳定性的过程称为正则化。

不同方法只是用不同信息决定“相信多少”：截断逆设置最低可信传递，维纳滤波利用信号与噪声统计，其他方法用平滑、稀疏或学习到的图像规律约束数据无法确定的部分。约束越强，结果通常越稳定，也越可能把罕见但真实的结构拉向常见外观。

#figure(
  image("../svg/regularized_filter_gains.svg", width: 98%),
  caption: [朴素逆与两类正则化方法的示意增益。正则方法不会无限放大弱传递频带，而会依据噪声统计或约束强度限制增益。],
) <fig:regularized-filter-gains>

没有一种设置对所有照片绝对最优：把真实细纹理判断为不可信会过度平滑，低估噪声又会恢复出颗粒和振铃。所谓最优始终相对于噪声模型、图像假设和评价目标。

#struct-box(title: [可以继续深入])[逆问题课程会用奇异值分解描述弱可观测模态，用 Tikhonov、维纳滤波或贝叶斯 MAP 形式推导正则解；光子计数数据还常使用 Richardson–Lucy 迭代。本书后文只需要共同结论：弱证据区域必须由正则化或先验决定，迭代更多不等于信息更多。]

=== PSF 估计误差

设真实传递为 $H$，复原却使用 $tilde(H)$。朴素逆得到

$
  hat(g)/tilde(H)-hat(f)
  =(H/tilde(H)-1)hat(f)
  +hat(n)/tilde(H).
$ <eq:psf-mismatch-error>

第一项是结构化模型误差，第二项是噪声放大。核宽度略错会留下残余模糊或过冲；核中心错位会产生相位偏差；忽略空间变化会让中心和边缘需要不同复原。增加迭代次数无法修复错误前向模型，反而可能把误差刻成振铃。

PSF 可由点源、斜边、星点、运动传感器或图像本身估计。任何估计都应给出尺度、位置、通道、噪声与置信区间，而不是只交付一张看起来像核的灰度图。

=== 盲反卷积的额外不适定性

盲反卷积同时估计清晰图像和模糊核。即使无噪声，同一观测也可能由许多“图像—核”组合产生；例如把一部分模糊留在图像中、另一部分分给核，仍可近似同一结果。非负、有限尺寸和运动轨迹假设能缩小解集，却不会自动保证真实核。盲复原的锐利输出可能只是更符合先验，不是被观测唯一证实。

== 复原的诊断与边界

#paragraph-heading[数据一致性不等于真实]

给定估计 $hat(f)$，残差

$
  bold(r)=bold(g)-H hat(bold(f))
$ <eq:restoration-residual>

应与噪声模型兼容：若残差仍含边缘和纹理，模型或正则过强；若残差方差远小于已知噪声，算法可能已拟合噪声。可是小残差只能说明输出重新模糊后接近观测；空空间中的任意分量都不改变残差。

因此诊断至少应同时查看：前向重投影、残差的空间结构与功率谱、不同 $lambda$ 的稳定性、PSF 扰动敏感度，以及是否出现负值、振铃或边界伪影。

#paragraph-heading[正则参数如何选择]

可用已知噪声水平的差异原则、交叉验证、广义交叉验证、证据最大化或风险估计选择 $lambda$。L 曲线用数据残差范数与正则项范数的对数图寻找折角，但折角不一定清晰，也不保证感知最优。

摄影输出还可能采用任务损失：文字识别、星点测量和人像显示对误差的权重不同。若目标是科学测量，应优先报告估计不确定度与偏差；若目标是视觉输出，可加入感知权重，但要承认它改变了“正确”的定义。

#paragraph-heading[多次锐化与复原不能简单叠加]

若两次线性滤波核为 $K_1,K_2$，总响应是 $K_2 K_1$；每次噪声增益和振铃都会保留。若中间发生裁切、阈值、降噪或量化，则整体不再能化成一个线性核。把未知的机内锐化 JPEG 当作未处理输入做反卷积，PSF 模型会同时包含光学模糊与隐藏处理，常出现双边缘。

最佳实践不是迷信某个固定顺序，而是保存早期数据、记录算子和尺度、尽量减少无意的重复处理，并在最终输出尺寸上检查结果。

== 常见误解

#paragraph-heading[“锐化把丢失细节找回来了”]

锐化提高现有频率的对比，也能把噪声和插值结构变得像细节。只有结合前向模型、噪声统计和可观测频带，才能把输出解释为复原估计；OTF 零点与采样混叠不能靠一般锐化逆转。

#paragraph-heading[“知道模糊核就能完美反卷积”]

即使核完全已知，只要传递函数很小或为零，逆问题仍对噪声不稳定或不可辨识。正则化必须在分辨率、噪声与偏差之间选择。

#paragraph-heading[“MTF 是实数，所以复原只需除以 MTF”]

MTF 是 OTF 的模。忽略相位会遗漏位移、不对称像差和运动拖尾方向；用 $|H|$ 代替复数 $H$ 可能把结构放到错误位置。

#paragraph-heading[“迭代越多越接近真实”]

在带噪数据上，最大似然迭代会逐渐拟合随机涨落和模型误差。提前停止、显式先验或约束决定有效正则强度；迭代次数不是单调画质指标。

#paragraph-heading[“AI 去模糊突破了光学信息极限”]

学习模型可以利用大量图像先验生成更可能的纹理，并在平均任务上优于简单正则化；它仍不能让观测空空间中的具体真实结构变得可辨识。输出可信度必须区分“与数据一致”和“由先验补全”。

#pagebreak(weak: true)

#struct-box(title: [本章小结])[
  - 一般线性成像由核 $h(bold(x),bold(xi))$ 描述；只有在移不变近似下才化为单一 PSF 卷积。
  - 空域卷积对应频域乘法；OTF 是复数，MTF 只保留幅度，不能替代相位完成复原。
  - DFT 频域相乘默认循环卷积；填充和边界延拓属于模型的一部分，不是无关实现细节。
  - 差分、拉普拉斯和反遮罩通过抬高中高频增加清晰感，也按 $|K|^2$ 放大相同频带的噪声功率。
  - halo 来自锐化核的负旁瓣或频率过冲；输入、创意和输出锐化对应不同尺度与目标。
  - 朴素逆滤波在 $|H|$ 很小时放大噪声，在 $H=0$ 的模态上没有定义。
  - 正则化在弱传递或低信噪比频带限制逆增益；限制方式取决于噪声、先验和评价目标。
  - PSF 失配产生结构化残差与振铃；盲反卷积同时估计图像和核，比已知核复原更不适定。
  - 数据一致性是必要条件而非真实性证明；应检查残差、参数稳定性、模型扰动和估计不确定度。
]

#pagebreak(weak: true)

== 思考与计算

#block[
  1. 设一维核 $h=(1,2,1)/4$，信号 $f=(0,0,1,1,1,0,0)$。分别在零填充、周期延拓和镜像延拓下计算边界附近卷积；哪些差异来自图像外先验而非核本身？
  2. 证明 @eq:psf-cascade-convolution：利用卷积结合律说明两个移不变模糊串联的总 PSF 为 $h_1*h_2$，并在频域说明总 OTF 为 $H_1 H_2$。
  3. 长度为 5 与 7 的离散序列用 FFT 计算线性卷积，最小填充长度是多少？若只填充到 8，会发生哪些循环重叠？推广到 $M times N$ 图像与 $P times Q$ 核。
  4. 对前向差分 $D f_m=f_(m+1)-f_m$ 推导 @eq:difference-frequency-response。再求中心差分 $(f_(m+1)-f_(m-1))/2$ 的响应，比较二者在 Nyquist 频率处的行为。
  5. 设输入为方差 $sigma_n^2$ 的独立白噪声。对三点锐化核 $k=(-alpha,1+2alpha,-alpha)$，用核能量求输出噪声方差，并与 @eq:filtered-noise-psd 的频域结论核对。
  6. 给定高斯低通 $G_sigma(nu)=exp(-2 pi^2 sigma^2 nu^2)$，画出 @eq:unsharp-frequency-response 在三组 $(alpha,sigma)$ 下的曲线。分别说明增益平台与过渡频率由哪个参数控制。
  7. 设某频率处 $H=0.02$，真实信号系数为 1，噪声标准差为 0.01。朴素逆后的噪声标准差是多少？若截断阈值 $epsilon=0.05$，该模态的估计偏差与方差如何变化？
  8. 用 @eq:psf-mismatch-error 分析：真实核为标准差 $sigma$ 的高斯，复原误用 $0.9 sigma$ 与 $1.1 sigma$ 时，高频分别倾向过冲还是残余模糊？为什么增加逆滤波增益会放大这种差异？
  9. 为“轻微手抖、未知方向、ISO 较高”的图像设计复原报告：写出前向模型、核参数化、噪声模型、正则化原则、边界条件、参数选择方法与残差诊断。明确哪些输出结构由数据支持，哪些主要由先验决定。
]
