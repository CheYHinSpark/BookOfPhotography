#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= RAW 数据与相机图像管线 <chap:raw-pipeline>

RAW 常被描述为“未经处理的照片”，仿佛文件里已经存在一幅等待显影的完整 RGB 图像。更准确的说法是：RAW 保存了靠近传感器读出端的离散测量、解释这些测量所需的校准条件，以及相机为了快速显示而附带的预览。对彩色滤波阵列相机，每个像素位置通常只观测一个光谱通道；黑电平、白电平、缺陷像素、镜头阴影、白平衡和颜色坐标都尚未被统一为可显示图像。RAW 因而不是“无解释的真相”，而是保留了更多可重新解释自由度的早期观测。

本章把相机图像管线写成一组带数据域的算子：偏置校正和线性化先把存储码值还原为相对曝光；去马赛克从不完整颜色采样估计三通道场；白平衡和颜色矩阵改变通道坐标；降噪、色调映射、锐化、编码和压缩继续改变统计量与可逆性。DNG 对 RAW 数据和校准标签给出了公开的容器模型，可参见 @adobedng2021；去马赛克的线性与自适应方法可参见 @malvar2004 和 @gunturk2005。

前面的章节分别建立了传感器读出、采样、色度和编码，本章第一次把这些环节按数据域与操作顺序合在一起。管线说明“已经记录的数据能怎样解释”，接下来的布光与视觉两章则暂时返回两端：先问如何在现场改善输入，再问最终观看真正需要优化什么，之后才进入更强的计算恢复。

#key-box[
  *RAW 管线的基本提问*　任何一步都应说明四件事：输入在哪个域，输出在哪个域，算子依赖哪些元数据，以及它是否一一对应。只写“提亮、降噪、校色”不足以判断其物理含义与信息损失。
]

== RAW 是带校准条件的测量记录

=== 单个 RAW 样本测到了什么

设第 $i$ 个像素的有效面积为 $A_i$，曝光门函数为 $h_i (t)$，其 CFA 通道为 $c_i in {R,G,B}$。忽略像素间串扰时，期望光电子数可写成

$
  mu_i
  = integral_(A_i) integral_t integral_lambda
  eta_(c_i)(lambda)
  E_(e,lambda)(bold(x),t)
  lambda/(h c)
  h_i (t)
  dif lambda dif t dif A.
$ <eq:raw-photoelectron-integral>

实际读出还包含泊松涨落、暗电流、读出噪声、模拟增益、偏置和量化。一个简化模型为

$
  r_i
  = cal(Q)_i(
    g_i [n_i + d_i] + o_i + epsilon_i
  ),
  quad n_i tilde "Poisson"(mu_i),
$ <eq:raw-dn-model>

其中 $r_i$ 是存储整数，$g_i$ 是电子到 DN 的转换增益，$d_i$ 是暗信号，$o_i$ 是为避免负码而加入的读出偏置，$epsilon_i$ 汇总其余读出误差。@eq:raw-dn-model 说明三个容易混淆的事实：

- RAW 码值不是光子数，也不必直接等于电子数；
- 零光照的码值通常不为零，因为文件保留了黑电平偏置；
- 数组中的整数只有结合增益、黑电平、饱和值和 CFA 标签才有定量意义。

某些相机会在写入 RAW 前做模拟或数字域坏点替换、行列噪声校正、无损压缩，甚至在特定模式下做多帧合成。只要处理位置仍早于常规 RGB 成像链，厂商仍可能把结果称为 RAW。因此“RAW”描述的是一类较早的数据接口，不保证逐电子、逐像素、逐步骤完全原封不动。

=== 一个 RAW 容器里不只有像素数组

#figure(
  image("../svg/raw_record_layers.svg", width: 98%),
  caption: [RAW 文件的逻辑层。马赛克码值、辐射校准、颜色条件、几何元数据和预览可以共存于同一容器；预览并不等于底层测量。],
) <fig:raw-record-layers>

@fig:raw-record-layers 中各层承担不同职责：

- *马赛克测量*保存空间位置上的单通道样本及其打包方式；
- *辐射校准*规定如何解释零点、尺度、非线性、缺陷与响应不均匀；
- *颜色条件*提供相机通道到参考色度坐标的近似关系；
- *几何元数据*说明有效画幅、方向、裁切和镜头相关映射；
- *预览图*让相机和浏览器无需完整解码便能显示近似成片。

同一整数数组若采用不同黑电平 $b$ 和白电平 $w$，就会得到不同归一化曝光；若采用不同颜色矩阵，也会得到不同标准 RGB。元数据不是旁注，而是测量模型的一部分。

=== 黑电平、白电平与线性化

若相机响应由单调函数 $f_i$ 把线性传感器信号映射为存储码值，解码器可用线性化表 $cal(L)_i approx f_i^(-1)$。设黑电平和白电平分别为 $b_i,w_i$，一个常用归一化为

$
  x_i
  = "clip"_( [0,1] )
  ( (cal(L)_i(r_i)-b_i)/(w_i-b_i) ).
$ <eq:raw-black-white-normalization>

若数据本身已在有效范围内近似线性，则 $cal(L)_i$ 可取恒等映射。这里的 $x_i=1$ 表示达到文件规定的通道饱和参照，不表示显示器应发出峰值白；$x_i=0$ 表示扣除估计偏置后的零点，也不保证噪声样本不会为负。过早裁到 $[0,1]$ 会删去黑电平附近的噪声统计和通道高光重建可能利用的余量。

#figure(
  image("../svg/black_white_normalization.svg", width: 98%),
  caption: [黑白电平归一化及黑电平模型的层级。线性化旨在恢复与曝光近似成比例的尺度，不能与后续创作曲线混为一谈。],
) <fig:raw-black-white-normalization>

黑电平可能是全局标量、每个 CFA 通道一个值、逐行变化的向量，或随温度与曝光时间变化的暗场。若遮光像素集合为 $cal(O)_(c,y)$，可用稳健位置估计

$
  hat(b)_(c,y)
  = "median" {r_i: i in cal(O)_(c,y)}
$ <eq:optical-black-median>

抑制热像素和异常读出，但遮光区与有效成像区也可能存在系统差异。校正不足会让暗部带色或出现行带；校正过度则会截掉真实的低信号分布。白电平也可能按通道、增益模式和读出方式变化，因为模拟链或 ADC 先于像素满阱达到上限。

=== 有效位深不等于打包位数

一个 14 位字段只说明最多有 $2^14$ 种整数表示。若黑电平约为 512、白电平约为 15500，可用码位少于完整 16384；若读出噪声标准差为数个 DN，最低若干位主要记录噪声而非可重复场景差异。反之，无损压缩可以显著减小文件，却不减少任何可解码码值。

设当前表示为随机变量 $X$，无损编码 $C$ 满足

$
  C^(-1)(C(X))=X.
$ <eq:lossless-codec-inverse>

它只保证对“当前表示”可逆。若更早的处理 $F$ 已经去马赛克、裁切或量化，通常只能保证

$
  C^(-1)(C(F(X)))=F(X),
  quad F(X) != X.
$ <eq:lossless-after-lossy-stage>

因此“无损 TIFF”不等于“与 RAW 信息相同”，“有损 RAW”也不必等于视觉上粗糙；应分别追踪处理历史、误差尺度和任务所需信息。

#paragraph-heading[预览 JPEG 为什么会误导判断]

相机屏幕、操作系统缩略图和许多文件浏览器首先读取嵌入预览。它通常已经应用白平衡、相机色彩风格、降噪、锐化、色调曲线和输出编码。于是预览直方图与高光警告对应某个渲染，而不是 @eq:raw-dn-model 的原始通道分布。

预览仍然有用：它近似回答“按当前相机意图输出会怎样”。但若问题是某通道是否在 RAW 中饱和、阴影是否仍有统计可辨信息，必须读取底层样本及其黑白电平。把预览和 RAW 看作同一张图的高低质量版本，会混淆测量域与显示域。

== 基本处理链：从传感器域到输出域

=== 用算子复合描述管线

令原始码值数组为 $bold(r)$，元数据和配置文件统称为 $m$。一条概念管线可以写成

$
  bold(y)
  = cal(Q) circle cal(E) circle cal(S) circle cal(T)
  circle cal(M) circle cal(D) circle cal(G)
  circle cal(F) circle cal(B) (bold(r);m).
$ <eq:raw-pipeline-composition>

这里从右向左依次表示：黑电平与线性化 $cal(B)$、缺陷/增益校准 $cal(F)$、白平衡 $cal(G)$、去马赛克 $cal(D)$、颜色变换 $cal(M)$、外观和色调映射 $cal(T)$、缩放/锐化 $cal(S)$、输出编码 $cal(E)$ 与量化 $cal(Q)$。这个顺序不是所有实现的唯一标准；降噪可能分布在传感器域、RGB 域与输出域，白平衡也可与线性去马赛克合并。但任何等价重排都需要额外条件。

#figure(
  image("../svg/raw_pipeline_dag.svg", width: 100%),
  caption: [RAW 处理链的有向图。每个节点既改变数值，也可能改变通道数、空间相关性、噪声模型和参考域；元数据分别约束不同节点。],
) <fig:raw-pipeline-dag>

=== 坏点、平场与镜头阴影校正

设线性化后的 CFA 值为 $x_i$，平场响应为 $v_i>0$，坏点集合为 $cal(H)$。响应归一可写成

$
  x_i^("flat")=x_i/v_i,
  quad i in.not cal(H).
$ <eq:raw-flat-field-correction>

暗角处 $v_i<1$，除以 $v_i$ 会恢复期望均值，同时把噪声标准差也约放大 $1/v_i$。若 $v_i$ 的估计有误，校正还会引入低频亮度偏差。坏点替换则由邻域估计 $hat(x)_i$ 代替原观测；它是不可逆插值，不能再把该位置当作独立实测样本。

镜头阴影可能同时具有空间和色彩依赖，可用每通道增益图 $v_(c)(bold(x))$ 描述。校正应在近似线性域执行，因为阴影是到达像面的辐照度乘性变化；若在已加 toe 或色调曲线的显示码值上直接乘增益，暗部与高光的物理比例不会正确恢复。

=== CFA 采样与去马赛克

令潜在三通道线性场为 $bold(u)(bold(x))=(u_R,u_G,u_B)^T$，CFA 掩模为 $m_c (bold(x)) in {0,1}$，且每个位置只有一个掩模为 1。理想观测为

$
  z (bold(x))
  = sum_(c in {R,G,B}) m_c (bold(x)) u_c (bold(x)) + n (bold(x)).
$ <eq:cfa-sampling-operator>

从一个标量 $z (bold(x))$ 恢复三个通道值并无逐像素唯一解。去马赛克必须使用空间平滑、边缘方向、跨通道相关或学习到的自然图像统计。简单双线性方法是固定线性算子；边缘自适应方法根据观测选择插值方向，因而是数据依赖的非线性或分段线性算子。

#figure(
  image("../svg/cfa_measurement_reconstruction.svg", width: 98%),
  caption: [CFA 对连续颜色场进行单通道欠采样，再由先验估计 RGB 场。高频纹理可能同时解释成亮度结构或颜色结构，所以假色与摩尔纹具有不可辨识性根源。],
) <fig:cfa-measurement-reconstruction>

去马赛克输出的每个 RGB 像素通常混合了邻域多个 RAW 样本，因此噪声从近似独立变为空间和通道相关。后续算法若仍把每个 RGB 样本视为独立同方差观测，会低估边缘与纹理处的不确定度。

=== 白平衡不是“选择一个色温”

在相机线性 RGB 中，白平衡常近似为对角增益

$
  bold(u)_("wb")
  =G bold(u),
  quad G="diag"(g_R,g_G,g_B).
$ <eq:raw-white-balance-gain>

增益可由灰参考、相机估计或用户指定。若蓝通道增益为 2.5，其信号和既有读出噪声标准差都乘 2.5，方差乘 $2.5^2$；白平衡不会补回蓝通道从未收集到的光子。对混合照明，同一个全局 $G$ 不能同时把不同光源下的所有中性物体都映到同一中性轴。

白平衡也影响高光：三个通道的饱和值和增益不同，增益后可能先后越过工作空间上限。为保留通道间比例，浮点场景参照管线常允许暂时大于 1；若每步都裁到显示范围，高光颜色会取决于处理顺序。

=== 相机颜色到标准颜色

相机三通道响应不是标准观察者配色函数。在线性近似下，可用 $3 times 3$ 矩阵把白平衡后的相机 RGB 映到 XYZ 或工作 RGB：

$
  bold(q)=M bold(u)_("wb").
$ <eq:camera-color-matrix>

矩阵通常由色卡样本和指定照明下的最小二乘或感知加权误差拟合。因为相机通道与 CIE 配色函数一般不满足精确线性张成关系，单个 $M$ 不能对所有光谱反射率和光源都精确。双照明配置文件会在两个标定条件间插值，却仍不能消除同色异谱误差。

矩阵输出可能为负或超出目标 RGB 色域。负值并不自动是坏数据，它可能表示该颜色在所选原色基下需要外推；过早逐通道裁零会改变色相。颜色矩阵负责坐标近似，色域映射负责把不可显示颜色压入目标集合，两者不应偷偷合并成一次无说明的裁切。

#paragraph-heading[降噪、锐化与“细节”的任务依赖性]

传感器域降噪能利用 CFA 原始噪声模型和未插值样本；RGB 域降噪更容易利用跨通道与感知结构；显示域降噪则可以按最终观看尺度优化。三者的目标不同，不能只以滑杆强度比较。

锐化通常放在至少两个位置：早期补偿光学、像素孔径和去马赛克造成的频率衰减，晚期针对输出尺寸与介质补偿主观清晰度。早期过强会把噪声和假色送入后续非线性步骤，晚期过强则直接产生可见 halo。所谓“RAW 更锐”没有独立意义；RAW 尚未定义最终采样核、对比曲线和观看尺度。

== 操作顺序与可交换性

=== 直接比较两种处理顺序

判断两个步骤能否换序，最可靠的方法是分别计算“先做 A 再做 B”和“先做 B 再做 A”，比较结果是否在任务条件下相同。即使两步都是线性操作，也不能自动认为顺序无关。

#figure(
  image("../svg/operator_commutator.svg", width: 98%),
  caption: [两个步骤的换序实验。直接比较两种顺序，比背诵一条对所有软件都不变的固定管线更可靠。],
) <fig:raw-operator-commutator>

以白平衡与颜色矩阵为例：白平衡分别缩放各通道，颜色矩阵又会混合通道。只要被混合的通道增益不同，交换二者就通常改变结果。软件可以把白平衡预先合并进一张新的颜色矩阵，却不能把原来的两个步骤随意互换。

#struct-box(title: [可以继续深入])[线性代数用交换子 $[A,B]=A B-B A$ 统一判断算子是否可交换，并可进一步分析整条处理图的等价变换。本书后文只使用“实际比较两种顺序和检查假设”这一结论。]

=== 偏置、增益与裁切

正确的黑电平校正后增益为

$
  G(bold(r)-bold(b))=G bold(r)-G bold(b).
$ <eq:gain-after-black-subtraction>

若错误地先增益再减原黑电平，得到 $G bold(r)-bold(b)$，两者相差 $(G-I)bold(b)$。这个常量偏差在暗部占比最大，会表现为偏色和错误噪声底。

裁切算子 $P(x)="min"("max"(x,0),1)$ 也不与增益交换：

$
  P(a x) != a P(x)
$ <eq:clipping-gain-noncommute>

对 $a>1$ 和接近高光的 $x$ 尤其明显。若先裁切再做曝光回拉，已经合并为 1 的不同输入仍无法区分；若保持浮点超范围值到色调映射阶段，则还可按通道和亮度关系平滑压缩。

=== 线性空间滤波何时可与颜色矩阵交换

设空间卷积 $H$ 对每个通道使用同一个核，颜色矩阵 $M$ 在所有像素位置恒定。由于求和与矩阵乘法都是线性的，

$
  H(M bold(u))=M(H bold(u)).
$ <eq:spatial-color-linear-commute>

此时二者可交换。若不同通道使用不同核、矩阵随位置变化、边界规则不同，或滤波权重依赖图像内容，等式便失效。双边滤波、边缘自适应插值、局部色调映射与多数学习型算子都属于数据依赖情形。

对白平衡和去马赛克也可作同样区分：若去马赛克是各通道独立的固定线性插值，常量 CFA 增益可以在插值前后等价移动；若插值方向由通道梯度、饱和状态或跨通道差异决定，增益会改变权重选择，换序不再严格等价。

=== 非线性曲线前后不能沿用同一“曝光加法”

在线性场景域，增加 $k$ 档对应乘 $2^k$。经过非线性曲线 $T$ 后，一般有

$
  T(2^k x) != 2^k T(x).
$ <eq:tone-exposure-noncommute>

显示参照软件中的“曝光”滑杆若直接作用于非线性码值，它只是某个外观变换，不再等价于物理曝光比例。类似地，在 sRGB 码值上平均、模糊或缩放会得到错误的能量平均；@chap:tone-encoding 的 @eq:encoded-half-linear-light 已给出最简单反例。

=== 噪声模型也必须随管线传播

设输入均值和协方差为 $bold(mu),Sigma$。线性变换 $A$ 后有

$
  bold(mu)'=A bold(mu),
  quad Sigma'=A Sigma A^T.
$ <eq:linear-noise-propagation>

非线性曲线的斜率随信号改变，因此同样大小的输入噪声会在阴影、中间调和高光得到不同放大率。经过局部色调、裁切或自适应滤波后，噪声甚至不再能由一个全局方差概括。

#figure(
  image("../svg/noise_jacobian_propagation.svg", width: 98%),
  caption: [均值和协方差沿处理链传播。颜色矩阵产生通道相关，重采样和去马赛克产生空间相关，非线性曲线使噪声尺度依赖局部信号。],
) <fig:noise-jacobian-propagation>

降噪器若假设泊松—高斯独立噪声，却接收到已白平衡、去马赛克和锐化的数据，其阈值与似然模型都可能失配。这也是“先锐化再降噪”通常更难的原因：锐化不仅增大高频噪声，还把它变为空间相关并与边缘结构混合。

== 场景参照与显示参照工作流

#paragraph-heading[两种数值回答不同问题]

场景参照线性 RGB 试图保留与相机估计的场景曝光成比例的关系，允许高光超过名义白点，也允许颜色变换产生负分量。显示参照 RGB 则在给定原色、白点、峰值、黑位和传递函数下规定输出设备应产生的光。

#figure(
  image("../svg/scene_display_reference.svg", width: 98%),
  caption: [场景参照到显示参照的方向性。外观变换把可测量的相对曝光关系压入目标设备约束；该步骤通常不是可逆坐标变换。],
) <fig:scene-display-reference>

场景参照编辑适合处理曝光合成、白平衡、物理线性模糊与颜色矩阵。显示参照编辑适合评估最终黑白点、输出对比、设备色域和观看环境。二者之间的外观变换包含创作选择，因此同一 RAW 可以有多个同样有效的显示解释。

#paragraph-heading[场景到显示通常不是一一对应]

设场景参照图像为 $bold(s)$，显示设备可行集合为 $cal(D)$。外观映射 $T:cal(S) arrow cal(D)$ 必须处理动态范围和色域不匹配。若 $cal(S)$ 比 $cal(D)$ 大，$T$ 不可能在所有方向上同时保持绝对亮度、颜色距离、局部对比与像素唯一性。

这不是文件位深不足造成的偶然缺陷，而是目标集合受限。更高精度可以让压缩过程更平滑，却不能让有限峰值显示器复现任意场景亮度。把已经显示映射的 JPEG 再导入场景参照流程，无法恢复原来的超范围值，只能依据先验重新估计。

#paragraph-heading[非破坏编辑保存的是操作图]

许多 RAW 软件不改写原始像素，而保存参数向量 $theta$ 与处理图 $F_theta$：

$
  bold(y)=F_theta (bold(r),m).
$ <eq:parametric-nondestructive-edit>

所谓“非破坏”是指原始文件仍在，参数可以修改，并非当前预览没有经历不可逆映射。若原 RAW、配置文件、软件版本或算子语义缺失，仅保存 $theta$ 未必能在未来重现完全相同的 $bold(y)$。可重复工作流需要同时记录输入、参数、颜色管理条件和渲染实现。

== 文件格式、压缩与归档

=== 先区分容器、编码、像素模型和状态

讨论“JPEG 与 TIFF 哪个更好”之前，应拆分四层：

1. *容器*如何组织图像、缩略图、辅助通道和元数据；
2. *编码*如何压缩字节，是可逆、近无损还是有损；
3. *像素模型*是 CFA、线性 RGB、非线性 RGB、YCbCr、整数还是浮点；
4. *处理状态*位于传感器域、场景参照域还是显示参照域。

#figure(
  image("../svg/format_information_layers.svg", width: 100%),
  caption: [常见格式族的分层比较。表中描述的是典型用法而非后缀的逻辑必然；同一容器可能容纳不同编码和位深。],
) <fig:format-information-layers>

JPEG 的经典编码通过颜色变换、分块离散余弦变换、量化与熵编码取得高压缩率，可参见 @wallace1991。量化是主要有损步骤；即使保存质量参数很高，反复解码—编辑—重编码仍可能累积误差。TIFF 更像灵活容器，既可存无压缩或无损压缩整数 RGB，也可存其他样本类型；“TIFF”本身不保证线性、宽色域或高位深。

HEIF/AVIF 一类现代容器可以组织单图、序列、缩略图、深度或透明辅助图，并支持高位深与 HDR 信令。它们的复杂性也说明：扩展名不足以确定图像物理意义，仍需读取色彩配置、传递函数、原色和参考亮度元数据。

#paragraph-heading[DNG 与专有 RAW]

DNG 试图用公开标签描述 CFA、黑白电平、线性化、颜色矩阵、裁切和预览等信息。它可以封装马赛克 RAW，也可以封装已去马赛克的线性图像；“是 DNG”不推出“仍是原始 CFA”。专有 RAW 可能包含公开标准容器、厂商私有标签和未知处理，解码器需要随相机型号更新。

把专有 RAW 转为 DNG 时，应区分三种情况：

- 原始马赛克数据与关键元数据被无损搬运；
- 原始数据被重新无损编码，但私有元数据未完整保留；
- 数据先被线性化、裁切、去马赛克或有损压缩，再写入 DNG。

三者都可能生成可用文件，却具有不同的未来可解释性。归档决策应检查转换报告和可验证内容，而不是只看新后缀。

#paragraph-heading[中间母版与交付文件]

中间母版的任务是保留后续编辑余量，通常偏好场景参照或高位深工作空间、无损压缩、明确颜色配置和必要辅助通道。交付文件的任务是兼容目标设备、控制带宽并固定外观，常使用显示参照编码、有损压缩和目标尺寸。

同一文件不必同时最适合两种任务。把 16 位线性 TIFF 直接发往只按 sRGB 解释的网页可能显示错误；把 8 位 JPEG 当作唯一母版则会限制重新映射和大幅调整。格式选择是信息需求与接口约束的匹配，不是“专业/业余”标签。

#paragraph-heading[可验证归档的最小集合]

长期归档至少可分三层：

- 原始测量：原 RAW 或经验证的无损等价副本；
- 决策记录：sidecar、编辑数据库导出、颜色配置和必要软件说明；
- 固定渲染：高质量母版及常用交付版本。

再为文件保存密码学校验值并采用多副本、异地和定期迁移策略，才能区分“文件仍存在”与“文件仍可正确解释”。RAW 归档的风险不只有介质损坏，也包括私有元数据、解码器和颜色配置的语义消失。

== 常见误解

#paragraph-heading[“RAW 就是相机看到的真实颜色”]

传感器只产生相机光谱响应下的通道测量。标准颜色还需要照明假设、白平衡、颜色矩阵和观察条件；不同合理管线可以给出不同显示结果。RAW 更接近早期测量，不等于唯一真实外观。

#paragraph-heading[“所有 RAW 都完全没处理”]

相机可能在写文件前做偏置校正、坏点处理、压缩或多帧融合。是否应称为“原始”取决于讨论层级；严谨做法是描述已知处理，而不是把 RAW 当作绝对纯净类别。

#paragraph-heading[“白平衡可以最后再做，反正只是三个乘数”]

在无限精度、无裁切、固定线性算子组成的理想链中，白平衡有时能被吸收入其他矩阵或移过线性插值；一旦出现通道混合、内容自适应插值、降噪阈值、非线性曲线或裁切，换序通常会改变结果，@eq:clipping-gain-noncommute 给出了最简单的裁切反例。

#paragraph-heading[“16 位 TIFF 一定比 14 位 RAW 信息更多”]

位数字段描述当前表示的码位数量，不描述它经历了哪些多对一变换。16 位 TIFF 可能只是把已去马赛克、裁切和色调映射的结果更细地量化；它可以更适合继续编辑，却不能恢复 RAW 的原始通道观测与高光余量。

#paragraph-heading[“无损保存意味着以后可以还原任何步骤”]

无损编码只对保存时的数组可逆。若数组此前已经过坏点替换、去马赛克、色域裁切或局部色调映射，编码器无法推断那些已合并的输入。可逆性必须沿整条算子链逐段判断。

#pagebreak(weak: true)

#struct-box(title: [本章小结])[
  - RAW 保存的是靠近传感器端的测量数组与校准条件，不是已经完成的唯一 RGB 照片。
  - 单个 RAW 码值混合了光电子统计、增益、偏置、读出噪声和量化；脱离黑白电平与 CFA 标签无法完整解释。
  - 黑电平、线性化和平场试图恢复相对曝光尺度；创作色调曲线属于更后的场景到显示映射。
  - CFA 去马赛克是欠采样颜色场的估计问题，会引入空间与通道相关，并可能产生不可唯一消除的假色。
  - 白平衡是通道增益，颜色矩阵是通道混合；两者一般不交换，裁切和非线性曲线也会使换序不可逆。
  - 线性矩阵会改变噪声方差并产生通道相关；非线性步骤又让噪声尺度依赖信号，噪声模型必须随管线更新。
  - 场景参照数值保留相对曝光关系，显示参照数值规定目标设备外观；二者之间的外观变换通常不是一一对应。
  - 文件后缀不能单独决定信息量；应分别检查容器、编码、像素模型、位深、处理状态和元数据。
  - 非破坏编辑保存操作图而不改写原始输入；可重复渲染还依赖配置文件、算子语义和软件实现。
]

#pagebreak(weak: true)

== 思考与计算

#block[
  1. 某 14 位 RAW 的黑电平为 512，白电平为 15360。用 @eq:raw-black-white-normalization 计算码值 400、512、4096、15360、16000 的未裁切与裁切归一值；说明负值和大于 1 的值各可能携带什么信息。
  2. 遮光区某行某通道有读数 $(510,511,511,512,512,513,514,900)$。比较均值与中位数黑电平估计，并说明热像素为何使 @eq:optical-black-median 更稳健。
  3. 设镜头阴影增益图在中心为 1、角落为 0.4。按 @eq:raw-flat-field-correction 校正后，角落信号均值和噪声标准差各放大多少？若平场估计有 2% 相对误差，会形成怎样的低频残差？
  4. 对 RGGB 的 $4 times 4$ 网格写出 @eq:cfa-sampling-operator 的掩模矩阵。若三通道在空间上完全常量，证明每个通道可由其样本唯一恢复；再给出一个高频颜色图样，使同一 CFA 观测具有两种不同 RGB 解释。
  5. 设黑电平 $bold(b)=(512,512,512)^T$，增益 $G="diag"(2,1,3)$。比较 $G(bold(r)-bold(b))$ 与 $G bold(r)-bold(b)$ 的常量偏差；它为何在暗部比高光更显眼？
  6. 对 $P(x)="min"("max"(x,0),1)$、$a=2$，分段求出 $P(a x)-a P(x)$。在哪些输入区间二者相等？哪些输入已经因裁切不可逆？
  7. 证明 @eq:spatial-color-linear-commute：设 $H$ 为所有通道共享的离散卷积核，$M$ 为空间恒定矩阵，交换有限求和次序即可。再列出三个使证明失效的实际条件。
  8. 某线性 RGB 像素协方差为 $Sigma="diag"(4,1,9)$，白平衡为 $G="diag"(2,1,1.5)$。用 @eq:linear-noise-propagation 求输出协方差。若随后用混合矩阵 $M$，解释为何输出通道一般相关。
  9. 构造一个含白平衡、去马赛克、颜色矩阵、色调映射和锐化的处理图。给每条边标注数据域、通道数、是否线性和是否一一对应；指出至少两组可交换步骤和三组不可交换步骤，并写明所需假设。
  10. 为个人照片设计三层归档方案：原始测量、决策记录和固定渲染。分别选择格式、颜色配置、校验和与冗余策略；说明若十年后缺少原软件，哪些结果仍可复现，哪些只能近似重建。
]
