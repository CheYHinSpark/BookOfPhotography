#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading
#import "../diagrams/geometric_optics.typ": thin-lens-imaging, aperture-pupils

#pagebreak(weak: true)

= 理想透镜与高斯成像 <chap:gaussian-optics>

镜头并不是把每一束光“搬运”到传感器，而是改变光线方向，使来自同一物点的一族光线在像空间重新会聚。焦距、放大率、对焦距离和入瞳都建立在这种几何关系上。真实摄影镜头可能包含十余片曲面透镜，但在像差尚不重要的尺度上，仍可用主平面、焦点和近轴光线概括。这套近似通常称为高斯光学。

本章从折射规律和近轴近似出发，建立薄透镜、主平面、光阑与瞳的模型。目标不是设计镜头，而是理解焦距、放大率、f 数和对焦在摄影中分别改变什么。几何光学的系统处理可参见 @hecht2017、@greivenkamp2004 与 @smith2007。

@chap:projective-geometry 把相机暂时压缩成一个投影中心，本章则解释镜头怎样让这个理想投影在传感器上形成实像。得到的共轭关系、入瞳和 f 数将直接进入景深、像面照度与衍射，所以本章是几何投影通向后续光量和清晰度问题的接口。

== 折射与近轴模型

光从一种介质进入另一种介质时，方向满足斯涅尔定律

$ n_1 sin theta_1 = n_2 sin theta_2. $ <eq:snell-law>

其中角度相对界面法线测量。折射率随波长变化，因此不同颜色的折射角与焦点略有差异，这正是色差的物理起点。摄影计算通常不需要从变分原理重新推导这条定律，只需知道镜头通过多次折射改变光线方向，而近轴模型只保留小角度下的一阶关系。

#struct-box(title: [可以继续深入])[费马原理用“光程取驻值”统一描述反射、折射和梯度折射率介质；镜头设计还会使用光线追迹和像差优化。它们适合希望进入物理光学或镜头工程的读者，不是理解后续摄影章节的前提。]

== 近轴近似与一阶光学

=== 小角展开

设光线与光轴夹角为 $theta$。当 $abs(theta)$ 足够小时，

$
  sin theta = theta - theta^3/6 + cal(O)(theta^5),
  quad
  tan theta = theta + theta^3/3 + cal(O)(theta^5),
$ <eq:paraxial-expansion>

$
  cos theta = 1-theta^2/2 + cal(O)(theta^4).
$ <eq:cosine-expansion>

高斯光学只保留光线高度与角度的一阶项，于是 $sin theta approx tan theta approx theta$。被丢弃的最低阶角度误差通常是三阶，这些高阶项正是单色几何像差出现的入口。

#figure(
  table(
    columns: (0.8fr, 1.2fr, 1.2fr, 1.35fr),
    align: (center, center, center, left),
    [*$theta$*], [$(theta-sin theta)/theta$], [$(tan theta-theta)/theta$], [*解释*],
    [$5 degree$], [$0.127%$], [$0.255%$], [典型近轴范围],
    [$10 degree$], [$0.507%$], [$1.028%$], [一阶误差已可测],
    [$20 degree$], [$2.018%$], [$4.270%$], [高阶项不可轻忽],
  ),
  caption: [小角近似的相对误差。角度必须以弧度代入级数。],
) <tab:paraxial-errors>

表中的百分比只衡量三角函数近似，并不能单独判断镜头像差。一个大视场系统仍可能让每一束局部光线以小角度穿过某些面；反之，大孔径轴上光束也可能远离近轴条件。

#paragraph-heading[一阶量与高阶量]

在旋转对称系统中，光线可用高度 $y$ 与斜率 $theta$ 描述。一阶模型保留 $y$、$theta$ 的线性组合，预测：

- 物像共轭位置；
- 横向与角放大率；
- 有效焦距和主平面；
- 入瞳、出瞳及近轴视场。

它忽略包含 $y^2$、$y theta$、$theta^2$ 及更高幂的修正。因此，一阶模型给出“所有同一物点光线会聚于一点”，真实镜头却会形成有限点扩散函数。后续章节会分别把波动衍射和几何像差加回模型。

== 薄透镜模型

=== 符号约定

令光轴坐标 $z$ 沿光传播方向从左向右增加，薄透镜位于 $z=0$。物平面坐标为 $z_o$，像平面坐标为 $z_i$。对通常的实物成实像情形，$z_o<0$、$z_i>0$。会聚透镜焦距 $f>0$，发散透镜焦距 $f<0$。

在此笛卡尔符号约定下，薄透镜方程为

$ 1/z_i - 1/z_o = 1/f. $ <eq:thin-lens-signed>

若只讨论实物和实像，并把正的距离大小记为 $s=-z_o$、$s'=z_i$，则熟悉的形式是

$ 1/f = 1/s + 1/s'. $ <eq:thin-lens-positive>

有符号形式更适合统一处理虚物与虚像；正距离形式更适合普通相机的数量估算。二者不能混用符号。

=== 三条近轴特征光线

对薄会聚透镜，常用三条光线构造像：

1. 平行于光轴的入射光线，出射后通过像方焦点 $F'$；
2. 通过物方焦点 $F$ 的入射光线，出射后平行于光轴；
3. 通过薄透镜光心的近轴光线近似不偏折。

#figure(
  thin-lens-imaging(width: 96%),
  caption: [薄会聚透镜的近轴成像。三条特征光线在同一像点相交；图中距离按统一比例绘制。],
) <fig:thin-lens-imaging>

// #figure(
//   image("../svg/thin_lens_imaging.svg", width: 96%),
//   caption: [薄会聚透镜的近轴成像。三条特征光线在同一像点相交；图中距离按统一比例绘制。],
// ) <fig:thin-lens-imaging>

这些是同一个一阶变换的三种方便取样，并不是镜头只允许三条光线通过。真实物点发出的连续光束都会受到同一近轴映射。

=== 横向放大率

由通过光心的相似三角形，横向放大率为

$ m = y_i/y_o = z_i/z_o. $ <eq:lateral-magnification-signed>

实物成实像时 $z_i/z_o<0$，因此像倒立。用正距离大小表示，

$ m = -s'/s. $ <eq:lateral-magnification-positive>

放大率是无量纲比值，不是“镜头把物体拉近多少”的主观感受。摄影中的画面占比还取决于传感器尺寸与最终裁切。

=== 一个对焦数量级

一支 $f=50 "mm"$ 的理想镜头对焦在 $s=2 "m"$ 的物体上。由 @eq:thin-lens-positive，

$
  s' = f s/(s-f)
  = (50 times 2000)/(2000-50) "mm"
  approx 51.28 "mm".
$ <eq:fifty-mm-focus-example>

相对于无穷远焦平面，像面只需后移约 $1.28 "mm"$；横向放大率约为 $-0.0256$。普通摄影对焦机构移动很小，却足以显著改变清晰距离，这也是镜头机械精度重要的原因。

=== 物距区间与像的性质

#figure(
  table(
    columns: (1.1fr, 1.15fr, 1.1fr, 1.45fr),
    align: (center, center, center, left),
    [*会聚透镜物距*], [*像距*], [*放大率*], [*像的性质*],
    [$s arrow.r infinity$], [$s'=f$], [$m arrow.r 0$], [焦平面上的微小倒像],
    [$s>2f$], [$f<s'<2f$], [$-1<m<0$], [倒立、缩小、实像],
    [$s=2f$], [$s'=2f$], [$m=-1$], [倒立、等大、实像],
    [$f<s<2f$], [$s'>2f$], [$m<-1$], [倒立、放大、实像],
    [$s=f$], [$s' arrow.r infinity$], [无有限值], [出射光平行],
    [$0<s<f$], [$z_i<0$], [$m>1$], [正立、放大、虚像],
  ),
  caption: [理想会聚薄透镜在不同物距下的共轭关系。],
) <tab:conjugate-regimes>

相机通常工作在 $s>f$ 的实像区。微距摄影让 $s$ 接近 $2f$ 甚至更小，像距和有效 f 数随之增加；放大镜则工作在物体位于焦内的虚像区。

== 从薄透镜到复合镜头

#paragraph-heading[主平面与焦点]

真实镜头有有限厚度，不能把所有折射压缩到一个物理平面。但任意轴对称近轴系统，在给定物方和像方折射率时，都可由一组基点描述：

- 物方与像方焦点 $F,F'$；
- 物方与像方主点 $H,H'$；
- 物方与像方节点 $N,N'$。

物距应从物方主平面量起，像距应从像方主平面量起。若镜头两侧介质相同，节点与主点重合；两侧介质不同时，两组点一般分离。

有效焦距是从主平面到相应焦点的距离，而不是从前镜片、卡口或镜头筒中心量起。远摄结构可以让物理镜头长度短于焦距；反望远结构则可为广角镜头留下较长后焦距。

#paragraph-heading[焦距不是镜头长度]

商品镜头标注的焦距通常是无穷远、指定参考波长下的有效焦距。内部对焦会改变透镜组间距，使系统主平面和有效焦距随对焦距离移动；近距离视角变化常被称为焦点呼吸。把镜头外壳长度或传感器到前镜片的距离当成焦距，会在几何上失去意义。

#struct-box(title: [可以继续深入])[若要研究镜头如何由材料、曲率和间距获得指定焦距，可继续学习薄透镜制造者公式、厚透镜基点和数值光线追迹。这些内容服务镜头设计，本书后文只使用有效焦距与主平面。]

== 光阑、入瞳与出瞳

=== 孔径光阑

镜头中的许多元件边缘都可能截断光线。对轴上物点而言，限制通过光束锥角的元件称为孔径光阑（aperture stop）。它可能是可变光圈叶片，也可能在最大光圈时由某片镜框充当。

从物方透过光阑前方的光学元件看见的光阑像，称为入瞳；从像方透过光阑后方元件看见的光阑像，称为出瞳。瞳是光阑的光学像，不一定与光圈叶片处在同一位置，也不一定同样大。

#figure(
  aperture-pupils(width: 96%),
  caption: [光阑、入瞳与出瞳的定义。前后透镜组分别把同一个物理光阑成像到物方与像方。],
) <fig:aperture-pupils>

// #figure(
//   image("../svg/aperture_pupils.svg", width: 96%),
//   caption: [光阑、入瞳与出瞳的定义。前后透镜组分别把同一个物理光阑成像到物方与像方。],
// ) <fig:aperture-pupils>

=== f 数使用入瞳直径

无穷远对焦时的几何 f 数定义为

$ N = f / D_("en"), $ <eq:fnumber-entrance-pupil>

其中 $D_("en")$ 是入瞳直径，不是前镜片直径，也不一定等于光圈叶片的机械开口。前镜片必须足够大以避免边缘视场被遮挡，因此其直径常大于 $f/N$。

像方数值孔径为

$ "NA"' = n' sin u', $ <eq:image-space-na>

在空气、无穷远和近轴条件下，$N approx 1/(2 "NA"')$。大数值孔径时不能再把 $sin u'$ 简化为 $u'$，且物方有限距离还会引入工作 f 数。

#paragraph-heading[视场光阑与渐晕]

限制可成像物方范围的元件称为视场光阑。孔径光阑控制轴上光束的角宽，视场光阑控制哪些场点进入系统；二者概念不同。离轴场点的光束还可能被镜框部分截断，形成机械渐晕和猫眼形焦外光斑。

瞳孔放大率定义为出瞳直径与入瞳直径之比。它影响近摄有效 f 数、像方光束角和焦外光斑形状。只知道标称 f 数不足以重建复杂镜头内的全部光束几何。

#struct-box(title: [可以继续深入])[ABCD 矩阵把传播、折射和薄透镜写成可连续相乘的 $2 times 2$ 线性变换，适合分析多片系统、主平面和变焦机构。本书后文只需要它给出的结论：复合镜头仍可用有效焦距、主平面和物像共轭描述。]

== 对焦在数学上改变什么

#paragraph-heading[改变共轭关系]

对焦的目标是让指定物平面与固定传感器平面满足物像共轭。传统整组对焦近似移动整个镜头，从而改变物距和像距；内部对焦则改变某些镜组间距和系统主平面。两者都在重新建立同一件事：让目标平面的像落在传感器上。

若传感器没有恰好位于共轭像平面，来自一个物点的光束会在传感器前后会聚，像面上留下有限离焦斑。景深并不是“仍然严格合焦的空间范围”，而是离焦斑小于某个允许阈值的范围。

#paragraph-heading[无穷远并不是一个机械常数]

理想薄透镜在无穷远物体上有 $z_i=f$。真实镜头的焦点会随波长、温度、制造误差和对焦组位置变化，镜头机械标尺上的无穷远位置通常留有余量。自动对焦估计的是某个成像评价函数或相位差，而不是直接测量数学上的无穷远。

#paragraph-heading[对焦与透视无关]

改变对焦位置可能伴随镜组移动和焦距呼吸，从而轻微改变取景范围；但中心投影的透视关系仍主要由相机投影中心的位置决定。把背景“拉近”的主要原因是拍摄位置改变，不是对焦环本身。

== 模型边界与常见误解

#paragraph-heading[“光心光线永不偏折”]

这只是薄透镜近轴构图规则。厚透镜中，对应的是通过节点的一阶角关系；实际光线在每个折射面都会改变方向。

#paragraph-heading[“焦距是镜片到传感器的距离”]

只有无穷远、薄透镜模型下，像距才等于焦距。复合镜头的焦距从主平面量起，而主平面甚至可能位于镜头实体之外。

#paragraph-heading[“f/2 的开口直径就是焦距除以 2”]

正确对象是入瞳直径。光圈叶片通过前组所成的像可能被放大或缩小；前镜片还必须容纳离轴光束，不能从 f 数直接认定其直径。

#paragraph-heading[“满足薄透镜方程就会形成完美点像”]

薄透镜方程只保证一阶光线在同一平面相交。有限孔径带来衍射，高阶折射带来像差，传感器还会进行面积积分与采样。合焦位置正确不等于系统具有无限分辨率。

#struct-box(title: [本章小结])[
  - 光线在介质界面满足斯涅尔定律；折射率随波长变化是色差的物理起点。
  - 高斯光学保留光线高度和角度的一阶项，忽略产生像差的高阶项。
  - 在本章符号约定下，薄透镜满足 $1/z_i-1/z_o=1/f$，横向放大率为 $m=z_i/z_o$。
  - 复合镜头的物像距离从主平面量起；有效焦距不是镜头外壳的机械长度。
  - f 数使用入瞳直径；入瞳与出瞳是孔径光阑通过前后镜组形成的光学像。
  - 对焦就是改变镜头位置或镜组参数，使目标物平面与传感器平面重新满足物像共轭。
]

== 思考与计算

#block[
1. 比较 $theta=15 degree$ 时 $sin theta$、$theta$ 与 $tan theta$。若系统对角度误差容许量为 $0.5%$，这一角度能否视为近轴？还需要哪些系统信息才能回答成像误差问题？
2. 一支 $85 "mm"$ 理想镜头分别对焦在 $10 "m"$、$2 "m"$ 和 $0.85 "m"$。计算像距、相对无穷远焦平面的伸长量和放大率。
3. 用有符号公式分析会聚透镜前方 $s=f/2$ 的物体。求虚像位置与放大率，并画出特征光线。
4. 一支标称 $50 "mm"$、f/1.4 镜头的入瞳直径约是多少？为什么不能据此断言前镜片直径恰好等于该值？
]
