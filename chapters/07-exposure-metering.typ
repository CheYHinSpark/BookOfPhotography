#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 曝光方程与摄影计量 <chap:exposure-metering>

“曝光三要素”把光圈、快门时间和 ISO 放在同一张三角形里，便于记忆，却掩盖了三者完全不同的物理角色。光圈改变像面方向锥体以及衍射、像差和景深；快门控制时间积分并改变运动模糊；ISO 主要规定信号增益和输出标定，并不让传感器收到更多光。真正的物理曝光量只有在像面辐照度对时间积分之后才出现。

本章从 @eq:image-irradiance-fnumber 继续，把场景辐亮度、镜头透过、快门门函数和光电转换连成一条有单位的方程；随后用以 2 为底的对数建立档、APEX 与 EV。后半章把测光表看成带有标定常数、空间权重和损失函数的统计估计器，说明反射率与照明为什么不可由一次亮度观测唯一分离。摄影曝光与计量的工程背景可参见 @ray2002、@kingslake1992、@allen2011 与 @iso2720。

@chap:radiometry 已经说明各种光量怎样定义，本章把它们收束成一次拍摄的设置与决策：光圈规定方向锥，快门规定时间窗，测光选择目标，ISO 暂时只作为标定条件。推导最终停在期望光电子数，下一章再解释传感器怎样把这些电子变成 RAW 数值。

#key-box[
  *曝光的三个层次*　场景具有辐亮度，像面获得辐照度，传感器在有限时间内积累曝光量与光电子。JPEG 看起来多亮属于后续映射；它不是前面任何一个物理量的定义。
]

== 从辐亮度到曝光量

=== 像面照度方程的复习与扩展

令 $L_o(lambda,bold(x),bold(omega))$ 为进入镜头的谱辐亮度，$Omega_p$ 为从像面位置 $bold(x)$ 看见的出瞳方向集合。忽略偏振，谱像面辐照度可写为

$
  E_(e,lambda)(lambda,bold(x))
  = integral_(Omega_p)
  tau(lambda,bold(x),bold(omega))
  L_o(lambda,bold(x),bold(omega))
  cos theta_i dif Omega.
$ <eq:spectral-image-irradiance>

轴上、近轴、物方辐亮度在瞳孔方向近似不变时，@eq:spectral-image-irradiance 退化为

$
  E_(e,lambda)(lambda)
  approx (pi tau(lambda) L_o(lambda))/(4N^2).
$ <eq:spectral-fnumber-exposure>

这里 $N=f/D$ 是几何 f 数。真实离轴系统可把多种衰减收进位置与波长相关因子 $V$：

$
  E_(e,lambda)(lambda,h)
  approx (pi L_o(lambda))/(4N^2)
  tau(lambda) V(lambda,h) cos^4 theta_i.
$ <eq:offaxis-exposure-factorization>

$V$ 可以包含机械渐晕、瞳孔畸变和遮挡；它未必与 $cos^4 theta_i$ 独立。后期暗角校正只能把边缘数字值乘大，不能补回没有到达的光子，因此边缘信号与噪声会一起被放大。

#figure(
  image("../svg/image_irradiance_controls.svg", width: 96%),
  caption: [像面照度的主要乘法因子。左图给出轴上 $1/N^2$ 标度；右图区分理想几何衰减、机械渐晕与数字补偿。曲线为示意。],
) <fig:image-irradiance-controls>

若把轴上能量透过率 $tau$ 吸收到一个等效光圈数中，可定义

$ T=N/sqrt(tau), $ <eq:tstop-definition>

使像面照度近似按 $1/T^2$ 缩放。T 数便于需要跨镜头保持光度连续的电影摄影，但不能概括离轴渐晕、色谱透过差异或像差。

对瞳放大率近似为 1 的薄对称镜头，放大率绝对值为 $abs(m)$ 时，有效 f 数近似

$ N_("eff") approx N(1+abs(m)). $ <eq:macro-effective-fnumber>

因此 1:1 成像时，标称 f/4 的几何锥角对应约 f/8 的有效照度标度，即需要约两档补偿。内对焦镜头会同时改变焦距与瞳放大率，精确值应由镜头数据或实测获得。

=== 快门是时间门函数

像面曝光量是辐照度在曝光区间上的积分：

$
  H_(e,lambda)(lambda,bold(x))
  = integral E_(e,lambda)(lambda,bold(x),t) s(bold(x),t) dif t,
$ <eq:spectral-exposure-integral>

其中无量纲 $s(bold(x),t) in [0,1]$ 是快门门函数。若照明恒定且所有像素同时以矩形门开启 $Delta t$，才有熟悉的

$ H_(e,lambda)=E_(e,lambda) Delta t. $ <eq:constant-exposure-product>

真实机械快门存在开启与关闭斜坡，焦平面快门在高速档形成移动狭缝，电子滚动快门则让不同像素行具有不同时间原点。定义有效曝光时间

$ Delta t_("eff")(bold(x))=integral s(bold(x),t) dif t $ <eq:effective-shutter-time>

可以校准平均光量，却不能消除不同位置的时间偏移。闪烁灯源、显示屏扫描和快速运动都会使 $E(t)$ 与 $s(t)$ 的相对相位进入结果。

=== 与光电子数连接

令一个像素有效感光面积为 $A_p$，量子效率为 $eta(lambda)$。忽略像素内部空间变化，光电子期望数近似为

$
  mu_e
  = A_p integral_(Lambda)
  eta(lambda) H_(e,lambda)(lambda)
  lambda/(h c) dif lambda.
$ <eq:exposure-to-electrons>

因单个光子能量为 $h c/lambda$，同样的辐射曝光在不同波长对应不同光子数；再乘量子效率后，各通道响应还会不同。@eq:exposure-to-electrons 是物理曝光与 @chap:sensor-physics 的接口。实际电子数会围绕 $mu_e$ 随机涨落，而不是每次都严格等于期望。

#figure(
  image("../svg/exposure_chain.svg", width: 98%),
  caption: [从场景辐亮度到光电子的曝光链。每一步都保留其单位与依赖变量，显示和色调映射位于链条之后。],
) <fig:exposure-chain>

#figure(
  table(
    columns: (1.15fr, 1fr, 1.25fr, 1.45fr),
    align: (left, center, left, left),
    [*量*], [*符号*], [*典型单位*], [*改变它的主要因素*],
    [场景谱辐亮度], [$L_o(lambda)$], [$"W" dot "m"^(-2) dot "sr"^(-1) dot "nm"^(-1)$], [照明、材料、观察方向],
    [像面谱辐照度], [$E_(e,lambda)$], [$"W" dot "m"^(-2) dot "nm"^(-1)$], [辐亮度、光圈、透过与渐晕],
    [谱辐射曝光量], [$H_(e,lambda)$], [$"J" dot "m"^(-2) dot "nm"^(-1)$], [辐照度与快门门函数],
    [电子数期望], [$mu_e$], [电子数], [曝光量、像素面积与量子效率],
    [输出码值], [$z$], [无量纲数码], [增益、黑电平、曲线与编码],
  ),
  caption: [摄影中常被统称为“亮度”或“曝光”的不同量。输出码值没有固定的辐射单位。],
) <tab:exposure-quantity-layers>

== 档与以 2 为底的对数

=== 倍率的加法坐标

若某个正量从 $q_1$ 变为 $q_2$，定义其变化档数为

$ Delta s=log_2(q_2/q_1). $ <eq:stop-definition>

$Delta s=1$ 表示加倍，$Delta s=-1$ 表示减半。对理想轴上曝光 $H prop tau Delta t/N^2$，从设置 1 到设置 2 的曝光量倍率为

$
  H_2/H_1
  approx (tau_2/tau_1)
  ((Delta t_2)/(Delta t_1))
  (N_1/N_2)^2.
$ <eq:exposure-ratio-settings>

取对数后，三个乘法因子变为档数相加：

$
  Delta s_H
  approx log_2(tau_2/tau_1)
  +log_2((Delta t_2)/(Delta t_1))
  -2log_2(N_2/N_1).
$ <eq:exposure-stop-sum>

所谓三分之一档就是倍率 $2^(1/3) approx 1.26$；它不是十进制数列的等间隔。快门时间每增加一档即乘 2，f 数每增加一档则乘 $sqrt(2)$，因为面积与直径平方成正比。

=== APEX 与 EV

APEX 系统把光圈和时间定义为

$
  A_v=2log_2 N,
  quad T_v=log_2(1/(Delta t)).
$ <eq:apex-av-tv>

曝光值定义为

$
  "EV"=A_v+T_v
  =log_2(N^2/(Delta t)).
$ <eq:ev-definition>

因此 EV 是相机设置组合的对数坐标，不是焦平面收到多少焦耳的独立物理单位。同一 EV 的光圈—快门组合在理想静态条件下给出相同 $Delta t/N^2$；但景深、衍射、像差和运动积分不相同。

#figure(
  image("../svg/ev_lattice.svg", width: 97%),
  caption: [光圈值 $A_v$ 与快门值 $T_v$ 的 APEX 格点。同一斜线满足 $A_v+T_v="EV"$；沿线移动保持理想曝光倍率，却改变其他成像算子。],
) <fig:ev-lattice>

=== EV100 与场景亮度

反射式测光常用标定方程

$ N^2/(Delta t) = L_v S/K, $ <eq:reflected-meter-equation>

其中 $L_v$ 是被测区域亮度，单位为 $"cd" dot "m"^(-2)$；$S$ 是标称感光度数值；$K$ 是反射式测光标定常数。$K$ 的选择属于工程约定，常见实现接近 12.5，但并非自然常数。令 $S=100$，得到

$
  "EV"_100
  =log_2(100 L_v/K).
$ <eq:ev100-luminance>

若取 $K=12.5$，则 $"EV"_100=log_2(8L_v)$。所以把 EV100 与场景亮度联系起来必须同时说明标定常数；只说“某场景是 EV 10”通常隐含 ISO 100 与某套测光约定。

提高 $S$ 会让 @eq:reflected-meter-equation 推荐更大的 $N^2/(Delta t)$，亦即更小的物理曝光量。这并不意味着传感器在相同光圈和快门下收到更多光。ISO 的增益、饱和与输出标定将在 @chap:sensor-physics 讨论。

=== 曝光补偿的代数意义

若相机给出的零补偿设置为 $"EV"_m$，正 $c$ 档曝光补偿通常执行

$ "EV"_("set")="EV"_m-c. $ <eq:exposure-compensation>

于是 $Delta t/N^2$ 乘以 $2^c$。曝光补偿改变的是测光器目标与相机设置，不改变场景辐亮度。手动曝光下某些相机只移动表尺指针，并不自动改变任何参数；自动 ISO 是否参与补偿也取决于控制策略。

== 互易律及其边界

=== 理想互易

在线性、静态、无饱和且照明恒定的条件下，只要

$ E_1 Delta t_1=E_2 Delta t_2, $ <eq:reciprocity-law>

两次曝光具有相同 $H$，进而给出相同的光电子期望。减一档光圈同时增加一档时间，就是这个乘积关系的直接应用。

但 @eq:reciprocity-law 只约束每个像素接收能量的时间积分。它没有约束光线方向分布、点扩散函数、主体轨迹、照明频谱或探测器附加噪声，因此“等曝光”不等于“等图像”。

#figure(
  image("../svg/reciprocity_limits.svg", width: 96%),
  caption: [互易律的适用范围。左侧两个矩形面积相同，表示积分曝光量相同；右侧因素会使摄影结果仍然不同。],
) <fig:reciprocity-limits>

#paragraph-heading[短时间与长时间端的失效]

在极短时间端，机械快门通光波形、闪光持续时间、灯光调制与滚动读出可能使名义时间不等于有效时间。在长时间端，暗电流、热像素、温升、天光变化和主体运动会加入额外信号；胶片还可能出现化学互易律失效，需要超过线性乘积所预测的曝光补偿。

数字传感器在线性范围内通常较好地服从光子积分，但这不代表任意长短时间都可互换。若较强瞬时辐照度使满阱饱和，再缩短时间也不能用同一线性模型外推；若较弱信号落到读出噪声附近，增加模拟增益也不能替代实际光子。

=== 闪光与环境光是两个时间尺度

设环境光近似恒定，闪光脉冲为 $E_f(t)$，则

$ H=E_a Delta t + integral E_f(t) s(t) dif t. $ <eq:ambient-flash-exposure>

在快门完全开放且闪光脉冲远短于 $Delta t$ 时，改变快门主要改变环境光项，闪光项近似不变；改变光圈则同时改变两项。超过焦平面快门同步速度后，狭缝无法同时看见一次短脉冲，除非使用持续多个扫描时段的高速同步闪光，其代价通常是峰值与总能量下降。

== 测光究竟测了什么

=== 入射式与反射式测光

入射式测光表位于主体处，通过平面或半球受光头对入射方向加权，估计照明在该位置提供的照度。反射式测光表位于相机方向，测量主体向观察方向发出的亮度；机内测光属于后者，并且还经过镜头透过、渐晕与测光光路。

#figure(
  image("../svg/metering_geometries.svg", width: 96%),
  caption: [入射式与反射式测光的几何差异。前者在主体位置估计入射照明，后者沿相机方向观测材料与照明共同形成的出射亮度。],
) <fig:metering-geometries>

入射表若采用余弦响应平面受光头，概念上接近测量平面照度；半球受光头则对来自不同方向的光有不同立体角权重，更适合估计立体主体的平均受光。两者读数不必相同，选择取决于待描述的几何。

反射式点测光可以精确选择画面区域，却无法仅凭读数区分“暗材料处在强光下”与“亮材料处在弱光下”。镜面高光还会显著依赖观察方向，不能用单一漫反射率解释。

=== 反射率—照明不可辨识性

在朗伯近似下，表面出射亮度满足

$ L_v=rho E_v/pi, $ <eq:lambertian-metering-product>

其中 $rho$ 是漫反射率，$E_v$ 是入射照度。一次反射式测量只得到二者乘积。对任意允许的比例 $a>0$，变换

$ (rho,E_v) arrow (a rho,E_v/a) $ <eq:albedo-illumination-ambiguity>

保持 $L_v$ 不变。因此相机不可能只从一个均匀区域的亮度自动知道它“本来是白色还是灰色”。要分离材料与照明，必须加入已知灰卡、多个照明条件、几何线索、光谱先验或场景语义。

=== “中灰”是标定目标，不是自然律

反射式测光器把某种加权亮度映射到预定输出位置，并为高光、误差和处理保留一定余量。这个位置常被口语化为“18% 灰”，但测光常数 $K$、灰卡标称反射率、相机 JPEG 中灰码值与 RAW 饱和余量不是同一个定义，也不必严格导出同一个百分数。

灰卡的用处是提供已知且近似光谱中性的参照。若灰卡没有位于主体照明中、表面不是朗伯体、角度引入镜面反射，或者相机使用了自动色调曲线，读数仍会偏离简单模型。

#warn-box[
  *“测光表总把世界变成 18% 灰”过于粗糙。*　测光表输出的是一组推荐设置；最终 RAW 码值还取决于镜头、传感器标定和 ISO，JPEG 明度再受白平衡、色调曲线与显示编码影响。
]

== 测光作为统计估计与决策

=== 空间权重不是物理定律

把画面离散为区域 $i=1,dots,n$，每个区域测得亮度 $L_i$。一种抽象的测光统计量可写为

$
  z = cal(T)({L_i,w_i}_(i=1)^n),
  quad w_i >= 0,
  quad sum_i w_i=1,
$ <eq:metering-statistic>

其中 $cal(T)$ 可能是线性平均、对数平均、截尾均值、分位数或由场景识别网络产生的估计。中央重点、评价测光、面部优先和高光重点之间的差别，本质上是权重 $w_i$ 与统计函数 $cal(T)$ 不同。

线性平均容易受小面积强光支配，对数平均对阴影和亮区更对称，却无法处理零值；分位数对离群点稳健，但会忽略超过所选分位的极少量高光。不存在对所有题材都最优的统计量。

=== 中间调目标与高光约束

令 $a prop Delta t/N^2$ 表示相机可调的曝光尺度，传感器线性信号近似 $q_i=a L_i$。若希望加权对数平均映射到目标 $q_0$，可取

$
  a_("mid")
  =q_0 / exp(sum_i w_i ln L_i).
$ <eq:midtone-exposure-objective>

若主要目标是让至多 $epsilon$ 比例的区域超过饱和值 $q_("sat")$，可按亮度的高分位数选择

$
  a_("hi")
  =q_("sat") / Q_(1-epsilon)({L_i}).
$ <eq:highlight-exposure-objective>

通常 $a_("hi")<a_("mid")$，即高光优先给出更小曝光，代价是阴影电子数与信噪比下降。若高光是无关紧要的灯泡，严格保护它可能损害主体；若高光是婚纱或云层纹理，牺牲它又可能不可接受。正确目标依赖题材损失函数。

#figure(
  image("../svg/metering_objectives.svg", width: 98%),
  caption: [中间调优先与高光优先的示意。两者面对同一亮度分布，却因目标映射与饱和约束不同而选择不同曝光尺度。],
) <fig:metering-objectives>

#paragraph-heading[直方图不是光子计数器]

直方图必须说明数据域。相机 JPEG 直方图通常已经经过白平衡、颜色矩阵、非线性曲线、局部处理和压缩；单色亮度直方图还可能隐藏某个颜色通道先行饱和。RAW 数据更接近传感器读出，却仍包含黑电平、坏点处理和相机特定编码。

因此“直方图碰右就是 RAW 过曝”不总成立，“JPEG 没碰右就一定安全”也不成立。严谨做法是检查各 RAW 通道相对饱和值的余量，并明确相机预览采用的白平衡与图像风格。高光警告是决策辅助，不是无条件物理判据。

=== 曝光建议应带不确定度

测光误差可粗略分解为

$
  delta "EV"
  =delta_("cal")+delta_("lens")+delta_("scene")
  +delta_("time")+delta_("model"),
$ <eq:metering-error-budget>

分别表示表体标定、镜头透过与渐晕、场景采样、快门时间以及统计模型误差。多次测量可以估计重复性，却不能自动发现共同的系统偏差；换一支镜头、旋转机身、改变测光区域或使用已知光源，有助于分离误差来源。

当关键高光只剩 $0.2$ 档余量，而系统综合不确定度约 $0.3$ 档时，报告“恰好不溢出”没有统计意义。包围曝光的理论价值正是以多次观测换取对模型偏差和场景动态范围的不确定性覆盖，而不只是“多拍几张保险”。

== 常见误解

#paragraph-heading[“ISO 是曝光三要素之一”]

若曝光专指 $H=integral E dif t$，ISO 不在积分中。它会改变相机推荐的光圈或快门，也会改变读出增益与输出标定，所以能间接改变自动曝光结果；在完全固定光圈和时间时，提高 ISO 不会增加到达传感器的光子数。

#paragraph-heading[“同一 EV 就是同一照片”]

同一 EV 只保持理想的 $Delta t/N^2$。改变光圈会改变景深、衍射、像差、暗角与焦外形状；改变时间会改变运动轨迹、闪烁相位与暗电流。等曝光组合是标量相等，不是完整前向算子相等。

#paragraph-heading[“测光正确就不会过曝”]

测光器只能依据被采样区域与既定目标给出建议。场景动态范围可能超过传感器容量，某个颜色通道可能先饱和，或者语义上重要的区域没有被赋予足够权重。所谓正确曝光必须明确要保护什么、允许牺牲什么。

#paragraph-heading[“后期拉回等于现场多收了一档光”]

数字乘法可以改变码值和显示明度，却不能增加原始光电子数。阴影提升会同时放大散粒噪声、读出噪声和量化误差；饱和区域已经失去的通道比例也不能由乘法恢复。

#struct-box(title: [本章小结])[
  - 物理曝光量是像面谱辐照度经过快门门函数的时间积分；显示明度属于更后的映射层。
  - 轴上扩展场景的像面辐照度近似正比于 $tau/N^2$；离轴还受渐晕、瞳孔几何与光谱响应影响。
  - 档是倍率的 $log_2$ 坐标；$"EV"=log_2(N^2/(Delta t))$ 是设置组合，不是光量单位。
  - 互易律只在静态、线性、无饱和等条件下约束积分曝光；它不保证运动、噪声与光学效果等价。
  - 反射式测光观测照明与材料反射的乘积，不能从单一亮度读数唯一分离二者。
  - 中间调优先与高光优先对应不同损失函数；不存在脱离题材与传感器约束的唯一正确曝光。
  - ISO 改变标定与增益，不直接出现在物理曝光积分中；其严格模型留待 @chap:sensor-physics。
]

== 思考与计算

#block[
  1. 一均匀朗伯面亮度为 $80 "cd" dot "m"^(-2)$，镜头透过率 $tau=0.81$。比较 f/2 与 f/5.6 的轴上像面照度倍率，并计算对应 T 数。
  2. 由 @eq:macro-effective-fnumber 计算 f/8 镜头在放大率 $m=0,0.5,1,2$ 时的有效 f 数与相对无穷远曝光补偿档数。说明公式的适用假设。
  3. 快门门函数在 $0.2 "ms"$ 内线性开启、保持全开 $4.6 "ms"$、再在 $0.2 "ms"$ 内线性关闭。计算有效曝光时间，并与名义 5 ms 比较。
  4. 证明 @eq:exposure-ratio-settings 取 $log_2$ 后得到 @eq:exposure-stop-sum。把 f/2.8、1/250 s 改为 f/5.6 时，为保持理想曝光量应使用多长时间？
  5. 取 $K=12.5$，计算 $L_v=0.125,1,128,4096 "cd" dot "m"^(-2)$ 对应的 EV100。反过来求 EV100=15 的亮度。
  6. 某相机测得零补偿设置为 f/4、1/500 s。分别施加 $+1.3$ 与 $-0.7$ 档补偿，在固定光圈时求理想快门时间；讨论实际离散档位造成的误差。
  7. 构造两组 $(rho,E_v)$，使其满足相同 $L_v=rho E_v/pi$。说明增加一个已知反射率灰卡读数后，哪些未知量可以被识别。
  8. 给定区域亮度 $L=(1,2,4,32,512)$ 与相等权重，计算对数平均。若 $q_0=0.18$、$q_("sat")=1$，分别求 @eq:midtone-exposure-objective 的 $a_("mid")$ 与取 $Q_(0.8)=32$ 时的 $a_("hi")$；比较阴影信号。
  9. 设计一个实验，区分某镜头边缘暗化来自机械渐晕还是相机数字补偿。要求记录 RAW、不同光圈、机内 JPEG 与噪声方差，并说明预测结果。
  10. 对“夜景应尽量提高 ISO 以增加曝光”这句话给出三种可能语境：自动曝光、手动固定光圈快门、受快门下限约束。分别判断它何时改变物理曝光量、何时只改变输出标定。
]
