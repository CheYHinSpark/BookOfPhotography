#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 光的描述层次 <chap:light-models>

摄影中的“光”至少有三种常用面貌。它可以是一束沿路径传播的射线，可以是具有振幅与相位的电磁波，也可以是在探测时呈现离散统计的光子。三种说法并不是彼此竞争，而是针对不同尺度和不同问题的有效模型。真正重要的是判断在当前问题中，哪些自由度可以忽略。

设想拍摄夜空中的一个点光源。要判断它在画面中的位置，追踪传播方向已经足够；要计算它即使准确合焦仍会扩展成多大的斑，就需要振幅与相位；要估计暗部斑点会有多大，则要计算有限曝光内到达了多少光子。对象没有改变，改变的是问题以及模型保留的自由度。

@chap:introduction 已经把摄影写成从场景到观测的前向链条。本章先为这条链选择描述光的语言：射线模型将交给后面的投影与对焦，波动模型将用于衍射和像差，光子统计则通向传感器与噪声。先分清模型层次，后面才不会用一种公式回答所有问题。

== 同一束光，三类问题

#figure(
  table(
    columns: (1.2fr, 0.6fr, 1fr, 1.5fr),
    align: (left, left, left, left),
    [*问题*], [*优先模型*], [*模型保留什么*], [*典型摄影任务*],
    [光从哪里到哪里？], [几何光学], [位置、方向、光程], [投影、视角、遮挡、对焦],
    [有限孔径怎样改变成像？], [波动光学], [振幅、相位、偏振], [衍射、干涉、镀膜、像差],
    [单次曝光有多大随机性？], [光子统计], [离散能量、到达事件], [散粒噪声、量子效率、弱光极限],
  ),
  placement: none,
  caption: [三种描述层次的适用范围。],
) <tab:light-models>

#paragraph-heading[几何光学：传播路径]

当系统的特征尺寸远大于波长，并且我们主要关心像的位置、视角和遮挡时，可以把光表示为射线。均匀介质中的射线沿直线传播，界面处服从反射定律和斯涅尔折射定律。薄透镜方程、针孔投影、景深和大部分镜头示意图都属于这一层。

几何光学极为有效，却不包含波的相位。代价是相位被丢弃。射线可以告诉我们失焦光束在像面上截出多大的几何光斑，却不能独自推出圆孔的艾里斑；它能指出光阑挡住了哪些方向，却不能解释小光圈星芒的精细结构。不过只要这些被丢弃的效应小于任务关心的尺度，几何模型就是合适的。

#paragraph-heading[波动光学：振幅与相位]

可见光是真空中波长约为 $380 "nm"$ 至 $780 "nm"$ 的电磁辐射。频率 $nu$、真空波长 $lambda$ 与光速 $c$ 满足

$ c = lambda nu. $ <eq:wave-relation>

在介质中，相速度和波长会因折射率改变，而频率在静止界面两侧保持连续。波动描述引入复振幅、相位、偏振和相干性，使衍射、干涉、薄膜色彩与像差的相位结构成为可计算对象。傅里叶光学把像形成视为复振幅经过瞳函数和传播算子的变换，是理解点扩散函数与光学传递函数的自然语言 @goodman2017。它比射线模型保留更多信息，但在只问投影几何时，这些自由度并不会自动带来更好的解释。

#paragraph-heading[光子模型：能量量子与计数涨落]

频率为 $nu$ 的单个光子具有能量

$ E_gamma = h nu = (h c) / lambda, $ <eq:photon-energy>

其中 $h$ 是普朗克常数。以 $lambda=550 "nm"$ 的绿光为例，$E_gamma approx 3.61 times 10^(-19) "J"$，所以 $1 "nJ"$ 的这种单色光对应的平均光子数约为 $2.77 times 10^9$。宏观能量可以看似连续，探测却由大量离散事件构成。相机传感器并不会把所有入射光子都转换成可读电子，量子效率、反射损失和像素结构都会降低有效计数。即便平均入射通量严格不变，有限时间内的实际计数仍会涨落。

在常见的独立到达近似下，有限曝光内的光子数服从泊松统计，方差等于均值，这是弱光摄影噪声的物理起点。若像素最终收集到 $N$ 个光电子，仅散粒噪声的标准差约为 $sqrt(N)$，相对涨落约为 $1\/sqrt(N)$。光子模型回答的就是相同条件下重复拍摄会有多大不确定度。

== 普通摄影的中间层：谱辐亮度

逐时刻追踪电磁场通常过于细致，只说场景“总共有多少光”又会丢掉成像所需的方向与空间信息。普通非相干摄影最常用的折中称为谱辐亮度

$ L_lambda (bold(x), bold(omega), lambda, t). $ <eq:light-field>

它描述在位置 $bold(x)$、时刻 $t$，沿方向 $bold(omega)$ 传播的辐射，怎样分布在波长 $lambda$ 上。可以把它理解为给射线附上随波长和时间变化的平均能流，但它不保留光学相位，也暂不描述一次曝光中光子的随机到达。

若波长用米计，谱辐亮度的 SI 单位为

$ "W" dot "m"^(-2) dot "sr"^(-1) dot "m"^(-1)
  = "W" dot "m"^(-3) dot "sr"^(-1). $ <eq:spectral-radiance-unit>

工程资料常按纳米给出最后一项，写作 $"W" dot "m"^(-2) dot "sr"^(-1) dot "nm"^(-1)$。两种写法描述同一物理量，但数值相差 $10^9$；谱密度的数值必须和所用波长单位一起读。

#paragraph-heading[与平方反比律的关系]

在真空或均匀无损介质中，辐亮度沿一条射线保持不变。若光线进入折射率为 $n$ 的介质，更一般的不变量是 $L\/n^2$。这句话说的是每单位投影面积、每单位立体角内的能流密度，不是说任意接收器得到的总功率都不随距离变化。

考虑一个固定大小的小方块。距离加倍后，它对镜头张开的立体角约变为原来的 $1\/4$，镜头从整个方块收到的总光也约变为 $1\/4$。与此同时，它在线性尺寸上缩小为 $1\/2$，像面面积也变为 $1\/4$。这就是“平方反比律”。

虽然总光减少了，但这些光也落在小了同样比例的像上，单位像面面积得到的光并不会仅因物距加倍而变为 $1\/4$。这就是“远处物体更小”与“理想均匀表面的像面亮度不按物距平方衰减”可以同时成立的原因。相反，灯照到墙上的辐照度常随灯距作平方衰减，因为此时比较的是墙面固定面积接收的总通量密度。

现实远景还会受大气散射、吸收、照明差异和镜头渐晕影响。辐亮度守恒并没有否认这些机制。@chap:radiometry 将用面积元与立体角把两种情形写成同一套积分。

#paragraph-heading[通光量与被动光学系统的约束]

通光量（étendue）是辐亮度描述中的一个量。对近轴、近似均匀的光束，可粗略写成 $G approx n^2 A Omega$，其中 $A$ 是光束占据的截面积，$Omega$ 是它包含的方向范围，$n$ 是折射率。因此它同时计量光束在“位置上有多宽”和“方向上有多散”，单位为 $"m"^2 dot "sr"$。

无损被动系统不能在保留全部光的同时把 $A$ 和 $Omega$ 都任意压小。若把一束光压到更小面积，通常必须接受更大的角度范围，反之亦然。镜头的 f 数会改变每个像点可接收的方向锥，正是这类面积与角度交换的摄影表现。

== 从连续光谱到少数通道

谱辐亮度仍然是关于位置、方向、波长和时间的连续函数，而照片只有有限个像素和通道。镜头、曝光与传感器依次对这些变量进行选择、加权和积分。视场删去未进入镜头的位置与方向，孔径只接收一部分方向，曝光把一段时间累积到一起，滤色与量子效率把连续光谱压成少数通道，像素再对有限面积积分。

因此，相机不是把 $L_lambda$ 原样“保存”下来，而是对它做一组有限测量。下面先单独观察波长这一维怎样被压缩。

光源有光谱分布，材料有随波长变化的反射率或透过率，传感器通道也有自己的光谱响应。对近似朗伯表面，在把孔径、投影和像素面积等几何因素汇入 $K$ 后，通道 $c$ 的期望电子数可写成

$
  mu_c = K Delta t integral_(Lambda)
  E_lambda (lambda) rho(lambda) q_c (lambda)
  lambda/(h c)
  dif lambda,
$ <eq:spectral-sensor-response>

其中 $E_lambda$ 是照明的谱辐照度，$rho$ 是表面光谱反射率，$q_c$ 是该通道的综合量子效率，$Delta t$ 是曝光时间，$lambda/(h c)$ 把辐射能换算为光子数。这个式子把前面的描述层次接在了一起：连续辐射度给出平均能量，光谱响应完成加权，光子能量关系把它变成期望计数。

令 $s(lambda)=E_lambda (lambda)rho(lambda)$ 表示到达相机前的光谱因素，并把其余波长权重记为 $r_c (lambda)$，则相机只得到

$ mu_c = integral_(Lambda) r_c (lambda) s(lambda) dif lambda. $ <eq:spectral-channel-functional>

每个通道只是对连续函数 $s(lambda)$ 的一次加权积分。三个通道给出三个数，不可能唯一确定一般的连续光谱。总能找到非零差异 $Delta s(lambda)$，使所有通道都满足

$ integral_(Lambda) r_c (lambda) Delta s(lambda) dif lambda = 0. $ <eq:spectral-nullspace>

因此，不同光谱可以产生相同的 RGB 观测，这就是相机同色异谱的线性代数根源。换一台相机或换一种照明，权重函数改变，它们便可能不再同色。白平衡只能调整已有通道的相对增益，不能重建积分时已经丢失的完整光谱。

#warn-box[
  *常见误解*　“RAW 保存了真实颜色”并不严格。RAW 保存的是相机滤色阵列与光谱响应下的有限通道观测；从这些数值到标准色度值，还需要照明假设、颜色矩阵和外观映射。
]

== 补回被省略的信息

谱辐亮度是普通摄影的默认中间层，不是适用于一切情形的完整状态。若现象正由它省略的自由度产生，就必须扩展描述。

=== 相干性：相位关系能否被平均掉

两列波形成稳定干涉，要求它们的相对相位在观测期间保持足够稳定。普通日光、白炽灯和多数摄影 LED 的不同发光点通常互不相干，传感器曝光又远长于电场振荡周期，因此许多交叉项会在时间或空间平均中消失。在这种条件下，来自不同场景点的*强度*可以近似相加，标量谱辐亮度与非相干强度点扩散函数便足够使用。

然而激光散斑、牛顿环和薄膜干涉则表明相位关系没有完全消失，此时必须回到复振幅：先叠加振幅，再取模平方。所谓相干长度或相干时间，正是在回答多大的光程差或时间差内，相位关系还值得保留。相干性因此不是与三种模型并列的第四种模型，而是判断能否从波动描述降到强度描述的条件。

=== 偏振：电场横向取向是否影响测量

标量辐亮度还把电场的横向取向压成了一个总强度。经过反射、散射或双折射后，光可能呈现部分偏振。若镜头前加入偏振片，测量结果便会依赖这个被省略的自由度。

对线偏振光，若其方向与理想线偏振片透振轴的夹角为 $theta$，透射强度满足马吕斯定律

$ I(theta) = I_0 cos^2 theta. $ <eq:malus>

介质界面的 s、p 偏振反射系数不同，布儒斯特角附近的非金属反射可以高度线偏振，所以转动偏振镜能压低玻璃、水面和非金属漆面的某些反光。但金属反射、粗糙表面的多次散射和许多取向混合的高光未必能被完全抑制。天空偏振又随太阳方向变化，超广角画面可能因此明暗不均。

普通传感器主要记录总强度，转动镜头前的偏振片则是在选择某类偏振分量。偏振镜因而不是笼统的“去反光滤镜”，而是一个改变测量条件的元件。

#struct-box(title: [可以继续深入])[完整偏振测量可用 Jones 向量、Stokes 向量和 Mueller 矩阵描述，并通过多次不同分析器角度估计偏振状态。本书后文只使用马吕斯定律和“不同材质反光的偏振程度不同”这一摄影结论。]

== 怎样判断该用哪一层

模型切换可以落实为几个数量级比较，而不必凭术语判断。

1. *先写目标量*：问位置、倍率和遮挡，先从射线开始；问斑点形状或条纹，保留波前；问重复曝光的涨落，加入光子与读出统计。
2. *比较结构尺度与波长*：若孔径或结构尺度 $a$ 远大于 $lambda$，且衍射角 $lambda/a$ 远小于目标分辨率，几何近似通常足够。对直径 $D$ 的圆孔，第一暗环的角半径约为

   $ theta_("Airy") approx 1.22 lambda/D. $ <eq:airy-angle-preview>

3. *比较计数与容许误差*：若有效电子数为 $N$，相对散粒涨落约为 $1\/sqrt(N)$。$N=10^6$ 时约为 $0.1%$，$N=100$ 时约为 $10%$；是否需要统计模型取决于任务容许的误差，而不只取决于画面看起来“暗不暗”。
4. *检查测量元件是否读取了被省略的量*：窄带光源、长光程干涉、偏振片或双折射材料会让相位或偏振进入观测，此时标量强度模型不再封闭。

#figure(
  table(
    columns: (1.5fr, 1.5fr, 2fr),
    align: (left, left, left),
    [*观察到的线索*], [*需要补回的信息*], [*模型升级*],
    [目标只改变位置和大小], [无], [谱辐亮度上的射线传输通常足够],
    [细节接近 $lambda\/a$], [相位与衍射], [从射线升级为波动光学],
    [出现稳定条纹或散斑], [相位相关], [使用相干或部分相干波动模型],
    [转动偏振片会改变结果], [偏振态], [使用包含偏振状态的扩展模型],
    [计数涨落不可忽略], [离散到达事件], [在平均辐射度之后加入光子统计],
  ),
  caption: [模型升级的触发条件：只补回会改变目标量的信息。],
) <tab:model-selection>

== 摄影语言中的几个澄清

#paragraph-heading[“相机记录光”]

这句话方向正确，但不够完整。相机记录的是经过镜头选择、像素面积与曝光时间积分、光谱响应加权、随机光电转换和电子读出的有限数据。它既没有记录所有方向的光，也没有保存完整波前。

#paragraph-heading[“自然光与人工光性质不同”]

“自然”与“人工”是来源分类，不能直接推出软硬、显色性或色温。更基本的区别是光谱、角分布、偏振、时间稳定性和相干性。阴天和直射太阳都属自然光，二者的角分布却极不相同。

#paragraph-heading[“颜色属于物体”]

物体有相对稳定的光谱反射性质，但照片中的颜色值由照明、材料、观察几何、传感器与颜色管线共同形成。把颜色完全归于物体，会使混合光、同色异谱和白平衡显得神秘。

#paragraph-heading[“弱光只是亮度较低”]

弱光同时改变估计问题的统计性质。光子数下降时，相对涨落上升；读出噪声和量化误差也更可能占主导。将暗图简单乘以常数能提高显示亮度，却不会创造缺失的信噪比。

#struct-box(title: [本章小结])[
  - 几何、波动与光子统计分别回答传播路径、波前叠加和探测涨落问题；实际成像常把三层串联使用。
  - 谱辐亮度 $L_lambda (bold(x),bold(omega),lambda,t)$ 是普通摄影的默认中间层；它保留位置、方向、波长和时间上的平均能流，但省略相位、偏振细节与离散到达事件。
  - 辐亮度沿无损传播保持不变并不意味着固定接收器的总功率不变；远处表面元收到的总光与它的像面面积会一起缩小。
  - 通光量衡量光束同时占据的面积和方向范围，是被动系统的传播约束。
  - 相机通道是连续光谱的少数加权积分，一般不能从 RGB 或 RAW 唯一恢复完整光谱。
  - 当目标尺度接近衍射尺度、相位相关仍稳定、偏振会被分析或光子计数很少时，应补回相应自由度。
]

== 思考与计算

// 1. 对同一个离焦点光源，分别说明几何光学、波动光学和光子统计能够预测什么、不能预测什么。
1. 分别计算波长 $450 "nm"$、$550 "nm"$ 和 $650 "nm"$ 光子的能量。相同辐射能量下，哪一种波长对应的光子数更多？
2. 墙上一个固定方块远离相机两倍。说明它对镜头张开的立体角、像面面积、镜头收到的总光和理想像面照度各怎样变化，并写出所用假设。
3. 把连续波长离散成 $m$ 个采样点、把三个通道响应写成 $3 times m$ 矩阵。为什么当 $m > 3$ 时通常存在不会改变三个通道值的非零光谱差异？
4. 两片理想偏振片夹角分别为 $0 degree$、$45 degree$、$90 degree$ 时，线偏振入射光的透过率是多少？若入射光完全非偏振，第一片偏振片还会带来什么系数？
5. 一像素平均收集 $400$ 个电子，只考虑散粒噪声。估计其标准差和信噪比；若曝光时间增加四倍，二者分别如何变化？
