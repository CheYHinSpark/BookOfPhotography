#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 噪声、信噪比与动态范围 <chap:noise-dynamic-range>

数字照片中的噪声不是一种统一的“颗粒”。光子与电子的离散到达产生不可消除的散粒涨落；像素和列电路加入读出噪声；热激发产生暗电荷；制造差异形成空间固定图样；量化和后期处理又改变噪声的尺度、相关性与可见形态。不同来源的均值、方差和相关结构不同，不能靠一个“高感噪点”标签解释。

本章把 @chap:sensor-physics 的电子与读出模型写成随机变量，先由泊松分布推导 $"SNR" prop sqrt(N)$，再建立含背景、暗电流、读出噪声与非均匀性的方差预算。随后比较动态范围的多种下限定义，并把 ETTR 表述为饱和、运动、景深和不确定度约束下的优化问题。最后在单位面积、整块传感器与同尺寸输出三种比较层次上分析像素和画幅。光子转移与传感器噪声计量可参见 @janesick2007 与 @holst2011。

@chap:sensor-physics 给出了从光子到码值的平均路径，本章补上每次曝光不会完全重复的随机部分。完成信号、噪声、饱和与比较条件的区分后，我们再回到空间成像：接下来的衍射、像差、运动和采样将说明有限信噪比之外，细节还会怎样被系统削弱或混合。

#key-box[
  *噪声问题必须先回答三个条件*　在哪个数据域——电子、RAW DN 还是显示值？比较的是同一像素随时间的涨落，还是不同像素的空间差异？固定的是曝光、输出尺寸、景深，还是相机菜单上的 ISO？
]

== 光子计数的随机性

=== 泊松过程

在经典非相干照明、探测事件近似独立且曝光时间远大于光场相干时间时，可把一个像素收集的光电子数写成

$ N_e tilde "Poisson"(mu_e). $ <eq:poisson-electron-count>

泊松分布的核心性质是

$
  "E"[N_e]=mu_e,
  quad "Var"[N_e]=mu_e.
$ <eq:poisson-mean-variance>

因此散粒噪声标准差与相对标准差分别为

$
  sigma_("shot")=sqrt(mu_e),
  quad sigma_("shot")/mu_e=1/sqrt(mu_e).
$ <eq:shot-noise-relative>

收集更多电子会让绝对涨落变大，却让它相对于信号的比例变小。这正是亮区看起来更平滑、暗区更不稳定的根本原因。

#figure(
  image("../svg/poisson_shot_noise.svg", width: 98%),
  caption: [泊松计数与平方根定律。计数均值增加时，分布的绝对宽度约按 $sqrt(mu)$ 增长，信噪比也按 $sqrt(mu)$ 增长。],
) <fig:poisson-shot-noise>

=== 信噪比不是对比度

若信号是期望电子数 $mu_s$，只存在自身散粒噪声，则定义

$ "SNR"_("shot")=mu_s/sqrt(mu_s)=sqrt(mu_s). $ <eq:shot-limited-snr>

SNR 是重复测量中均值相对于随机标准差的比值，不是图像中相邻物体亮暗差。将一张图整体乘以常数 $g$ 会让信号与已有噪声同时乘 $g$，SNR 不变；增加实际曝光才会增加 $mu_s$ 并改变散粒统计。

若希望散粒受限 SNR 提高 $k$ 倍，需要 $k^2$ 倍电子。增加一档曝光使电子数乘 2，SNR 只乘 $sqrt(2)$；以幅度比的分贝表示，提升约为

$ 20log_10(sqrt(2)) approx 3.01 "dB". $ <eq:snr-per-stop-db>

=== 背景也贡献噪声

设目标信号、背景光与暗电荷分别为相互独立的泊松变量 $N_s,N_b,N_d$，读出噪声 $epsilon_r$ 均值为零、方差为 $sigma_r^2$。观测为

$ Y=N_s+N_b+N_d+epsilon_r. $ <eq:additive-electron-observation>

即使可以准确减去背景与平均暗电荷，估计

$ hat(S)=Y-mu_b-mu_d $ <eq:background-subtracted-signal>

的方差仍为

$
  "Var"[hat(S)]
  =mu_s+mu_b+mu_d+sigma_r^2.
$ <eq:background-noise-variance>

所以信号 SNR 为

$
  "SNR"
  =mu_s/sqrt(mu_s+mu_b+mu_d+sigma_r^2).
$ <eq:general-electron-snr>

减去天空背景会消除均值偏置，却不会撤销背景光子的到达涨落。光污染滤镜若减少背景的同时也损失目标光，结果取决于两者光谱与透过率，不能只看背景变暗多少。

== 电子噪声与非均匀性

=== 读出噪声

读出噪声汇集浮动扩散节点、源跟随器、列放大器、参考电压、ADC 与时钟耦合等影响。经电子/ DN 标定后，常把一次读出的总时间噪声近似为输入折算高斯变量

$ epsilon_r tilde cal(N)(0,sigma_r^2), $ <eq:read-noise-gaussian>

其中 $sigma_r$ 的单位为电子。该近似在分析均值与方差时很有用，但真实分布可能有长尾、行列相关、随机电报噪声或增益状态依赖；只报告 RMS 不能描述所有异常像素。

提高模拟增益可以降低下游噪声折算到输入端的权重，但不会消除增益之前的读出噪声。HCG、低速读出和多次采样可能降低 $sigma_r$，代价通常包括满阱、帧率、功耗或时间。

=== 暗电流与温度

无光时，热激发仍会产生电子。若暗电流率为 $i_d$ 电子每秒，曝光时间为 $t$，则

$ N_d tilde "Poisson"(mu_d), quad mu_d=i_d t. $ <eq:dark-current-poisson>

平均暗信号可以用暗场估计并相减，暗电流散粒标准差 $sqrt(i_d t)$ 仍然存在。暗电流通常随温度快速上升，但具体倍增温差依工艺与温区而异；“每升高若干摄氏度翻倍”只是局部经验式，不是统一常数。

#struct-box(title: [可以继续深入])[半导体器件物理会用 Arrhenius 型模型、激活能和缺陷能级解释暗电流随温度的变化。摄影上更可靠的做法是在实际温度、曝光时间和增益下测量暗场，而不是套用一个通用翻倍常数。]

长曝光降噪常在拍摄后记录一张同时间暗帧并相减。它能减去稳定热像素与暗信号图样，却又引入暗帧自身的随机噪声，并占用同等时间；温度和时间若变化，校正也会失配。

=== 固定图样与时间噪声必须分开

把像素 $i$ 的响应写成

$
  Y_i
  =(1+delta g_i)(N_(s,i)+N_(b,i))
  +d_i t+epsilon_(r,i),
$ <eq:pixel-nonuniformity-model>

$delta g_i$ 表示光响应非均匀性（PRNU），$d_i$ 表示暗信号非均匀性（DSNU）。对同一像素重复拍摄，稳定的 $delta g_i,d_i$ 是偏差而非随机时间方差；对均匀平场比较不同像素，它们却表现为空间固定图样。

平场除法和暗场减法可以校准稳定图样，但会受标定噪声、温度、曝光、增益模式和波长失配限制。降噪算法若把固定图样当作独立白噪声，可能留下条纹；若把真实细纹理当作固定图样，又会损失细节。

=== 方差预算与主导区间

对固定像素的时间序列，若各随机项独立，方差相加：

$
  sigma_("temp")^2
  =mu_s+mu_b+mu_d+sigma_r^2+sigma_q^2.
$ <eq:temporal-noise-budget>

若比较许多像素的空间方差，还可出现近似乘法项

$ sigma_("PRNU")^2 approx sigma_g^2 mu^2, $ <eq:prnu-spatial-variance>

其中 $sigma_g$ 是相对响应标准差。于是低信号区常由读出噪声主导，中间区由散粒噪声主导，高信号平场的跨像素差异可能由 PRNU 主导。

#figure(
  image("../svg/noise_budget.svg", width: 98%),
  caption: [噪声预算与光子转移曲线示意。左图按输入电子标准差比较各项；右图中散粒区方差与均值成正比，高信号空间方差可能因 PRNU 上翘。],
) <fig:noise-budget>

#struct-box(title: [可以继续深入])[传感器计量常用光子转移曲线：对不同曝光的均匀场比较 RAW 均值与方差，可估计电子—码值转换增益、读出噪声和满阱容量。严格实验还要用成对图像去除固定图样，并控制黑电平、渐晕、温度和通道。后文不要求实际拟合这条曲线。]

== 动态范围的多种定义

=== 上限相对明确，下限依赖阈值

令线性读出的电子饱和阈值为 $N_("sat")$，选择的最低可用信号为 $N_("floor")$。电子尺度动态范围定义为

$
  "DR"=N_("sat")/N_("floor").
$ <eq:dynamic-range-ratio>

若用档数或分贝表示，分别为

$
  "DR"_("stops")=log_2 "DR",
  quad "DR"_("dB")=20log_10 "DR".
$ <eq:dynamic-range-units>

上限通常由满阱、模拟链或 ADC 首个饱和环节决定。下限却可能取 $sigma_r$、SNR=1、SNR=10、某个色差阈值或特定打印可见度，因此不同测试机构的“动态范围”不能只凭数字横向比较。

在只含信号散粒与常数背景方差 $B=mu_b+mu_d+sigma_r^2$ 的模型中，要求 SNR 至少为 $q$，最低信号满足

$
  N_("min")(q)
  =(q^2+sqrt(q^4+4q^2 B))/2.
$ <eq:snr-threshold-electrons>

例如 $N_("sat")=60000$、$sigma_r=2$ 且无背景时，以 $sigma_r$ 为下限约得 14.9 档；若要求 SNR=10，下限约为 104 个电子，只剩约 9.2 档。两者都可以数学正确，却回答不同问题。

#figure(
  image("../svg/dynamic_range_definitions.svg", width: 98%),
  caption: [动态范围与高光余量的不同阈值。图中使用同一示例传感器；改变最低可用 SNR 定义，会显著改变报告档数。],
) <fig:dynamic-range-definitions>

=== 高光余量与阴影可用性不对称

对某个重要高光信号 $N_h$，距饱和的余量为

$ H_h=log_2(N_("sat")/N_h) $ <eq:highlight-headroom>

档。高光一旦裁切，线性测量从等式变成下界；阴影则通常没有同样锐利的“黑色裁切”，而是随着 SNR 下降逐渐失去颜色、纹理与可估计性。因此高光余量与阴影容量不是关于中灰对称的两个盒子。

黑电平以下仍可能存在经统计估计可恢复的信息，黑电平以上也不保证视觉可用。RAW 解码器的降噪与色调映射会改变可见阈值，所以“阴影还有几档”必须附带输出尺寸、观看条件与质量准则。

#paragraph-heading[单像素、整幅图与最终输出]

单像素动态范围比较一个采样单元的饱和与噪声底。整幅图还可通过空间平均、多像素合并、双转换增益、交错曝光或多帧融合获得不同的有效范围；但这些方法可能牺牲空间分辨率、时间一致性或运动鲁棒性。

最终 JPEG 或显示器可以用非线性色调映射把很宽的场景范围压进有限码值和显示对比度。这改变的是表达映射，不等于传感器在一次曝光中线性测得了同样宽的范围。输入动态范围、传感器动态范围、RAW 可用范围和输出显示范围必须分层报告。

== 曝光策略作为优化问题

=== 在饱和约束下更多光总是改善 SNR

令曝光尺度 $a$ 同时缩放目标与背景电子数，单位尺度下信号和背景为 $s_i,b_i$。忽略暗电流随时间的额外变化，区域 $i$ 的 SNR 为

$
  "SNR"_i(a)
  =(a s_i)/sqrt(a(s_i+b_i)+sigma_r^2).
$ <eq:snr-exposure-scale>

对 $a>0$，该函数单调增加：更多实际光电子总会改善该区域的测量 SNR，尽管边际收益按平方根下降。若只有一个饱和上限，最优 $a$ 会到达约束边界。

但摄影不是只选一个标量 $a$。曝光尺度近似 $a prop tau t/N^2$，改变 $t$ 会影响运动和暗电流，改变 $N$ 会影响景深、衍射和像差。一个更完整的决策可写为

$
  max_(N,t) sum_i w_i U("SNR"_i(N,t))
  -lambda_m cal(L)_("motion")(t)
  -lambda_o cal(L)_("optics")(N),
$ <eq:exposure-multiobjective>

并满足各重要通道

$ mu_(i,c)(N,t) <= N_("sat",c)-m_c, $ <eq:highlight-saturation-constraint>

其中 $m_c$ 是为测光误差和场景变化预留的安全余量。权重 $w_i$ 与损失函数仍由题材决定。

=== ETTR 的成立条件

“向右曝光”可以理解为：在 RAW 近似线性、重要高光不饱和且其他成像约束可接受时，增加物理曝光，使亮度分布尽量靠近饱和边界。它利用 @eq:snr-exposure-scale 的单调性，把阴影从读出噪声主导区推向散粒主导区。

#figure(
  image("../svg/ettr_constraints.svg", width: 98%),
  caption: [ETTR 的约束解释。曝光不足浪费可收集电子；约束内右移提高阴影 SNR；越过边界则裁切重要高光。直方图位置为示意。],
) <fig:ettr-constraints>

ETTR 只有在以下条件大致成立时才是合理策略：

- 检查的是线性 RAW 通道而非受色调曲线影响的 JPEG 亮度直方图；
- 能识别哪些高光需要保留，并知道各通道相对饱和点；
- 场景在测光与曝光期间稳定，照明没有显著闪烁；
- 增加曝光不会带来不可接受的运动模糊、景深变化、衍射或温升；
- ISO 与转换增益选择没有先于物理势阱截断高光；
- 预留的余量大于测光、构图与主体运动的不确定度。

通过提高 ISO 把 JPEG 直方图推右不等于 ETTR，因为固定光圈和快门时电子数没有增加。真正的右移来自更长时间、更大有效孔径、更强照明或更高透过率。

=== 通道饱和与“高光重建”

若通道 $c$ 未饱和，校准后可估计 $N_c$；若记录达到最大码，只能写成

$ N_c >= N_("sat",c). $ <eq:clipped-channel-inequality>

一个通道饱和会破坏颜色比例，三个通道都饱和时通常只剩亮度下界。所谓高光重建会利用未饱和通道、邻域颜色、边缘结构、材料先验或其他曝光推断缺失值；它可能视觉可信，却不是从裁切像素中读回原始测量。

#figure(
  image("../svg/channel_clipping.svg", width: 96%),
  caption: [彩色通道裁切的信息变化。未饱和时通道比例可测；单通道裁切后只知道下界；三通道裁切时颜色与真实亮度都不可辨识。],
) <fig:channel-clipping>

#paragraph-heading[包围曝光与场景运动]

多张不同曝光可让短曝光测高光、长曝光测阴影，扩展静态场景的有效范围。设第 $j$ 张曝光尺度为 $a_j$，融合实质是在不同区间选择未饱和且 SNR 较高的测量，再归一化到共同辐射尺度。

如果主体、相机或照明变化，多个观测不再对应同一未知场景；配准和去鬼影会成为新的逆问题。手持 HDR 的“更大动态范围”因此同时依赖时间一致性、运动估计与融合先验，不能只由包围档数相加得到。

== 画幅、像素与总光量

=== 单位面积与单像素

在相同场景辐亮度、f 数、快门、透过率与量子效率下，像面单位面积的电子密度近似相同。记该密度为 $rho_e$，有效像素面积为 $A_p$，则

$ mu_p=rho_e A_p. $ <eq:pixel-signal-area>

较大像素因此在同一单位面积曝光下收集更多电子，单像素散粒 SNR 较高；但它同时用更大面积平均空间细节。若传感器总面积固定，把同一区域划成更多小像素并在输出端合并，区域总光子数并不会因分割本身消失。

=== 像素合并

设 $n$ 个小像素各有信号均值 $mu$、独立读出噪声 $sigma_r$。数字求和得到

$
  "E"[S_n]=n mu,
  quad "Var"[S_n]=n mu+n sigma_r^2.
$ <eq:digital-binning-sum>

其 SNR 为

$
  "SNR"_("sum")
  =(n mu)/sqrt(n mu+n sigma_r^2)
  =sqrt(n) mu/sqrt(mu+sigma_r^2).
$ <eq:digital-binning-snr>

若为保持相同输出亮度而取平均，均值仍为 $mu$，标准差除以 $sqrt(n)$。电荷域或模拟域合并若能在一次读出前求和，可能只付一次部分读出噪声；普通 CMOS 数字像素合并往往先分别读出，不能假定具有同样优势。

#figure(
  image("../svg/format_pixel_binning.svg", width: 98%),
  caption: [同一面积的像素划分与合并。光子总数由面积和曝光决定；分别读出会累加读出方差，求和、平均与电荷域合并具有不同输出尺度。],
) <fig:format-pixel-binning>

=== 整块传感器与同等输出

对传感器上一块对应相同场景区域的总面积 $A_R$，期望总电子数为

$ mu_R=rho_e A_R. $ <eq:region-total-electrons>

因此在同一场景、视角、f 数和快门下，更大传感器具有更大的成像面积并收集更多总光；若最终缩放到相同输出尺寸，这些电子可以通过空间平均转化为更高区域 SNR。这个结论来自总面积，不是“每个像素神奇地更灵敏”。

公平比较还必须说明是否固定景深。更大画幅为相同视角使用更长焦距；若要求相同景深并相应收小光圈，单位面积曝光与总光优势会改变。快门、镜头透过、像素数、量子效率和读出技术也可能不同。所谓“画幅优势几档”只能在一组明确约束下计算。

#paragraph-heading[像素尺寸不是单变量排名]

较小像素提供更高采样密度和更灵活的空间平均，却可能有更低单像素满阱、更多读出节点与更强串扰；较大像素有较高单像素电子数，却以较低原生采样率为代价。背照式结构、堆叠、HCG、微透镜与工艺代际都可能改变权衡。

正确问题不是“大像素是否更好”，而是：在相同视场、曝光、传感器面积、输出分辨率和处理条件下，系统对目标空间频率保留多少信号，累积多少光子，并引入怎样的读出与相关噪声？

== 常见误解

#paragraph-heading[“高 ISO 会制造散粒噪声”]

散粒噪声由实际光电子计数决定。高 ISO 照片常更噪，通常因为自动曝光同时减少了光圈或快门，使电子数下降；在固定曝光下改变 ISO，散粒统计不变，只改变读出尺度、后级噪声权重和余量。

#paragraph-heading[“把背景减掉就没有背景噪声”]

背景均值可以估计并扣除，背景到达的泊松涨落仍留在方差中。暗帧和平场校正同理：它们消除偏差，却不会倒转已经发生的随机事件。

#paragraph-heading[“动态范围是一台相机唯一固定的档数”]

动态范围取决于 ISO、转换增益、饱和路径、噪声阈值、输出尺度与测试方法。没有下限质量准则的单个档数是不完整陈述。

#paragraph-heading[“ETTR 就是让 JPEG 直方图贴右”]

JPEG 曲线、白平衡和图像风格会移动直方图，ISO 也能移动码值而不增加电子。ETTR 的物理目标是增加未饱和 RAW 区域的实际光电子数。

#paragraph-heading[“像素越大，整张照片必然越干净”]

大像素单点通常收光更多，但整图比较还涉及传感器总面积、像素数、缩放、读出噪声、量子效率与空间频率。把不同输出分辨率的一比一放大截图直接排名，会把采样尺度误当成系统质量。

#struct-box(title: [本章小结])[
  - 泊松计数满足均值等于方差，散粒受限 SNR 为 $sqrt(mu)$；SNR 加倍需要四倍电子。
  - 背景和暗电荷即使均值被减去，仍分别贡献 $mu_b$ 与 $mu_d$ 的方差。
  - 时间噪声、空间固定图样和相关噪声必须分开；独立随机项按方差相加。
  - 动态范围上限由饱和决定，下限由 SNR 或视觉质量阈值决定，因此档数必须附带定义。
  - 高光饱和是硬截断，阴影可用性是渐进阈值；色彩通道裁切会破坏比例信息。
  - ETTR 是在饱和、运动、景深、光学和不确定度约束下增加物理曝光，不是移动 JPEG 码值。
  - 同一面积的总光子数不由像素分割决定；数字合并会累加各像素读出方差。
  - 画幅和像素比较必须固定视场、景深、曝光、输出尺寸与技术条件，不能做单变量叙事。
]

== 思考与计算

#block[
  1. 某像素期望收集 $25,400,10^4$ 个电子。仅考虑散粒噪声时，分别计算标准差、相对标准差和 SNR。若每种曝光增加两档，各量怎样变化？
  2. 目标信号 $mu_s=100$、背景 $mu_b=300$、暗电子 $mu_d=9$、读出噪声 $sigma_r=3$ 电子。用 @eq:general-electron-snr 计算 SNR；若完美减去背景均值，为什么结果中的 $mu_b$ 不能删除？
  3. 暗电流为 $0.2 "e"^-"/s/pixel"$，分别曝光 1 s、30 s 和 300 s。计算暗电子均值与散粒标准差；再分析减去一张同曝光暗帧后随机方差如何变化。
  4. 传感器饱和为 50000 电子、读出噪声为 1.5 电子。分别以 $sigma_r$、SNR=1 和 SNR=10 定义下限，计算动态范围档数；说明哪个数字更适合天文测量、哪个更接近高质量摄影阴影。
  5. 对 @eq:snr-exposure-scale 求导，证明在参数为正时 SNR 随曝光尺度 $a$ 单调增加。再加入暗电流 $i_d t$ 与运动损失，解释为什么最优快门不一定无限长。
  6. 某重要高光的三个 RAW 通道电子数预计为 $(24000,18000,12000)$，各通道饱和为 30000。若曝光增加 $0.5,1,1.5$ 档，判断各通道何时裁切，并说明裁切后还能确定哪些颜色比例。
  7. 四个小像素各收集 400 电子，读出噪声均为 3 电子。计算单像素、四像素数字求和与平均的信号、方差和 SNR；再与同面积大像素只读出一次、读出噪声为 3 电子的结果比较。
  8. 两台相机传感器面积比为 4，相同视角、f 数、快门、量子效率和输出尺寸。忽略读出噪声，推导总电子数与缩放后散粒 SNR 比；若为相同景深把大画幅光圈缩小两档，结论如何变化？
  9. 设计一个 ETTR 实验，分别使用 RAW 通道直方图与 JPEG 亮度直方图决定曝光。要求记录实际快门/光圈、ISO、通道饱和、阴影均值—方差和运动模糊，并预测两种直方图可能给出不同结论的原因。
]
