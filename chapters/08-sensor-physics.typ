#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 图像传感器的物理模型 <chap:sensor-physics>

镜头在像面建立的是连续的光子通量分布，数字相机最终保存的却是一组有限位宽的整数。两者之间至少经历光子吸收、电子—空穴对生成、电荷收集、电容上的电荷—电压转换、模拟放大、采样保持、模数转换与数字编码。把整条链统称为“感光度”，会把器件效率、容量、增益和输出约定混成一个不存在的单参数。

本章从 @eq:exposure-to-electrons 出发，先讨论量子效率、像素势阱、满阱容量、微透镜与填充因子；再建立 CMOS 像素和列读出模型，推导数据率与滚动快门的时空采样。最后依照数字相机曝光标定的思想解释 ISO、转换增益、模拟增益、ISO 不变性和扩展档。器件模型可参见 @holst2011、@janesick2007 与 @fossum1997；ISO 术语的标准背景见 @iso12232。

@chap:exposure-metering 计算的是平均能收到多少光电子，本章沿着器件链追踪这些电子如何成为电压和码值。先把平均响应、容量与增益分清，下一章才能在同一坐标上加入随机涨落，并准确说明噪声和动态范围来自哪里。

#key-box[
  *本章的四层坐标*　入射光子数 $N_gamma$、收集电子数 $N_e$、模拟电压 $V$ 和数字码值 $D$ 是四种不同量。只有给出量子效率、转换增益、模拟增益、黑电平和 ADC 标定，才能在它们之间换算。
]

== 从光子到电子

=== 光子吸收与光电转换

单个波长为 $lambda$ 的光子能量为

$ E_gamma=h c/lambda. $ <eq:photon-energy-sensor>

硅的室温带隙约为 $1.12 "eV"$，因此可见光光子有足够能量在吸收后产生电子—空穴对。超过带隙的多余能量通常迅速转化为晶格振动；在普通可见光成像条件下，一个被有效探测的光子至多按一次主要计数事件进入像素，而不是因为蓝光能量更高就自动产生数倍 RAW 码值。

产生载流子还不等于形成可读电子。光子可能被表面层或金属结构吸收，电子—空穴对可能复合，扩散中的电子也可能被相邻像素收集。把“产生并最终进入指定像素势阱”的总概率记为外量子效率

$
  eta(lambda)
  = "E"[N_e(lambda)]/"E"[N_gamma(lambda)].
$ <eq:external-quantum-efficiency>

对给定的 $N_gamma$ 个同波长光子，若每个光子的探测事件近似独立，则

$
  N_e " | " N_gamma
  tilde "Binomial"(N_gamma,eta).
$ <eq:binomial-photon-detection>

若入射光子数本身服从泊松分布，独立概率筛选后，电子数仍为泊松分布。这一性质将在 @chap:noise-dynamic-range 用于散粒噪声推导。

#figure(
  image("../svg/photon_to_electron_pixel.svg", width: 97%),
  caption: [像素的光学堆栈与概率探测。微透镜和滤光片把部分光子导向硅光电二极管；只有被吸收、分离并收集的事件进入电子势阱。],
) <fig:photon-to-electron-pixel>

=== 谱响应与彩色通道

量子效率不是单个百分数，而是波长函数。若通道 $c$ 的彩色滤光片透过率为 $tau_c(lambda)$，红外截止与其他堆栈透过率为 $tau_o(lambda)$，则可把通道有效响应写成

$ R_c(lambda)=eta_("Si")(lambda) tau_c(lambda) tau_o(lambda). $ <eq:channel-spectral-response>

像素的电子期望于是为

$
  mu_(e,c)
  =A_p integral_(Lambda)
  R_c(lambda) H_(e,lambda)(lambda)
  lambda/(h c) dif lambda.
$ <eq:channel-electron-expectation>

红、绿、蓝响应彼此重叠，且并不等于人眼锥体响应。相同勒克斯、不同光谱的光源可以产生不同电子数；相同总电子数也可能对应不同通道比例。白平衡只能在读出之后重标通道，不能改变拍摄时的谱探测概率。

#figure(
  image("../svg/quantum_efficiency_rgb.svg", width: 96%),
  caption: [硅器件包络与 RGB 通道有效光谱响应示意。真实曲线还取决于彩色滤光片、红外截止滤镜、微透镜、入射角与制造工艺。],
) <fig:quantum-efficiency-rgb>

“峰值量子效率 80%”只说明某个波长附近的概率，不能单独预测宽谱照明下的信号。应把场景谱曝光与完整 $R_c(lambda)$ 相乘积分，并说明响应是按光子数还是按辐射功率归一化。

=== 微透镜、填充因子与串扰

几何填充因子定义为

$ F=A_("photo")/A_("pixel"), $ <eq:pixel-fill-factor>

其中 $A_("photo")$ 是光敏区在像素平面上的面积。前照式像素的金属互连会遮占一部分表面，微透镜可把原本落在非光敏区域的光线重新集中到光电二极管；背照式结构把互连移到光入射面的背后，通常允许更高的光学利用率。

但有效收集率不只由二维面积比决定。大入射角下，微透镜焦点可能偏离光敏区，深层吸收产生的载流子也可能横向扩散。传感器边缘常使用偏心微透镜配合主光线角度；若镜头出射角锥超出设计，可能出现颜色偏移、阴影或通道串扰。

串扰可以看成传感器内部的空间混合：本应属于像素 $i$ 的载流子有一部分被像素 $j$ 收集。它会平滑高频纹理、降低颜色纯度并改变点扩散函数，所以“像素越小只影响噪声”并不完整。

== 电荷容量与饱和

=== 从电子数到电压

积分结束后，像素把电荷转移到浮动扩散节点。令电子电荷量的绝对值为 $q_e$，节点有效电容为 $C_("FD")$，则电子数变化对应的电压幅度近似为

$
  Delta V
  =g_c N_e,
  quad g_c=q_e/C_("FD").
$ <eq:charge-voltage-conversion>

$g_c$ 的单位常写为 $"V/e"^-$ 或 $"µV/e"^-$，称为电荷—电压转换增益。实际读出极性可能相反；这里讨论幅度。注意有些资料把“系统转换增益”定义为 $"e"^-/"DN"$，恰与 $"DN/e"^-$ 形式互为倒数，比较数字前必须确认定义。

=== 满阱容量

若可用电压摆幅为 $Delta V_("max")$，单由浮动扩散电容估计的容量为

$
  N_("FW")
  approx C_("FD") Delta V_("max")/q_e.
$ <eq:full-well-capacity>

真实饱和阈值取光电二极管势阱容量、转移门、浮动扩散节点、模拟链摆幅与 ADC 满量程中最先到达者。电子溢出还可能进入邻近像素形成 blooming；现代像素常使用溢流结构抑制，但被截断的原始信号仍不可恢复。

#figure(
  image("../svg/full_well_conversion_gain.svg", width: 96%),
  caption: [转换增益与满阱容量的基本权衡。在相同电压摆幅下，较小有效电容给出更高 $"µV/e"^-$，却通常以更小电子容量为代价。],
) <fig:full-well-conversion-gain>

#paragraph-heading[双转换增益]

部分传感器可切换 $C_("FD")$ 或相关读出路径。低转换增益模式（LCG）用较小的 $"µV/e"^-$ 斜率容纳更多电子，适合高光；高转换增益模式（HCG）让每个电子产生更大电压，使下游电路更容易分辨弱信号，但可能更早达到电压或 ADC 上限。

营销语境中的“双原生 ISO”有时对应这种转换增益切换，有时还包含额外模拟放大器、不同 ADC 路径或数字处理。只有测得 RAW 斜率、饱和电子数与输入折算读出噪声的突变，才能判断其物理机制；菜单中出现两个推荐 ISO 并不足以证明存在两套完全等价的“原生灵敏度”。

== CMOS 像素与读出链

=== 一个概念性的像素周期

典型四晶体管有源像素可抽象为以下步骤：

1. 经复位与转移路径清空光电二极管，开始积分；
2. 光电二极管在曝光期间积累电子；
3. 临近读出时复位浮动扩散节点并采样复位电平；
4. 转移门把光电二极管中的电荷送入浮动扩散节点；
5. 源跟随器与行选择开关把信号送到列电路，采样后与复位样本相减；
6. 模拟增益与 ADC 把结果变为数字码值。

相关双采样（CDS）用同一像素的复位样本与信号样本作差，可以消除共同偏置并降低一部分复位与低频噪声，但不能消除曝光期间产生的光子涨落、暗电荷或所有时间变化噪声。

#figure(
  image("../svg/cmos_readout_pipeline.svg", width: 98%),
  caption: [从电子到 RAW 码值的概念读出链。不同噪声在不同位置加入；模拟增益只会相对于其后的噪声改变输入折算权重。],
) <fig:cmos-readout-pipeline>

=== 模拟增益、ADC 与数字数值

把 CDS 后信号的绝对幅度记为 $g_c N_e$，前级噪声为 $epsilon_("pre")$，模拟增益为 $g_a$，增益后的噪声为 $epsilon_("post")$，偏置为 $b_V$，则 ADC 输入可写成

$
  V_("ADC")
  =g_a(g_c N_e+epsilon_("pre"))
  +epsilon_("post")+b_V.
$ <eq:adc-input-model>

若 ADC 满量程为 $V_("FS")$、位宽为 $B$，理想量化步长为

$ Delta V_q=V_("FS")/2^B. $ <eq:adc-quantization-step>

忽略非线性后，数字输出近似

$
  D
  ="clip"(
    "round"((V_("ADC")-V_0)/(Delta V_q)),
    0,2^B-1
  ).
$ <eq:raw-digital-number>

在局部线性区，每个数字单位对应的输入电子数约为

$ K_e=(Delta V_q)/(g_a g_c), $ <eq:electrons-per-dn>

单位为 $"e"^-/"DN"$。提高模拟增益使 $K_e$ 变小，即同一电子差异占据更多数字码；它也使 ADC 满量程对应的电子阈值下降。数字增益发生在 ADC 之后，只能重排或截断已经量化的码值。

RAW 并非“完全未经处理”。相机通常会加入非零黑电平，屏蔽负噪声值，并进行列偏置、坏点或黑电平校正；有的格式还无损压缩或使用非线性编码。严谨分析应先读取元数据和黑电平，再把 DN 换算到相对电子尺度。

#paragraph-heading[14 位不等于 14 档动态范围]

$B$ 位 ADC 只提供最多 $2^B$ 个码位。有效可用位数还受模拟噪声、非线性、饱和阈值与编码方式限制。反之，一个 12 位 ADC 也可以通过多转换增益、双 ADC 或多帧组合覆盖超过单一路径 12 档的场景。位数是数字分箱数，不是动态范围的同义词；动态范围的严格定义留待 @chap:noise-dynamic-range。

== 读出速度与时间结构

=== 数据带宽

若图像含 $N_x times N_y$ 个像素，每像素 $B$ 位，帧率为 $f_("fps")$，未压缩有效数据率至少为

$ R=N_x N_y B f_("fps"). $ <eq:sensor-data-rate>

一台 $6000 times 4000$、14 位、10 fps 的相机仅有效像素数据率就约为

$ 6000 times 4000 times 14 times 10=3.36 "Gbit/s", $ <eq:data-rate-example>

即约 $420 "MB/s"$，尚未包括消隐、元数据、纠错与接口开销。提高分辨率、位宽和帧率会同时增加列 ADC 数量、时钟、功耗、温升、缓存和存储压力，因此高速模式常伴随裁切、像素合并、较低位宽或有损压缩。

=== 滚动快门的时空采样

对总读出时间为 $T_("read")$、有效高度为 $H$ 的滚动快门，归一化行坐标 $y/H$ 的曝光起点可近似写为

$
  t_s(y)
  =t_0+(y/H)T_("read").
$ <eq:rolling-shutter-start-time>

该行记录的仍是长度为 $Delta t$ 的时间积分，但积分区间为

$ [t_s(y),t_s(y)+Delta t]. $ <eq:rolling-shutter-window>

若物体在像面沿水平方向以 $v_x$ 像素每秒匀速运动，顶部与底部的采样时刻差会产生近似位移

$ Delta x approx v_x T_("read"). $ <eq:rolling-shutter-skew>

这个几何倾斜主要由总扫描时间决定，并不等于单行曝光时间造成的运动模糊。缩短 $Delta t$ 可以让每一行更清晰，却不一定减小整幅图的倾斜。

#figure(
  image("../svg/rolling_global_shutter.svg", width: 98%),
  caption: [全局快门与滚动快门的时空采样。滚动快门各行曝光长度可以相同，但时间原点依次推迟，因此移动竖线会被重建为斜线。],
) <fig:rolling-global-shutter>

#paragraph-heading[全局快门并不等于无运动模糊]

真正的全局快门让所有像素共享曝光起止时刻，再把电荷暂存并读出。它消除逐行时间偏移，但在有限 $Delta t$ 内仍会积分主体运动。片上存储节点还可能占用面积、降低满阱或增加噪声；堆叠式设计可缓解这些代价，但不会消灭能量、带宽和功耗约束。

有些模式采用全局复位、逐行结束或机械快门与滚动读出组合。判断运动几何时，应询问的是每行实际积分区间，而不是只看“电子快门”或“全局复位”标签。

== ISO 的严格解释

=== ISO 是输出标定与曝光建议

数字相机 ISO 标准试图规定：在某种曝光、输出与饱和条件下，相机应给出怎样的感光度数值或推荐曝光指数。标准包含基于输出、饱和或厂商推荐的不同评定路径；它不是硅材料的一项固定自然常数。

固定镜头光圈、快门时间和场景后，@eq:channel-electron-expectation 给出的 $N_e$ 分布不随 ISO 菜单值改变。ISO 可以影响自动测光所选的 $N$ 与 $Delta t$，也可以改变模拟增益、转换增益模式、ADC 参考或数字映射，但它不倒流回光电二极管增加已经到达的光子。

#figure(
  table(
    columns: (1.1fr, 1.1fr, 1.55fr, 1.55fr),
    align: (left, center, left, left),
    [*参数*], [*常见单位*], [*它真正改变什么*], [*固定曝光时不改变什么*],
    [量子效率 $eta$], [电子/光子], [光子转成电子的概率], [入射光子数],
    [转换增益 $g_c$], [$"µV/e"^-$], [每个电子产生的电压], [势阱中已有电子数],
    [模拟增益 $g_a$], [倍率或档], [ADC 前信号尺度], [光子与光电子统计],
    [ADC 位宽 $B$], [bit], [数字分箱数量], [满阱本身与光子数],
    [数字增益 $g_d$], [倍率或档], [ADC 后码值], [既有信号和噪声的信息量],
    [ISO 标称值 $S$], [ISO], [曝光建议与输出标定], [固定 $N,Delta t$ 下的入射光],
  ),
  caption: [传感器相关“增益”与 ISO 的层次。固定曝光时，它们都不会增加入射光子数。],
) <tab:sensor-gain-layers>

=== 高 ISO 为什么有时仍然有用

由 @eq:adc-input-model 可见，模拟增益 $g_a$ 同时放大信号和前级噪声，却不放大后级噪声 $epsilon_("post")$。折算到输入电子端，后级噪声贡献大致随 $1/g_a$ 减小；因此在读出链下游噪声或量化较显著时，适度提高模拟 ISO 可以让弱信号更充分利用 ADC 码位。

代价是 ADC 满量程对应的电子数近似

$
  N_("ADC-sat")
  approx (V_("FS")-b_V)/(g_a g_c),
$ <eq:adc-electron-saturation>

随 $g_a$ 增加而下降。如果物理势阱尚未满，较高 ISO 也可能先在读出链或 RAW 编码处截断高光。于是“高 ISO 改善阴影”与“高 ISO 减少高光余量”可以同时成立。

#paragraph-heading[基准 ISO 与转换增益切换]

“基准 ISO”在不同语境中可能指最低非扩展 ISO、制造商用于色彩标定的工作点、LCG 下恰好让势阱接近 ADC 满量程的设置，或图像质量测试选定的参考点。没有统一上下文时，这个词不能唯一推出量子效率、满阱或最佳画质。

当传感器从 LCG 切到 HCG，$g_c$、满阱和输入折算读出噪声可能同时跳变。此时 ISO—RAW 斜率不是一条连续直线；比较相邻 ISO 应检查饱和 DN、电子/ DN 标定和噪声方差，而不能只看 JPEG 亮度是否每档加倍。

=== ISO 不变性

考虑固定物理曝光的两条路径：路径 A 在 ADC 前使用较高模拟增益；路径 B 用较低增益读出，再在 RAW 线性域或后期乘以相同倍率。若传感器后级噪声与量化误差相对于前级信号很小，且两条路径都未饱和，则匹配亮度后的结果可能非常接近，称为近似 ISO 不变。

#figure(
  image("../svg/iso_invariance_paths.svg", width: 98%),
  caption: [固定曝光下的两条增益路径。较高模拟增益让信号先越过部分下游噪声与量化，但更早接近 ADC 满量程；低 ISO 后期提升保留高光，却会把已有读出误差一并放大。],
) <fig:iso-invariance-paths>

把 ISO 设置 $j$ 的局部 RAW 模型写成

$ D_j=b_j+k_j N_e+epsilon_j, $ <eq:iso-raw-linear-model>

其中 $k_j$ 为 $"DN/e"^-$，$b_j$ 为黑电平。校准后的电子估计为

$ hat(N)_(e,j)=(D_j-b_j)/k_j. $ <eq:iso-electron-normalization>

若不同 ISO 的 $hat(N)_(e,j)$ 在固定曝光、未裁切区域具有近似相同的分布和空间结构，才有证据称该范围 ISO 不变。仅比较缩小后的 JPEG 噪点不能排除色调曲线、降噪和锐化差异。

ISO 不变不等于 ISO 无关：ISO 仍会改变自动曝光、预览、直方图、RAW 余量、增益模式与后续处理。它只陈述一个有限条件命题——固定曝光后，把增益放在相机内或线性后期，信息损失是否接近。

=== 扩展 ISO

扩展高 ISO 常在最高实际模拟增益之后继续做数字乘法：

$ D'= "clip"("round"(2^s D),0,D_("max")). $ <eq:extended-high-iso>

它会让预览和 JPEG 更亮，却不会产生新的光电子或新的 ADC 码位，反而可能更早截断高光。扩展低 ISO 常把基准读出结果数字压暗，使 JPEG 看似获得较低感光度；若 RAW 仍在相同模拟工作点饱和，它也不会增加原始高光信息。

某些相机会在低 ISO 使用真实的较低模拟增益或不同 ADC 参考，这可能改变电子饱和阈值；某些高 ISO 会触发 HCG 或另一条低噪声读出路径，这也可能带来实际差异。因此“扩展 ISO 一定纯数字”同样过强，最终应以 RAW 斜率、饱和点和读出噪声的测量为准。

== 常见误解

#paragraph-heading[“更大像素的量子效率必然更高”]

量子效率是每个入射光子成为可收集电子的概率；像素面积决定接收多少光子。大像素可以在相同单位面积曝光下收集更多总光子，却不必具有更高 $eta(lambda)$。工艺、微透镜、堆栈和入射角都会改变效率。

#paragraph-heading[“14 位 RAW 就有 14 档动态范围”]

位宽只规定离散码位数。若模拟噪声跨越多个码、黑电平占去范围或满阱在 ADC 满量程之前出现，实际可分辨范围会更小；多增益合成又可能让有效场景范围大于单一 ADC 路径的位数直觉。

#paragraph-heading[“全局快门会冻结一切运动”]

全局快门消除行间时间偏移，但每个像素仍在 $Delta t$ 内积分。冻结运动需要控制曝光时间或照明脉冲，而不是只改变读出同步方式。

#paragraph-heading[“ISO 不变相机永远用最低 ISO 即可”]

过低 ISO 可能让预览和测光不便，也可能浪费 ADC 码位；某些相机会在较高 ISO 切换到 HCG。反过来，过高 ISO 会减少高光余量。ISO 不变性必须限定设置范围、曝光固定、RAW 线性与不饱和条件。

#struct-box(title: [本章小结])[
  - 外量子效率 $eta(lambda)$ 是波长相关探测概率；通道信号由谱曝光与完整响应函数积分得到。
  - 微透镜、填充因子、入射角和载流子串扰共同决定有效收集率与空间混合。
  - 电子数经 $g_c=q_e/C_("FD")$ 转为电压；高转换增益便于读弱信号，却常与满阱容量权衡。
  - CMOS 读出依次包含电荷转移、CDS、模拟增益、ADC 和数字编码；RAW 码值不是电子数本身。
  - ADC 位宽是分箱数量，不等于动态范围；数据率约随像素数、位宽和帧率的乘积增长。
  - 滚动快门给各行不同时间原点；全局快门消除行间偏移，却不消除曝光期间的运动积分。
  - ISO 是曝光建议与输出标定，并可控制增益路径；固定光圈快门下，它不增加入射光子。
  - ISO 不变性比较固定曝光下的增益位置；扩展 ISO 通常不增加原始信息，但实际模式应以 RAW 测量判断。
]

== 思考与计算

#block[
  1. 波长分别为 $450,550,650 "nm"$ 的单个光子能量是多少电子伏特？与硅带隙比较，并解释为什么能量差不会在线性可见光传感器中简单变为多个 RAW 计数。
  2. 某通道接收 $10^4$ 个同波长光子，外量子效率为 0.62。计算电子数的期望与条件二项分布方差；若光子数本身为泊松变量，电子数方差如何变化？
  3. 给定 $C_("FD")=8 "fF"$、$Delta V_("max")=1.0 "V"$，用 @eq:full-well-capacity 估计满阱电子数与 $g_c$。若切换到 $4 "fF"$ 且摆幅不变，二者怎样变化？
  4. 12 位 ADC 满量程为 $1.2 "V"$，$g_c=80 "µV/e"^-$，模拟增益为 1、4、16。计算量化步长、$K_e$ 与 ADC 对应电子饱和阈值；讨论何时由物理满阱先饱和。
  5. 对 @eq:adc-input-model，把各电压量除以 $g_a g_c$，写成输入电子等效模型。说明为什么 $epsilon_("post")$ 的等效贡献随 $g_a$ 增大而减小，而 $epsilon_("pre")$ 不会。
  6. 计算 $8192 times 5464$、14 位、20 fps 未压缩数据率，并换算为 $"GB/s"$。若接口只能持续 $1.0 "GB/s"$，提出三种满足带宽的模式改变并说明各自损失。
  7. 一滚动快门传感器总扫描时间为 $18 "ms"$，运动主体像面速度为 $900 "px/s"$。估计顶部与底部的水平错位。若单行曝光从 1/100 s 缩到 1/1000 s，错位与行内模糊各怎样变化？
  8. 固定曝光下，ISO 100 与 ISO 800 的线性 RAW 斜率分别为 $0.5$ 与 $4.0 "DN/e"^-$，黑电平分别为 512 与 2048 DN。写出两者的电子归一化公式，并比较 RAW 值 1512 与 10048 对应的电子数。
  9. 为“扩展 ISO 50 是否增加高光余量”设计判别实验。固定场景与曝光序列，比较 ISO 50 与 100 的 RAW 饱和 DN、达到饱和的光量、线性斜率和 JPEG 输出；列出数字下拉与真实模拟减益两种模型的预测。
]
