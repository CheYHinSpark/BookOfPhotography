#import "../cyhs_template.typ": key-box, struct-box, warn-box, paragraph-heading

#pagebreak(weak: true)

= 照片质量诊断与最小实验 <chap:quality-diagnosis-experiments>

面对一张“不够好”的照片，最省事的动作往往是拖动清晰度、降噪或饱和度；最有效的动作却通常发生在更前面：先把模糊的感受写成可观察症状，再判断它在哪一环首次出现。本章把前面关于几何、光学、传感器、计算与输出的知识重组为“症状—原因—证据—实验—处理”的工作方法，不再把理论重新讲一遍。

诊断的目标也不是为照片判分。它要回答三个可行动问题：信息是否曾被记录；若仍存在，哪一步改变了它；若已丢失，下次拍摄应改变哪个条件。这样才能区分可逆的解释错误、可重新处理的算法选择，以及只能重拍的观测缺失。

@chap:output-color-archive 把照片送到最终设备并保留证据，本章从这个终点沿成像链逆向返回现场。它不再增加新理论，而是把前面各章重排成症状、候选原因、反证和最小实验；诊断所得结论最终进入下一次机位、光线、曝光和处理决策，使全书从直线流程变成可反复迭代的闭环。

#key-box[
  *质量诊断的三条规则*　先按真实用途和最终尺寸定义症状；从最终观看向 RAW 与现场逐级回溯，找异常首次出现的位置；用只改变一个变量、保留原始证据并重复采样的小实验验证原因。
]

== 质量诊断总表

#paragraph-heading[症状不是原因，参数也不是证据]

“不锐”“噪点多”“颜色怪”只描述结果。同样的软边缘可以来自失焦、主体运动、衍射、降噪、错误缩放或过大的观看倍率；看到高 ISO 也不能证明噪声由 ISO 造成，因为真正缺少的可能是到达传感器的光子。

有效描述至少包含：异常位于何处、沿什么方向、具有多大空间尺度、在哪个版本首次可见，以及在目标输出上是否真的影响任务。例如“眼睛睫毛沿水平方向拖成约 6 个交付像素，而衣领静止边缘清楚”，比“人像有点糊”更接近可检验假设。

#figure(
  image("../svg/diagnosis_cycle.svg", width: 98%),
  caption: [质量诊断闭环。处理动作放在证据与实验之后；若观察不符合预测，应更新原因模型，而不是继续堆叠补偿。],
) <fig:diagnosis-cycle>

一次诊断可按图 @fig:diagnosis-cycle 运行：

1. 在最终尺寸、介质和观看距离上写出症状，不先放大到任意比例找瑕疵；
2. 保留现场记录、RAW、参数、中间导出、交付文件和实际展示结果；
3. 提出两三个能产生该症状的候选原因，并写下各自应出现的伴随证据；
4. 设计一次只改变关键变量的实验，规定样本数和判断标准；
5. 根据结果选择重新处理、改变输出、改变拍摄条件或接受限制。

#paragraph-heading[沿证据链寻找“首次分歧”]

最终图像是多阶段变换的结果。若 RAW 的三个通道仍有高光层次，而交付 JPEG 没有，问题落在显影、色调或输出；若 RAW 已饱和，换软件不会恢复唯一场景值。若本地母版正常、从平台下载的文件出现色块，则应检查平台缩放与压缩，而不是重新磨皮。

#figure(
  image("../svg/evidence_chain.svg", width: 98%),
  caption: [从现场到观看的证据链。向前追溯寻找异常首次出现的阶段，再替换单一阶段向后验证。],
) <fig:evidence-chain>

所谓“首次分歧”不是寻找一个绝对原始的视觉真相，而是在可访问版本之间定位变化。RAW 也已受到光学、采样和传感器处理影响；它只是比最终 JPEG 更靠近观测、保留更多可重新解释的余量。

#paragraph-heading[一次只改一个变量，但不要只拍一张]

控制变量不是要求现实完全不变，而是让竞争解释尽量少。试验前先记录：假设、固定量、自变量、响应量、预期方向与会推翻假设的结果。若同时改变光圈、快门、ISO、机位和显影预设，即使照片更好，也无法知道哪一项起作用。

随机过程还要求重复。手持抖动、自动对焦和降噪纹理每次都不同。若一组 $n$ 张中有 $k$ 张达到事先规定的清晰阈值，命中率估计为
$
  hat(p) = k/n,
  quad
  "SE"(hat(p)) approx sqrt((hat(p)(1-hat(p)))/n).
$ <eq:diagnostic-hit-rate>

样本很少时这个标准误只是粗略提示，但它已经优于“各挑最好的一张”。连续指标可同时报告中位数与较差分位数：器材在最佳状态能做到什么，与现场多数时候能否做到，是两个问题。

#paragraph-heading[模糊贡献可以近似相加，责任不能凭感觉分配]

若多个点扩散函数近似独立、高斯且系统处于线性范围，可用

$
  sigma_"obs"^2 approx
  sigma_"opt"^2 + sigma_"focus"^2 + sigma_"motion"^2
  + sigma_"sample"^2 + sigma_"process"^2
$ <eq:diagnostic-blur-budget>

作为模糊预算。它不是普遍精确公式：拖影、像差、振铃和混叠通常并非高斯。但它提醒我们，观察到的软不是单一“镜头锐度”，而且当某一项已占主导时，继续改善其他小项收益有限。

#paragraph-heading[症状与成像阶段的候选原因]

#figure(
  image("../svg/symptom_cause_matrix.svg", width: 98%),
  caption: [六类常见症状与成像阶段的候选原因。矩阵用于提出假设；最终归因仍依赖方向、尺度、位置和中间证据。],
) <fig:symptom-cause-matrix>

#paragraph-heading[画面虚：先看焦点与空间分布]

检查最清晰平面是否落在预期位置。若前景比主体清楚，优先怀疑对焦位置；若焦点处清楚而前后迅速变软，是景深分配；若全画面随缩小光圈先改善后下降，可能是像差与衍射的平衡；若只有边角或特定方向异常，则检查场曲、像散、偏心和机位倾斜。

验证应使用静止、高反差且包含中心、边角与不同距离的目标，在同一最终输出上比较。直接加锐化会同时增强噪声和光晕，却不能告诉我们清晰平面在哪里。

#paragraph-heading[画面糊或变形：沿拖影方向找时间证据]

相机抖动常让整个画面的点和边缘沿相近轨迹延伸；主体运动只影响运动物体；滚动快门更常产生随读出方向变化的倾斜、弯曲或局部时间错位。防抖失效、三脚架振动和快门冲击还可能只在某一速度区间出现。

对照静止背景、运动主体和 EXIF 快门，观察拖影长度、方向与画面位置。防抖只能补偿相机运动，不能冻结主体；电子快门减少机械冲击，也不自动消除滚动读出形变。

#paragraph-heading[画面噪或脏：区分观测噪声与处理伪影]

随机亮度和彩色颗粒可能来自光子涨落、读出噪声与暗电流；大片色斑可能由阴影强提、白平衡增益和局部色调放大；蜡状纹理、虫纹和方块则更像降噪、去马赛克或压缩。先比较线性或低处理 RAW、显影中间状态与最终输出，并统一亮度与尺寸。

“同 ISO”不是同曝光，“同屏幕百分比”也不是同输出。若要判断 ISO 自身影响，必须固定到达传感器的曝光量；若要判断光子不足，必须改变曝光量而保持其他关键条件。

#paragraph-heading[高光失去层次：先问通道数据是否仍在]

中性物体三个通道可能一起饱和，高彩度物体则常先剪切一个通道，导致颜色转向。相机直方图通常基于 JPEG 预览，未必等于 RAW 通道。先用 RAW 读数检查饱和，再区分测光目标、白平衡预览、显影曲线与 HDR/SDR 映射。

RAW 尚有余量时可以改变白平衡、曝光解释和色调映射；所有相关感光单元都已饱和时，只能借邻域或模型生成看似连续的外观，不能声称恢复了唯一真实纹理。

#paragraph-heading[色彩异常：沿“光谱—解释—输出”分层]

同一灰物在画面不同位置呈不同色，先怀疑混合光、反射或光源光谱；整张图统一偏色，可检查白平衡和相机配置；只有某软件、平台或显示器异常，则检查嵌入配置、变换、色域映射与显示环境。

中性卡能提供白平衡参照，却不能让缺失光谱成分重新出现；色卡能帮助比较映射，也不保证所有材质在不同光源下同色。肤色可作语义检查，但受个体、妆容、照明和风格影响，不是固定 RGB 数字。

#paragraph-heading[细节不自然：回看第一处假结构]

黑白边缘外的亮暗环通常指向过度锐化或局部对比；平坦区的水彩块与重复纹理常来自过强降噪；彩色细线、摩尔纹和棋盘结构提示去马赛克与混叠；凭空重复的窗格、毛发或文字可能来自生成式恢复；8×8 块、振铃和色边则要检查 JPEG。

把所有处理暂时关闭，再逐项启用并导出中间状态。伪影首次出现的步骤最值得调整；后续步骤可能只是把它放大。AI 结果应按任务标注为推断或生成，不能因视觉可信就当作观测证据。

#paragraph-heading[六个最小实验的统一准备]

#figure(
  image("../svg/six_minimal_experiments.svg", width: 98%),
  caption: [六个覆盖拍摄、记录、处理与输出的最小实验。每个实验都有明确控制量、自变量和判据。],
) <fig:six-minimal-experiments>

六个实验共用一份准备清单：稳定机位与照明，关闭会暗中改变结果的自动功能；拍摄 RAW+JPEG 并记录固件、镜头、距离、焦点、曝光与处理版本；每组命名一致；在观看前写下预测；所有比较统一到同一构图、亮度、像素尺寸与介质。自动对焦、防抖或自动白平衡若正是测试对象，则保留开启，但其他自动项应锁定。

#struct-box(title: [最小实验记录表])[
  *问题*：要区分哪两个原因？

  *控制量*：场景、机位、焦点、照明、曝光、处理和输出中哪些固定？

  *自变量*：只改变哪一项，分几档，每档拍几张？

  *响应量*：测拖影长度、边缘对比、通道余量、噪声，还是盲评命中率？

  *判据*：什么结果支持或推翻假设？差异多大才影响真实输出？
]

#paragraph-heading[解释实验的三条纪律]

*先写预测，再揭晓结果。* 若看完照片才决定指标，很容易选择支持偏好的裁切、区域和样片。可把文件随机改名或请他人盲排，先规定比较区域与容许差异，再揭晓设置。主观评价并非无效，但它需要与条件、任务和不确定性一起记录。

*报告效应大小，不只报告“有差异”。* 大样本能让很小差异显得稳定，却未必影响成片；小样本看不出差异，也不等于两方案相同。报告输出上的拖影像素、命中率变化、可恢复高光档位、文件尺寸或盲评偏好，并说明真实任务阈值。

*结论只在测试范围内成立。* 一支镜头在近距离平面目标的最佳光圈，不自动等于远景、立体人像或逆光的最佳设置；一次室内防抖测试也不能覆盖行走、低温和纵向摇拍。好的结论带着边界：“在这个焦距、输出与命中标准下”，而不是把局部观察升级为永恒排名。

== 最小实验一：焦距、距离与透视

#paragraph-heading[A 组：固定机位，只改变焦距]

把相机固定，选择前、中、后三个距离明确的物体。用短焦和长焦拍摄，不移动相机；把短焦图裁切并缩放到与长焦相同构图、尺寸和亮度。若针孔模型和视点不变，空间点的投影比例由深度决定。两个同高物体在距离 $Z_1,Z_2$ 处的像高比满足

$
  h_1/h_2 = (f H_1/Z_1)/(f H_2/Z_2)
  = (H_1 Z_2)/(H_2 Z_1),
$ <eq:perspective-fixed-viewpoint-ratio>

焦距 $f$ 消去，因此主体与背景的几何比例应一致。可见差别主要来自视场裁切、采样、景深、像差和曝光，而不是“焦距压缩透视”。

#paragraph-heading[B 组：改变机位，保持主体大小]

改用长焦并后退，或短焦并靠近，使主体在画面中同样大。现在视点改变，主体到前后景的相对距离也改变，遮挡关系和深度比例应明显不同。这才是常被称为“透视压缩”的来源。

#warn-box[
  不要用自动几何校正、呼吸明显的近摄镜头或不同对焦点掩盖结论。若短焦裁切后像素不足，先把两者降到共同输出尺寸；实验检验的是几何比例，不是比较哪支镜头更锐。
]

== 最小实验二：光圈、景深与衍射

在三脚架前布置一张平面细节目标、靠近和远离焦平面的纹理物，以及画面边角细节。固定机位、焦距与对焦位置，从全开到小光圈逐档拍摄。使用静止场景、基础 ISO，并用快门或稳定照明补偿曝光；快门变化不能引入振动或光源闪烁。

比较三个区域：焦平面中心显示像差与衍射的平衡；边角显示场曲、像散和偏心；前后景显示景深扩展。先在相同亮度下统一到真实输出尺寸，再看中心极限解析。预期通常不是“某一光圈处处最佳”，而是任务相关的折中：缩小光圈改善部分像差并扩大景深，继续缩小则衍射扩散增加。

若镜头有焦点漂移，先锁定一次焦点测试“拍摄时缩光圈”的综合结果，再另做一组每档重新对焦以分离漂移。不要把两种实验混在一起。

#struct-box(title: [本实验的输出])[
  为每个目标输出尺寸记录一个可用光圈区间，而不是只记“最锐光圈”。区间应说明主体深度、允许的前后景模糊、边角要求和观看距离；换题材后可以不同。
]

== 最小实验三：快门、运动与防抖

#paragraph-heading[A 组：主体运动]

让带刻度或高反差标记的物体以尽量恒定速度横向运动，固定相机、焦距、距离、光圈与 ISO，只改变快门。若物体横向速度为 $v$, 距离为 $Z$，薄透镜远摄近似下像面拖影长度为

$
  L approx (f v t)/Z,
$ <eq:motion-experiment-trace>

其中 $t$ 是曝光时间。测量拖影方向与像素长度，检查是否近似随 $t$ 成正比。实际主体有转动、加速度和景深，公式只提供数量级；它仍能帮助把运动模糊与圆对称失焦区分开。

#paragraph-heading[B 组：手持与防抖]

改拍静止目标，在多个快门档和焦距下分别关闭、开启防抖，每档至少拍十余张。事先规定“合格”边缘对比或盲评标准，报告命中率与较差分位数，不只展示最好样片。若测试机身与镜头协同防抖，还应记录模式、固件、半按等待时间和取景方向。

主体运动组与手持组必须分开。防抖开启后背景更稳而人物仍糊，是正常结果；三脚架上某些系统反而可能产生低频漂移，应按设备说明和实测判断。

== 最小实验四：曝光、ISO 与噪声

这个实验必须拆成两组，否则很容易把光子数与 ISO 混为一谈。场景应同时含中灰、深阴影、细纹理和接近饱和的彩色高光，并避免照明闪烁。

#paragraph-heading[A 组：固定传感器曝光，只改变 ISO]

固定光圈、快门、照明和机位，只改变 ISO，拍 RAW。显影时把各图归一到同一亮度，关闭或统一降噪与锐化。因为到达传感器的光子期望近似相同，差异主要来自模拟/数字增益位置、读出链、量化与高光编码余量。较高 ISO 可能压低后续读出噪声的相对影响，也会更早让 RAW 数值或 JPEG 高光达到上限；具体拐点依相机而异。

#paragraph-heading[B 组：固定 ISO，改变到达传感器的曝光量]

固定 ISO 和光圈，通过快门或可控照明逐档改变曝光量；静止场景可用三脚架避免运动。再把结果归一到同一亮度比较阴影。简化模型下，收集 $N$ 个光电子、读出噪声标准差为 $sigma_r$ 时，

$
  "SNR" approx N/sqrt(N + sigma_r^2).
$ <eq:exposure-noise-experiment-snr>

增加真实曝光量通常比拍后提亮更有效，因为它提高了光子信号；但高光满阱、运动、景深和拍摄时刻构成上限。把 A、B 两组并排，才能看清“ISO 设置”和“曝光不足”各自改变什么。

#warn-box[
  JPEG 的自动色调、空间降噪和白平衡会让颗粒比较失真。先检查 RAW 与通道饱和，再在相同输出尺寸上评价最终可见噪声；百分之百视图只用于定位，不是唯一质量标准。
]

== 最小实验五：RAW 与 JPEG

选择含中性物、高彩度物、接近剪切高光、深阴影、肤色与细密纹理的场景，一次曝光同时记录 RAW 和最高质量 JPEG。锁定白平衡、曝光与相机风格，并保存直出参数。

实验分两轮。第一轮不追求外观一致，观察相机 JPEG 已经完成的白平衡、颜色矩阵、色调曲线、局部处理、降噪、锐化、镜头校正和压缩。第二轮从 RAW 显影出与 JPEG 尽量相同的整体亮度和白平衡，再比较高光重建、阴影提亮、细纹理、色边和锐化光晕。所有结果缩放到同一最终尺寸并使用同一色彩管理链。

差异应分成三类：RAW 保留但 JPEG 已固化的决策；JPEG 中依靠相机模型产生、可在 RAW 软件中用不同方法重做的外观；原始观测本就没有记录、两者都无法唯一恢复的信息。这样比较的目的不是宣布格式胜负，而是知道什么时候需要处理余量，什么时候直出已满足交付。

== 最小实验六：后期顺序与输出尺寸

从同一 RAW 建立三个非破坏分支，统一最终亮度、颜色、长边和文件格式：

- 分支 A 先做基础重建与瑕疵校正，在场景或线性相关阶段降噪，完成色调与局部编辑后缩放，最后做输出锐化；
- 分支 B 在噪声仍强时先大幅锐化和局部对比，再强降噪、缩放；
- 分支 C 使用 AI 降噪或超分辨率，并保留算法、版本和强度，再按同一目标输出。

在每个关键节点导出无额外锐化的中间图，比较边缘光晕、平坦区颗粒、重复纹理、细线断裂、颜色溢出和压缩伪影。评价分三个尺度：放大视图用于定位算法痕迹；最终屏幕或打印用于判断实际可见性；正常观看距离用于判断照片任务是否受损。

顺序没有脱离算法与题材的绝对教条。关键是某一步的假设是否被前一步破坏：锐化会把噪声塑造成假边缘，强色调会让隐藏伪影显现，过早有损压缩会污染后续处理，缩放又会改变最合适的锐化半径。因此应保存母版和参数，让输出专属步骤可以重做。

== 全书总结：在信息丢失之前做决定

#figure(
  image("../svg/complete_imaging_chain.svg", width: 98%),
  caption: [从场景到观看的完整质量链。每一阶段都可能成为瓶颈；粉色框标出若干无法由后续步骤唯一逆转的损失。],
) <fig:complete-imaging-chain>

#paragraph-heading[拍摄是在分配五类有限资源]

第一是光：光源、入瞳、曝光时间和反射率决定有多少光子携带信息。第二是空间：机位、视角、对焦、像差、衍射和采样决定哪些尺度能被区分。第三是时间：快门、读出、主体运动和拍摄时刻决定哪个过程被积分。第四是容量：满阱、位深、动态范围和存储限制决定可容纳的数值范围。第五是注意与操作：对焦、构图、可靠性和现场决策决定理论能力是否真正落到照片上。

这些资源可以交换，但不是任意互换。延长快门能增加光子，却可能失去瞬间；缩小光圈扩大景深，却减少通光并增加衍射；多帧能平均噪声，却要求配准和场景一致性。摄影技术的核心不是把单个指标推到最大，而是让当前任务最稀缺的资源获得余量。

#paragraph-heading[后期是在数据余量、模型假设与输出目标之间取舍]

RAW 处理、降噪、复原、多帧和 AI 都利用数据中的冗余与关于场景的先验。它们能重新解释仍存在的信号、压低随机误差并生成有用外观，却不能保证找回已剪切、混叠、错焦或从未采到的唯一真实值。算法越强，越应区分观测、推断与生成。

后期也没有脱离输出的“最大质量”。降噪强度、锐化半径、色域映射、位深和压缩应由观看尺寸、介质、距离与用途决定。百分之百视图适合查错，最终介质才决定哪些差异有意义。

#paragraph-heading[器材质量是任务约束下的系统适合度]

机身、镜头、照明、支撑、计算和显示共同构成系统。更多像素不能补偿主体拖影，更大画幅不能自动修正视点，更强 AI 不能替代正确曝光，更宽色域也不能让无管理终端一致。器材选择应从输出、光线、运动、工作距离、可靠性和携带约束反推，而不是从规格表寻找无条件冠军。

#paragraph-heading[视觉质量由物理记录与观看解释共同形成]

清晰度、噪声、颜色和动态范围有可测物理基础，但照片是否成立还受对比适应、注意、尺度、构图、语义与风格影响。测量帮助我们知道系统做了什么；观看任务决定哪些变化值得优化。二者不是互相替代，而是同一评价的两端。

#paragraph-heading[最可靠的改进顺序]

先确认目标输出与症状；再检查现场几何、时刻、光与曝光是否让信息进入系统；随后检查光学、对焦、运动与传感器是否成为瓶颈；然后才调整重建、降噪、色调、色彩和锐化；最后在真实平台或纸张上验证，并保留可重建母版与归档证据。

当信息仍在，尽量用可逆处理；当信息不确定，明确先验和置信度；当信息已丢失，承认边界并改变下次拍摄。这比为每一种瑕疵寻找一个滑块更慢一点，却会让每次失败真正变成下一次拍摄的知识。

#struct-box(title: [本章与全书小结])[
  - 从目标输出的具体症状向前追溯，找到异常首次出现的阶段；参数标签和最终观感都不能单独证明原因。
  - 虚、变形、噪声、高光、颜色和假细节都有跨阶段原因，要结合位置、方向、尺度、RAW 与中间状态区分。
  - 最小实验固定其余条件、一次改变一个变量、预写判据并重复采样；最好样片不能代表现场可靠性。
  - 六个实验分别分离视点、光圈、运动、光子与 ISO、记录格式和处理顺序，并始终统一最终输出。
  - 质量来自对光、空间、时间、容量与操作的分配；关键是信息在哪一环丢失，以及下次能否提前改变决定。
]

== 综合练习

#block[
  1. 选一张“不够锐”的照片，按位置、方向、尺度、首次出现版本和输出影响重写症状；列出三个候选原因及其反证。
  2. 保存同一 RAW 的基础显影、降噪、色调、锐化、缩放和平台下载版本；定位一种伪影首次出现处，并替换单一步骤验证。
  3. 完成焦距实验：固定机位裁切检验投影比例，改变机位检验遮挡与前后景比例；说明采样和景深如何干扰结论。
  4. 为一种题材画“可用光圈区间”：记录各 f 数下中心、边角、近景和远景是否合格；解释为何最优区间不等于单一最高解析值。
  5. 三个快门档、两个防抖状态各拍 15 张。预定阈值并计算式 @eq:diagnostic-hit-rate 的命中率与标准误；最好样片能代表可靠性吗？
  6. 分做固定曝光改 ISO 与固定 ISO 改曝光两组测试。统一亮度、白平衡和尺寸，比较阴影噪声与高光余量，解释两组为何不能合并。
  7. 用同一次 RAW+JPEG 比较高光、阴影、肤色、纹理和色边；把差异分成“可重新解释”“处理已固化”和“原始观测缺失”。
  8. 从同一 RAW 建三条后期分支并盲评；找出光晕、涂抹或假纹理首次出现的步骤，比较放大查看与正常观看的排序。
  9. 为一个项目画出从场景到归档的质量链；标出三处不可逆风险、三处余量节点，以及失败时成本最低的验证实验。
]
